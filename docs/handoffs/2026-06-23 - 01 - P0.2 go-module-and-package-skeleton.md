# Handoff — P0.2: stand up `go.mod` + package skeleton

**Date:** 2026-06-23 · **Counter:** 01
**Next session focus:** **P0.2** from the TDD §8 backlog — make the `apogee.go` keystone a
real in-tree Phase-0 checkpoint (compiles + `go vet` passes from inside the repo), and begin
the package skeleton.

---

## Where we are

Phase 0, just past **P0.1 (hook mutation API)** — now complete. The keystone facade exists
and type-checks; nothing else in the repo is Go yet (no `go.mod`, no `internal/`, no `cmd/`).

**Authoritative context (read these — do not re-derive):**
- TDD + backlog: [`docs/design/technical-design.md`](../design/technical-design.md) — §8 is the
  prioritized worklist; **start at P0.2**, then P0.3.
- Phased plan: [`docs/plans/implementation-plan-apogee-merge.md`](../plans/implementation-plan-apogee-merge.md)
  — §3/§3a (target layout + dependency policy), §4 Phase 0 (deliverables), Standing Requirements.
- ADRs 0001–0009: [`docs/adr/`](../adr/). Glossary: [`CONTEXT.md`](../../CONTEXT.md).
- **P0.1 output (this session):**
  - [`apogee.go`](../../apogee.go) — public API facade, **836 lines, panic-stub bodies**,
    gofmt-clean. The hook mutation surface is now filled in.
  - [`docs/design/hook-mutation-api.md`](../design/hook-mutation-api.md) — the P0.1 survey +
    accessor design, with per-operation→mechanism traceability (§7) and applied decisions (§8).

**What P0.1 added to `apogee.go`** (so you know what the skeleton must support): the
read-only `LoopView`/`ConversationView`, the `Message`/`Role`/`ToolDef`/`Budget` substrate,
and method sets on the opaque `Request`/`Response`/`Conversation`. `PreToolExec`/
`PostToolResult` gained a `LoopView` arg; `PostResponseDecision.Payload` → `Inject string`.
Full detail in the design doc — don't duplicate it.

---

## The task: P0.2

Per TDD §8 P0.2: *"Stand up `go.mod` + minimal `internal/` stubs so `apogee.go` compiles and
`go vet` passes — turns the sketch into a real Phase-0 checkpoint."*

Concretely:
1. **`go.mod`** — module `github.com/airiclenz/apogee`, `go 1.26` (apogee-sim pins 1.26.2;
   this repo's toolchain is **1.26.4**). No deps yet — defer Cobra/Bubble Tea/MCP-SDK pins to
   P0.3 (plan §3a lists the justified set).
2. **Package skeleton** from the TDD §3 / plan §3 layout: `cmd/apogee/`, `internal/{agent,
   provider,processing,tools,context,session,mcp,security,mechanisms,platform,tui}`. Minimal —
   a doc.go or a single stub per package is enough to make the tree real. **Do NOT bulk-import
   apogee-sim** (plan §4 Phase 0 / ADR 0001 — mechanisms are ported deliberately in Phase 4).
3. **Verify in-tree:** `gofmt -l`, `go vet ./...`, `go build ./...`, ideally `go vet -race`.

### Scoping note — this is smaller than it looks, with one fork
`apogee.go` is **self-contained**: it imports only stdlib (`context`, `encoding/json`,
`errors`, `time`) and its own types, with `panic` stub bodies. I already proved it `go vet`s
and `go build`s cleanly in a throwaway module (module + `go 1.26`, nothing else). **So the
literal P0.2 acceptance — "compiles + vets" — is satisfied by adding `go.mod` alone.**

The real decision for the next session is **how much skeleton to stand up now**:
- **Minimal:** just `go.mod` (+ maybe `cmd/apogee/main.go` so `apogee --help` runs, a plan
  Phase-0 deliverable). Locks the compile checkpoint with least churn.
- **Fuller:** add the empty `internal/` package tree now so later phases drop into place.

Recommend the **fuller** skeleton but keep packages empty (a `// Package x …` doc.go), since
empty packages still `vet` clean and the layout is already decided (TDD §3).

### Watch out: the Phase-0 *capstone* needs real bodies (sequence it after P0.2)
Plan §4 Phase 0 has a deliverable beyond "compiles": *a throwaway in-process harness that
constructs an `Agent`, `Step`s it, snapshots + resumes, and registers an experimental hook.*
That **cannot run against `panic` stubs** — it needs minimal real implementations of the
construct→Step→Snapshot→Resume→`AddExperimental` path. This is the natural step **after**
P0.2's compile checkpoint and is where the API surface first gets exercised for real. Don't
try to fold it into P0.2; flag it as the next slice. (`Step`/quiescent-boundary semantics:
ADR 0007. Cancellation is a Phase-0 primitive — ADR 0007 / plan §6 #24a.)

---

## Cheap housekeeping to fold in (TDD §7 / §8 #12)

Low-effort, do alongside if convenient — all already decided, just not yet propagated:
- **Fix `README.md:68`** — it says the bench is "driven through Apogee's headless mode",
  which contradicts ADR 0001 (bench drives the real loop via Go import; headless is optional).
- **Ratify the five §4.1 TDD sketch-decisions** into the plan/ADRs (esp. public `Confiner`).
- TDD §5/§8 status rows still show P0.1 as "S/∅" — update to reflect P0.1 done if you touch them.

Not P0.2 work, but noted so it isn't lost: design-doc open decisions #2/#3 (self-regulation
is registry/loop-managed; mechanism deps injected at construction) are **applied at the API
level**; their *implementation* is loop-engine/Phase-4 territory.

---

## Git / environment state

- Branch **`main`**. Untracked: `apogee.go`, `docs/design/hook-mutation-api.md`, `.gitignore`,
  this handoff. **Nothing committed this session.**
- Per session policy: **branch before committing** the Phase-0 scaffold (don't commit P0.1 +
  P0.2 straight onto `main`). Commit/push only when the user asks.
- Go **1.26.4** present (`go version` → linux/arm64). Sibling repos reachable for reference:
  `/workspace/repos/apogee-sim` (the bench), `/workspace/repos/apogee-code` (the TS oracle).

---

## Suggested skills

- **`/coding-standards`** — **mandatory** for all new Go (plan Standing Requirement 1; load
  `coding-standards.go.md` + `testing.go.md` before writing any `.go`). This is a gate, not
  optional.
- **`pr-lifecycle`** — when landing the Phase-0 scaffold: branch off `main`, commit P0.1 +
  P0.2, open the PR.
- *(Later, not P0.2)* **`feature-implementation`** for the per-subsystem ports in Phase 1+;
  **`run`/`verify`** once there's a runnable harness or `apogee --help`.
