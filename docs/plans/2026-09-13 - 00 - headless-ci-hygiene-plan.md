# Headless live narration, seam line kind, sharded CI, parallel e2e — plan

**Goal:** Close the four open "headless / CI hygiene" beads: `headless --format text` narrates live on stderr (apogee-czf), the seam-closing notices reach `--format json` behind an opt-in flag (apogee-5cf), CI runs the sharded suite (apogee-7ui), and the `cmd/apogee` e2e tests run under `t.Parallel` with per-test leak attribution (apogee-ed5, `cmd/apogee` half).
**Date:** 2026-09-13
**Status:** unexecuted
**Sized for:** ~200k-context host
**Base commit:** 6d56a51e

**Sources:**
- `bd show apogee-czf apogee-5cf apogee-7ui apogee-ed5`
- `docs/adr/0075-*` (headless Event lines; D6 stdout contract, D10 additive kinds), `docs/adr/0076-*` amendment A5 (sink-only seam notices)
- `cmd/apogee/headless.go` (`pruneNoticeSink` 167–208, install 823, `--format` 340), `internal/eventjson/{encode,writer}.go`, `internal/tuitest/leak.go`, `cmd/apogee/e2e_support_test.go`, `scripts/test-shards.sh`, `.github/workflows/ci.yml:54`
- `docs/manual/headless.md`, `docs/manual/building.md`, `docs/design/test-drivers.md`

**Ratified design calls (owner, 2026-09-13):**
- **Scope:** headless/CI bundle only; Reaction follow-ons, session fork, demand-gated additives stay unplanned. Beads apogee-rxj, apogee-4kb, apogee-8wy closed as done. ≤ 8 items.
- **czf live shape:** stderr narration live; stdout stays the answer alone at the end; the post-hoc block keeps its wording and order.
- **czf wording:** arrow lines — `→ <tool> <summary>` on call, `← <tool> ok` / `← <tool> error: <first line>` on result, `sub-agent <name>: started|finished|cancelled` on phase.
- **5cf gating:** opt-in `headless --format json --seams`; the default stream is unchanged (ADR 0076 A5 stays true by default).
- **5cf shape:** ONE kind `seam_closed` (data `seam`, `fired`) mirroring `domain.SeamClosedEvent`; `Kinds()` 18 → 19.
- **7ui:** `ci.yml` runs `make test` with `APOGEE_TEST_SHARDS=2`; the measured CI run is an owner-run check after the next push, recorded on the bead.
- **ed5:** pprof labels for leak attribution; `t.Setenv` neutralised in `TestMain`; `t.Parallel` sweep in `cmd/apogee` only — `internal/tui` becomes a follow-up bead.

**Regression check (2026-09-13, 6d56a51e):**
- 1: guard folded — sub-agent lines at Depth 1 (sink test stamps Depth 1); `TestHeadlessComposesTheRunnerSpec` / `TestHeadlessFormatJSONEncoderIsOutermost` named; `StripEscapesToLine`; `headless.md:117–121`; prune line unchanged at every depth — yields to ADR 0075 D6.
- 2: recast — `data.seam` = the closing-notice name via `Moment.Closing()`; `cmd/apogee/headless_test.go` in Files, `TestHeadlessFormatJSON` in Acceptance; `TestEncodeSkipsTheSeamClosedEvent` named as replaced; the forward to inner is unconditional; count/sink-only sweep by grep — ADR 0076 A5 (`docs/adr/0076-…md:274–275`) and `internal/domain/events.go:328–330` superseded for the opt-in stream only.
- 3: guard folded — assertions use the closing-notice spelling.
- 4: guard folded — the bead note says the owner-run measurement is taken after item 7 lands.
- 5: guard folded — `assertNoAmbientApogeeConfig` takes `testing.TB`; "set" is `os.Getenv(name) != ""`.
- 6: guard folded — frame constants lose the `(` suffix (debug=1 `pkg.func+0x` form); timer-started goroutines are outside the check, said in What and in test-drivers.md.
- 7: recast — `driveConfigWatch` no-op, fast `watchTiming` once in `TestMain`; transitive exclusion rule; `docs/design/test-drivers.md:857–858`, `:899–900` superseded; script/Makefile edits written against item 5's text.
- re-check 2: SAFE.
- re-check 7: guard folded — Setenv/Chdir exclusion made transitive over the nine `t.Setenv` helpers (`guardHome`, `presentRemote`, …); test-drivers.md rewrite covers the whole `:899–912` paragraph plus rule 9; building.md edit written against item 4's text.

**Standing requirements:**
- `skills: coding-standards`
- Any authorized deviation from item text lands as a dated NOTES line under the item.
- Beads: item 1 closes apogee-czf; items 2–3 close apogee-5cf; item 4 closes apogee-7ui (with the owner-run note); items 5–7 close apogee-ed5 and file its `internal/tui` follow-up. Closing happens at closeout, with the CHANGELOG entries.

**Out of scope:** streaming assistant text or tokens to stdout; a `--quiet` for the narration; the `internal/tui` `t.Parallel` sweep (follow-up bead, item 7); any change to ADR 0076 A5's default; apogee-6fp, apogee-rw6, apogee-zci and every bench/owner-run/grill bead.

## 1. Headless live narration on stderr (apogee-czf) — ✅ DONE (2026-09-13)

NOTES (2026-09-13): `TestE2ENamingReachesAHeadlessSubAgentLine` (cmd/apogee/e2e_naming_test.go) went red — its whole-stderr `strings.Contains(errOut, namingTask)` absence check now trips on the narration's legitimate `→ sub_agent <task>` call line; narrowed to the `sub-agent:` line itself, which is the claim the test states (no task beside the generated name). Not in the plan's Files; not a regression of the claim.
NOTES (2026-09-13): consequential edit — docs/adr/0075-the-headless-event-stream-is-a-versioned-driver-protocol.md: made necessary by the `pruneNoticeSink` → `narrationSink` rename and the narration joining D6's `--format json` exception (one parenthetical after D6's "one exception" sentence; the historical context at :12 left as written).
NOTES (2026-09-13): consequential edit — internal/run/doc.go: made necessary by the rename ("headless wires only its own prune-notice sink, which renders domain.PruneEvent and nothing else" was no longer true).
NOTES (2026-09-13): consequential edit — internal/tui/transcript_test.go: made necessary by the rename (a comment naming `cmd/apogee's pruneNoticeSink`).
NOTES (2026-09-13): the bead's "prints nothing until done" premise was updated with `bd update apogee-czf --append-notes` (the plan's What asks for it); `.beads/issues.jsonl` is that write's auto-export and rides in FILES so the committed fallback matches the register.
NOTES (2026-09-13): the summary and the error's first line are clipped through the existing `clipSubAgentTask` (`headlessTaskMax` = 80 runes, ellipsis inside the cap) rather than a second 80-rune clip — one width for every label this Driver prints; an error result whose first line is empty prints `← <tool> error` rather than a colon with nothing after it; `TestNarrationSinkWordsTheResult` added beyond the plan's list to pin the three result shapes and the Depth-1 gate on tool lines.
NOTES (2026-09-13): `TestHeadlessNarratesToolCallsLiveOnStderr` drives the real `run.Once` against the scripted `eventlines.yaml` upstream over the real process streams (`captureProcessStreams`), so the order claim (call → result → summary) is measured against the engine, not a stub.

**What:** Generalise `pruneNoticeSink` (`cmd/apogee/headless.go:167–208`) into `narrationSink` — same shape (`inner`, `out`, `quiet`), same forwarding, same prune line — that also prints, as each Event arrives and only at `Depth == 0`:
- `domain.ToolCallEvent`: `→ <Call.Tool> <summary>`, where summary is the value of the first string-valued member of `Call.Arguments` in JSON order (decode with `json.Decoder` tokens to keep order), truncated to 80 runes with `…`; empty when there is none (then no trailing space). Escape-stripped through `sanitize.StripEscapes`.
- `domain.ToolResultEvent`: `← <tool> ok` or `← <tool> error: <first line of Result.Content>` (same strip/truncation). The tool name is looked up from the `ToolCallEvent` with the same id, remembered in the sink; an unmatched result prints `← <CallID>`.
- `domain.SubAgentPhaseEvent`: `sub-agent <name>: started|finished|cancelled` (`Cancelled` wins over the phase word). `<name>` is the `name` argument of the spawning `sub_agent` call (matched by `Call.ID == CallID`), replaced by a later `SubAgentNamedEvent` name; falls back to the CallID.
The sink is a value type today; it now holds a map, so install it as a pointer (`&narrationSink{...}` at line 823). `quiet` still silences everything under `--format json`. Update the sink's doc comment, the manual's text-format section (`docs/manual/headless.md:50–62`: narration is live, stdout is unchanged) and the bead's "prints nothing until done" premise. `Depth > 0` events print nothing — the sub-agent lines stand in for them.
**Regression guard.** Depth gating is per line family: tool call/result lines print at `Depth == 0`; the `sub-agent …: started|finished|cancelled` line and the `SubAgentNamedEvent` rename print at `Depth == 1` — the engine stamps the CHILD's identity (`base.Depth++`, `internal/agent/dispatch.go:488–489`, `:511–512`), matched by `EventBase.CallID == the Depth-0 sub_agent Call.ID`; the sink test stamps its phase/named events Depth 1. The prune line keeps printing at every depth, unchanged (`headless.go:201–203` has no depth check; `PruneEvent` doc `internal/domain/events.go:359–361`) — only the three new line families are depth-gated. The summary, the result's first line and the sub-agent name are stripped with `sanitize.StripEscapesToLine` (`StripEscapes` keeps `\n`, `internal/sanitize/sanitize.go:36`). The pointer install turns two green tests red: `TestHeadlessComposesTheRunnerSpec` (`headless_test.go:612`) asserts `cfg.Events.(*narrationSink)` and `TestHeadlessFormatJSONEncoderIsOutermost` (`:2482`) compares `reflect.TypeOf(&narrationSink{})`. The manual edit also covers `docs/manual/headless.md:117–121` ("the prune notice and the live narration go quiet"). The item yields to ADR 0075 D6 (`docs/adr/0075-…md:102–105`): stderr is outside its contract, the prune line's behaviour is unchanged, and the narration joins the prune notice's `--format json` exception.
**Files:** `cmd/apogee/headless.go`, `cmd/apogee/headless_test.go`, `docs/manual/headless.md`
**Tests:** rename `TestPruneNoticeSinkForwardsEveryEvent` → `TestNarrationSinkForwardsEveryEvent` (keeps the pinned prune wording); `TestHeadlessComposesTheRunnerSpec` and `TestHeadlessFormatJSONEncoderIsOutermost` re-pointed at `*narrationSink` (see the guard); new `TestHeadlessNarratesToolCallsLiveOnStderr` — a scripted upstream that makes one tool call then answers, asserting stderr carries exactly `→ read_file <path>` then `← read_file ok` BEFORE the `wrote`/`turns:` block and stdout carries the answer alone (extend `TestHeadlessAnswerLandsOnTheProcessStdout`'s fixture); `TestNarrationSinkSummarisesTheFirstStringArgument` (table: path-first, no strings, > 80 runes, escape sequence); `TestNarrationSinkNamesTheSubAgent` (named by call arg, renamed by `SubAgentNamedEvent`, cancelled; phase/named fixtures stamped Depth 1 under the Depth-0 call's id); `TestHeadlessFormatJSONSuppressesTheNarration` (extend `TestHeadlessFormatJSONSuppressesThePruneNotice`).
**Acceptance:** `go build ./... && go test -race -count=1 -run 'TestHeadless|TestNarrationSink|TestPruneNotice' ./cmd/apogee/`
**Commit:** `feat(headless): narrate tool calls, results and sub-agent phases live on stderr under --format text`

## 2. `seam_closed` line kind in eventjson (apogee-5cf, part 1) — ✅ DONE (2026-09-13)

NOTES (2026-09-13): consequential edit — internal/tui/fold_test.go: made necessary by the seam closure no longer being sink-only on the headless stream (comment on the "SeamClosedEvent is inert in the view" fold case reworded; no assertion changed)
NOTES (2026-09-13): consequential edit — docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md: made necessary by `eventjson.Encode` now mapping the event — A5's "sink-only: `Encode` returns `ok=false`" sentence gains a dated parenthetical recording the opt-in kind and that the default stream is unchanged
NOTES (2026-09-13): `TestWriterForwardsEveryEventToInner` compares with `reflect.DeepEqual` instead of `!=` — a `SeamClosedEvent` holds a slice and an `any`, so the interface `!=` it used would panic on the new entry
NOTES (2026-09-13): `TestEncodeSkipsAnUnknownEvent` left as is — it already used `Encode(nil)`, not `SeamClosedEvent`

**What:** Recast at the regression check (2026-09-13). `internal/eventjson`: add `kindSeamClosed = "seam_closed"` to `Kinds()` (after `audit`, before the frames — 19 kinds) and an `Encode` arm for `domain.SeamClosedEvent` with data `{seam: string(ev.Seam.Closing()), fired: []string}` (`fired` never null — `[]` when empty; `Value` is NOT encoded). Add `Options.Seams bool` (`writer.go:29–33`); the Writer's wrapping sink drops a `SeamClosedEvent` unless `Seams` is set — gating lives in the Writer, `Encode` stays a pure mapping. Update `Encode`'s doc comment (lines 64–70) so `SeamClosedEvent` is no longer listed as skipped, and the manual's kinds table (`docs/manual/headless.md:150–178`, heading "The eighteen line kinds" → nineteen) with one `seam_closed` row noting it is opt-in (item 3 documents the flag). `TestManualListsEveryEventLineKind` (`cmd/apogee/docs_eventlines_test.go`) must pass without change.
**Regression guard.** `data.seam` carries the CLOSING-NOTICE name (`pre-request-finished`, `post-response-finished`, `pre-tool-exec-finished`, `post-tool-result-finished`, `history-rewrite-finished`) — derive it from the event's Seam via `Moment.Closing()` if the event carries the seam Moment rather than the notice; item 2's manual row, table and item 3's test assertions all use that spelling. Item 2's Acceptance additionally runs `go test -race -count=1 -run 'TestHeadlessFormatJSON' ./cmd/apogee/` and item 2's Files gain `cmd/apogee/headless_test.go` for whatever adjustment `TestHeadlessFormatJSONStreamsEveryEvent` needs so no cmd/apogee test is left red between items 2 and 3. Further: the Writer writes no LINE for a `SeamClosedEvent` unless `Seams` is set — the forward to inner is unconditional, because the Reaction Runner beneath the Writer builds the five closing notices from that very event (`internal/reactions/match.go:163–176`). `TestEncodeSkipsTheSeamClosedEvent` (`internal/eventjson/encode_test.go:341–357`) pins `ok=false` and is deleted/replaced by `TestEncodeSeamClosed`. The count/sink-only sweep is rule + grep, not a closed list: `grep -rn -i 'eighteen\|sixteen\|sink-only' internal/eventjson internal/domain/events.go cmd/apogee/headless_test.go docs/manual/headless.md` — every count and every sink-only claim naming the seam closure is rewritten (`internal/eventjson/encode.go:9`, `doc.go:13`, `docs/manual/headless.md:136`, `:152`, `cmd/apogee/headless_test.go:2389`, `internal/domain/events.go:328–330`). This supersedes, for the opt-in stream only, `internal/domain/events.go:328–330` and ADR 0076 A5 (`docs/adr/0076-…md:274–275`: "sink-only: `eventjson.Encode` returns `ok=false` for them") — the default stream still carries no `seam_closed` line, so A5 stays true by default (ratified call).
**Files:** `internal/eventjson/encode.go`, `internal/eventjson/writer.go`, `internal/eventjson/doc.go`, `internal/eventjson/encode_test.go`, `internal/eventjson/writer_test.go`, `internal/domain/events.go`, `cmd/apogee/headless_test.go`, `docs/manual/headless.md`
**Tests:** `TestKindsAreEighteen` → `TestKindsAreNineteen`; `TestEncodeSeamClosed` replaces `TestEncodeSkipsTheSeamClosedEvent` (tabled over the five seam Moments of `internal/domain/reaction.go:30–34`, asserting `data.seam` is each one's `Closing()` name from `reaction.go:56–60`; empty `fired` encodes `[]`); `TestWriterDropsSeamClosedByDefault` (also asserts inner saw the event) and `TestWriterEmitsSeamClosedWhenSeamsIsSet` (the line still carries the envelope fields every other kind does); `TestWriterForwardsEveryEventToInner` gains a `SeamClosedEvent`; `TestEncodeSkipsAnUnknownEvent` re-pointed at `domain.WireEvent` if it used `SeamClosedEvent`; `TestHeadlessFormatJSONStreamsEveryEvent` (`cmd/apogee/headless_test.go:2390–2399`) filters `seam_closed` out of `want` — the default stream — and item 3 pins the `--seams` stream separately.
**Acceptance:** `go test -race -count=1 ./internal/eventjson/ && go test -race -count=1 -run 'TestManualListsEveryEventLineKind|TestHeadlessFormatJSON' ./cmd/apogee/`
**Commit:** `feat(eventjson): seam_closed line kind behind Options.Seams`

## 3. `headless --format json --seams` (apogee-5cf, part 2) — ✅ DONE (2026-09-13)

NOTES (2026-09-13): `--seams` is judged inside runHeadless's text branch, so `--format <unknown> --seams` still gets the `--format` refusal — the format is the larger mistake and the plan's "same class as the --format rejection" wording left the precedence open.

Depends on item 2.
**What:** Add the `--seams` bool flag next to `--format` (`cmd/apogee/headless.go:340`), help text `also emit seam_closed lines (--format json only)`; set `eventjson.Options{Seams: seams}` at the `eventjson.New` call (line 390). `--seams` without `--format json` is a usage error in the same class as the `--format` rejection at line 400 (exit 2, no run started), worded `apogee headless: --seams needs --format json`. Document the flag in `docs/manual/headless.md` under "Machine-readable output" (a short paragraph: what it adds, why it is off by default — volume, ADR 0076 A5 — and the five seam names) and in the `seam_closed` table row from item 2.
**Regression guard.** Assertions use the closing-notice spelling per the item-2 decision.
**Files:** `cmd/apogee/headless.go`, `cmd/apogee/headless_test.go`, `docs/manual/headless.md`
**Tests:** `TestHeadlessSeamsEmitsSeamClosedLines` — a scripted run with one tool call under `--format json --seams` yields `seam_closed` lines for `pre-tool-exec-finished` and `post-tool-result-finished` and the run frames still close; `TestHeadlessFormatJSONOmitsSeamClosedByDefault`; `TestHeadlessSeamsWithoutJSONIsAUsageError` (exit 2, exact message, `Turns == 0` path — extend `TestHeadlessUsageErrorsNeverStartARun`).
**Acceptance:** `go test -race -count=1 -run 'TestHeadlessSeams|TestHeadlessFormatJSON|TestHeadlessUsageErrors' ./cmd/apogee/`
**Commit:** `feat(headless): --seams opts the seam_closed lines into --format json`

## 4. CI runs the sharded suite (apogee-7ui) — ✅ DONE (2026-09-13)

NOTES (2026-09-13): consequential edit — .beads/issues.jsonl: made necessary by the `bd update apogee-7ui --notes` the item prescribes (the passive export picked the note up; its diff is the one bead row).

**What:** `.github/workflows/ci.yml:51–54`: the "go test (race)" step becomes `run: make test` with `env: APOGEE_TEST_SHARDS: 2` and a comment stating the runner size (4 vCPU / 16 GB) and why the cap (race-enabled binaries' memory). Leave the second race step (`platform`/`probe`, line ~151) as is. `docs/manual/building.md:79–80`: replace "CI still runs that form" with "CI runs `make test` under `APOGEE_TEST_SHARDS=2`". Record on the bead (`bd update apogee-7ui --notes`) that the measured wall-time/OOM check is an owner-run pass after the next push; the bead closes at closeout with that note.
**Regression guard.** The bead note states the owner-run OOM/wall-time measurement is taken AFTER item 7 lands (the two changes land together on the runner: 5 `go test` processes plus per-shard parallel TUI programs).
**Files:** `.github/workflows/ci.yml`, `docs/manual/building.md`
**Tests:** none in Go — the change is the runner itself.
**Acceptance:** `make actionlint && APOGEE_TEST_SHARDS=2 ./scripts/test-shards.sh` (this item's change IS the suite runner, so one local run under the cap is its targeted check; expect all packages green).
**Commit:** `ci: run the sharded suite (make test) with a two-shard cap`

## 5. Neutralise `APOGEE_*` once in `TestMain` (apogee-ed5, part 1)

**What:** `cmd/apogee/main_test.go` `TestMain` (line 42): after the HOME override, `os.Unsetenv` the seven vars `config.EnvConfig, EnvServer, EnvEndpoint, EnvModel, EnvMode, EnvBypass, EnvWorkspace`. `assertNoAmbientApogeeConfig` (`e2e_support_test.go:532–544`) becomes a pure assertion: `t.Fatalf` if any of the seven is set, no `t.Setenv`. Tests that deliberately `t.Setenv` an `APOGEE_*` var (`headless_test.go`, `root_test.go`, `undo_test.go`, `docs_env_test.go`, `e2e_naming_test.go`, `keymigrate_test.go`, `e2e_fillnotice_test.go`, `e2e_reactions_test.go`) are untouched and stay serial. Update the header comment of `scripts/test-shards.sh` (lines 5–16) and the `Makefile` `test` comment (170–176): the `t.Setenv` constraint is gone; the leak-check constraint remains until item 6.
**Regression guard.** `assertNoAmbientApogeeConfig` (`cmd/apogee/e2e_support_test.go:532`) widens its signature to `testing.TB` — every caller passes a `*testing.T` — so `TestAssertNoAmbientApogeeConfigFailsWhenSet` can drive it with the recording double; "set" is spelled `os.Getenv(name) != ""`, as `applyEnv` (`internal/config/config.go:2809–2812`) and today's `EnvConfig` check already read empty as unset.
**Files:** `cmd/apogee/main_test.go`, `cmd/apogee/e2e_support_test.go`, `scripts/test-shards.sh`, `Makefile`
**Tests:** `TestAmbientApogeeConfigIsClearedForTheSuite` — asserts the seven vars are unset at test start; `TestAssertNoAmbientApogeeConfigFailsWhenSet` — `t.Setenv(config.EnvMode, "auto")` then the helper against a recording `testing.TB` double reports a fatal.
**Acceptance:** `go vet ./cmd/apogee/ && go test -race -count=1 -run 'TestAmbient|TestAssertNoAmbient|TestE2E' ./cmd/apogee/`
**Commit:** `test(cmd/apogee): clear ambient APOGEE_* once in TestMain so launch helpers no longer t.Setenv`

## 6. Per-test leak attribution with pprof labels (apogee-ed5, part 2)

**What:** `internal/tuitest/leak.go`: `CheckLeaks(t)` mints a per-call id, labels the calling goroutine with `pprof.SetGoroutineLabels(pprof.WithLabels(context.Background(), pprof.Labels("tuitest.leakcheck", id)))` — every goroutine the test starts from then on inherits it, the program's included — and its cleanup reports any goroutine still carrying that label after `leakGrace`, read from `pprof.Lookup("goroutine").WriteTo(&buf, 1)` (the debug=1 form carries `# labels:` lines). The cleanup first clears its own labels. The inherited-snapshot/`startedSince` diff goes away (an inherited goroutine never carries a fresh label); `leakMarkers` and `timerFrames` stay as the second filter (the marker filter now matches the debug=1 `pkg.func` lines). Keep the signature `CheckLeaks(t testing.TB)` and the failure wording `%d goroutine(s) outlived the test by %s`. Binding standard: one deep module — the profile parsing is a private func over `[]byte`, so `leak_test.go` drives it with canned profiles. Update the `CheckLeaks` paragraph in `docs/design/test-drivers.md:362–368`.
**Regression guard.** The three frame constants (`checkerFrame` `leak.go:56`, `harnessFrames` `:60`, `timerFrames` `:63`) lose their `(` suffix and match the debug=1 profile's `pkg.func+0x…` form (runtime/pprof prints `charm.land/bubbletea/v2.Tick.func1+0x..`, never `name(...)`) — otherwise the 10 s heartbeat tick (`internal/tui/heartbeat.go:217`) outlives `leakGrace` on every driven run. `leak_test.go`'s canned profiles are real debug=1 excerpts, the Tick case in the `+0x` form, so the "Tick timer frame ignored" test bites. Reach limit, stated in What and in the `test-drivers.md:362–368` paragraph: the check covers goroutines started from the test goroutine; a `time.AfterFunc`-started one carries no label (`internal/tui/sink.go:100`'s `closeWindow`) and is outside the check.
**Files:** `internal/tuitest/leak.go`, `internal/tuitest/leak_test.go`, `docs/design/test-drivers.md`
**Tests:** rewrite `leak_test.go` against the new parser: labelled stack found, unlabelled ignored, `Tick` timer frame ignored, two concurrent leak-checked subtests (each with `t.Parallel()`, one leaking a labelled goroutine) — only the leaker fails (use a recording `testing.TB` double); existing `driver_test.go` / `pty_test.go` callers pass unchanged.
**Acceptance:** `go test -race -count=1 ./internal/tuitest/ && go test -race -count=1 -run 'TestE2E' ./cmd/apogee/`
**Commit:** `test(tuitest): attribute leaked goroutines per test with pprof labels`

## 7. `t.Parallel` sweep over the `cmd/apogee` e2e tests (apogee-ed5, part 3)

Depends on items 5 and 6.
**What:** Recast at the regression check (2026-09-13). Add `t.Parallel()` as the first statement of every `Test*` in `cmd/apogee/*_test.go` that calls one of `launchTUI`, `launchTUIConfigured`, `launchTUIIn`, `launchTUIOn`, `startSession`, `launchPTY`, `launchPTYWithEnv` (or `CheckLeaks`) AND is not excluded by this rule: stays serial any test whose body calls `t.Setenv`/`os.Setenv`/`t.Chdir`/`os.Chdir`, or assigns a package-level variable (grep `^\t(runOnce|discoverBeat|[a-zA-Z]+) = ` inside test funcs and every `t.Cleanup(func() { X = ` restore pattern — enumerate the package vars `cmd/apogee` tests reassign before editing). A subtest under a parallel parent that shares the parent's fixture stays serial within it. Update `docs/manual/building.md:59–80` (the "cannot be `t.Parallel`" claim at 61–63; keep the sharding paragraph — shards still bound `internal/tui`), the `scripts/test-shards.sh` header and `Makefile` comment (drop the leak-check constraint; sharding is now for `internal/tui` and cross-package balance). File the follow-up: `bd create --title="internal/tui: t.Parallel sweep over the driver tests" --type=task --priority=3` referencing apogee-ed5 and this plan. Record before/after `go test -race -count=1 ./cmd/apogee/` wall time as a NOTES line.
**Regression guard.** (a) `driveConfigWatch` becomes a no-op and the fast `watchTiming` is set ONCE in `TestMain` (cmd/apogee/main_test.go, beside the HOME override) — production never reassigns the seam (wire_live.go:34–44); (b) the exclusion rule is TRANSITIVE: first enumerate every package-level var any `cmd/apogee/*_test.go` helper reassigns (grep every `_test.go` for `^\t<var> = ` and `t.Cleanup(func() { <var> = ` — the known ones are `configWatchTiming`, the schedule clock swapped by `useFakeScheduleClock`, the launcher swap in e2e_livestate_test.go, `runOnce` in `headlessEventLines`), then exclude every Test that reaches such a helper directly or through another helper; state that after (a) the in-process launch path itself reassigns nothing, so launches are parallel and only tests reaching a swapping helper stay serial; (c) `docs/design/test-drivers.md` lines 857–858 and 899–900 (the e2e tests recorded as serial by design) are superseded by this item — add the file to item 7's Files and a sentence in What that rewrites those passages to the new rule (parallel by default; serial only for tests that reach a seam-swapping helper or Setenv); (d) item 7's edits to the scripts/test-shards.sh header and the Makefile comment are written against item 5's text, not BASE (state it in What). (e) Re-check (2026-09-13): the `t.Setenv`/`t.Chdir` exclusion is TRANSITIVE exactly like (b) — first enumerate every `_test.go` helper calling `t.Setenv`/`os.Setenv`/`t.Chdir` (`grep -n 't.Setenv\|os.Setenv\|t.Chdir' cmd/apogee/*_test.go` → `guardHome`, `presentDesktop`, `presentRemote`, `headlessRunOn`, `headlessRunUnderEnv`, `headlessFillNotice`, `headlessAgainst`, `headlessHooksIn`, `driveInterruptedHeadless`), then keep serial every Test reaching one directly or through another helper (`TestE2EApprovalForcesALookAtTheControlPlane`, `TestE2EApprovalForcedLookSurvivesAutoMode`, `TestJudgeForcedApprovalPaneReadsAsHelp`, `TestE2EAnnounced…` via `guardHome`; `TestE2EPresentServesWithoutLeakingTheToken` via `presentRemote`) — `t.Parallel()` after a helper's `t.Setenv` panics. (f) The `docs/design/test-drivers.md` rewrite covers the WHOLE serial-budget paragraph (`:899` to its end, ≈ `:912`; the G: names 899–905) and rule 9 at `:857–858` — the new rule plus the before/after wall time the NOTES line records, so no sentence still announces a serial budget. (g) Item 7's `docs/manual/building.md:59–80` edit is written against item 4's text (item 4 rewrites lines 79–80), exactly as (d) says for the script header and Makefile comment.
**Files:** `cmd/apogee/*_test.go` (the e2e test files only), `cmd/apogee/main_test.go`, `cmd/apogee/e2e_support_test.go`, `docs/manual/building.md`, `docs/design/test-drivers.md`, `scripts/test-shards.sh`, `Makefile`
**Tests:** the sweep itself; no new tests.
**Acceptance:** `go test -race -count=1 ./cmd/apogee/` green three times in a row (`-count=3` is fine), wall time below the 168 s serial baseline; `go vet ./cmd/apogee/`
**Commit:** `test(cmd/apogee): run the e2e tests in parallel`
