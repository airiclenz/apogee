# 2026-09-19 - 01 - ten urgent beads

**Goal:** close the ten most urgent open beads — the defects (`apogee-2un` Stage A, `apogee-tfp`, `apogee-zuu`, `apogee-9se`, `apogee-clb`), the shipped-plan residual (`apogee-bxi`) and four sanctioned small surfaces (`apogee-3b3`, `apogee-1d8`, `apogee-rw6` `/help` only, `apogee-d6t`). Every design call was grilled with the owner on 2026-09-19 and is binding below.
**Date:** 2026-09-19 · **Status:** unexecuted · **sized for:** ~200k-context host · **Base commit:** dd98567a

**Sources:** `bd show <id>` per bead; docs/adr/0007, 0013, 0017, 0022, 0025, 0034, 0039, 0046, 0071, 0076 (+ 2026-09-19 addendum), 0077; CONTEXT.md entries Exchange, Turn, Step cap, Reaction, Session, Delegation; docs/manual/{commands,configuration,reactions,sessions}.md; layout.md.

**Ratified design calls (owner, 2026-09-19):**
- **Urgency:** defects and shipped-plan residuals first; bench, other-OS and grill-gated beads out; `apogee-37s` dropped for `apogee-2un`.
- **2un:** Stage A only — the host *settles* a cancelled Exchange (finished Turns stay; `endCancelled` already drops the in-flight Turn); the cut is a committed engine note on the last tool result: `[engine — cancelled] the user stopped the run here; the results above stand, the reply was not given [end engine — cancelled]`; cancel, restored-session submit and `foldLoopError` settle, `/clear` still aborts. Stage B stays on the bead.
- **tfp:** trigger = `FinishLength` ∧ no tool calls ∧ empty text ∧ reasoning present; one identical re-send at 2× the cap sent (unclamped), before the post-response cascade; own `capRetrySpent` latch; `StreamResetEvent` for the TUI; supersedes ADR 0046 D4's "no retry".
- **zuu:** a structural token-budget engine note, twin of the step notice, at 75 % of `delegate-max-tokens` (default stays 20M); not a ladder rung.
- **9se:** keyed servers (`api-key`/`api-key-cmd`/`api-key-env`, `wire: anthropic`) default `parallel-agents` 4; discovery outranks it; pin 1 wins; amends ADR 0039 D2.
- **clb:** an engine note `[engine — delegations]` on every request tail once the Exchange holds ≥2 delegations or any non-completed one; no tool, no key, on under Bypass.
- **3b3:** `platform.AcquireLock` on `<sessions>/<id>.lock`; every door (`--resume`, `--continue`, `/sessions` resume and delete) refuses with `session <id> is open in another apogee (pid N) — fork it to work alongside`; `--continue` never skips to another session.
- **1d8:** `advise:`/`gate:` accept the `run:` webhook mapping; body = seam payload; reply body = advice text / `allow|deny|ask` first line; advise fails open, gate escalates to ask; no URL guard (parity with observe).
- **/help:** a transcript note from `commandSpecs` plus a fixed key legend; `whileRunning`, `noRecall`.
- **d6t:** a fourth report pane `/advice` through `popup.go` like `/thinking`; whole session by Turn, fenced text shown; ADR 0076 D6 dated note.
- **VERSION:** no version items; the owner bumps at closeout.

**Regression check (2026-09-19, dd98567a):**
- 2: guard folded — `loop_test.go` rewrite; only the reasoning rows carry the retried tail; ADR 0046 D4 and its Rejected bullet superseded.
- 3: guard folded — every `rearmStepNotice`/`stepNoticeLive` site gains the twin (compact.go, prune.go); the ≥ 1M fill notice reads `1.3M`; the token note lands beside the step note in `appendToolResult`.
- 4: guard folded — `wire: anthropic` keyed by wire, not by construction; `floor < 1` reads 1 with `observe`/`current` reading it; keyed e2e arm asserts both rows standing; the launcher-built loopback entry stays at 1; item 4 owns the code comments.
- 5: guard folded — the wider grep is the sweep; prose only; template goldens dropped; ADR 0039 D2 superseded by the dated note.
- 6: guard folded — keep test is a RoleTool message past `exchangeStart`; the note is ephemeral (no cut marker in the record); `state_test.go` and a prompt asset.
- 7: guard folded — `lateEngine.SettleExchange`; both announced strings stated; the record clause moves to item 6; comment rule over `AbortExchange|scrapped|scraps`.
- 8: decision folded — the ADR 0022/0025 notes and the manual say the marker is live-session only; item 8 amends item 6's CONTEXT sentence.
- 9: guard folded — one deferred append; spawn index at start under the mutex; `sub.outputTarget` recorded; delegations note stamped before the wrap-up.
- 10: recast — hold at the first Save and on a resumed record; doors probe-and-release; Delete/Prune release before removing the lock, superseding internal/platform/lock.go:75-77; no Firing hold; `session.HeldError`.
- 11: recast — the hold is parked in `sessionHost.Load` and adopted by `Activate`; refusal is a Load error; `foldRecordWrite` notes a delete error; ADR 0022 rule.
- 12: recast — halves swapped: domain and agent run a `WebhookHandler`; seam closure routes both out-of-process kinds; layering checked by `go list`; no `CapAdvice`; internal/reactions/doc.go:12-14 superseded.
- 13: recast — halves swapped: config accepts the mapping plus manual, ADR 0076 D8 note and CONTEXT; comment and manual rules.
- 14: guard folded — the newline chord follows the editor's idle legend; the stop is `esc×2 stop`.
- 15: recast — `framePaneSet` widened to uint16 with a guard; advice-pane key claimant and pointer pane; Action `notice` folded; rows keyed by run; per-frame wrap; fold_test.go prose amended.
- 16: guard folded — the verb re-opens as `/thinking` does, no toggle; the `/settings can show` sentence swept by rule.
- 10 (re-check): guard folded — Activate/Save of the id the host holds keeps the live hold (in-process flock refuses with its own pid); Store.Hold MkdirAll-then-AcquireLock and resolveResumeArg probes the id Load RETURNED; `sessionRecordsIn` counts `.json` only; Delete removes `<id>.lock` on every path and validateID-checks first; ADR 0022 D7 (`docs/adr/0022…:106-109`) superseded by this item's commit, the note rides item 11.
- 11 (re-check): decision folded — the hold guards only against OTHER instances: Load neither probes nor parks an id this host holds, Activate keeps it, Delete releases a matching unadopted hold; the ADR 0022 note states the parked hold's lifetime; builds on item 10's landed Activate/Load.
- 12 (re-check): guard folded — the `async && !servesClass` branch stays, reworded to `observe, advise or gate`, with the engine-origin shape case pinned; the runnable key derives from r.Class so the observe `run: url:` pins stay byte-identical; yields to docs/design/confinement-execution-contract.md:1267-1270 (the permit row gains the no-permit sentence); internal/reactions/doc.go names `internal/userexec` too.
- 13 (re-check): guard folded — the docs rule widens to `grep -rn 'argv only\|argv list\|commands that advise' README.md docs/ CONTEXT.md`; README.md and docs/design/reaction-core-greenfield.md join Files.
- 15 (re-check): guard folded — paneAdvice joins transcriptSlotPanes after paneThinking and the acceptance run widens to the slot/claim/pointer/overlay pins; the board is capped (256, oldest dropped); one two-level row shape (`[<run label> · ]turn N` heading, then `<reaction> (<origin> origin) @ <moment>` + Detail); the "three reports" prose swept by rule.

**Standing requirements:**
- `skills: coding-standards`
- Each item names its bead; beads close and CHANGELOG entries land at closeout, never mid-run.
- A deviation from item text lands as a dated `NOTES` line under the item.
- An item that supersedes an ADR sentence greps the quoted phrase across `docs/`, `CONTEXT.md`, `internal/`, `cmd/` and amends every hit.

**Out of scope:** `apogee-2un` Stage B; `apogee-37s`; a URL guard for any webhook class; lowering `defaultDelegateMaxTokens`; a per-call absolute context notice; `apogee-rw6`'s other rows; every bench-gated, other-OS or grill-gated bead.

## 1. demorig: verify the ffmpeg/ffprobe/gifsicle adapters on this host (apogee-bxi)

**What.** Verification; no code unless it fails. `sudo apt-get install -y gifsicle`; run the gated `TestFfmpegAdapters` (`cmd/demorig/align_test.go`); then one real render with a synthesised take: `S=$(mktemp -d); ffmpeg -v error -f lavfi -i color=c=black:s=2500x1360:r=24:d=3.4 -f lavfi -i testsrc=s=2500x1360:r=24:d=86.6 -filter_complex "[0][1]concat=n=2:v=1:a=0" -pix_fmt yuv420p $S/take.mp4 && go run ./cmd/demorig render graphics/demo/storyboards/hero.yaml $S/take.mp4 cmd/demorig/testdata/session-hero.json -o $S/hero.gif`. Expect the summary line `<path>  <size>  <secs>s`, a 1250 px-wide GIF, no `warning:` on stderr, `optimizeGIF` taken. Record tool versions in a dated `NOTES` line here. On failure fix the adapter (`align.go`/`filtergraph.go`/`render.go`, golden `cmd/demorig/testdata/filtergraph-hero.txt`).
**Files:** cmd/demorig/align.go, cmd/demorig/filtergraph.go, cmd/demorig/render.go (only on failure); this plan's NOTES line.
**Read first:** cmd/demorig/align_test.go — TestFfmpegAdapters; cmd/demorig/align.go — ffmpegFirstPainter.FirstPaint, takeDuration; cmd/demorig/render.go — renderTake, optimizeGIF; cmd/demorig/filtergraph_test.go — TestRenderCommand_DryRun; graphics/demo/storyboards/hero.yaml — frame, align
**Tests.** `TestFfmpegAdapters` un-skipped; `TestRenderCommand_DryRun` green.
**Acceptance.** `which gifsicle && go test ./cmd/demorig/ -run 'TestFfmpegAdapters|TestRenderCommand' -v 2>&1 | grep -q '^--- PASS: TestFfmpegAdapters'`; the render above exits 0 and `ffprobe -v error -show_entries stream=width -of csv=p=0 $S/hero.gif` prints `1250`.
**Commit:** `test(demorig): verify the ffmpeg, ffprobe and gifsicle adapters against the live tools`

## 2. Retry a capped no-text reply once at a raised cap (apogee-tfp)

**What.** Defect: a reasoning model spends the cap thinking (30a3b2df), the reply is `FinishLength` with no text, the Turn faults although an identical retry succeeds. In `respondAndReview` (`internal/agent/loop.go`) after `assembleResponse` and before `a.fire(MomentPostResponse…)`: when the ratified trigger holds and `!t.capRetrySpent`, set the latch, emit `domain.StreamResetEvent`, re-send the identical `req` with `req.SetSampling(MaxTokens: 2×<cap sent>)` (unclamped; overrides a hook-set value as `newProjection` does) and continue with the new reply. `capRetrySpent bool` joins `turnRun` (`turn.go`) beside `restreamSpent`, its own budget. Thread the applied cap into `emptyReplyFault`; `cappedReplyErrFmt`'s tail becomes `— retried once at %d tokens and hit the cap again`. `cappedDelegateReplyErrFmt` and compaction's capped-summary path stay no-retry. Bypass-safe. ADR 0046: dated note under D4 replacing "no retry Mechanism", the Rejected "Retrying or continuing a reply that hit the cap" bullet struck with the date, the 2026-09-15 note amended; fix the `cappedReplyErrFmt`/`reviewedOutcome` comments and CONTEXT.md's output-cap entry.
**Regression guard.** `internal/agent/loop_test.go` `TestCutOffReplyLeavesTheRetryToTheReader` scripts ONE `cutOffScript` and pins the old tail (`… a retry may succeed on a reasoning model`, loop_test.go:56): rewrite it to script two cut-off replies and pin the new tail, or fold it into `TestCappedReasoningReplyFaultsNamingTheRaisedCap` and delete it. In `TestCutOffReplyNamesTheOutputCap` only the reasoning rows (1 and 3, depth 0 and 1) are rewritten to two cut-off scripts / 2 provider calls / doubled `MaxTokens`; the fault keeps `raise max-output-tokens: for this server or narrow the task` and appends `— retried once at %d tokens and hit the cap again` ONLY when the latch fired — row 2 (no reasoning) never retries and keeps today's wording. Supersedes ADR 0046 D4 (:78 "no retry Mechanism") and the Rejected bullet (:98) as the What says; CHANGELOG.md:419's "the engine still runs no retry of its own" is historical and stays.
**Files:** internal/agent/loop.go, internal/agent/turn.go, internal/agent/emptyreply_test.go, internal/agent/loop_test.go, docs/adr/0046-the-engine-bounds-every-reply-with-an-output-cap.md, CONTEXT.md
**Read first:** internal/agent/loop.go — respondAndReview, emptyReplyFault, cappedReplyErrFmt; internal/agent/turn.go — turnRun.restreamSpent; internal/domain/hooks.go — Request.SetSampling; internal/agent/emptyreply_test.go — cutOffScript, TestCutOffReplyNamesTheOutputCap; internal/agent/loop_test.go — TestCutOffReplyLeavesTheRetryToTheReader
**Tests.** `TestCutOffReplyNamesTheOutputCap`'s reasoning rows (1 and 3) script two cut-off replies (2 provider calls, doubled `MaxTokens`, the retried tail); row 2 unchanged; `TestCutOffReplyLeavesTheRetryToTheReader` rewritten to two cut-off replies and the new tail (or folded into `TestCappedReasoningReplyFaultsNamingTheRaisedCap`); new `TestCappedReasoningReplyRetriesOnceAtTwiceTheCap`, `TestCappedReasoningReplyFaultsNamingTheRaisedCap`, `TestCapRetryLatchIsSeparateFromTheReStreamLatch`, `TestCappedReplyWithoutReasoningDoesNotRetry`; `TestCappedReplyWithTextFaultsOnlyOnADelegate`, `TestEmptyReplyGuardYieldsToRecoveredRetry`, `TestReStreamLatchIsPerTurn` green.
**Acceptance.** `go build ./... && go test ./internal/agent/ -run 'CutOffReply|CappedRe|CapRetry|EmptyReply|ReStream' -count=1 && test "$(grep -c 'no retry Mechanism' docs/adr/0046*.md)" = 0`
**Commit:** `fix(agent): retry a capped no-text reasoning reply once at twice the cap`

## 3. Token-budget notice for bounded delegates (apogee-zuu)

**What.** Defect (session 20260918T143011Z): a child at 10.7M cumulative prompt tokens under a 20M cap, with a 1.3M window keeping the ADR 0077 ladder silent, hears nothing. Add `tokenBudgetNotice` beside `stepBudgetNotice` (`internal/agent/stepnotice.go`): `Message.WithEngineNote` topic `token budget`, depth ≥ 1, silent when `a.tokenCap == 0`, once on the tool result closing the Turn at which `a.usage.prompt >= ceil(tokenNoticeShare × a.tokenCap)`, `tokenNoticeShare = 0.75` beside `stepNoticeShare`; same latch and re-arm seams (`stepNoticeLive` siblings on `Agent`, `rearmNotices`, `HasEngineNote` by topic) and append site (`dispatch.go`). Asset `internal/agent/prompts/token-notice.txt`: `tokens: %s of %s spent — %s left before the wrap-up Turn; write your output now`; `formatTokens` (`fillnotice.go`) gains an `M` tier. On under Bypass, no key. ADR 0077 dated addendum (a second structural bound notice; ladder unchanged); `docs/manual/configuration.md` `delegate-max-tokens` entry and the template comment state the 75 % notice and name `working-window:` as the per-call lever; CONTEXT.md Step cap entry names both notices.
**Regression guard.** The token note lands in `appendToolResult` beside the step note so the pinned fence order (tool output, step note, token note, wrap-up last — `TestWrapUpDirectiveRidesTheClosingToolResult`) holds. Every site that calls `rearmStepNotice` or reads `stepNoticeLive` gains the token twin — `grep -rn 'rearmStepNotice\|stepNoticeLive' internal/agent/` — `compact.go` `foldFor` (:303, the re-arm after a fold that ran) and `prune.go` `autoPrune` (:58, the `HasEngineNote`-by-topic re-arm) included; `TestTokenNoticeIsToldAgainAfterAFoldSwallowedIt` cannot go green without them. `formatTokens` also renders the context-fill notice (fillnotice.go:77): the fill notice at a ≥ 1M window reads `1.3M` from now on, pinned by the `TestFormatTokens` M case.
**Files:** internal/agent/stepnotice.go, internal/agent/agent.go, internal/agent/dispatch.go, internal/agent/compact.go, internal/agent/prune.go, internal/agent/fillnotice.go, internal/agent/prompts/token-notice.txt, internal/agent/stepnotice_test.go, internal/agent/fillnotice_test.go, internal/domain/advice.go, docs/adr/0077-the-context-fill-notice-is-the-first-engine-advise-reaction.md, docs/manual/configuration.md, internal/config/defaults/config.yaml, CONTEXT.md
**Read first:** internal/agent/stepnotice.go — stepBudgetNotice, rearmStepNotice; internal/agent/dispatch.go — appendToolResult; internal/agent/agent.go — Agent.stepNoticeLive, Agent.tokenCap; internal/agent/compact.go — foldFor; internal/agent/prune.go — autoPrune; internal/agent/fillnotice.go — formatTokens
**Tests.** Twins of the step-notice suite: `TestTokenNoticeFiresOnceAtThreeQuartersOfTheCap`, `TestTokenNoticeIsSilentAtDepthZeroUnboundedAndUnderTheThreshold`, `TestTokenNoticeFiresUnderBypassAndIsNoBuiltin`, `TestTokenNoticeIsToldAgainAfterAFoldSwallowedIt`, `TestTokenNoticeIsToldAgainAfterAPruneStubbedIt`, `TestTokenNoticeRidesOneResultOfAManyCallTurn`; `TestFormatTokens` M case (the ≥ 1M fill notice reads `1.3M`); `TestStepNotice*`, `TestContextFillNotice*`, `TestSubAgent_TokenBudgetEndsTheChildThroughTheWrapUp`, `TestWrapUpDirectiveRidesTheClosingToolResult` green; template goldens regenerated if the comment moves.
**Acceptance.** `go build ./... && go test ./internal/agent/ ./internal/domain/ ./internal/config/ -run 'TokenNotice|StepNotice|ContextFillNotice|FormatTokens|TokenBudget|Template|Default' -count=1`
**Commit:** `fix(agent): a token-budget engine note tells a delegate at 75% of delegate-max-tokens`

## 4. Keyed servers default parallel-agents to 4 — resolver and call sites (apogee-9se)

**What.** Defect: fan-out ran serial for 15 h on a hosted server because "no signal means 1". `internal/config/config.go`: `const defaultKeyedParallelAgents = 4`; `DefaultParallelAgents(e ServerEntry) int` = 4 when `keySourceOf(e).kind() != keySourceNone` (`keyresolve.go`; `wire: anthropic` keyed by construction) else 1; `ResolveParallelAgents(pinned, discovered, floor int)` keeps rank pin ≥ 1 → discovered ≥ 1 → floor. `parallelAgentsCap` (`cmd/apogee/upstream.go`) gains a `floor` set in `follow`/`relist`; `delegation.go`, `wire_firing.go`, `probecontext.go` pass `DefaultParallelAgents(entry)`. Unkeyed LAN/loopback stays serial unless it advertises slots. Resolver and `ServerEntry` comments state the rule; docs are item 5.
**Regression guard.** `DefaultParallelAgents` returns 4 when `keySourceOf(e).kind() != keySourceNone || e.Wire == "anthropic"` — `wire: anthropic` is NOT keyed by construction (`ValidateServers` requires no key source on it; the codec sends no `x-api-key` when the key is empty), so the "keyed by construction" parenthetical is dropped from the item and the comment. `ResolveParallelAgents` treats `floor < 1` as 1 (the "anything below 1 falls through" posture its comment states) and `observe` and `current` read `c.floor` beside `follow`/`relist`: `newParallelAgentsCap`'s zero-value cap would otherwise hand the engine 0, and `current()` is the only route a Firing's `TotalSlots` (schedule.go) gets the session width by, its own entry's key fields being cleared (wire_settings.go). The keyed e2e arm asserts both delegation rows standing at once (the `TestE2EParallelDelegationsFollowAServerSwitch` shape — parallel-two-delegations.yaml emits exactly two `sub_agent` calls, so "4 in flight" is unobservable) and keys the entry with `api-key:` plus `plaintext-key-ok: true` (or `api-key-cmd:` via keysource_test.go's `keyCommandFor`) so the ADR 0047 migration offer stays quiet. A `/model` profile load builds its entry with the launcher's own api-key (launcher.go `cfg.APIKeyFor`): keep that launcher-built entry at floor 1 — either `DefaultParallelAgents` also requires a non-loopback endpoint (launcher.go's `ip.IsLoopback()` idiom), or the item states the keyed-loopback window as intended and item 5 rewrites configuration.md:1184. Ownership: item 4 owns every code comment in internal/config and cmd/apogee (config.go, upstream.go, upstream_test.go) that says an unpinned non-advertising server runs one delegation at a time; item 5 owns prose only (ADR, template, manual, CONTEXT) — the code-comment hits of item 5's wider grep outside those files (internal/domain/config.go `ParallelAgents` comment, internal/provider/discovery.go `Beat.TotalSlots` comment, cmd/apogee/headless.go discoverSlots comment) are amended here with them.
**Files:** internal/config/config.go, internal/config/keyresolve.go, internal/config/config_test.go, cmd/apogee/upstream.go, cmd/apogee/delegation.go, cmd/apogee/wire_firing.go, cmd/apogee/probecontext.go, cmd/apogee/headless.go (comment), internal/domain/config.go (comment), internal/provider/discovery.go (comment), cmd/apogee/upstream_test.go, cmd/apogee/e2e_parallel_test.go
**Read first:** internal/config/config.go — ResolveParallelAgents, ValidateServers; internal/config/keyresolve.go — keySourceOf; cmd/apogee/upstream.go — parallelAgentsCap, follow, current; cmd/apogee/e2e_parallel_test.go — parallelHome; cmd/apogee/launcher.go — profile-load move entry
**Tests.** `TestResolveParallelAgents` arms: keyed floor 4, keyed + discovery keeps discovery, keyed pin 1 stays 1, unkeyed non-loopback stays 1, floor 0 reads 1; new `TestDefaultParallelAgentsFollowsTheKeySource` (a keyless `wire: anthropic` entry included; the launcher-built loopback entry at 1); `TestE2EParallelDelegationsStaySerialWithoutThePin` keeps its unkeyed arm and gains a keyed arm asserting both rows standing at once; `TestParallelAgentsCapCurrentReadsWithoutInstalling` (`current()` before any bind stays 1), `TestParallelAgentsCapFollowsTheBoundServer`, `…RelistMovesThePinInPlace`, `TestScheduleFiringCarriesTheParallelAgentsWidth`, `TestHeadlessInstallsTheParallelAgentsCap` green.
**Acceptance.** `go build ./... && go test ./internal/config/ -run 'ParallelAgents' -count=1 && go test ./cmd/apogee/ -run 'ParallelAgents|ParallelDelegations|ScheduleFiringCarries|HeadlessInstalls' -count=1`
**Commit:** `fix(config): keyed servers default parallel-agents to 4 when nothing pins or advertises a width`

## 5. Keyed default — template, manual, CONTEXT and ADR 0039 (apogee-9se)

**What.** Depends on item 4. ADR 0039 D2 dated note: pin-else-discover-else-floor, floor 4 keyed / 1 otherwise; rewrite the "No signal — a cloud server … means 1" sentence and the headline. Template `parallel-agents` comment (serial on a keyed server is spelled `parallel-agents: 1`), `docs/manual/configuration.md`'s entry, CONTEXT.md's "no signal means 1" line. Guard rule: every sentence saying an unpinned, non-advertising server runs one delegation at a time — `grep -rn "means 1\|one at a time\|else-1" docs/ CONTEXT.md internal/config/`.
**Regression guard.** The rule is the wider grep — `grep -rn "means 1\|one at a time\|else-1\|else 1\|falls back to 1\|serial floor\|strictly serial" docs/ CONTEXT.md internal/ cmd/` — and every hit describing the parallel-agents default is amended (ADR 0045 §5's "else 1", the template, configuration.md:1167/1172/1184, CONTEXT.md:276 among them); ADR 0033/schedule "strictly serial" hits are Firings, not delegations — leave those. Item 5 owns prose only (ADR, template, manual, CONTEXT); the grep's code-comment hits in internal/ and cmd/ are item 4's. ADR 0039 decision 2 (:54 "No signal — a cloud server … means 1") is superseded by this item's dated note. No golden under internal/config/testdata carries the comment — "template goldens regenerated" is dropped from Files and Tests.
**Files:** docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md, docs/adr/0045-sub-agents-route-to-the-flagged-server-with-its-own-posture.md, internal/config/defaults/config.yaml, docs/manual/configuration.md, CONTEXT.md
**Read first:** docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md — decision 2, headline; docs/manual/configuration.md — "Several sub-agents at once" paragraph; CONTEXT.md — Parallel agents entry; internal/config/defaults/config.yaml — parallel-agents key comment; docs/adr/0045-sub-agents-route-to-the-flagged-server-with-its-own-posture.md — §5 cap sentence
**Tests.** `go test ./internal/config/ -run 'Template|Default|Golden'` green.
**Acceptance.** `go test ./internal/config/ -count=1 && ! grep -rn 'else-1' CONTEXT.md docs/manual/configuration.md`
**Commit:** `docs(config): the keyed-server parallel-agents floor in the template, manual, CONTEXT and ADR 0039`

## 6. Engine: SettleExchange keeps finished Turns and marks the cut (apogee-2un, Stage A)

**What.** Defect: after Esc×2 the TUI's `AbortExchange` drops every finished Turn (48 sessions saved `messages: null`) although `endCancelled` dropped only the in-flight one. Add `turnLifecycle.settle()` (`internal/agent/turn.go`) and `Agent.SettleExchange() (dropped bool)` (`agent.go`) beside `abort`/`AbortExchange`: if the conversation past `exchangeStart` holds anything beyond the opening user message, keep it, append the ratified engine note (topic `cancelled`) to the **last tool-result message** of the Exchange via `Message.WithEngineNote` (delivered Interjected rows after it stay), no `rearmFill`, `closeExchange()`, clear `pendingInput`, return false; else fall through to `abort()`, return true. No-op outside an Exchange. `AbortExchange` doc: abort is the explicit throw-away (`/clear`), settle is "the human moved on". CONTEXT.md Exchange entry gains the closed-without-answer shape.
**Regression guard.** The keep test is "a `RoleTool` message past `exchangeStart`" (`conv.At(i).Role == domain.RoleTool` for `i > exchangeStart`); everything else — the lone opening and the embedder-reachable `[user(opening), user(Interjected)]` with no finished Turn (Turn 0 cancelled leaves `inExchange` true; `Interject` refuses only on `!inExchange`) — falls through to `abort()`, there being no tool result to carry the note. Delivered Interjected rows after the noted tool result stay, as the What already says: a cancel right after a delivered interjection (`TestInterjectSurvivesCancelledTurn`) settles as `[…, tool(note), user(Interjected)]` and the next Submit opens user→user — accepted as the already-shipped faulted-Exchange tail (`endAbandoned`, loop.go:150/181), and the CONTEXT.md shape says which. The `[engine — cancelled]` note is ephemeral by ADR 0076 D6 (Advice `json:"-"`, `recordContent` strips it) — a saved record and a resumed conversation hold the kept tool results with no cut marker; the CONTEXT.md Exchange sentence says so. `TestSnapshot_RoundTripsASettledExchange` lives with its `TestSnapshot_*` twins in `internal/agent/state_test.go` (and is where "the saved record keeps the finished Turns" is observed — item 7's TUI fake cannot); the note text belongs in `internal/agent/prompts/<name>.txt` via `mustPrompt` (stepnotice.go `stepNoticeLine` is the twin), not a Go literal.
**Files:** internal/agent/turn.go, internal/agent/agent.go, internal/agent/prompts/<name>.txt (the cancelled note), internal/agent/statemachine_test.go, internal/agent/interject_test.go, internal/agent/state_test.go, internal/domain/advice.go, CONTEXT.md
**Read first:** internal/agent/turn.go — turnLifecycle.abort, closeExchange, end (endCancelled row); internal/agent/agent.go — AbortExchange; internal/domain/advice.go — Message.WithEngineNote; internal/domain/hooks.go — Message.MarshalJSON (recordContent strip); internal/agent/stepnotice.go — stepNoticeLine (mustPrompt); internal/agent/state_test.go — TestSnapshot_RoundTripsExchangeBoundaryForAbort
**Tests.** New `TestSettleExchange_KeepsFinishedTurnsAndNotesTheCut`, `TestSettleExchange_LoneUserMessageFallsBackToAbort`, `TestSettleExchange_InterjectedWithoutAToolResultFallsBackToAbort`, `TestSettleExchange_NoExchangeIsNoop`, `TestSettleExchange_KeepsADeliveredInterjectionAfterTheNote`, `TestSnapshot_RoundTripsASettledExchange` (the record keeps the finished Turns and carries no cut marker); `TestAbortExchange_*`, `TestStep_CancelMidTool`, `TestAbortExchangeDropsInterjections`, `TestInterjectSurvivesCancelledTurn`, `TestEngineNoteNeverReachesTheRecord` green.
**Acceptance.** `go build ./... && go test ./internal/agent/ ./internal/domain/ -run 'SettleExchange|AbortExchange|CancelMidTool|Interject|EngineNote|Snapshot' -count=1`
**Commit:** `fix(agent): SettleExchange closes a cancelled Exchange keeping its finished Turns`

## 7. TUI: cancel, loop error and restored-session submit settle instead of abort (apogee-2un, Stage A)

**What.** Depends on item 6. Add `SettleExchange() bool` to the TUI `Engine` seam (`internal/tui/tui.go`) and the test fake. `foldCancelled`, `foldLoopError` and the submit-into-restored-interrupted-session path (`model.go`) call it; `transcript.markAborted()` runs only when it returns true, so `forkPoints` counts a settled prompt as a real opening. `/clear` (`commandrun.go`) keeps `AbortExchange`; fix its "aborts on every live cancel" comment. `saveAtIdle` unchanged, now persisting the kept Turns. Update the `Aborted` doc (`internal/session/transcript.go`) and `TestContinueAfterLiveCancelStaysCanned`'s comment (`/continue` is still canned, now over a wire that holds the work).
**Regression guard.** `cmd/apogee/wire_engine.go` `lateEngine` is a second implementer of `tui.Engine` (wire.go:74 pins only `*apogee.Agent`): `func (e *lateEngine) SettleExchange() bool` forwards when bound and returns false unbound (the no-op `AbortExchange` already takes there) — without it `go build ./cmd/apogee` and the item's own `QuitMidAnswer` acceptance fail. Both announced wordings change and are stated here: `interruptedNote` (internal/tui/sessions.go:101) becomes `this session was interrupted mid-task — /continue picks up where it left off; sending a new message keeps the finished steps and continues from there`, and the submit note (model.go:1871, pinned verbatim by model_test.go:3558) becomes `closed the interrupted work — its finished steps stand; continuing from your message`; docs/manual/sessions.md:97-98 says the same and is item 8's. `fakeEngine` holds no conversation (seam_test.go `Snapshot` returns `snapshotFn` or a zero Session), so the "saved record keeps messages" clause moves to item 6's `TestSnapshot_RoundTripsASettledExchange` or a cmd/apogee stub e2e (cancel mid-tool after a finished Turn, then read `sess.sessionRecords()`); the TUI test pins `SettleExchange` called, `AbortExchange` not, no aborted mark, snapshot saved. Rule, not a list: every comment naming the cancel fold's abort — `grep -rn "AbortExchange\|scrapped\|scraps" internal/tui/ cmd/apogee/` — is amended (worker.go `deliverInterjections` :250-262, doc.go:396-399's stale "re-stages" claim, tui.go:831-832 and :879-883 Engine doc, heartbeat.go:493, transcript.go:333-335, interject.go:478-479 among them).
**Files:** internal/tui/tui.go, internal/tui/model.go, internal/tui/commandrun.go, internal/tui/transcript.go, internal/tui/sessions.go, internal/tui/worker.go, internal/tui/doc.go, internal/tui/heartbeat.go, internal/tui/interject.go, internal/tui/model_test.go, internal/tui/transcript_test.go, internal/tui/seam_test.go (the fake Engine), internal/session/transcript.go, cmd/apogee/wire_engine.go
**Read first:** internal/tui/model.go — foldCancelled, foldLoopError, submit (the m.eng.InExchange branch); internal/tui/tui.go — Engine; cmd/apogee/wire_engine.go — lateEngine.AbortExchange; internal/tui/seam_test.go — fakeEngine; internal/tui/transcript.go — markAborted, forkPoints
**Tests.** New `TestCancelSettlesKeepingFinishedTurns` (`SettleExchange` called and `AbortExchange` not, no `Aborted` mark, prompt in `forkPoints`, snapshot saved), `TestCancelOnALoneOpeningStillMarksAborted`, `TestLoopErrorSettlesTheExchange`, `TestSubmitOnInterruptedSettlesWithNote` (replaces `…AbortsWithNote`; pins the new note string), `TestClearOnInterruptedStillAborts` (renamed from `…AbortsThenClears`); `TestCancelledMarkLandsOnlyOnTheTwoFolds`, `TestCancelCommitsThePartialBeforeTheNote`, `TestCancelDropsADeliveredRow`, `TestForkPointsSkipCancelledPrompts` re-pointed to the lone-opening case; `TestE2EQuitMidAnswerStillSavesTheSession` green.
**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'Cancel|LoopError|Interrupted|ForkPoints|ContinueAfter|Clear' -count=1 && go test ./cmd/apogee/ -run 'QuitMidAnswer' -count=1`
**Commit:** `fix(tui): a cancelled Exchange keeps its finished Turns — only /clear still throws the Exchange away`

## 8. Docs: the settled-Exchange shape in ADRs, manual and CONTEXT (apogee-2un, Stage A)

**What.** Depends on item 7. Dated notes: ADR 0013 §5 (Stage A ships; a cancelled pool's whole-parent-Turn rollback unchanged, Stage B on `apogee-2un`); ADR 0017 §3 caller list gains `SettleExchange`; ADR 0025 Consequences ("`AbortExchange` discards it…", the 2026-08-03 ruling): a settled Exchange keeps delivered rows; ADR 0022's cancelled-Turn paragraph: the record keeps finished Turns; ADR 0039's "Cancel is unchanged" names Stage A. `docs/manual/commands.md` (esc esc) and `docs/manual/sessions.md` updated. Guard rule: every sentence saying a cancel or abort drops, scraps or discards the whole Exchange — `grep -rn "scrapped Exchange\|whole Exchange\|discards it with the rest\|AbortExchange" docs/ CONTEXT.md layout.md`.
**Regression guard.** The ADR 0022/0025 notes and docs/manual/sessions.md state the marker is live-session only and the record carries none; item 8 amends item 6's CONTEXT.md sentence, never re-adds one.
**Files:** docs/adr/0013*.md, docs/adr/0017*.md, docs/adr/0022*.md, docs/adr/0025*.md, docs/adr/0039*.md, docs/manual/commands.md, docs/manual/sessions.md, CONTEXT.md
**Read first:** docs/adr/0025-interjections-commit-at-the-between-steps-boundary.md — "Delivery has fates" bullet, Amended 2026-08-03 block; docs/adr/0022-sessions-persist-per-turn-as-dual-representation-records.md — "Resume semantics: a progress-saved Turn re-attempts" paragraph; docs/adr/0017-the-exchange-is-a-derived-domain-working-value.md — §3 caller list; docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md — §5 "(b) cancel mid-sub-agent rolls back the whole parent Turn";
docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md — "Cancel is unchanged" (Consequences); docs/manual/sessions.md — the "killed mid-task" bullet; CONTEXT.md — Exchange entry (Turns and stepping)
**Tests.** None (docs).
**Acceptance.** `test "$(grep -c SettleExchange docs/adr/0017*.md)" -ge 1 && grep -q 'Stage A' docs/adr/0013*.md`
**Commit:** `docs(adr): record the settled-Exchange shape across ADR 0013, 0017, 0022, 0025 and 0039`

## 9. Delegate ledger engine note for the coordinator (apogee-clb)

**What.** Defect: coordinators misremember which delegates faulted or wrote output. Add `delegationLedger` beside `retainedDelegates` (`internal/agent/children.go`): mutex-guarded `[]delegationRecord{spawnIndex, callID, name, outcome (completed|capped|faulted|cancelled|refused), cause (head of the fault/refusal text), outputPath}`; `runSubAgent` (`subagent.go`) appends after `delegationResult`; cleared beside `a.retained.clear()` at Exchange open (`loop.go`). `buildRequest` stamps `req.NoteOnTail("delegations", …)` next to the wrap-up directive, same `AppendToSystem` fallback, when the ratified trigger holds; rebuilt per request, never in the snapshot. One line per record in spawn order: `#<n> <name> — <outcome>[: <cause>] — output <present|missing|none>`, presence by `os.Stat` at render time (as `outputMissing()`). Facts, no imperatives; structural, no key, on under Bypass. ADR 0076's 2026-09-19 addendum gains a line; the manual's `sub_agent` section and CONTEXT.md's Delegation entry describe it.
**Regression guard.** The ledger appends from a single deferred site in `runSubAgent` (or from `runDelegation`, dispatch.go:645, which sees every result/outcome) and derives the outcome from the `ToolResult`/`dispatchOutcome`: `runSubAgent` returns before `delegationResult` on every refusal (depth bound, bad args, unknown `continue`, seat/roster/construct) and its panic recover sets the result in a defer, so an append "after delegationResult" never records a `refused` sibling. A pool fan-out runs `runSubAgent` concurrently (dispatch.go `runPool`), so the spawn index is taken at the START of `runSubAgent`/`runDelegation` under the ledger mutex (or keyed on the batch's call order) and the render sorts by it — completion order is not spawn order. `delegationRecord.outputPath` records `sub.outputTarget` (the child's workspace-resolved target; "none" when it is "" or the child ran in Plan mode, as `outputMissing` does), never the unresolved `args.OutputPath`, which an `os.Stat` against the process cwd reports missing for a present file. The delegations `NoteOnTail` is stamped BEFORE the wrap-up `NoteOnTail` in `buildRequest` so the wrap-up stays on the very end (`TestWrapUpDirectiveRidesTheClosingToolResult`).
**Files:** internal/agent/children.go, internal/agent/agent.go, internal/agent/subagent.go, internal/agent/dispatch.go, internal/agent/loop.go, internal/agent/children_test.go, internal/agent/subagent_test.go, docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md, docs/manual/ (the file documenting `sub_agent`), CONTEXT.md
**Read first:** internal/agent/subagent.go — runSubAgent, delegationResult, outputMissing; internal/agent/children.go — retainedDelegates; internal/agent/loop.go — buildRequest (wrapUp NoteOnTail/AppendToSystem site); internal/agent/dispatch.go — runDelegation, the pool worker; internal/agent/subagent_test.go — TestWrapUpDirectiveRidesTheClosingToolResult
**Tests.** `TestDelegationLedgerRecordsEveryOutcomeInSpawnOrder` (a pool whose later sibling finishes first), `TestDelegationLedgerRecordsARefusedSibling`, `TestDelegationLedgerNoteIsAbsentForOneCompletedDelegation`, `TestDelegationLedgerNoteRidesTheTailAfterAFault`, `TestDelegationLedgerNoteRidesAheadOfTheWrapUp`, `TestDelegationLedgerNoteFallsBackToTheSystemPromptOnAnAssistantTail`, `TestDelegationLedgerReportsOutputPresenceAtRenderTime` (the resolved target), `TestDelegationLedgerIsForgottenAsTheNextExchangeOpens`, `TestDelegationLedgerNoteNeverReachesTheRecord`; `TestWrapUpDirective*`, `TestRetainedDelegates_*`, `TestFanOut_CappedChildrenAreRetainedFromThePool` green; README tool counts untouched.
**Acceptance.** `go build ./... && go test ./internal/agent/ -run 'DelegationLedger|WrapUpDirective|Retained|FanOut_Capped' -count=1`
**Commit:** `feat(agent): a delegations engine note hands the coordinator the host-side delegate ledger`

## 10. Session hold: one live instance per session (apogee-3b3)

**What.** Recast at the regression check (2026-09-19). `session.Store` (`internal/session/store.go`) gains `Hold(id) (release func() error, err error)` over `platform.AcquireLock(<dir>/<id>.lock)` (kernel-owned, no stale state — ADR 0034 D7); `Delete`/`Prune` hold first and remove `<id>.lock` after the JSON, returning `*platform.LockHeldError` when held elsewhere; `scan` already ignores `.lock`. `sessionHost` (`cmd/apogee/wire_session.go`) holds at construction on a resumed record, at the first mint and at `Rotate`/`Activate` (releasing the previous hold), and gains `Close()` called from `wire_live.go` teardown; headless and daemon-firing hosts hold their minted ids. `resolveResume`/`resolveResumeArg`/`resolveContinue` refuse a held session with exactly the ratified line (`(pid N)` omitted when unknown). Tests that resume a record while its first host lives `Close()` it first.
**Regression guard.** Hold at the FIRST Save (the record's birth) and at construction on a resumed record — never at mint, so a run that never saves touches no disk; Rotate releases, Activate re-holds; resolveResume/resolveResumeArg/resolveContinue PROBE-AND-RELEASE (acquire, release at once) and the host's construction hold is the one real flock — never both holding; Delete/Prune acquire the hold, remove the JSON, RELEASE (close the handle) and only then os.Remove(<id>.lock) tolerating ErrNotExist — and internal/platform/lock.go's "NEVER removed, not even on release" comment gains the one stated exception (a session record's lock follows its record; the race is benign because the record is already gone), listing internal/platform/lock.go under Files; drop the Firing hold entirely (remove cmd/apogee/headless.go and cmd/apogee/daemonfire.go from Files; a Firing's record is held by whoever resumes it); host.Close() is called from rootWiring.close in cmd/apogee/wire.go after the engine's Close (list wire.go, drop wire_live.go); e2eSession.sessionRecords keeps only .json entries (list cmd/apogee/e2e_support_test.go); saveAt closes its host after the Save; the amend rule is "every test that builds a host and then resumes, prunes or deletes its record in the same process releases first", not a closed list; the refusal error is one type session.HeldError{ID, PID} wrapping *platform.LockHeldError whose Error() IS the ratified line ("(pid N)" omitted when 0), returned by Store.Hold/Delete/Prune, and every door prints err.Error(). Supersedes internal/platform/lock.go:75-77 ("The file is created when absent and is NEVER removed, not even on release") by that stated exception. Re-check (2026-09-19): flock refuses a second open of the same path in ONE process with this process's own pid (internal/platform/lock_unix.go:14-22, TestAcquireLockRefusesASecondHolder), so Activate (and Save) of the id the host already holds keeps the live hold — no release, no re-acquire — pinned in TestSessionHostRotateReleasesAndActivateReHolds; Store.Hold runs `os.MkdirAll(s.dir, dirPerm)` before AcquireLock (save's own order, store.go:233) and resolveResumeArg probes the id of the record Load RETURNED — after a successful store.Load, never the raw arg and never a path-resumed record's re-minted id (TestResolveResumeLegacyPath's store dir is never made); the `.json`-only amend rule names both directory readers — `sessionRecordsIn` (cmd/apogee/e2e_delegation_test.go) like `sessionRecords` — so a hold taken before the JSON lands cannot satisfy TestE2EDelegationRecordSurvivesSIGKILL's wait; Delete removes `<id>.lock` after release on EVERY path, error included (a missing or vanished id — `host.Delete("no-such-session")`, TestPruneReportsFirstErrorAndKeepsSweeping — leaves no lock behind), keeps the `%w`-wrapped os.Remove error, and Hold validates the id (validateID) before joining the path exactly as Delete does (TestDeleteRejectsUnsafeID). Supersedes ADR 0022 D7 (docs/adr/0022-sessions-persist-per-turn-as-dual-representation-records.md:106-109, "last-write-wins per file, documented, not locked") by this item's commit; item 11 carries the dated note, so 10 lands one commit ahead of the ADR.
**Files:** internal/session/store.go, internal/session/doc.go, internal/session/store_test.go, internal/platform/lock.go, cmd/apogee/wire_session.go, cmd/apogee/wire.go, cmd/apogee/wire_session_test.go, cmd/apogee/e2e_support_test.go, cmd/apogee/e2e_delegation_test.go
**Read first:** internal/session/store.go — Store.Delete, Store.Prune, Store.save, validateID, scan; internal/platform/lock.go — AcquireLock, LockHeldError; internal/platform/lock_unix.go — lockFile; cmd/apogee/wire_session.go — newSessionHost, sessionHost.Save, sessionHost.Rotate, sessionHost.Activate, resolveResumeArg, resolveContinue;
cmd/apogee/wire_live.go — rootWiring.wireSession (resolveResume → gcSessions → newSessionHost order); cmd/apogee/wire.go — rootWiring.close, gcSessions; cmd/apogee/wire_session_test.go — saveAt, TestResolveResumeLegacyPath, TestSessionHostDeleteRemovesTheSnapshotStore;
cmd/apogee/e2e_support_test.go — e2eSession.sessionRecords, e2eSession.start; cmd/apogee/e2e_delegation_test.go — sessionRecordsIn; internal/session/store_test.go — TestPruneReportsFirstErrorAndKeepsSweeping, TestDeleteRejectsUnsafeID; internal/platform/lock_test.go — TestAcquireLockRefusesASecondHolder
**Tests.** `TestStoreHoldRefusesASecondHolder`, `TestStoreDeleteRefusesAHeldSession`, `TestStoreDeleteAndPruneRemoveTheLockAfterReleasing`, `TestHeldErrorSpellsTheRatifiedLine` (`(pid N)` omitted when 0), `TestSessionHostHoldsAtTheFirstSaveAndReleasesOnClose` (a run that never saves creates no `.lock`), `TestSessionHostRotateReleasesAndActivateReHolds` (Activate and Save of the held id keep the live hold — no release, no re-acquire), `TestStoreDeleteAndPruneRemoveTheLockAfterReleasing` also covers a missing or vanished id (no `.lock` left behind; `TestPruneReportsFirstErrorAndKeepsSweeping` green), `TestStoreHoldRejectsUnsafeID` (validateID before the path), `TestResolveResumeRefusesAHeldSessionWithTheFriendlyLine` (exact string), `TestResolveResumeProbesAndReleasesForTheHostsHold`, `TestResolveContinueRefusesRatherThanSkips`; `TestSessionHostRoundTripsThroughResume`, `TestSessionHostRotateAndLoadActivate`, `TestResolveResumeByPathRemintsID`, `TestResolveResumeLegacyPath` (sessions dir never made), `TestE2EForkKeepsThePrefixAndSurvivesAResume`, `TestE2EDelegationRecordSurvivesSIGKILL`, `TestGCSessionsAppliesTheConfiguredPolicy`, `TestSessionHostDeleteRemovesTheSnapshotStore` (the `no-such-session` arm leaves no lock) and the headless retention subtests green under the rule (`saveAt` closes its host; `sessionRecords` and `sessionRecordsIn` count `.json` only).
**Acceptance.** `go build ./... && go test ./internal/session/ ./internal/platform/ -count=1 && go test ./cmd/apogee/ -run 'SessionHost|ResolveResume|ResolveContinue|Fork|SurvivesAResume|Headless|Firing' -count=1`
**Commit:** `feat(session): a live session is held by its instance; resuming it elsewhere is refused`

## 11. Session hold at the /sessions doors, ADR 0022 note, manual (apogee-3b3)

**What.** Recast at the regression check (2026-09-19). Depends on item 10 — builds on item 10's landed `sessionHost.Activate`/`Load`, never on BASE. `/sessions` resume (`internal/tui/sessions.go` `resumeLoaded` → `Activate`) and delete surface a `LockHeldError` as a transcript note with the ratified line, view untouched (`TestSessionBrowserResumeErrorLeavesViewUntouched` shape); `SessionHost` (`tui.go`) changes only if the error cannot cross the seam as-is. ADR 0022 D7 and its Rejected "A cross-instance file lock now" gain a dated note; `docs/manual/sessions.md` documents the hold, the line and forking; CONTEXT.md's Session entry names the hold.
**Regression guard.** The hold is taken in sessionHost.Load and parked as pending — Activate adopts a matching pending hold, else acquires; Rotate, Close and the next Load release an unadopted one — so the refusal is a Load error that the existing "could not load session:" branch (internal/tui/sessions.go) notes with the view untouched, scripted through fakeSessionHost.loadErr; foldRecordWrite (internal/tui/sessionsave.go) gains a note branch for a writeDelete error and fakeSessionHost (internal/tui/seam_test.go) a deleteErr — list both files; the TUI prints err.Error() of session.HeldError, no platform import; the ADR 0022 guard is a rule — every sentence recording last-write-wins or the cross-instance lock as a TODO (D7, Rejected, Consequences) gets the dated note — with `grep -n -i 'cross-instance\|last-write-wins' docs/adr/0022*.md`. Re-check decision (owner, 2026-09-19): the hold guards only against OTHER instances — sessionHost.Load neither probes nor parks when the id is one this host already holds (the active id, or an unadopted pending one); Activate of that id keeps the live hold; sessionHost.Delete releases a matching unadopted hold before Store.Delete (as Rotate, Close and the next Load already do); both pinned by TestSessionHostLoadParksTheHoldForActivate (self-resume arm) and a delete-after-failed-restore arm; the ADR 0022 note states the parked hold's lifetime (parked by Load, adopted by Activate, released by Rotate/Close/Delete/the next Load); item 11 builds on item 10's landed sessionHost.Activate/Load, never on BASE — say so in its What.
**Files:** internal/tui/sessions.go, internal/tui/sessions_test.go, internal/tui/sessionsave.go, internal/tui/seam_test.go, internal/tui/tui.go, cmd/apogee/wire_session.go, cmd/apogee/wire_session_test.go, docs/adr/0022*.md, docs/manual/sessions.md, CONTEXT.md
**Read first:** internal/tui/sessions.go — acceptBrowser, loadSession, resumeLoaded, sessionConfirmKey, deleteSession; internal/tui/sessionsave.go — foldRecordWrite, writeCmd, recordWriteKind; cmd/apogee/wire_session.go — sessionHost.Load, sessionHost.Activate, sessionHost.Rotate, sessionHost.Delete;
internal/tui/seam_test.go — fakeSessionHost.Load, fakeSessionHost.Delete, fakeSessionHost.loadErr; internal/tui/sessions_test.go — TestSessionBrowserResumeErrorLeavesViewUntouched, TestSessionBrowserResumeErrorLeavesActiveSessionUntouched, TestSessionBrowserDeleteActiveRotates; internal/tui/fork.go — foldFork;
docs/adr/0022-sessions-persist-per-turn-as-dual-representation-records.md — decision 7, Rejected "A cross-instance file lock now", Consequences "recorded TODOs"; docs/manual/sessions.md — the `--continue`/`/sessions` bullets
**Tests.** `TestSessionBrowserResumeOfAHeldSessionNotesAndStays` (exact line through `fakeSessionHost.loadErr`; view and active session untouched), `TestSessionBrowserDeleteOfAHeldSessionNotesAndStays` (through `deleteErr`; the `foldRecordWrite` note branch), `TestSessionHostLoadParksTheHoldForActivate` (Activate adopts; Rotate, Close and the next Load release an unadopted one; a fork's child reaches `resumeLoaded` without Load; self-resume arm: Load of the active id or an unadopted pending one neither probes nor parks and Activate keeps the live hold; delete-after-failed-restore arm: Delete releases the matching unadopted hold before Store.Delete and succeeds), `TestSessionBrowserResumeOfTheCurrentSessionReloads` (the `/sessions` ⏎ on the current row path notes no refusal); `TestSessionBrowserResume*`, `TestSessionBrowserResumeErrorLeavesActiveSessionUntouched` green.
**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'SessionBrowser' -count=1 && grep -q 'fork it to work alongside' docs/manual/sessions.md`
**Commit:** `feat(tui): /sessions refuses to resume or delete a session held by another apogee`

## 12. Sync-lane webhooks — domain and agent run a WebhookHandler for advise:/gate: (apogee-1d8)

**What.** Recast at the regression check (2026-09-19). `domain.servesClass` (`internal/domain/reaction.go`) lets a `WebhookHandler` serve every class; `Reaction.Validate`'s "reacts as class observe" sentence goes; `validateHandlerRunnable`'s `run: url:` wording becomes key-aware. Extract `webhookHeaders`, `postFailure` and the drain cap from `internal/reactions/webhook.go` into a new `internal/webhook` package (imports `domain` only) used by `internal/reactions` and `internal/agent`. Add `runSyncWebhook` beside `runSyncArgv` (`internal/agent/syncexec.go`): POST the identical seam payload JSON, mapping headers with `headers-env` read at send time, `http.Client{Timeout: syncTimeout(r)}`, no `syncPermitCtx`. `adviseArgv` → `adviseUser` and `gateAnswer` dispatch on `r.Handler.(type)`: advise reads ≤ `AdviceCap`+1 bytes (`io.LimitReader`), `RedactSecrets`; empty body ⇒ nothing; gate reads ≤ 64 KiB and parses with `parseGateAnswer` unchanged (status codes never map to verdicts). Non-2xx, transport error, timeout or unset `headers-env` ⇒ advise fails open with one `reportReaction` failed row, gate ⇒ `askGate`. Bypass skips as argv does. No config change, so nothing is loadable yet; the config, manual, ADR 0076 D8 note and CONTEXT are item 13.
**Regression guard.** RECAST — swap the halves: item 12 becomes "Sync-lane webhooks — domain and agent run a WebhookHandler for advise:/gate:" holding the internal/webhook extraction, runSyncWebhook, the adviseUser/gateAnswer dispatch, domain.servesClass + Validate + validateHandlerRunnable wording, and their tests (no config change, so nothing is loadable yet). The advise route is chosen one level above `adviseUser`: the `MomentPostToolResult` closure in `seamPayload` (internal/agent/reactions.go:703 `r.Handler.(domain.ArgvHandler)`) routes both out-of-process kinds (`ArgvHandler` and `WebhookHandler`) to `adviseUser`; only the Go func still reaches the `PostToolResultFunc` assertion — a webhook left at `wrongHandler` loses every advise trailer for that call silently (`fireCascade` returns nil advice, `firePostToolResult` drops the error). No `CapAdvice` in `adviseUser`: the ≤ `AdviceCap`+1 read is the webhook's bound and `adviceOf` caps once downstream (reactions.go:498-500, :466-467 record that no advise route caps its own text). Every comment in `internal/domain/reaction.go` that says a webhook serves observe alone — `grep -n 'observe alone\|cannot' internal/domain/reaction.go` (the `WebhookHandler` type doc :308-310, :343) — is amended; `internal/config/reactions.go`'s are item 13's. Layering is checked observably — `test "$(go list -deps ./internal/webhook | grep apogee/internal | grep -v internal/webhook)" = github.com/airiclenz/apogee/internal/domain` (or a deps_test on internal/platform/winlabel/deps_test.go's pattern) — since no ADR 0010 import-guard test exists in internal/domain. Supersedes internal/reactions/doc.go:12-14 ("imports internal/domain … and internal/security … and nothing else in the tree"): the sentence is amended to name `internal/webhook` — and, by decision (owner, 2026-09-19), the amended internal/reactions/doc.go sentence names userexec beside domain and security (already stale at BASE). Re-check (2026-09-19): the "reacts as class observe" sentence does not GO — the `async && !servesClass` branch (internal/domain/reaction.go:474-480) is the only refusal of an engine-origin ArgvHandler/WebhookHandler with class shape-view/shape-work (isAllowedCell admits the cell; without it the cascade fails every Moment with wrongHandler): keep the branch with servesClass answering observe|advise|gate for BOTH kinds and reword the sentence to `run: a command or webhook reacts as class observe, advise or gate, not %q`, adding that engine-origin shape case to the table test; a domain.Reaction carries no config key, so the key-aware `run: url:` wording derives the key from r.Class (observe ⇒ `run:`, advise ⇒ `advise:`, gate ⇒ `gate:`) and the observe pins in TestReactionValidateRefusesAnUnrunnableHandler stay byte-identical. Yields to docs/design/confinement-execution-contract.md:1257,1267-1270 (the sync-lane permit row, "advise: or gate: argv … spawns under a SubprocessPermit"): the row stays true for argv and gains the one sentence that a sync webhook takes no permit — list the contract under Files.
**Files:** internal/domain/reaction.go, internal/domain/reaction_test.go, internal/webhook/webhook.go, internal/webhook/webhook_test.go, internal/reactions/webhook.go, internal/reactions/webhook_test.go, internal/reactions/doc.go, internal/agent/syncexec.go, internal/agent/reactions.go, internal/agent/gate.go, internal/agent/advise_webhook_test.go, internal/agent/gate_test.go, docs/design/confinement-execution-contract.md
**Read first:** internal/domain/reaction.go — servesClass, isAsyncHandler, Validate, validateHandlerRunnable, WebhookHandler; internal/agent/reactions.go — seamPayload (MomentPostToolResult closure), adviseArgv, adviceOf, fireLeg, bypassSkips, wrongHandler; internal/agent/gate.go — gateAnswer, askGate, parseGateAnswer;
internal/agent/syncexec.go — runSyncArgv, syncTimeout, reportReaction; internal/reactions/webhook.go — webhookSender.Run, webhookHeaders, postFailure, maxResponseDrain; internal/reactions/exec.go — defaultExecutor.Run, encodePayload;
internal/agent/advise_argv_test.go — adviseArgvAgent, adviseArgvCall, userAdvise, readCall; internal/agent/gate_test.go — gateAgent, gateFirings, userGate; internal/domain/reaction_test.go — TestReactionValidateAppliesThePerClassRulesToAnAsyncHandler, TestReactionValidateRefusesAnUnrunnableHandler
**Tests.** `TestReactionValidateAppliesThePerClassRulesToAnAsyncHandler` drops "webhook outside observe is refused", gains an accepted `advise:` webhook case and an engine-origin shape-view/shape-work case for both out-of-process kinds still refused with the reworded sentence; `TestAsyncHandlersNameNoSeam` green; new `TestValidateHandlerRunnableNamesTheKeyItChecks` covers the three keys derived from r.Class, with `TestReactionValidateRefusesAnUnrunnableHandler`'s observe `run: url:` pins byte-identical. Over `httptest.Server` with a `WebhookHandler` built in-test: `TestAdviseWebhookInjectsTheReplyBody`, `TestAdviseWebhookFailsOpenOnNon2xx`, `TestAdviseWebhookTakesTheAdviseClassDeadline`, `TestAdviseWebhookRedactsTheReply`, `TestAdviseWebhookIsSkippedUnderBypass`, `TestGateWebhookAnswersAllowDenyAsk`, `TestGateWebhookFailureEscalatesToAsk`, `TestGateWebhookIgnoresTheStatusCodeAsAVerdict`; `TestWebhookSender*` green after the extraction.
**Acceptance.** `go build ./... && go test ./internal/domain/ ./internal/webhook/ ./internal/reactions/ ./internal/agent/ -run 'Reaction|Webhook|Handler|Advise|Gate' -count=1 && test "$(go list -deps ./internal/webhook | grep apogee/internal | grep -v internal/webhook)" = github.com/airiclenz/apogee/internal/domain`
**Commit:** `feat(agent): advise: and gate: webhooks post the seam payload and read the reply on the sync lane`

## 13. Sync-lane webhooks — the config accepts the mapping, manual, ADR 0076 D8 note, CONTEXT (apogee-1d8)

**What.** Recast at the regression check (2026-09-19). Depends on item 12. `advise:`/`gate:` accept the `run:` shape: rename `webhookFromRun` → `webhookFromMapping(key, …)` (`internal/config/reactions.go`), route the three keys through one `handler` path; refusal sentences become `advise: is an argv list or a webhook mapping {url:, headers:, headers-env:}` (`gate:` likewise). `docs/manual/reactions.md`: the :71 `advise:` row and its `gate:` sibling, the "mapping or bare string is refused" sentence, the handler table and the advise/gate sections gain the webhook form; ADR 0076 D8's "sync lane's handlers are argv only — bead apogee-1d8" gets a dated note; CONTEXT.md handler entry updated.
**Regression guard.** RECAST — item 13 becomes "Sync-lane webhooks — the config accepts the mapping, manual, ADR 0076 D8 note, CONTEXT" (webhookFromMapping, the refusal sentences, reactions_test flips, docs/manual/reactions.md:71 sentence and sections, ADR note, CONTEXT), Depends on item 12; the commit messages follow the new split. Every comment in `internal/config/reactions.go` that says a sync key takes argv only — `grep -n 'argv list and nothing else\|cannot\|argv only' internal/config/reactions.go` (`entryReactions`' doc :81-82, `argvList`'s doc :250-252 "a webhook cannot advise or gate") — is amended. The docs guard is a rule: every sentence in docs/manual/reactions.md, docs/manual/configuration.md and CONTEXT.md that says a webhook serves `run:`/observe alone, its reply is discarded, or `advise:`/`gate:` take an argv list — `grep -n 'argv list\|webhook answers\|discarded unread\|observe alone\|A webhook .run:' docs/manual/reactions.md docs/manual/configuration.md CONTEXT.md` (reactions.md:5-7, :70-71, :249, :263-264; configuration.md:467-473; CONTEXT.md:1358, :1395) — is amended, so docs/manual/configuration.md joins Files. Item 13 follows item 12 in the same run: nothing loads a sync webhook between the two commits, and the loader's refusal sentence and the manual's `advise: is an argv list` change together here. Re-check (2026-09-19): the docs rule widens to `grep -rn 'argv only\|argv list\|commands that advise' README.md docs/ CONTEXT.md` — it reaches README.md:279 ("commands that advise the model … or gate a tool call"), docs/manual/README.md:12 and docs/design/reaction-core-greenfield.md:111 ("argv only — a mapping is refused by sentence; webhooks for the sync lane are bead apogee-1d8"), which the earlier list missed; every hit that says a sync key is argv only is amended, so README.md and docs/design/reaction-core-greenfield.md join Files.
**Files:** internal/config/reactions.go, internal/config/reactions_test.go, docs/manual/reactions.md, docs/manual/configuration.md, docs/manual/README.md, README.md, docs/design/reaction-core-greenfield.md, docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md, CONTEXT.md
**Read first:** internal/config/reactions.go — entryReactions, handler, argvList, webhookFromRun, stringMap, runShapeError, ReactionEnvNames; internal/config/reactions_test.go — TestLoadFileConfigRefusesMalformedReactions, TestLoadFileConfigResolvesTheReactionsBlock, writeReactionsConfig;
docs/manual/reactions.md — the `advise:`/`gate:` rows and "Sending a webhook"/"Advising the model" sections; docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md — D8 stage-3 addendum ("argv only — a sync-lane webhook is bead apogee-1d8"); CONTEXT.md — Handler entry, Reaction config entry
**Tests.** The `advise: is a webhook mapping` / `gate: is a webhook mapping` cases flip to acceptance with the parsed `WebhookHandler`; bare-string cases keep refusing with the new sentence; `TestLoadFileConfigResolvesTheReactionsBlock`, `TestLoadFileConfigRefusesMalformedReactions` green.
**Acceptance.** `go build ./... && go test ./internal/config/ -run 'Reaction|Webhook|Handler' -count=1 && ! grep -q 'argv list and nothing else' internal/config/reactions.go && ! grep -q 'discarded unread' docs/manual/reactions.md && ! grep -rqn 'argv only' README.md docs/manual/ docs/design/reaction-core-greenfield.md CONTEXT.md`
**Commit:** `feat(config): advise: and gate: accept a webhook mapping`

## 14. `/help` lists the commands and keys (apogee-rw6, /help row only)

**What.** Add `help` to `commandSpecs` (`internal/tui/command.go`, alphabetical between `fork` and `inspect`; summary `list the commands and keys`; `whileRunning: true`, `noRecall: true`) and `case "help"` in `runCommand` (`commandrun.go`) that `addNote`s `helpNote()` (new `internal/tui/help.go` + `doc.go` line): one `/name — summary` line per `commandSpecs` row, a blank line, then a fixed key legend in the idle legend's vocabulary (`⏎ send`, `⇧⏎ / ⌥⏎ newline`, `↑/↓ recall`, `esc esc stop`, `⌃c quit`, `⇧⇥ mode`, `PgUp/PgDn scroll`). Same note shape as `/skills` and `/version`. `docs/manual/commands.md` gains the row.
**Regression guard.** The newline chord is per-terminal: `helpNote` takes the editor's idle legend (or its `keyDisambiguation` flag) and spells it as prompteditor.go:112-113 does — `⌥⏎ newline` until key disambiguation is confirmed, `⇧⏎/⌥⏎ newline` after — so /help never names a key the box does not deliver; the stop is spelled as the running legend does (`esc×2 stop`, prompteditor.go:114), not `esc esc`.
**Files:** internal/tui/command.go, internal/tui/commandrun.go, internal/tui/help.go, internal/tui/doc.go, internal/tui/command_test.go, internal/tui/help_test.go, docs/manual/commands.md
**Read first:** internal/tui/command.go — commandSpecs; internal/tui/commandrun.go — runCommand (case "version", case "skills"); internal/tui/prompteditor.go — idleLegend, idleShiftPlaceholder, runningPlaceholder; internal/tui/transcript.go — addNote; internal/tui/command_test.go — TestCommandTableDrivesParserAndMenu; docs/manual/commands.md — the verb table
**Tests.** `TestHelpNoteListsEveryVerb` (walks `commandSpecs`, asserts the legend trailer under both idle legends); `TestCommandTableDrivesParserAndMenu` (`wantParsed` gains `help`), `TestCommandSpecsReadAlphabetically`, `TestOnlyResetAndPureUIVerbsAreNotRecallable` (adds `help`), `TestSafeWhileRunningReadsTheLine` (`{"/help", true}`), `TestDocMapNamesEveryFile` green.
**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'Help|CommandTable|CommandSpecs|NotRecallable|SafeWhileRunning|DocMap' -count=1`
**Commit:** `feat(tui): /help lists every command with its summary and the key legend`

## 15. `/advice` report pane — fold and render (apogee-d6t)

**What.** Recast at the regression check (2026-09-19). A fourth `reportKind` `adviceReport` (`internal/tui/reportpane.go`: `pane()` → new `paneAdvice` in `model.go`, `follows()` true, hit-test list in `mouse.go`, `layout.md` slot text) rendered through `popup.go` exactly as `/thinking`, content in new `internal/tui/advicepane.go` (+ `doc.go` line). Source: `foldEvent` (`fold.go`) appends every `domain.ReactionFiredEvent` with `Action == "advise"` to an advice board on the `Model` (`{turn, reaction, origin, moment, detail}`), whole session, newest last; the pane groups by Turn with a header row `turn N · <reaction> (<origin> origin) @ <moment>` then `Detail` wrapped at `readableWrapColumn`. Engine notes book no firing and are absent — say so in the pane's doc comment. No engine change. The verb is item 16.
**Regression guard.** Widen framePaneSet to uint16 in the same edit with a compile-time guard (var _ [16 - paneKinds]struct{} or a test asserting paneKinds fits the set) so a tenth pane fails loudly; add the "advice pane" claimant to keyClaimOrder after "thinking pane" and paneAdvice to pointerPanes, list internal/tui/keyclaim_test.go and internal/tui/mouse_test.go under Files with their two pins extended; the frameOverlays field is answered in model.go height(), frameOverlays() and reportpane.go block(); the board folds ReactionFiredEvent with Action "advise" (Detail = the fenced text) AND Action "notice" (the context-fill notice's rung, shown as its Detail) — pinned by TestAdviceBoardFoldsAdviseFiringsInOrder; rows are keyed by runOf(e.EventBase) beside turn, each group headed "<run label> · turn N" in thinkingHeading's spelling, and wrapped at thinkingWrapColumn (per-frame), not readableWrapColumn; amend fold_test.go's "inert outside the debug view … and nowhere else" prose for ReactionFiredEvent. Re-check (2026-09-19): paneAdvice joins `transcriptSlotPanes` (model.go) after paneThinking, closing the slot — stackTranscriptSlot draws only that list, so a pane left out of it is charged rows by frameOverlays.height() yet never painted and never gets a frameSpans rect — and TestTranscriptSlotPanesStateTheStackingOrderOnce (walks paneKinds) plus the KeyClaimOrder/PointerPanes/FrameOverlayBlocks pins run in the acceptance, whose `-run` widens to `…|SlotPanes|KeyClaimOrder|PointerPanes|FrameOverlayBlocks`; the board is bounded like its siblings (maxThinkingRecords=64, maxWireRecords=20) — a record cap (oldest dropped, stated in the doc comment, e.g. 256) that keeps "whole session" for any real session, since a user `advise:` fires on every tool call with a Detail up to AdviceCap — pinned in TestAdviceBoardFoldsAdviseFiringsInOrder's bite; one two-level row shape, stated once: group heading `[<run label> · ]turn N` (popupRowHeading, thinkingHeading's spelling), then per firing a plain row `<reaction> (<origin> origin) @ <moment>` followed by its Detail wrapped at thinkingWrapColumn — the What's `turn N · <reaction> …` heading is that plain row, never the group heading — TestAdvicePaneGroupsRowsByRunAndTurn pins both rows. Decision (owner, 2026-09-19): the prose counting "three reports" (reportpane.go, model.go, mouse.go, mouse_test.go comments) is updated by rule — every comment counting the report kinds — with `grep -rn "three report" internal/tui/`.
**Files:** internal/tui/reportpane.go, internal/tui/advicepane.go, internal/tui/fold.go, internal/tui/mouse.go, internal/tui/model.go, internal/tui/doc.go, internal/tui/reportpane_test.go, internal/tui/advicepane_test.go, internal/tui/keyclaim_test.go, internal/tui/mouse_test.go, internal/tui/fold_test.go, layout.md
**Read first:** internal/tui/reportpane.go — reportKind, pane, follows, reportState, reportContent, reportSpec, frameOverlays.block; internal/tui/model.go — framePane, framePaneSet, openPanes, transcriptSlotPanes, stackTranscriptSlot, frameOverlays, keyClaimOrder, paneClaim; internal/tui/thinkingpane.go — thinkingWrapColumn, thinkingHeading, thinkingRows, thinkingContent, runThinkingCommand;
internal/tui/mouse.go — pointerPanes, reportPointer; internal/tui/fold.go — foldEvent; internal/tui/transcript.go — runOf; internal/agent/reactions.go — bookFiring, adviceOf, isAdvice, reactionAction, actionAdvise; internal/agent/fillnotice.go — actionNotice;
internal/tui/reportpane_test.go — TestReportKindsResolveDistinctly, TestTranscriptSlotPanesStateTheStackingOrderOnce, TestFrameOverlayBlocksAnswerForEveryPane, followCases, growThinkingRecords; internal/tui/keyclaim_test.go — TestKeyClaimOrderMatchesTheDocumentedPrecedence; internal/tui/mouse_test.go — TestPointerPanesWalkInTheClickChainOrder; internal/tui/fold_test.go — foldCases
**Tests.** `TestAdviceBoardFoldsAdviseFiringsInOrder` (Action `advise` and `notice`; the record cap drops the oldest), `TestAdvicePaneGroupsRowsByRunAndTurn` (the `[<run label> · ]turn N` heading row and the `<reaction> (<origin> origin) @ <moment>` firing row), `TestTranscriptSlotPanesStateTheStackingOrderOnce` green with paneAdvice in the slot, `TestAdvicePaneFollowsTheNewestFiring`, `TestFramePaneSetHoldsEveryPane` (or the compile-time guard); `TestKeyClaimOrderMatchesTheDocumentedPrecedence` and `TestPointerPanesWalkInTheClickChainOrder` extended with the advice pane; `TestReportKindsResolveDistinctly`, `TestFollowingReportsTrackTheTail`, `TestTheTwoReportRectsStackInTheSlotsStatedOrder`, `TestFrameOverlayBlocksAnswerForEveryPane`, `TestDocMapNamesEveryFile`, the autocomplete_test.go / sessions_test.go dropdown pins green.
**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'Advice|ReportKinds|FollowingReports|ReportRects|DocMap|Thinking|SlotPanes|KeyClaimOrder|PointerPanes|FrameOverlayBlocks' -count=1 && ! grep -rqn 'three report' internal/tui/`
**Commit:** `feat(tui): an advice report pane folds every advise firing the model saw`

## 16. `/advice` verb, manual and ADR 0076 note (apogee-d6t)

**What.** Depends on items 14 and 15. Add `advice` to `commandSpecs` (summary `what advice the model saw, by Turn`; `whileRunning: true`, `noRecall: true`) and a synchronous `case "advice"` in `runCommand` mirroring `/thinking`'s open/toggle; `docs/manual/commands.md` gains the row; ADR 0076 D6 dated note: the ledger surfaces as `/advice`, not `/settings` (a pane over the config key registry).
**Regression guard.** No report verb toggles: `runThinkingCommand` (thinkingpane.go:200-205) sets `open: true` unconditionally and re-arms the follow, as `runUsageCommand` and `runInspectCommand` do — closing is esc or a click (`reportKey`, `handleReportClick`). The `/advice` verb re-opens (open + follow + top past the last row) exactly as `runThinkingCommand` does, and the test reads "opens; a second /advice re-arms the follow; esc closes". The D6 sentence "`/settings` can show what the model saw this Turn" also stands at CONTEXT.md:1426 and internal/domain/advice.go:23: the rule is `grep -rn "settings\` can show\|/settings can show" docs/ CONTEXT.md internal/ cmd/` and every hit describing the advise ledger is amended (internal/config/registry_test.go's rows are the config registry, not the ledger — untouched).
**Files:** internal/tui/command.go, internal/tui/commandrun.go, internal/tui/command_test.go, internal/tui/advicepane_test.go, docs/manual/commands.md, docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md, CONTEXT.md, internal/domain/advice.go
**Read first:** internal/tui/commandrun.go — runCommand (case "thinking"); internal/tui/thinkingpane.go — runThinkingCommand; internal/tui/thinkingpane_test.go — TestThinkingCommand; internal/tui/command_test.go — TestCommandTableDrivesParserAndMenu; docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md — decision 6; CONTEXT.md — the advise-slot entry (`/settings` can show); internal/domain/advice.go — package doc; docs/manual/commands.md — the verb table
**Tests.** `TestAdviceCommand` (opens; a second /advice re-arms the follow; esc closes), `TestAdviceOpensOnTheNewestFiring`; item 14's command-table pins gain `advice`.
**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'Advice|CommandTable|CommandSpecs|NotRecallable|SafeWhileRunning' -count=1 && grep -q '/advice' docs/manual/commands.md`
**Commit:** `feat(tui): /advice opens the advice report pane`
