# Fan-out ceiling and delegation facts — plan

**Goal:** Bound how many `sub_agent` calls one reply may fan out (the overflow is refused and re-issued), tell the model the delegation bounds before its first call, and show the human each delegate's applied step cap at spawn plus what Esc will discard mid-group. Closes the 2026-09-20 `/code-audit` session (`~/.apogee/sessions/20260920T070407Z-0fd8eaff.json`): 56 dispatches in one reply at width 4, 35 never started, the coordinator blind for 1h41m and the whole Turn rolled back on Esc.
**Date:** 2026-09-20
**Status:** unexecuted
**Sized for:** ~200k-context host
**Base commit:** f30c37e0

**Regression check (2026-09-20, f30c37e0):**
- 1: guard folded (the manual key paragraph, the settingsrows fixture, the insertion point between `delegate-max-steps` and `delegate-max-depth`)
- 2: recast (one `fanOutCeiling()` helper, an explicit ledger write for a ceiling-refused call, the width-1 depth-0 case; supersedes ADR 0039 decisions 1–2's wording — item 7 records it)
- 3: guard folded (`MaxSteps` in the acceptance pattern)
- 4: recast (the stated width is latched per seat; supersedes ADR 0069 decision 6 and the `fanOutWidthNoteFormat` comment — item 7 records both)
- 5: guard folded (`entryState`/`painted()`, the `!subAgentReported` gate, the first-tool-call narrowing, `layout.md`)
- 6: recast (the long form only when it fits the room, else today's hint)
- 7: recast (the ADR 0039 amendment supersedes decisions 1–2's wording; the ADR 0069 decision 6 supersession; the dispatch.go comment)
- Second pass (regression-3, same date and commit; owner's re-check decisions applied):
- 2: recast again (ONE latched-per-seat width seam `statedDelegationWidth()` read by the stated and the enforced number; the ceiling test sits in `prepareCall` beside `preemptDelegation`; `subagent.go` and `children.go` join the ledger's ONE-site comment; the dispatch.go sentences named; `e2e_livestate_test.go` guarded through `delegate-fanout-rounds: 0`)
- 4: recast again (the width and ceiling clauses read `statedDelegationWidth()` / `fanOutCeiling()`; the beat test drives a target-down AND a SetParallelAgents beat)
- 5: ratified call recorded (the row wears its cap from the child's first tool call — the goal's "at spawn" reads as that)
- 6: guard folded (F and Q exclude ceiling-refused heads — the `sub-agent not started:` prefix)
- 7: guard folded (the prose grep and rule replaced; `commands.md`'s Esc×2 "results stay" sentence qualified; the ADR 0069 decision 6 supersession worded)

**Sources:**
- `docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md` (D4 + the 2026-09-14 pre-emption amendment — the refusal shape this plan reuses)
- `docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md` (step cap; "a request only tightens")
- `docs/adr/0023-the-system-prompt-is-a-configured-template-rendered-per-request.md` §6 (orientation block, prefix-cache stability)
- `internal/agent/dispatch.go` — `runPool`, `preemptDelegation`, `skippedDelegationContent`, `fanOutWidthNote`, `everySlotRan`
- `internal/agent/children.go` — delegate ledger, `delegationRefused`
- `docs/layout/tool-layout.md` "Grouped Sub-agents" (running/scheduled row readings)

**Ratified design calls (owner, 2026-09-20 grill):**
- **Overflow:** calls past the ceiling are REFUSED with an error-shaped result in the pre-emption shape; never held and re-issued by the engine.
- **Ceiling size:** `delegate-fanout-rounds` (default 2) × the live width the reply is sized by; `0` switches the ceiling off. No floor; applies at every depth (a delegate's width is 1, so its ceiling is `rounds`).
- **Facts to the model:** a new orientation bullet `Delegation bounds:` rendered whenever `sub_agent` is on the agent's menu; the `max_steps` schema text points at it. No engine note, no per-request tool description.
- **`max_steps`:** stays clamp-only; no ceiling key.
- **Human sees the cap:** `SubAgentPhaseEvent` (Started) carries the applied cap and the requested one; the running row wears `· 80 steps` / `· 80 steps (120 asked)`.
- **Esc hint:** the first-Esc status hint says what a second Esc discards and that a queued message keeps it, only while a pooled group is in flight.
- **Skills:** `~/repos/skills/code-audit` and `code-audit-sequential` are edited in a separate run after this plan lands — out of scope here.
- **Record:** amend ADR 0039 in place; one line in ADR 0013; CONTEXT.md, manual, defaults template.
- **Cap on the running row (owner, 2026-09-20 re-check):** the row wears its cap from the child's FIRST TOOL CALL (the started-but-unspanned head has no summary line to carry it); the goal's "at spawn" reads as that.

**Standing requirements:**
- skills: coding-standards
- Bubble Tea v2 names only (`tea.KeyPressMsg`, `msg.String()`); the `Model` is copied by value — no no-copy types held by value.
- Every model-facing string and every TUI wording below is binding text; test the exact string the program emits.
- Authorized deviations land as a dated NOTES line under the item.

**Out of scope:**
- `apogee-2un` Stage B (Esc keeps finished siblings) — stays parked; item 7 adds a note to the bead.
- Skill edits under `~/repos/skills` (follow-on run, see the ratified call).
- A `delegate-max-steps` ceiling key; the owner's own `~/.apogee/config.yaml`.
- Any version identifier.

## 1. `delegate-fanout-rounds` config key

**What:** Add the file-only integer key `delegate-fanout-rounds` (default 2, `0` = ceiling off, negative refused) on the exact footing of `delegate-max-steps`: registry row (`Editable: true`, `Validate`, `Desc: "Rounds of the server's parallel-agents width one reply may fan out (calls past rounds × width are refused and must be delegated again); 0 switches the ceiling off; takes effect at the next start."`), `Options.DelegateFanOutRounds`, a pointer `FileConfig` field (0 is a value), the config.go landing row, the `/settings` row with `reachesWithoutAMember` and an `applyDelegateFanOutRounds` mirroring `applyDelegateMaxSteps`, and `domain.DelegationConfig.FanOutRounds` folded in `cmd/apogee/wire_config.go` beside `MaxSteps`. Document the key in `internal/config/defaults/config.yaml` directly under the `delegate-max-steps` block, in the template's voice (what it bounds, why relative to width, the `0` spelling). Any test that pins the registry's key list, the defaults template's documented keys or the `/settings` row set is extended, not skipped.

**Regression guard.** `docs/manual/configuration.md` gains the key's paragraph beside `delegate-max-steps` (same section, same voice, the key back-ticked) in THIS item, not item 7: `TestManualDocumentsEverySettingsKey` (`cmd/apogee/docs_settings_test.go`) walks `KeyRegistry` against the manual, matches this item's `-run '…Settings'` and goes red at this commit otherwise — item 7 keeps only the orientation-bullet prose and `commands.md`. `cmd/apogee/settingsrows_test.go` gains a `"delegate-fanout-rounds"` value in `TestSettingsRowsFormatEffectiveValues`'s `want` and the `fabricatedSettings()` field (one row per registry key is pinned). The registry row, the `keyAccessors` entry, the `settingsTable` entry and the template block all go between `delegate-max-steps` and `delegate-max-depth` — `TestRegistryFollowsTheTemplateOrder` (`internal/config/defaults_test.go`) pins registry order to the template's first mention and `TestSettingsTableIsInRegistryOrder` (`cmd/apogee/wire_settings_test.go`) pins the table to registry order.

**Files:** `internal/config/config.go`, `internal/config/registry.go`, `internal/config/options.go`, `internal/config/defaults/config.yaml`, `internal/domain/config.go`, `cmd/apogee/wire_config.go`, `cmd/apogee/wire_settings.go`, `docs/manual/configuration.md`, `internal/config/config_test.go`, `internal/config/registry_test.go`, `cmd/apogee/wire_settings_test.go`, `cmd/apogee/settingsrows_test.go`
**Read first:** internal/config/registry.go — KeyRegistry (delegate-max-steps row), validateDelegateMaxSteps; internal/config/config.go — keyAccessors (delegate-max-steps entry), fileConfig.DelegateMaxSteps; cmd/apogee/wire_settings.go — settingsTable, applyDelegateMaxSteps; cmd/apogee/settingsrows_test.go — fabricatedSettings; cmd/apogee/docs_settings_test.go — manualDocumentsSetting

**Tests:** registry: default 2, `-1` refused with the key named, `0` accepted; config: an absent key lands 2, an explicit `0` lands 0; wire_config: `DelegationConfig.FanOutRounds` reads the option; wire_settings: the row lands on the holder like `delegate-max-steps`; settingsrows: `TestSettingsRowsFormatEffectiveValues` formats the fabricated value; docs: `TestManualDocumentsEverySettingsKey` finds the key in the manual; order: `TestRegistryFollowsTheTemplateOrder` and `TestSettingsTableIsInRegistryOrder` pass with the row in place.

**Acceptance:** `go build ./... && go test ./internal/config/... ./internal/domain/... ./cmd/apogee/ -run 'FanOut|Delegate|Registry|Defaults|Settings'`

**Commit:** `feat(config): delegate-fanout-rounds bounds how many delegations one reply may fan out`

## 2. The fan-out ceiling refuses the calls past it

**What:** Recast at the regression check (2026-09-20). Depends on item 1. In `internal/agent/dispatch.go`, in `prepareCall` beside `preemptDelegation` — after the ToolCallEvent and the pre-tool-exec Moment, keyed on the call's index within the reply's delegation group, at both widths — compute `ceiling = fanOutCeiling()` = `cfg.Delegation.FanOutRounds × statedDelegationWidth()` (1 on a delegate) — `rounds 0` ⇒ no ceiling. The first `ceiling` `sub_agent` calls in emitted order run as today; every later `sub_agent` call in the reply is committed through `skipDelegation`'s path (finished phase alone, no started phase, no audit entry, cleared `run` flag) with the constant-format result
`sub-agent not started: this reply fanned out %d delegations and the ceiling is %d (%d rounds × width %d) — the first %d ran; delegate the rest again once their results are in`.
Leaf tools in the same reply are never counted or refused. Pre-emption (`preemptDelegation`) still decides at dequeue among the calls that run. The refused slot is a `delegationRefused` ledger row whose cause is that result's head line, as any refusal is. A group with a refused slot states no width line (`everySlotRan` is false — the refusal already names the width). Amend `dispatchGroup`'s doc sentence "the whole group is prepared" (dispatch.go:317) and `fanOutWidthNoteFormat`'s "the slots past the width waited for a worker" (dispatch.go:523) to state the ceiling.

**Regression guard.** ONE width seam for the stated and the enforced number (owner, 2026-09-20 re-check): `statedDelegationWidth()` on Agent, latched PER SEAT as two fields — the session width seeded from cfg.ParallelAgents at construction and written by SetParallelAgents; the far width written only by a non-nil SetDelegationTarget, kept across a nil one (a target-down beat) and cleared by SetDelegationSeat — answering far-if-latched else session, and 1 on a delegate. `fanOutCeiling()` = rounds × statedDelegationWidth(); the refusal text's `width %d` and item 4's clauses read that seam alone; fanOutWidthFor sizes only the pool and is deleted from item 2's What. The ceiling test sits in prepareCall beside preemptDelegation — after the ToolCallEvent and the pre-tool-exec Moment — keyed on the call's index within the reply's delegation group, at both widths, so the refusal lands on its own row in every Driver. The explicit ledger write is the ceiling's own second site: `internal/agent/subagent.go` joins Files for its "the ONE site" comment and children.go's `open` doc gains the ceiling case. The dispatch.go sentences to amend are dispatchGroup's "the whole group is prepared" and fanOutWidthNoteFormat's "the slots past the width waited for a worker". `cmd/apogee/e2e_livestate_test.go` is guarded through the top-level key `delegate-fanout-rounds: 0` in launcherHome's body (a launcher move does not touch cfg.Delegation), not a parallel-agents pin. Retained from the first pass: the width-1 depth-0 case is named in What: an unkeyed, unpinned local server has width 1, so at the default rounds 2 a reply of three or more delegations refuses the third — ratified (owner, 2026-09-20: "no floor, apply at every depth"). A ceiling-refused call gets an explicit ledger write (open + record with `delegationRefused`, cause `delegationCause(result.Content)`, name via `delegationLabel`) taken AFTER dispatchGroup's reserve loop so refused rows number behind the reserved slots; skipDelegation's pre-emption path stays as it is (no ledger row) — the write is the ceiling's own. Files gain `cmd/apogee/e2e_livestate_test.go` (`delegate-fanout-rounds: 0` in its launcherHome body so fanout.yaml's three delegations still all run after the pin-less move — the intent of that test is the launcher move, not the fan-out), `cmd/apogee/testdata/stubllm/fanout-ceiling.yaml` (ten sub_agent calls) and a home helper pinning `parallel-agents: 4` in fanOutWidthHome's shape; the e2e acceptance also runs `-run 'E2ELiveState'`. This supersedes ADR 0039 decisions 1–2's wording (`docs/adr/0039-…md`: "a cap of 1 reproduces today's behaviour exactly"; "nothing changes for an unkeyed server until it advertises slots or the owner opts in") for the ceiling — item 7 records the supersession.

**Files:** `internal/agent/dispatch.go`, `internal/agent/agent.go`, `internal/agent/construct.go`, `internal/agent/delegationtarget.go`, `internal/agent/delegationseat.go`, `internal/agent/subagent.go`, `internal/agent/children.go`, `internal/agent/fanout_test.go`, `internal/agent/children_test.go`, `cmd/apogee/e2e_fanout_test.go`, `cmd/apogee/e2e_livestate_test.go`, `cmd/apogee/testdata/stubllm/fanout-ceiling.yaml`
**Read first:** internal/agent/dispatch.go — dispatchGroup, prepareCall, preemptDelegation, skipDelegation, delegationWidth; internal/agent/children.go — delegationLedger.reserve, open; internal/agent/subagent.go — runSubAgent (ledger defer);
internal/agent/agent.go — SetParallelAgents; internal/agent/delegationtarget.go — SetDelegationTarget; internal/agent/delegationseat.go — SetDelegationSeat; cmd/apogee/e2e_livestate_test.go — launcherHome, childRequests;
cmd/apogee/launcher.go — launcherWiring.load (the pin-less move entry); cmd/apogee/upstream.go — parallelAgentsCap.follow; internal/agent/fanout_test.go — fanOutAgent, routedResponder, assertSkippedDelegation

**Tests:** `TestStatedDelegationWidth_LatchesPerSeat` (seeded from `cfg.ParallelAgents`, moved by `SetParallelAgents`; the far width written by a non-nil `SetDelegationTarget`, kept across a nil one, cleared by `SetDelegationSeat`; 1 on a delegate); `TestFanOut_CeilingRefusesTheCallsPastIt` (width 4, rounds 2, 10 calls: #1–#8 run and commit in call order, #9–#10 carry the exact text with `width 4` read from the seam, each still emits its ToolCallEvent and fires the pre-tool-exec Moment before the refusal, a finished phase alone, no width line, ledger rows 9–10 `refused` — the ceiling's own write, numbered behind the eight reserved slots); `TestFanOut_CeilingReadsTheLatchedFarWidth` (a far target latched at 3 with the session at 1: ceiling 6, and it stays 6 across a nil target); `TestFanOut_CeilingOffRunsEveryCall` (rounds 0, 10 calls all run); `TestDispatchSerially_CeilingAppliesAtDepthOne` (delegate, rounds 2, 3 calls: 2 run, 1 refused); `TestFanOut_LeafToolsNeverCountTowardTheCeiling`; e2e: the ceiling test drives `fanout-ceiling.yaml` under a `parallel-agents: 4` home and the parent's next request carries the refusal results beside the eight real ones; `TestE2ELiveStateLauncherMoveKeepsTheSessionWorking` still sees all three of fanout.yaml's delegations run under `launcherHome`'s `delegate-fanout-rounds: 0`.

**Acceptance:** `go test ./internal/agent/ -run 'FanOut|DispatchSerially|DelegationLedger|StatedDelegationWidth' && go test ./cmd/apogee/ -run 'E2EFanOut|E2ELiveState'`

**Commit:** `feat(agent): a reply's fan-out is bounded by delegate-fanout-rounds × width — the overflow is refused and re-issued`

## 3. The applied step cap rides the Started phase event

**What:** Extract the clamp at `internal/agent/subagent.go` (`runSubAgent`, the `args.MaxSteps` switch) into one pure resolver `resolveStepCap(configured, requested int) (applied, requested int)` — `requested` returned non-zero only when the ask was above a positive `configured`; both `runSubAgent` and the `continue` restart path read it. Add `StepCap int` and `CapRequested int` to `domain.SubAgentPhaseEvent` (doc: set on `SubAgentStarted` only, 0 = unbounded / nothing asked); `runDelegation` resolves them from the call's parsed `SubAgentArgs` before emitting the started phase. The result-side clamp note (`stepCapClampNoteFormat`) stays as the model's account. Every producer of `SubAgentPhaseEvent` and every consumer (grep `SubAgentPhaseEvent{` and `.Phase ==` across `internal/` and `cmd/`) is listed in the implementer's NOTES; consumers that ignore the fields need no change.

**Regression guard.** The acceptance's `-run` pattern gains `MaxSteps`: `TestSubAgent_MaxStepsArgumentOnlyLowersTheCap` (`internal/agent/subagent_test.go`) is the one test pinning the clamp this item extracts, and `'StepCap|FanOut|Clamp|Continue'` does not match its name.

**Files:** `internal/domain/events.go`, `internal/agent/subagent.go`, `internal/agent/dispatch.go`, `internal/agent/subagent_test.go`, `internal/agent/fanout_test.go`
**Read first:** internal/agent/subagent.go — runSubAgent (the args.MaxSteps switch), delegationResult (stepCapClampNoteFormat), delegation.stepCap; internal/agent/agent.go — Agent.capRequested; internal/agent/dispatch.go — runDelegation, emitSubAgentPhase; internal/eventjson/encode.go — subAgentPhaseData; internal/agent/subagent_test.go — TestSubAgent_MaxStepsArgumentOnlyLowersTheCap

**Tests:** `TestResolveStepCap` table (below cap, at cap, above cap, request against unbounded, no request); `TestFanOut_StartedPhaseCarriesTheAppliedCap` (configured 80, asked 120 ⇒ `StepCap 80, CapRequested 120`; asked 40 ⇒ `40, 0`; nothing asked ⇒ `80, 0`); the existing clamp-note tests (`TestSubAgent_MaxStepsArgumentOnlyLowersTheCap`, `TestSubAgent_ContinueSpawnsAChildFromTheFoldWithAFreshCap`) still pass unchanged.

**Acceptance:** `go build ./... && go test ./internal/domain/ ./internal/agent/ -run 'StepCap|MaxSteps|FanOut|Clamp|Continue'`

**Commit:** `feat(agent): the started phase event carries a delegate's applied step cap and the cap it asked for`

## 4. The `Delegation bounds:` orientation bullet

**What:** Recast at the regression check (2026-09-20). Depends on items 1 and 2. Add one bullet to `internal/agent/prompts/orientation.txt` between the read-only-roots line and the existing `Delegations:` seats line, with its own `orientation…Line` constant: `- Delegation bounds: %s.` — rendered in `orientationBlock` whenever `sub_agent` is on this agent's roster (`a.tools.Lookup(tools.SubAgentToolName)`), from clauses joined by `; ` in this order: `up to N run at once` (always; `N = a.statedDelegationWidth()`, item 2's latched seam), `a reply may fan out at most C — calls past that are refused and must be delegated again` (only when `FanOutRounds > 0`; `C = a.fanOutCeiling()`), `a reply's whole group returns together` (always), `each delegate is capped at S Turns (a max_steps above that is clamped)` (only when `cfg.Delegation.MaxSteps > 0`). The seats bullet is untouched. Change the `max_steps` schema description in `internal/tools/sub_agent.go` to: `optional; a lower cap for this delegation only, in Turns — see the Delegation bounds line of the host orientation for the configured cap; a request above it is clamped and the result says so.` Stability: the width moves only on the doors ADR 0023 §6 already lists for this block (heartbeat slot discovery, `/server`, `/sub-agents-server`); note that in the block's doc comment. Update ADR 0023 §6's bullet list of the block's inputs.

**Regression guard.** The bullet's width clause and ceiling clause read `statedDelegationWidth()` / `fanOutCeiling()` (item 2's latched seam); `TestOrientation_DelegationsBulletIsConstantAcrossABeat` latches a target, then drives a target-down beat AND a SetParallelAgents beat, asserting the bounds bullet byte-identical across both. Retained from the first pass: the stated width is LATCHED per seat, never read live — item 2's two-field seam (session and far), not a single latch, since the production beat runs SetParallelAgents unconditionally beside a concurrent SetDelegationTarget — so the bullet moves only on the human's doors and on a cap's first statement, never on a flap. Tests name `TestOrientation_PlainToolStatesNoDelegationsBullet` (byte-identical expectation gains the bounds line between roots and Delegations) and `TestOrientation_OmitsFactsTheSessionDoesNotHave` (counts three lines, or builds a roster without sub_agent) as tests the item updates. This supersedes ADR 0069 decision 6's "no beat-driven text of any kind" for this one latched number and the dispatch.go:531-533 comment's "the orientation block is deliberately not where the width lives" — item 7 records both supersessions.

**Files:** `internal/agent/prompts/orientation.txt`, `internal/agent/orientation.go`, `internal/agent/agent.go`, `internal/agent/orientation_test.go`, `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `docs/adr/0023-the-system-prompt-is-a-configured-template-rendered-per-request.md`
**Read first:** internal/agent/orientation.go — orientationBlock, orientation*Line constants, orientationLineCount, delegationSeats; internal/agent/subagent.go — publishesSeatChoice (the nil-roster guard the bounds gate must copy);
internal/agent/orientation_test.go — TestOrientation_DelegationsBulletIsConstantAcrossABeat, seatOrientationAgent, routedTarget; internal/tools/sub_agent.go — subAgentSchemaTemplate;
cmd/apogee/wire_verbs.go — rootWiring.beat; cmd/apogee/upstream.go — parallelAgentsCap.observe

**Tests:** `TestOrientation_DelegationBoundsStateWidthCeilingAndCap` drives a wire-capturing upstream and asserts the exact bullet `- Delegation bounds: up to 4 run at once; a reply may fan out at most 8 — calls past that are refused and must be delegated again; a reply's whole group returns together; each delegate is capped at 80 Turns (a max_steps above that is clamped).`; `…_OmitsTheCeilingAndCapClausesWhenOff` (rounds 0, MaxSteps 0); `…_BoundsLineAbsentWithoutSubAgent` (a delegate at max depth); `TestOrientation_DelegationsBulletIsConstantAcrossABeat` latches a target, then drives a target-down beat AND a `SetParallelAgents` beat, asserting the bounds bullet byte-identical across both; `TestOrientation_PlainToolStatesNoDelegationsBullet`'s byte-identical block gains the bounds line between roots and Delegations; `TestOrientation_OmitsFactsTheSessionDoesNotHave` counts three lines (or builds a roster without sub_agent); the schema test pins the new `max_steps` text; `TestOrientation_SubAgentInheritsTheBlock` still passes.

**Acceptance:** `go test ./internal/agent/ -run 'Orientation' && go test ./internal/tools/ -run 'SubAgent'`

**Commit:** `feat(agent): the orientation block states the delegation bounds — width, fan-out ceiling and step cap`

## 5. The running sub-agent row wears its step cap

**What:** Depends on item 3. In `internal/tui/transcript.go`, `addSubAgentPhase` folds `StepCap`/`CapRequested` off a Started phase onto the head entry as view-only fields beside `phase` (never persisted — a replayed record's rows are finished). In `internal/tui/subagentblock.go` the RUNNING reading gains one trailing segment: `N tool calls · <used>/<window> · 80 steps`, or `· 80 steps (120 asked)` when a request was clamped; nothing when `StepCap` is 0. The scheduled row (`scheduled`) and the finished reading are unchanged. Update `docs/layout/tool-layout.md` "Grouped Sub-agents" running-row rule and any golden that paints a running delegation.

**Regression guard.** `StepCap`/`CapRequested` land on `entryState` beside `phase` in `internal/tui/paintcache.go` and through the unkeyed `entry.painted()` literal — the painters read `paintInput`, never `entry`, so fields on `entry` alone compile and paint nothing (no new key term: the Started phase already flips `spanFlags` in the same event). `subAgentSummary` composes the line for every spanned member, reported or not, so the cap segment is appended only while `!subAgentReported(head)` — a finished member paints no cap. The segment shows once the child has made its first tool call: a started child with no call behind it is neither scheduled nor spanned and stands as the bare head view (`renderSubAgentGroup`'s fallthrough), so the running-row tests build a `readCall` behind the head as `TestSubAgentScheduledUntilItStarts` does. `layout.md`'s "A sub-agent run collapses to its call block" paragraph ("and while the run works that is the whole of it") is amended beside `docs/layout/tool-layout.md` — rule: every sentence that spells the running row's summary, `grep -n 'tool calls · <used>' layout.md docs/layout/tool-layout.md CONTEXT.md` (CONTEXT.md spells the finished form only and stays). The row wears its cap from the child's FIRST TOOL CALL (the started-but-unspanned head has no summary line to carry it) — the amender's smaller option stands; the goal's "at spawn" reads as that. Record this as a ratified call (owner, 2026-09-20 re-check) in the header list.

**Files:** `internal/tui/transcript.go`, `internal/tui/paintcache.go`, `internal/tui/subagentblock.go`, `internal/tui/subagentblock_test.go`, `internal/tui/transcript_test.go`, `docs/layout/tool-layout.md`, `layout.md`
**Read first:** internal/tui/subagentblock.go — subAgentSummary, renderSubAgentGroup, subAgentReported, subAgentScheduled; internal/tui/paintcache.go — entryState, entry.painted; internal/tui/transcript.go — addSubAgentPhase; internal/tui/subagentblock_test.go — TestSubAgentScheduledUntilItStarts

**Tests:** a transcript test feeding `SubAgentStarted{StepCap: 80, CapRequested: 120}` with a `readCall` behind the head and asserting the painted row ends `· 80 steps (120 asked)`; the same with `CapRequested 0` ending `· 80 steps`; `StepCap 0` paints today's reading byte-for-byte; a finished member (phase `SubAgentFinished`) paints no cap; a scheduled row still says `scheduled`; a resumed record paints unchanged.

**Acceptance:** `go test ./internal/tui/ -run 'SubAgent|Transcript|Golden'`

**Commit:** `feat(tui): a running sub-agent row shows the step cap it runs under and the cap it asked for`

## 6. The first-Esc hint names what a second Esc discards

**What:** Recast at the regression check (2026-09-20). In `internal/tui/model.go` `statusRight`, while `lastEsc` is primed AND the open Turn holds a pooled `sub_agent` group (two or more `sub_agent` heads of the open Turn, none `done`), the hint reads, by the heads' phases: with `F ≥ 1` finished (`phase == SubAgentFinished && !done`): `press esc again to stop — drops F finished delegation(s); ⏎ a message keeps them` (`delegation` singular for F = 1, `delegations` otherwise); with `F = 0` and `Q ≥ 1` queued (`phase == ""`): `press esc again to stop — ⏎ a message instead skips the Q queued`; otherwise the hint is today's `press esc again to stop`. The status line's existing right-slot truncation applies; no new row. Record the two wordings in `layout.md` beside the existing hint.

**Regression guard.** The long-form hint is composed only when it fits the room the left slot leaves — statusLine holds both widths, so it either hands statusRight the room or picks the form itself — and falls back to today's `press esc again to stop` otherwise; the width-1 (serial) case may show the skips wording for one queued head, which is true there too. Tests add a width-80 case asserting the fallback via `statusCells`/`assertStatusRightTail`, not only `statusRight()`. F (finished) and Q (queued) exclude ceiling-refused heads — a head whose result carries the `sub-agent not started:` prefix is neither finished work nor queued; add a test: 8 running/finished + 2 refused ⇒ the counts ignore the 2.

**Files:** `internal/tui/model.go`, `internal/tui/model_test.go`, `layout.md`
**Read first:** internal/tui/model.go — statusRight, statusLine, lastEsc; internal/tui/transcript.go — ownGroup, ownGroupAt (the open Turn's adjacent sub_agent heads), hasOpenToolCall, entry.phase, entry.done; internal/tui/subagentblock.go — subAgentReported;
internal/tui/model_test.go — TestModelStopKeys, assertStatusRightTail, statusCells; internal/tui/transcript_test.go — subAgentCall, subAgentStarted, subAgentReport; layout.md — the armed-`esc` line and `esc×2 stop`

**Tests:** three status-line tests (finished 3 / running 4 / queued 1 ⇒ the drops wording with `3 finished delegations`; finished 1 ⇒ `1 finished delegation`; finished 0 / queued 5 ⇒ the skips wording; a lone delegation or an idle model ⇒ the plain hint), each asserting the exact rendered string; a width-80 case (`newTestModel`'s width, a left phrase in place) asserting through `statusCells`/`assertStatusRightTail` that the tail is today's `press esc again to stop`; a refused-heads case (8 running/finished + 2 heads whose result head line starts `sub-agent not started:`) asserting F and Q ignore the 2.

**Acceptance:** `go test ./internal/tui/ -run 'Esc|StatusRight|StatusLine'`

**Commit:** `feat(tui): the first-Esc hint says what stopping discards and that a queued message keeps it`

## 7. Record the decision — ADR 0039, ADR 0013, CONTEXT, manual, bead

**What:** Recast at the regression check (2026-09-20). Depends on items 1–6. `docs/adr/0039-…`: a dated `> **Amended 2026-09-20 — a reply's fan-out is bounded by a ceiling.**` note under decision 4, in the 2026-09-14 amendment's shape: the ceiling (`delegate-fanout-rounds × width`, every depth, `0` off), the refusal result verbatim, the ledger row, no width line, why refuse rather than hold; a one-line pointer in its 2026-09-19 note that the Stage B loss is now at most one round. `docs/adr/0013-…`: one line at the step-cap paragraph stating the orientation `Delegation bounds:` line announces the cap and the started phase event carries it. `CONTEXT.md`: a **Fan-out ceiling** entry under **Parallel agents**, and one sentence on the `Delegation bounds:` bullet where the orientation block is defined. `docs/manual/configuration.md`: the key beside `delegate-max-steps` (same section, same voice) and the bounds bullet in the orientation description; `docs/manual/commands.md`: the Esc hint's two wordings where Esc×2 is described, and the Esc×2 paragraph's "the tool calls and their results stay" amended to say a delegation group still in flight is dropped whole — the first-Esc hint says how many finished delegations — and that a queued message keeps them. Rule for prose: every sentence stating that a reply's delegations all run or all queue without bound is amended — `grep -rn "queue for free slots\|queued for free slots\|whole group is prepared\|waited for a worker" docs CONTEXT.md layout.md internal/agent/dispatch.go`. Then `bd update apogee-2un --notes "2026-09-20: the fan-out ceiling (plan 2026-09-20 - 00) bounds the loss Stage B would recover to one round (delegate-fanout-rounds × width)."`; if `bd` refuses, record the command in NOTES and leave it to the closeout.

**Regression guard.** The ADR 0039 amendment supersedes decisions 1 and 2's wording explicitly — "a cap of 1 reproduces today's behaviour exactly" and "nothing changes for an unkeyed server until it advertises slots or the owner opts in" now hold for the WIDTH only; the ceiling applies at width 1 (ceiling = rounds), ratified by the owner 2026-09-20 ("no floor, apply at every depth"). It also records the ADR 0069 decision 6 supersession for the latched width the bounds bullet states (item 4) as "one latched-per-seat number, moved only by the human's doors and a cap's first statement", and amends the dispatch.go fanOutWidthNoteFormat comment that says the orientation block is not where the width lives. The `docs/manual/configuration.md` key paragraph lands in item 1 (its guard); this item adds only the bounds bullet there. Replace the unmeetable prose grep with `grep -rn "queue for free slots\|queued for free slots\|whole group is prepared\|waited for a worker" docs CONTEXT.md layout.md internal/agent/dispatch.go` and the rule "every sentence stating that a reply's delegations all run or all queue without bound". `docs/manual/commands.md` (the Esc×2 paragraph, "the tool calls and their results stay") is amended to say a delegation group still in flight is dropped whole — the first-Esc hint says how many finished delegations — and that a queued message keeps them. Item 7 also records the ADR 0069 D6 supersession as "one latched-per-seat number, moved only by the human's doors and a cap's first statement".

**Files:** `docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md`, `docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md`, `docs/adr/0069-the-top-level-model-picks-the-delegation-seat.md`, `internal/agent/dispatch.go`, `CONTEXT.md`, `docs/manual/configuration.md`, `docs/manual/commands.md`
**Read first:** docs/adr/0039-…md — decision 1-2 (:44-66), decision 4 (:99-110), the 2026-09-19 and 2026-09-14 notes (:112-131); docs/adr/0013-…md — the 2026-09-18 amendment (:297, step cap/continue); docs/adr/0069-…md — decision 6 (:94-98);
CONTEXT.md — **Parallel agents** (:285), **Orientation block** (:1085); docs/manual/configuration.md — `delegate-max-steps:` (:744), the orientation description; docs/manual/commands.md — the Esc double-tap paragraph (:89-99);
internal/agent/dispatch.go — dispatchGroup doc, fanOutWidthNoteFormat comment (:521-535); Makefile — no `lint-docs` target, `go vet ./...` is the fallback

**Tests:** none (docs); `make lint-docs` if the target exists, else `go vet ./...`.

**Acceptance:** `grep -c "Amended 2026-09-20" "docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md"` prints 1; `grep -n "delegate-fanout-rounds" docs/manual/configuration.md CONTEXT.md` each hit; the prose grep above returns no unamended line outside `docs/plans/` and `docs/skill-runs/` (which quote the phrases).

**Commit:** `docs(delegation): ADR 0039/0013, CONTEXT and the manual record the fan-out ceiling and the delegation bounds line`
