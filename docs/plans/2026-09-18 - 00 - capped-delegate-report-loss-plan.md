# Capped delegates keep their report — engine fold, tail-fenced wrap-up, one write, structural step notice, continue

**Goal:** A delegate that hits a bound (step, token or time cap) never hands its parent a fabricated tool dump: the engine folds the child's conversation into a summary the parent always receives, the wrap-up ask sits where the model reads it, one `write_file` survives the wrap-up, the step-budget notice fires for every delegate, and the parent can continue a capped delegate from that fold instead of re-spawning it.
**Date:** 2026-09-18 · **Status:** unexecuted · **Sized for:** ~200k-context host
**Base:** `6635f881`

**Sources:** `docs/handoffs/2026-09-18 - 00 - capped-delegate-report-loss.md` (F1–F5, P1–P6, §7); `CONTEXT.md` **Step cap**, **Sub-agent**; ADR 0013 §5, 0022 D8 (+2026-08-25 addendum), 0025 §6, 0046, 0076 D6 + Rejected, 0077 D1/D2 (+2026-09-15 addendum); `docs/plans/archived/2026-08-26 - 00 - delegate-token-runaway-plan.md`, `…/2026-09-01 - 02 - capped-delegate-wrap-up-plan.md`, `…/2026-09-14 - 03 - delegation-bounds-and-loop-resilience-plan.md`; `docs/reviews/archived/session-mining-2026-09-14.md` (headline 3, §9).

**Ratified design calls** (owner, 2026-09-18, unless marked writer):
- **P1 placement:** the wrap-up directive is fenced onto the closing tool result of the capping Turn; the system-prompt copy remains only as the fallback when the tail is not a tool result. (Both wires already degrade a tool message to user role on a tool-less request, so on the plain wrap-up the fence reaches the model as the tail of a user message — the tail-message variant and the fence coincide; no model-profile switch.)
- **Engine fence header (writer):** engine-structural text on a tool result uses its own fence `[engine — <topic>]` … `[end engine — <topic>]` on the advice ledger, never an `AdviceSpan` (whose header names a Reaction id).
- **P3 body:** on every bound the result body is the engine fold under `[engine summary]`, followed by the child's closing reply under `[delegate's closing report]` — or, when item 3 judges it a non-report, under `[delegate's closing report — read as narration, not a finding]` (P2 below).
- **P5 classing:** the step-budget notice is engine-structural at depth ≥ 1 — on everywhere including Bypass; the `step-budget-notice` key, `/settings` row, `Options`/`Config`/`Generation` fields and reserved id are removed; ADR 0077 gets a superseding note.
- **P4 orientation (writer):** the "pass it as `output_path`" sentence lives in the `sub_agent` tool description only; the Delegations orientation bullet is seat-gated and positional, so it is not touched.
- **P6 state:** the parent retains, in memory only, `{task, name, tools, output_path, fold, closing report, bound}` per capped child until its Exchange ends; `continue` spawns a fresh child whose opening context is the task plus the fold. Nothing is serialised — ADR 0022 D8 and ADR 0013 §5 stand.
- **P6 handle:** `sub_agent` gains `continue: "<name>"`; keyed by delegation name, latest capped child with that name wins; unknown name → error result naming the retained names.
- **P6 budget:** each continuation is a new Run with `min(max_steps, delegate-max-steps)` steps — "only lowers" stays true per Run; no engine limit on continuations.
- **P2 non-report (owner, 2026-09-18):** a non-report is never dropped — variant head + `[engine summary]` + fold + `[delegate's closing report — read as narration, not a finding]` + the text; blank text is item 2's wordless path, never a non-report.

**Regression check (2026-09-18, 6635f881):**
- 1: guard folded — the fold-brief test pins the exact message pair; the tool-less fact is item 2's `compactCompleter`.
- 2: guard folded — wordless child defined here only; every capped-child script/fixture answers the fold first; the engine fold's usage event is told apart from a context fold.
- 3: recast — a non-report is never dropped; yields to `internal/agent/subagent.go:143-145` ("the parent loses nothing it could have read").
- 3 (re-check): guard folded — `[File:` rule over the first three non-blank lines; intent at any sentence start, receipt = `^[A-Z][A-Z_-]*: `; head worded by shape; fixtures copied from the session file; yields to `subagent.go:165-167` (list stays closed, nothing faulted).
- 4: guard folded — the on-the-tail assertion runs on a tool-result tail; two `requestSystemText` readers named; `step-notice.yaml` switches to `last_message:`.
- 5: guard folded — `outputMissing()` keeps the Plan leg; the shared e2e wrap-up turn stays text-only.
- 6: guard folded — asset renamed `step-notice.txt`; every `buildBuiltins(` caller updated; the notice books no firing; supersedes ADR 0077:96-97 via item 7's addendum.
- 7: guard folded — template block deleted, retired key stripped by the migration; Files by grep rule.
- 8: guard folded — the namer is joined before the report; the map is cleared from `loop.go` `step()` or through `exchangeObserver`, never from `turnLifecycle`.
- 9: guard folded — post-join name for retention and the continue line; `continue` on `PromptArgKeys`; `name` inherited and re-announced.
- 10: guard folded — Acceptance gates on what the item adds.

**Standing requirements:**
- `skills: coding-standards`
- Bubble Tea v2 names; `Model` copied by value (no `strings.Builder` by value).
- Cap → NON-error result, first line marks it partial; wrap-up Turn EXTRA and uncounted; `output_path` explicit, no task-text regex — all kept.
- Every head string keeps the `[delegate stopped at its <bound>;` prefix `delegationBoundHead` (`internal/tui/toolregistry.go`) matches.
- Live tests (`APOGEE_LIVE_ENDPOINT`) are extended where named but never an Acceptance gate.

**Out of scope:** F6 (delegate context growth below the window — own bead); the `/refocus` skill's batch sizes and retry prefix (outside this repo); `apogee-clb` (host-side delegate ledger), `apogee-tfp`, `apogee-sbj`, `apogee-2un`; a `(continued)` TUI affordance beyond the new run block; version bumps.

## 1. Non-mutating `context.Summarize` with a delegate-fold brief — ✅ DONE (2026-09-19)
NOTES (2026-09-19): consequential edit — internal/context/doc.go: made necessary by the two new prompt assets and the exported Summarize/Brief (the package doc enumerates the prompts/ files by name).
NOTES (2026-09-19): an unknown `Brief` value is an error returned before any upstream call (pinned by TestSummarizeRejectsUnknownBrief) — the plan's signature left the guard unspecified; an error rather than a panic keeps the library contract.

**What:** Split the request-building half of `Compact` in `internal/context/compact.go` into an exported `Summarize(ctx, c Completer, msgs []domain.Message, prefixEnd int, maxTranscriptChars int, brief Brief) (string, error)` that returns the summary text and touches no `Conversation`; `Compact` calls it and keeps its `conv.Replace` behaviour byte-for-byte. `Brief` selects the instruction pair: the existing `summary-instruction.txt`/`summary-tail-instruction.txt` (`BriefContinue`) or a new `internal/context/prompts/delegate-fold-instruction.txt` + `delegate-fold-tail.txt` (`BriefDelegateFold`) — "summarise for the agent that delegated this task: files read (by path), facts established, what was concluded, what remains unfinished; no file contents; path:line references". `renderBudgetedTranscript` is reused unchanged (prefix + tail).
**Regression guard.** The delegate-brief test cannot observe tools: `Completer.Complete` takes only `msgs` (`internal/context/compact.go`) and the fake completer sees messages, never a menu. Recast that test as "the request is exactly `[system: delegate-fold-instruction, user: transcript + delegate-fold-tail]`"; the tool-less fact belongs to `compactCompleter` (item 2, `NewRequest` with nil tools, `internal/agent/compact.go`).
**Files:** `internal/context/compact.go`, `internal/context/compact_test.go`, `internal/context/prompts/delegate-fold-instruction.txt`, `internal/context/prompts/delegate-fold-tail.txt`
**Read first:** `internal/context/compact.go` — Compact, Completer, renderBudgetedTranscript, summaryInstruction, summaryTailInstruction; `internal/context/compact_test.go` — fakeCompleter, TestCompactSendsSystemPromptAndTranscript; `internal/domain/hooks.go` — Conversation.Revision
**Tests:** `Summarize` leaves the input slice and a wrapping `Conversation` unchanged (`revision` equal before/after); the delegate brief's request is exactly `[system: delegate-fold-instruction, user: transcript + delegate-fold-tail]`; `Compact` tests unchanged and green.
**Acceptance:** `go build ./... && go test ./internal/context/`
**Commit:** `feat(context): Summarize splits the fold's request out of Compact, with a delegate-fold brief`

## 2. The fold is the report — engine summary on every bound (P3) — ✅ DONE (2026-09-19)
NOTES (2026-09-19): consequential edit — internal/tui/toolregistry.go: made necessary by the new head tail (the delegationBoundHead doc comment quoted the old `its last visible text follows` head verbatim).
NOTES (2026-09-19): consequential edit — internal/agent/compact.go: made necessary by the DelegateFold flag (compactCompleter gains a `delegateFold` field so the fold's UsageEvent carries it; the one existing `compactCompleter{a}` literal became keyed).
NOTES (2026-09-19): construct_test.go's `Delegation.MaxSteps = 5` is on a top-level Agent that reaches no bound, so it needed no fold reply and is not in FILES; the frame `t04-step-cap-block.txt` is byte-identical after the change (the collapsed row shows the verdict, not the head) and is not in FILES either.
NOTES (2026-09-19): live_delegate_cap_test.go's `liveParentGrowthCeiling` re-derived from 4096 to `2 * compactMaxTokens` — the body now carries a fold bounded at compactMaxTokens (4096) plus the report, so the old ceiling could fail a correct run; live test, never an acceptance gate.
NOTES (2026-09-19): full `go test ./cmd/apogee/` saw one unrelated timing flake, TestE2EStreamCancelKeepsWhatArrived (no delegation; a 5 s wait for the prompt under a loaded run) — green when re-run alone; pre-existing, not touched.

**What:** In `internal/agent/agent.go` `finishAtStepCap`, before the wrap-up `step`, run `apogeectx.Summarize` over `a.conv.Messages()` with `compactCompleter{a}` (the CHILD receiver — its model, budget, dialect; usage recorded `Maintenance=true`, never a TurnEvent) and `BriefDelegateFold`; store the text on the child (`a.capFold`). Skip on a cancelled ctx; on error store `[engine summary unavailable — <cause>]`. Never set `a.compacting` or latch `turns.foldFaulted`. In `internal/agent/subagent.go` `delegationResult`'s `StepCapped` branch the body becomes `[engine summary]\n<fold>\n\n[delegate's closing report]\n<lastVisibleText or stepCapNoTextMarker>`; the three heads' tail changes from `partial result — its last visible text follows]` to `partial result — engine summary and closing report follow]` (`stepCapResultFormat`, `tokenCapResultFormat`, `timeCapResultFormat`). Body notes and the steered trailer keep their order after the body. Supersedes "last visible text" as the partial body (2026-08-26 plan; CONTEXT.md **Step cap**). Plan-wide premise: every positional stub script and stubllm fixture that serves a capped child answers ONE extra request (the fold) before the wrap-up, and stubllm `last_message:` matchers are anchored on directive/clause text, never on the child's task text (the fold's rendered transcript echoes it).
**Regression guard.** The wordless child's result is defined here and only here — head (item 2's "partial result" tail), `[engine summary]` + fold, then `[delegate's closing report]` + `stepCapNoTextMarker`; item 3's detector never sees a blank reply. Plan-wide premise, stated in this item's What: every positional stub script and stubllm fixture that serves a capped child answers ONE extra request (the fold) before the wrap-up, and stubllm `last_message:` matchers must not match the child's task text echoed inside the fold's transcript (anchor them on the directive/clause text).
Rule, not the 14 pins: every test whose child reaches a bound through `Run` gets a fold reply spliced before its wrap-up script and its call-count/index pins re-derived — `grep -n 'cappedChildTurns\|narratedChildTurns\|Delegation.MaxSteps = ' internal/agent/*_test.go` (`subagent_test.go`, `agent_test.go`, `stepnotice_test.go`, `construct_test.go`). e2e classifies a request as the child's by the task text in ANY message (`childToolMenus`/`childRequests`): add a fold turn to `delegate-cap.yaml` ABOVE the unanchored `last_message: Read the workspace files` turn, keyed `system:` on a phrase of `delegate-fold-instruction.txt`, tell child requests apart from the fold by that system text (or pin the extra entry), and name the three e2e pins in Tests.
`compactCompleter`'s `Maintenance` UsageEvent at depth ≥ 1 is what the TUI reads as "the child folded" (`internal/tui/fold.go` `foldStats` → `transcript.addCompacted`, persisted as `session.EntryKindCompacted`): distinguish the engine fold's usage event (a field on `domain.UsageEvent`, or a dedicated event) so `foldStats` does not call `addCompacted` for it; pin it in `internal/tui` with a depth-1 `Maintenance` event carrying the flag.
**Files:** `internal/agent/agent.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/agent_test.go`, `internal/agent/stepnotice_test.go`, `internal/agent/construct_test.go`, `internal/agent/live_delegate_cap_test.go`, `internal/domain/events.go`, `internal/tui/fold.go`, `internal/tui/fold_test.go`, `internal/tui/subagentblock_test.go`, `cmd/apogee/e2e_delegation_test.go`, `cmd/apogee/testdata/frames/t04-step-cap-block.txt`, `cmd/apogee/testdata/stubllm/delegate-cap.yaml`, `CONTEXT.md`, `docs/manual/configuration.md`
**Read first:** `internal/agent/agent.go` — Run, finishAtStepCap; `internal/agent/compact.go` — compactCompleter.Complete; `internal/agent/subagent.go` — delegationResult, stepCapNoTextMarker; `internal/agent/subagent_test.go` — TestSubAgent_StepCapReturnsAPartialResultToTheParent, cappedChildTurns; `cmd/apogee/e2e_delegation_test.go` — childToolMenus; `internal/tui/fold.go` — foldStats
**Tests:** stub upstream answers the fold request → parent result = head + `[engine summary]` + fold + `[delegate's closing report]` + text; fold request faults → `[engine summary unavailable — …]` and the closing report still lands; wordless child → marker under the closing-report sub-head; the fold's usage event is `Maintenance`, carries the engine-fold flag and no TurnEvent is emitted for it; `foldStats` with a depth-1 flagged `Maintenance` event writes no `addCompacted` note; every positional capped-child test gains the fold reply and re-derived `responder.calls`/`requests[...]` pins (`TestSubAgent_StepCapReturnsAPartialResultToTheParent` and the grep rule's siblings); e2e pins `childToolMenus` `[true,true,true,false]` and `[true,true,true,true]` and `childRequests != 4` re-derived with the fold told apart by its system text; every existing head pin updated (14 in `subagent_test.go`, the three `envelope*Marker` literals, e2e `stepCapSlot`, the frame); `delegationBoundHead` still matches the new heads (add the three strings to `toolregistry_test.go`); live test asserts the report contains `[engine summary]`.
**Acceptance:** `go build ./... && go test ./internal/agent/ ./internal/tui/ ./cmd/apogee/ -run 'StepCap|WrapUp|Delegat|SubAgent|Envelope|Bound'`
**Commit:** `feat(agent): a bounded delegate's result carries an engine-authored fold of its conversation`

## 3. Detect a non-report and say so (P2) — ✅ DONE (2026-09-19)
NOTES (2026-09-19): the four non-report fixtures and the [277] receipt report are copied verbatim from the session file into `internal/agent/testdata/closingshape/*.txt` (five files not on the item's Files list) — [442] is 31 KB and the shapes under test live in line positions a Go string literal would obscure.
NOTES (2026-09-19): `isNonReport(text) bool` is added as specified, but the production path calls the classifier beneath it, `closingShapeOf(text) closingShape`, because the variant head is worded by shape; `capResultHead` takes that shape as its parameter and a new `cappedResult()` judges the text once so head and sub-head cannot disagree.
NOTES (2026-09-19): `internal/agent/seat_test.go` is on the item's Files list but needed no edit — its bare-Agent "step capped" row asserts the plain head on `lastVisibleText == ""` and stays green as the blank-text rule requires; it was run, not changed.
NOTES (2026-09-19): consequential edit — docs/manual/configuration.md: made necessary by the non-report head and sub-head (the manual's `delegate-max-steps` paragraph said the closing text always lands under `[delegate's closing report]`).

**What:** Recast at the regression check (2026-09-18). Add `isNonReport(text string) bool` beside `isAcknowledgement` in `internal/agent/subagent.go`: never true for blank text (item 2's wordless path); true when `floor.HasToolCallMarkup` fires, when any of the first three non-blank lines matches the `read_file` header shape `^\[File: .+, \d+ lines total` (`internal/tools/read_file.go:243`), when lines matching `^[\w./-]+:\d+:` are the MAJORITY of non-blank lines, or when the last non-blank line carries a pinned intent at any sentence start — `(?i)(^|[.!?]\s+)(let me|i'll|i will|now i|next i|next, i)\b` — AND no receipt line (`^[A-Z][A-Z_-]*: ` — PHASE/STATUS/OUT/SUMMARY/COUNTS/FLAG) precedes it. A fixture per shape: the non-report rows are copied from `~/.apogee/sessions/20260918T143011Z-9788d447.json` `transcript.entries[n].text` (n = 442, 580, 892, 1106), not from the handoff's one-line quotes (which misdescribe [442] and [580]). Depends on item 2. When it fires, `delegationResult` uses the variant heads `[delegate stopped at its step cap (%d steps); no closing report — the delegate's last reply reads as %s, not a finding; engine summary follows]` with %s one of `tool-call markup` / `a file dump` / `a grep dump` / `narration of its next step` (token/time likewise; `capResultHead`, `internal/agent/subagent.go:109-117`, is the one site) and the body is `[engine summary]` + fold + `[delegate's closing report — read as narration, not a finding]` + the text — the text is never dropped. `delegationBoundHead` needs no regex change (prefix unchanged) — pin that with a test; `delegationVerdict` wording unchanged.
**Regression guard.** Ratified design call (owner, 2026-09-18): a non-report is never dropped — result = variant head + `[engine summary]` + fold + `[delegate's closing report — read as narration, not a finding]` + the text (the `markupResultHead` posture; keeps subagent.go's "the parent loses nothing it could have read"). `isNonReport` is NOT true for blank text (the wordless path stays item 2's); the `path:line` rule fires only when such lines are the MAJORITY of non-blank lines; the intent-line rule fires only when the last non-blank line starts with a pinned intent AND the text has no `PHASE`/`STATUS`/`SUMMARY`-style receipt line before it. Add `internal/agent/seat_test.go` to Files and name its bare-Agent row plus `subagent_test.go`'s in Tests. The item yields to `internal/agent/subagent.go:143-145` (a refused reply keeps its text) — that documented decision stands.
Re-check (2026-09-18): the `[File:` rule fires when ANY of the first three non-blank lines matches `^\[File: .+, \d+ lines total` (`internal/tools/read_file.go:243`) — [442]'s header is its second non-blank line, [892]'s its first; a report that merely cites a path does not fire. The intent rule matches at any sentence start of the last non-blank line ([580] and [1106] carry `Let me` mid-line) and the receipt exemption is a line matching `^[A-Z][A-Z_-]*: ` ([277]'s PHASE/STATUS/OUT/SUMMARY/COUNTS/FLAG) — settling What's `PHASE/STATUS/SUMMARY` against Tests' `PHASE/STATUS/OUT/SUMMARY`.
The variant head is worded by shape (`reads as %s, not a finding`, %s ∈ `tool-call markup` / `a file dump` / `a grep dump` / `narration of its next step`) — three of the four fixtures narrated nothing; the `[delegate stopped at its <bound>;` prefix and `delegationBoundHead` (`internal/tui/toolregistry.go:844-845`) stay untouched.
The item yields to `internal/agent/subagent.go:165-167` (the closed acknowledgement list; no wording heuristic FAULTS a finding): the intent rule faults nothing — the text is forwarded whole under the narration label per the owner's P2 call — and the list stays closed; a one-line report opening `I'll note …` is demoted to narration, never dropped.
**Files:** `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/seat_test.go`, `internal/tui/toolregistry_test.go`, `internal/tui/subagentblock_test.go`, `CONTEXT.md`
**Read first:** `internal/agent/subagent.go` — capResultHead, delegationResult, isAcknowledgement, lastVisibleText; `internal/floor/salvage.go` — HasToolCallMarkup; `internal/tools/read_file.go` — the `[File: %s, %d lines total, showing lines %d-%d]` header; `internal/tui/toolregistry.go` — delegationBoundHead; `internal/agent/subagent_test.go` — TestSubAgent_ParentNoticeOnEveryOutcomeButCancelled
**Tests:** table test over the shapes — markup, `[File:` header at the first ([892]) and the second ([442]) non-blank line (non-report) vs a report that cites a path (report), `path:line` majority ([1106]) (non-report) vs three such lines in a longer report (report), a mid-line trailing intent ([580]) (non-report) vs the same line after a `^[A-Z][A-Z_-]*: ` receipt ([277]) (report), a `DelegateReportBlock`-shaped report (report), blank (not a non-report); the four shape heads each match `delegationBoundHead`; parent result for a narrating child = variant head + `[engine summary]` + fold + `[delegate's closing report — read as narration, not a finding]` + text; the bare-Agent "step capped" rows of `TestSeat_FallbackNoteIsTheBodysLastLine` (`seat_test.go`) and `TestSubAgent_ParentNoticeOnEveryOutcomeButCancelled` (`subagent_test.go`) and `TestSubAgent_StepCapMarksAWordlessDelegate` stay green on `lastVisibleText == ""`; TUI verdict for the variant head is the bound verdict, not `done`.
**Acceptance:** `go build ./... && go test ./internal/agent/ ./internal/tui/ -run 'NonReport|StepCap|Bound|Verdict'`
**Commit:** `feat(agent): a wrap-up reply shaped like tool output is named a non-report, not forwarded as a finding`

## 4. The wrap-up ask rides the closing tool result (P1)

**What:** `internal/domain/advice.go` gains the engine fence: `RenderEngineNote(topic, text)` (`[engine — <topic>]` … `[end engine — <topic>]`), `Message.WithEngineNote(topic, text)` on the same ledger `recordContent` strips, and `(*Request).NoteOnTail(topic, text) bool` in `internal/domain/hooks.go` — applies the note to `r.messages[n-1]` when it is `RoleTool`, idempotent on topic, bumps `revision`, returns false otherwise. `internal/agent/loop.go` `buildRequest` under `wrappingUp()`: `if !req.NoteOnTail("wrap-up", directive) { req.AppendToSystem(wrapUpMarker, directive) }`; rewrite the "per-request and stands alone" comment. Strengthen `wrapUpDirectiveTail` (`internal/agent/subagent.go`): append "Do not continue the task. Do not write what a tool would have printed — your reply is read as your report, nothing else." `wrapUpMarker` stays a substring. Fixtures `delegate-cap.yaml` switch the two `when.system:` matchers (`no further tool calls are possible`, `write_file once, for survey.md only`) to `last_message:`. Docs: CONTEXT.md **Step cap** ("appended to the system message" → the closing tool result), ADR 0076 addendum (D6's seam now also carries engine-structural text under its own header; the Rejected "user after tool result" row is unaffected because the wire's tool-less degradation, not the engine, produces the user role), the `loop.go` comment.
**Regression guard.** Also switch `cmd/apogee/testdata/stubllm/step-notice.yaml`'s wrap-up turn from `system: no further tool calls are possible` to `last_message:` in this item (it is orphaned the moment the directive leaves the system prompt); add that path to Files. Rule: `grep -rn 'system: no further tool calls' cmd/apogee/testdata` finds nothing.
`TestWrapUpRequestWithdrawsToolsAndSaysWhy` as rewritten cannot pass: `wrapUpAgent` latches `capped()` and `runWrapUpAgent` Submits, so that one request's tail is the USER message — `NoteOnTail` returns false and the directive lands in the system fallback. Put the "on the tail, NOT in `requestSystemText`" assertion on a run whose tail is a tool result (`outputPathAgent` or `cappedChildTurns` + `wrapUpRequest`); keep the existing test as the fallback pin. `TestSubAgent_OutputPathKeepsWriteFileInTheWrapUp` and `TestSubAgent_WrapUpStaysToolLessWithoutAnOutputPath` read the directive through `requestSystemText` on REAL capped runs (tail = tool result): name them in Tests and assert on `wrapUpRequest(...)`'s last message content (tool role under `output_path`, degraded user role without). CONTEXT.md **Step cap** has no "appended to the system message" phrase — the sentence to edit is "that request goes out with the tool menu withdrawn, telling the delegate…".
**Files:** `internal/domain/advice.go`, `internal/domain/hooks.go`, `internal/domain/advice_test.go`, `internal/agent/loop.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/live_delegate_cap_test.go`, `cmd/apogee/testdata/stubllm/delegate-cap.yaml`, `cmd/apogee/testdata/stubllm/step-notice.yaml`, `CONTEXT.md`, `docs/adr/0076-one-reaction-core-with-an-origin-by-class-policy-matrix.md`
**Read first:** `internal/agent/loop.go` — buildRequest, toolMenu; `internal/domain/hooks.go` — NewRequest, Request.AppendToSystem; `internal/domain/advice.go` — RenderAdvice, Message.WithAdvice, recordContent; `internal/agent/subagent.go` — wrapUpDirective, wrapUpDirectiveTail; `internal/agent/subagent_test.go` — wrapUpAgent, runWrapUpAgent, outputPathAgent, wrapUpRequest, requestSystemText
**Tests:** `TestWrapUpRequestWithdrawsToolsAndSaysWhy` (user-message tail) stays the system-fallback pin; a tool-result-tail run (`outputPathAgent` or `cappedChildTurns` + `wrapUpRequest`) asserts the directive is on the last message's content and NOT in `requestSystemText`; `TestSubAgent_OutputPathKeepsWriteFileInTheWrapUp` and `TestSubAgent_WrapUpStaysToolLessWithoutAnOutputPath` read the directive off `wrapUpRequest(...)`'s last message (tool role under `output_path`, degraded user role without); `step-notice.yaml`'s wrap-up turn matches on `last_message:` and `e2e_stepnotice_test.go` stays green; a wrap-up whose tail is an assistant message (faulted-then-retried shape) falls back to the system copy; `NoteOnTail` idempotent, revision bumped, `recordContent` strips it; e2e delegation fixtures green; live test asserts the last child request's tail carries the directive.
**Acceptance:** `go build ./... && go test ./internal/domain/ ./internal/agent/ ./cmd/apogee/ -run 'WrapUp|Advice|EngineNote|NoteOnTail|Delegat'`
**Commit:** `feat(agent): the wrap-up directive is fenced onto the closing tool result, not the system prompt`

## 5. One `write_file` survives the wrap-up without `output_path` (P4)

**What:** `internal/agent/subagent.go` `wrapUpWriter()` drops the `outputTarget == ""` leg (Plan-mode and roster legs stay); `wrapUpCalls` with no target keeps the FIRST `write_file` call and drops the rest; `internal/agent/resolution.go`'s wrap-up row admits that one write anywhere the Mode admits (Mode rules unchanged — ask-before still prompts); with a target the existing "only `%s` may be written" rule stands. `outputMissing()` reads `a.outputTarget != "" && a.Mode() != domain.ModePlan` directly, not `wrapUpWriter()`. Directive clause: with a target the existing `wrapUpOutputClauseFormat`; without one "You may still call write_file once, to save your report or partial output before you reply." `internal/tools/sub_agent.go` `subAgentSpec.description` gains "If the task names a file the sub-agent must write, pass it as output_path." Supersedes 2026-09-01 item 2's wholesale withdrawal by one tool and 2026-09-14 item 8's "only when named"; keeps "no task-text regex". Docs: `docs/manual/configuration.md` ("Without `output_path` the final turn has no tools at all" → one write), CONTEXT.md **Step cap**.
**Regression guard.** `outputMissing()` reads `a.outputTarget != "" && a.Mode() != domain.ModePlan` (the registry leg is implied by resolveOutputPath); `TestSubAgent_PlanModeWrapUpOffersNoWriter` stays as pinned. The shared `delegate-cap.yaml` plain-clause wrap-up turn stays text-only; the ONE subtest that drives the no-output_path write gets its own `mode: allow-edits` config exactly as the output_path subtest does.
**Files:** `internal/agent/subagent.go`, `internal/agent/resolution.go`, `internal/agent/loop.go`, `internal/agent/subagent_test.go`, `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `cmd/apogee/e2e_delegation_test.go`, `cmd/apogee/testdata/stubllm/delegate-cap.yaml`, `docs/manual/configuration.md`, `CONTEXT.md`
**Read first:** `internal/agent/subagent.go` — wrapUpWriter, wrapUpCalls, outputMissing, resolveOutputPath; `internal/agent/resolution.go` — resolve; `internal/agent/subagent_test.go` — outputPathAgent, TestSubAgent_PlanModeWrapUpOffersNoWriter; `cmd/apogee/e2e_delegation_test.go` — childToolMenus
**Tests:** `…WrapUpStaysToolLessWithoutAnOutputPath` becomes `…WrapUpOffersOneWriteWithoutAnOutputPath` (menu = `[write_file]`, first write runs, second dropped); Plan mode stays tool-less (`TestSubAgent_PlanModeWrapUpOffersNoWriter` as pinned, an `output_path` Plan child still completes without error); with `output_path` the refusal of a write elsewhere is unchanged; the missing-output note still fires only with a target outside Plan mode; `wantPlainSubAgentSchema` and `childToolMenus` pins updated; the shared `delegate-cap.yaml` plain-clause wrap-up turn matches the exact new clause on `last_message:` and answers text only; the one subtest that drives the no-`output_path` write runs under its own `mode: allow-edits` config.
**Acceptance:** `go build ./... && go test ./internal/agent/ ./internal/tools/ ./cmd/apogee/ -run 'WrapUp|SubAgent|Schema|Delegat'`
**Commit:** `feat(agent): one write_file survives the wrap-up for every delegate, and sub_agent says when to pass output_path`

## 6. The step-budget notice is structural — the engine fires it at depth ≥ 1 (P5a)

**What:** `internal/agent/stepnotice.go` stops being a Reaction handler: `buildBuiltins` (`internal/agent/builtins.go`) loses the `stepNotice` parameter and the `classedBuiltin(stepBudgetNoticeID, …)` row; `internal/agent/dispatch.go` `appendToolResult` calls `a.stepBudgetNotice(...)` directly after Reaction advice and, when it fires, applies `Message.WithEngineNote("step budget", text)` (item 4's fence) to the closing tool result. Same threshold (`stepNoticeShare` 0.75, ceil), the same text asset renamed `internal/agent/prompts/step-notice.txt` (`mustPrompt("step-notice.txt")`), same once-per-Run latch and `rearmStepNotice` on rollback, silent at depth 0 or `stepCap <= 0`; fires under Bypass. The structural notice books no firing, like the clamp: no `ReactionFiredEvent`, so the headless `reaction_fired` line and the TUI reaction row for it go. `gen.StepBudgetNotice` is no longer read anywhere in `internal/agent/` (the field itself goes in item 7). Depends on item 4.
**Regression guard.** Rename the text asset `internal/agent/prompts/step-budget-notice.txt` → `internal/agent/prompts/step-notice.txt` (`mustPrompt("step-notice.txt")`) in this item so item 7's grep is clean; add the asset path to Files.
`buildBuiltins` losing its third parameter breaks compile of the whole `internal/agent` test package: `internal/agent/builtins_test.go` and `internal/agent/reactions_test.go` call it with three arguments — both join Files; rule: every caller `grep -rn 'buildBuiltins(' internal/agent` finds is updated. Off the ladder the notice books no `ReactionFiredEvent`: `TestE2EStepNoticeBooksOneFiring` (`cmd/apogee/e2e_stepnotice_test.go`) and `stepNoticeFirings` (`internal/agent/stepnotice_test.go`) are dropped, and What says so. Superseded source: ADR 0077 lines 96-97 ("off by default under the hard invariant, switched off by Bypass") — item 7's addendum supersedes it; the same rule is prose in `internal/config/defaults/config.yaml` (item 7 deletes it) and AGENTS.md's "ships off until bench evidence".
**Files:** `internal/agent/stepnotice.go`, `internal/agent/stepnotice_test.go`, `internal/agent/prompts/step-notice.txt`, `internal/agent/builtins.go`, `internal/agent/builtins_test.go`, `internal/agent/reactions_test.go`, `internal/agent/dispatch.go`, `internal/agent/construct.go`, `internal/agent/agent.go`, `internal/agent/subagent.go`, `internal/agent/doc.go`, `cmd/apogee/e2e_stepnotice_test.go`, `cmd/apogee/testdata/stubllm/step-notice.yaml`
**Read first:** `internal/agent/stepnotice.go` — stepBudgetNotice, stepNoticeThreshold, rearmNotices; `internal/agent/dispatch.go` — appendToolResult; `internal/agent/builtins.go` — buildBuiltins; `internal/agent/stepnotice_test.go` — stepNoticeChild, stepNoticeFirings; `internal/agent/reactions_test.go` — TestBuiltinReactionsAreTheSevenFloorGuardsWhenEveryGuardIsOn; `cmd/apogee/e2e_stepnotice_test.go` — headlessStepNotice
**Tests:** notice lands on the closing tool result at step ceil(0.75·cap) with the engine fence, once, under Bypass too, never at depth 0; `stepnotice_test.go` cases rewritten off the Reaction switch, `stepNoticeFirings` dropped; e2e `step-notice.yaml` fires with no config key set, `TestE2EStepNoticeBooksOneFiring` dropped (no `reaction_fired` line); every `buildBuiltins(` caller compiles with two arguments; `reactions_test` builtin-ladder pins lose the row.
**Acceptance:** `go build ./... && go test ./internal/agent/ ./cmd/apogee/ -run 'StepNotice|StepBudget|Builtin|Ladder|Bypass'`
**Commit:** `feat(agent): the step-budget notice is a structural floor for every delegate, fenced on the closing tool result`

## 7. Remove the `step-budget-notice` key, settings row and fields (P5b)

**What:** Depends on item 6. Delete `StepBudgetNotice` from `internal/config/config.go`, the registry row in `internal/config/registry.go`, `Options` (`internal/config/options.go`), `domain.Config` (`internal/domain/config.go`), `domain.Generation` (`internal/domain/reaction.go`), `stepBudgetNoticeKey` in `internal/config/reactions.go`, the id from `reservedIDs()` (`internal/agent/reactions.go`), and the wiring in `cmd/apogee/wire_config.go` and `cmd/apogee/wire_settings.go` (`setStepBudgetNotice`, `applyStepBudgetNotice`, the `/settings` row). The starter template block for `step-budget-notice` in `internal/config/defaults/config.yaml` and its later mention are deleted; a home config still carrying the key is stripped silently in `internal/config/configmigrate.go` beside `dropRetiredSubAgentsFlags` — no startup notice for a home seeded from the old template. Docs: `docs/manual/configuration.md` `### step-budget-notice` section replaced by a paragraph under `delegate-max-steps` describing the structural notice; `docs/manual/reactions.md` rows; `docs/manual/commands.md` `/settings` row; ADR 0077 superseding addendum (the step notice leaves the Reaction ladder — cap announcement is part of the bound's contract, evidence: the 2026-09-18 handoff §3 F3 + session-mining headline 3; `context-fill-notice` untouched); CONTEXT.md **Step cap**. The seven-guard and sixteen-Moment counts do not change — assert so.
**Regression guard.** Delete the starter template block for `step-budget-notice` in `internal/config/defaults/config.yaml` and its later mention; strip the retired key silently in `internal/config/configmigrate.go` beside `dropRetiredSubAgentsFlags` and add it to `TestParseConfigFileNeverReportsAKeyTheMigrationOwns` — no startup notice for a home seeded from the old template; the Files rule is `grep -rln 'StepBudgetNotice\|stepBudgetNoticeID\|step-budget-notice' --include=*.go .` (add `internal/agent/setlive_test.go`, `internal/agent/setreactions_test.go`, `internal/config/configmigrate.go`, `internal/config/configmigrate_test.go`, `internal/config/defaults/config.yaml`); the Acceptance grep excludes nothing because item 6 renamed the asset.
**Files:** by the grep rule above — today: `internal/config/config.go`, `internal/config/config_test.go`, `internal/config/registry.go`, `internal/config/registry_test.go`, `internal/config/options.go`, `internal/config/reactions.go`, `internal/config/reactions_test.go`, `internal/config/configmigrate.go`, `internal/config/configmigrate_test.go`, `internal/config/unknownkeys_test.go`, `internal/config/defaults/config.yaml`, `internal/domain/config.go`, `internal/domain/reaction.go`, `internal/domain/reaction_test.go`, `internal/agent/reactions.go`, `internal/agent/setlive_test.go`, `internal/agent/setreactions_test.go`, `cmd/apogee/wire_config.go`, `cmd/apogee/wire_settings.go`, `cmd/apogee/wire_config_test.go`, `cmd/apogee/wire_settings_test.go`, `cmd/apogee/wire_boot_test.go`, `cmd/apogee/wire_engine_test.go`, `cmd/apogee/wire_firing_test.go`, `cmd/apogee/settingsrows_test.go`, `docs/manual/configuration.md`, `docs/manual/reactions.md`, `docs/manual/commands.md`, `docs/adr/0077-the-context-fill-notice-is-the-first-engine-advise-reaction.md`, `CONTEXT.md`
**Read first:** `internal/config/config.go` — fileConfig, keyAccessors; `internal/config/registry.go` — KeyRegistry; `internal/config/unknownkeys.go` — unknownKeys, unknownKeyNotice; `internal/agent/reactions.go` — reservedIDs; `cmd/apogee/wire_settings.go` — settingsTable, setStepBudgetNotice, applyStepBudgetNotice
**Tests:** a config file with `step-budget-notice: true` loads without error, without effect and without an unknown-key notice (`TestParseConfigFileNeverReportsAKeyTheMigrationOwns` gains the key); the starter template no longer carries the key; `/settings` row list pin loses the row; a user reaction id `step-budget-notice` is no longer reserved (the `step-budget-notice` subtest of `TestSetReactionsRefusesAReservedBuiltinID` flips); `setlive_test.go` compiles without the field; `FloorGuardKeys` and Moment pins unchanged; `grep -rn 'step-budget-notice\|StepBudgetNotice' --include=*.go .` returns nothing, no exclusion.
**Acceptance:** `go build ./... && go test ./internal/config/ ./internal/domain/ ./internal/agent/ ./cmd/apogee/`
**Commit:** `refactor(config): retire the step-budget-notice switch — the notice is structural now`

## 8. The parent retains a capped delegate's fold until its Exchange ends (P6a)

**What:** `internal/agent/children.go` gains `retainedDelegates`: a mutex-guarded `map[string]retainedDelegate{task, name, tools (raw arg), outputPath, fold, closingReport, bound, spawnCallID}` keyed by delegation name. `runSubAgent` (`internal/agent/subagent.go`) stores an entry after `delegationResult` when `res.StepCapped` (any bound), using `sub.displayName()` read after the delegation namer is joined (empty name → not retained); the same name replaces an earlier entry. The map is cleared as a new Exchange opens — in `loop.go` `step()` right after `a.turns.open()` returns non-nil, or through `exchangeObserver` (see guard); `encodeState`/`restoreSnapshot` (`internal/agent/state.go`) never carry it. Depends on item 2 (fold) and item 3 (closing report). No model-facing change in this item.
**Regression guard.** Join the delegation namer before reading the child's name for retention (one guard settles items 8 and 9's "at that moment"): in `runSubAgent` hoist `if stopNaming != nil { stopNaming() }; naming.Wait()` above `sub.delegationResult(...)` — the defer's copies stay as no-ops — since the namer's late-drop check is `nctx.Err()`, still nil while `delegationResult` runs ahead of the defer.
`turnLifecycle.openExchange` cannot clear the map as stated: `turnLifecycle` (`internal/agent/turn.go`) holds `conv` and `observer` and no Agent, the map is an Agent field, and adding `exchangeOpened()` to `exchangeObserver` breaks `countingObserver` (`internal/agent/turn_test.go`). Either clear it in `loop.go` `step()` right after `if in := a.turns.open(); in != nil {` — no interface change, `turn.go` drops off Files — or extend `exchangeObserver` and list `internal/agent/construct.go` (Agent impl beside `exchangeClosed`) and `internal/agent/turn_test.go` (`countingObserver`) as Files.
**Files:** `internal/agent/children.go`, `internal/agent/children_test.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/loop.go` (or, under the observer route, `internal/agent/turn.go`, `internal/agent/construct.go`, `internal/agent/turn_test.go`), `internal/agent/state_test.go`
**Read first:** `internal/agent/subagent.go` — runSubAgent, delegationResult, startDelegationNaming; `internal/agent/children.go` — childRegistry; `internal/agent/agent.go` — displayName, setName; `internal/agent/turn.go` — turnLifecycle, exchangeObserver; `internal/agent/loop.go` — step (a.turns.open); `internal/agent/state.go` — encodeState
**Tests:** capped child retained with its fold and task; a namer that answers after the cap still lands its name before retention reads it (retained under that name); completed child not retained; two capped children with one name → latest; new Exchange clears; `Snapshot` bytes identical with and without retained entries; concurrent fan-out (`runPool`) retention is race-clean (`-race`).
**Acceptance:** `go build ./... && go test -race ./internal/agent/ -run 'Retain|StepCap|Snapshot|Exchange'`
**Commit:** `feat(agent): the parent retains a capped delegate's fold and task for the rest of its Exchange`

## 9. `sub_agent` gains `continue: "<name>"` (P6b)

**What:** Depends on item 8. `internal/tools/sub_agent.go`: `SubAgentArgs.Continue json:"continue"`; schema property (before `output_path`; `run_on` stays last per ADR 0069): `"optional; the name of a delegate that stopped at a bound earlier in this conversation. The sub-agent restarts from that run's engine summary with a fresh step cap; task says what to do next."` `PromptArgKeys` returns `[]string{"task", "name", "continue"}` (the value is the same prose that rode `name`). `runSubAgent`: on `continue`, look the name up; unknown → error result `[no delegate named %q to continue — retained: a, b]` (`— none` when empty); found → remove the entry, child task = `<original task>\n\n[previous attempt — engine summary]\n<fold>\n\n[continuation instructions]\n<args.Task>`, `name`/`tools`/`output_path` inherited from the retained entry unless the call sets them (an inherited name is re-announced with `emitSubAgentNamed(turn, call.ID, name)` for the new spawn ID), `max_steps` clamped exactly as a fresh spawn (`stepCapClampNoteFormat` unchanged), a new spawn call ID → new run block and Started/Finished pair (TUI, headless, `run.SubAgentUsage` unchanged). A capped child's result body gains, after the body notes and before the steered trailer, `[to continue this delegate: sub_agent with continue: "<name>"]` — only when the post-join name is non-empty. Depth limit applies unchanged.
**Regression guard.** Same as item 8 — the name read for the continue line and the retention key is the post-join `displayName()`.
"`PromptArgKeys` unchanged" puts `continue` under the dangerous-action guard: `Inspect` (`internal/security/dangerous.go`) scans every string arg not in `PromptArgKeys`, `sub_agent` is not read-only, so `continue: "audit .git/config"` trips `write-git-control-plane` (TierHardRefuse) on the very call the result told the model to make. Return `[]string{"task", "name", "continue"}` and update the `[task name]` pin in `TestSubAgentDeclaresBothArgumentsAsDelegationPrompts` (`internal/tools/sub_agent_test.go`). A continue call that omits `name` builds an UNNAMED child (`newChildAgentOn(seat, call.ID, task, delegationName(args.Name))`), so under the nil Namer every agent test runs with, item 8 retains nothing on the second cap: inherit `name` from the retained entry when the call sets none (the struct already carries it) and emit `emitSubAgentNamed(turn, call.ID, name)` for the new spawn ID so the TUI block wears the continued delegation's name.
**Files:** `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/dispatch.go`, `cmd/apogee/e2e_delegation_test.go`, `cmd/apogee/testdata/stubllm/delegate-cap.yaml`, `internal/tui/subagentblock_test.go`
**Read first:** `internal/tools/sub_agent.go` — subAgentSchemaTemplate, SubAgentArgs, PromptArgKeys; `internal/agent/subagent.go` — runSubAgent, delegationResult, splitUserSteeredTrailer; `internal/security/dangerous.go` — Inspect; `internal/agent/dispatch.go` — emitSubAgentNamed; `cmd/apogee/testdata/stubllm/delegate-cap.yaml` — the child turn-1 `last_message: Read the workspace files one at a time` matcher (unanchored: it also claims the continued child's opening message, so the continue turn must sit above it)
**Tests:** `wantPlainSubAgentSchema` updated; `TestSubAgentDeclaresBothArgumentsAsDelegationPrompts` pins `[task name continue]` and a `continue` value naming `.git/config` passes `Inspect`; a continue call without `name` inherits the retained name, emits `SubAgentNamedEvent` for the new spawn ID and is retained anew under it; continue of a retained name spawns a child whose first request carries the fold and the continuation instructions, with a fresh cap and the entry consumed; unknown name → the error result, exact text; a continued child that caps again is retained anew; stubllm e2e drives cap → continue → completed result end to end; the continue line is present in the capped result and absent from a completed one; `splitUserSteeredTrailer` still parses the trailer after the new line.
**Acceptance:** `go build ./... && go test ./internal/tools/ ./internal/agent/ ./cmd/apogee/ ./internal/tui/ -run 'Continue|SubAgent|Schema|Delegat|StepCap|Trailer'`
**Commit:** `feat(tools): sub_agent continue: "<name>" restarts a capped delegate from its engine fold with a fresh cap`

## 10. Docs for continuation — CONTEXT.md, manual, ADR addenda (P6 docs)

**What:** Depends on item 9. CONTEXT.md **Step cap** and **Sub-agent**: the fold, the non-report head, the fenced directive, the one wrap-up write, the structural notice and `continue` (retained in memory for the Exchange; "`max_steps` only lowers" restated as per Run). `docs/manual/configuration.md` delegation section: how to continue, what is inherited, that a resume after the Exchange has nothing to continue. ADR 0013 addendum: §5 stands — a continuation is a new atomic child Run inside a parent tool dispatch, no snapshot mid-child, resume still before/after. ADR 0022 addendum: D8 stands — the retained fold is never a Session record. `docs/plans/archived/2026-08-26 - 00 - delegate-token-runaway-plan.md` gets no edit (archived); the supersession is recorded in the ADR addenda. Bead `apogee-clb` gets a note that P6 covers its "never re-dispatch a capped task verbatim" half (`bd update apogee-clb --notes`).
**Regression guard.** The absence grep already exits 1 at BASE ("its last visible text follows" occurs nowhere under CONTEXT.md, docs/manual or docs/adr; the one live mention, CONTEXT.md **Step cap** "falls back to the child's last visible text", is item 2's), so it cannot bite. Gate on what the item ADDS, exit 0 each: `grep -n 'continue: "' CONTEXT.md docs/manual/configuration.md` and `grep -n '2026-09-18' docs/adr/0013-*.md docs/adr/0022-*.md`; keep the absence grep as a secondary assert.
**Files:** `CONTEXT.md`, `docs/manual/configuration.md`, `docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md`, `docs/adr/0022-sessions-persist-per-turn-as-dual-representation-records.md`
**Read first:** `CONTEXT.md` — **Sub-agent**, **Step cap**, the "wrap-up directive" avoid-note; `docs/manual/configuration.md` — the delegate-max-steps / `output_path` paragraphs under "The terminal UI"; `docs/adr/0013-…` — "Amendment (2026-08-07)" as the addendum pattern, §5; `docs/adr/0022-…` — D8, "Addendum (2026-08-25)"
**Tests:** none (docs); `grep -rn 'last visible text' CONTEXT.md docs/manual` returns nothing (the one live mention is rewritten by item 2; archived plans are outside that grep).
**Acceptance:** `go build ./... && grep -n 'continue: "' CONTEXT.md docs/manual/configuration.md && grep -n '2026-09-18' docs/adr/0013-*.md docs/adr/0022-*.md && ! grep -rn "its last visible text follows" CONTEXT.md docs/manual docs/adr`
**Commit:** `docs(delegation): CONTEXT.md, manual and ADR 0013/0022 addenda describe the fold, the fenced wrap-up and continue`
