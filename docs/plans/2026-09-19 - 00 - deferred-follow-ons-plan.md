# Deferred follow-ons — implementation plan

**Goal:** Close the five small beads deferred out of the 2026-09-16..18 plans: two delegate-engine bugs reasoned from code (`apogee-if9`, `apogee-6ig`), the symlinked-root disclosure bug (`apogee-qm8`), and two test gaps (`apogee-2kj`, `apogee-n5k`). Every item is self-contained; the Files sets are pairwise disjoint.
**Date:** 2026-09-19
**Status:** unexecuted
**Sized for:** ~200k-context host
**Base commit:** 8637d183

**Sources:**
- Beads `apogee-if9`, `apogee-6ig`, `apogee-qm8`, `apogee-2kj`, `apogee-n5k` (`.beads/issues.jsonl` — `bd` has no database on this host)
- `docs/handoffs/2026-09-18 - 00 - capped-delegate-report-loss.md` (items 1–2 context), ADR 0013/0022 addenda (the fold and `continue`), ADR 0074 (undo journal — item 3's lexical-root caveat)
- `layout.md` §approval pane and `docs/layout/tool-layout.md` (the `→ resolves to` contract: drawn only when the two differ)
- `docs/handoffs/2026-09-17 - 00 - pi-test-suite-timing.md` (execution host)

**Ratified design calls:**
- **`apogee-qm8` shape (owner, 2026-09-19):** compare against the root's real path inside `internal/tools`; `Named` and `resolveRoots` stay as they are. Rejected: resolving the workspace at `resolveRoots`.
- **Scope (owner, 2026-09-19):** `apogee-a4i` / `apogee-dak` (archived-plan regex bookkeeping) stay out.
- **`apogee-if9` shape (writer):** keep `take` where it is; every refusal between the take and a successful `Submit` gives the entry back (`retain(prior)`). Rejected: a `lookup`-then-late-`take`, which lets two continues of one name in a fan-out both spawn from the same fold.
- **`apogee-6ig` shape (writer):** `autoPrune` re-arms the notice when no tool message still carries the `step budget` engine note; the domain gains one exported query for that. `PruneResult` stays a count.
- **`apogee-n5k` shape (writer):** a package-level construction seam `newProbeAgent` in `probecontext.go`, swapped by one serial test; the seam-guard test covers it automatically.

**Regression check (2026-09-19, 8637d183):**
- 1: guard folded — case (1)'s parent must publish the seat choice (`NewSubAgentWith(SubAgentOptions{SeatChoice: true})`); the plain `NewSubAgent()` of `subAgentConfig` ignores `run_on`, and `continueArgs` carries no `run_on`/`tools`.
- 2: guard folded — `HasEngineNote` must check the note's header at the span's offset; `dropStaleAdvice` only clears a ledger when the offset exceeds the new content, so a stub longer than a short noted body keeps the row.
- 3, 4, 5: SAFE.

**Standing requirements:**
- `skills: coding-standards`
- Deviations from item text land as a dated NOTES line under the item.
- Bead hygiene at closeout: `bd` has no database here (`bd where` resolves `.beads` but `bd show` fails) — record each closed bead id in the closeout report instead of `bd close`; the owner closes them.

**Execution host (Raspberry Pi 4 — `docs/handoffs/2026-09-17 - 00 - pi-test-suite-timing.md`):**
- `-race` cannot run here (TSan needs 48-bit VAs, kernel has 39). Run every `go test -race …` WITHOUT `-race` and add a NOTES line `race evidence deferred: no-TSan host`.
- No landlock: irrelevant to this plan (no item drives a confined write).
- 4 slow cores: keep `-run` filters as written; any invocation matching more than one test carries `-p 2 -parallel 2`.

**Out of scope:** `apogee-zuu` (delegate context-growth nudge — a design item), `apogee-ws7` (argument-role declaration — open design call), `apogee-a4i` / `apogee-dak`, resolving the workspace root at `cmd/apogee/wire.go`, extending `PruneResult` with stubbed indices, any change to what `journalTarget()` hands the undo journal.

---

## 1. `sub_agent continue` keeps the retained delegate when the call is refused (`apogee-if9`) — ✅ DONE (2026-09-19)

NOTES (2026-09-19): consequential edit — CONTEXT.md: made necessary by the take/give-back change — "the entry is consumed by the continuation" now reads "consumed by the continuation that SPAWNS; a continue refused on its own arguments keeps it".
NOTES (2026-09-19): consequential edit — docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md: made necessary by the same change — the addendum's "the entry is consumed" sentence now says a refused continue keeps it (apogee-if9).
NOTES (2026-09-19): the test's "lookup still holds the fold" assertion is read after a parent Exchange that ends right after the refusal, and the corrected retry runs in a second parent Exchange holding refusal + retry (retained entries are cleared when a new Exchange opens, so one Exchange cannot be paused between the two calls); both cases fail on the pre-item tree with "the refusal forgot the delegate".

**What:** Fix `apogee-if9` in `(*Agent).runSubAgent` (`internal/agent/subagent.go`): `a.retained.take(args.Continue)` consumes the entry before the seat refusal (`parseDelegationSeat`), the roster refusal (`requestedChildTools` — which can fire on the INHERITED `prior.tools` when the parent's menu changed since the cap), the `could not construct sub-agent` and the `could not start sub-agent` refusals, so a refused continuation loses the fold and a corrected retry meets `unknownContinueResult`. Binding shape: keep the take where it is (the unknown-name refusal stays first and still costs no child); bind `giveBack := func() { a.retained.retain(prior) }` right after a successful take (a no-op closure when `args.Continue == ""`) and call it on every refusal return between the take and the `sub.Submit` success — the four listed above; after `Submit` succeeds the child owns the fold (a second cap re-retains it via `res.StepCapped`). The `recover()` at the top of the function is NOT extended (a panic is not a refusal). Update the comment block above the take ("a refusal costs no child") to say that a refusal also costs no fold. Coding-standards calls: one closure, no new method on `retainedDelegates`; the refusal texts are unchanged.
**Regression guard.** Case (1) is not provoked by `subAgentConfig` + `continueArgs`: `subAgentConfig` registers the plain `tools.NewSubAgent()`, so `publishesSeatChoice(a.tools)` is false and `run_on: "banana"` is IGNORED, not refused (`TestSeat_RunOnIsIgnoredWhereItWasNeverPublished`), and `continueArgs` carries no `run_on`/`tools`. Build case (1)'s parent with `tools.NewSubAgentWith(tools.SubAgentOptions{SeatChoice: true})` plus the `read_thing` fake (idiom of `seatChoiceRegistry`, `seat_test.go`), and marshal `tools.SubAgentArgs{Continue, Task, RunOn | Tools}` directly through a sibling of `continueArgs` for both refused calls.
**Files:** `internal/agent/subagent.go`, `internal/agent/subagent_test.go`
**Read first:** internal/agent/subagent.go — runSubAgent, parseDelegationSeat, requestedChildTools, publishesSeatChoice; internal/agent/children.go — retainedDelegates.take, retain, lookup; internal/agent/subagent_test.go — runCappedSurveyParent, continueArgs
**Tests:** `TestSubAgent_ARefusedContinueKeepsTheRetainedDelegate` in `subagent_test.go`, table over the two refusals a test can provoke from the call alone: (1) `run_on: "banana"` on a parent whose tools publish the seat choice — a registry built from `tools.NewSubAgentWith(tools.SubAgentOptions{SeatChoice: true})` plus the `read_thing` fake, NOT `subAgentConfig` (idiom of `seat_test.go`'s `seatChoiceRegistry` and its `invalid run_on "banana"` fixture); (2) `tools: ["no_such_tool"]`. Each case: run `runCappedSurveyParent` so `retainedSurveyName` is retained (idiom of `TestSubAgent_ContinueSpawnsAChildFromTheFoldWithAFreshCap`), send the refused continue marshalled directly from `tools.SubAgentArgs{Continue, Task, RunOn | Tools}` via a sibling helper of `continueArgs`, assert the result is the refusal text AND `a.retained.lookup(retainedSurveyName)` still returns the entry with its `fold`; then send a corrected continue and assert it spawns (`subAgentResultFor` holds the child's report, not `unknownContinueFormat`). Bite: both cases fail on the pre-item tree (the second continue is refused as unknown).
**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'TestSubAgent_.*Continue|TestSubAgent_.*Retained' -p 2 -parallel 2`
**Sidecar (CHANGELOG *Fixed*):** "`sub_agent continue` no longer forgets the retained delegate when the continuation call itself is refused (a bad `run_on`, an unknown tool name): the fold stays retained and a corrected retry continues from it."
**Commit:** `fix(agent): a refused sub_agent continue keeps the retained delegate`

## 2. The step-budget notice is re-told after a prune stubs the noted result (`apogee-6ig`) — ✅ DONE (2026-09-19)

NOTES (2026-09-19): consequential edit — internal/agent/agent.go: made necessary by the prune re-arm — the `stepNoticeLive` field comment named the fold as the only thing that clears the latch; it now names the prune stub too.
NOTES (2026-09-19): the regression-guard paragraph's "its noted body must exceed the stub's length" was read as the inverted spelling of the Tests paragraph's rule (body SHORTER than the stub, so the ledger row survives `dropStaleAdvice`); the test asserts the stub is longer than the body and that the row survived, so only the header check re-arms.
NOTES (2026-09-19): race evidence deferred: no-TSan host.

**What:** Fix `apogee-6ig`: `autoPrune` (`internal/agent/prune.go`) stubs old tool results via `apogeectx.Prune` → `Conversation.SetMessageContent` → `dropStaleAdvice`, which silently drops the `step budget` engine note's ledger row while `a.stepNoticeLive` (`internal/agent/stepnotice.go`) stays true, so a long-capped delegate is never told again; `rearmStepNotice` is reached only from `foldFor` and `rearmNotices`. Binding shape: (a) `internal/domain/hooks.go` gains `func (c *Conversation) HasEngineNote(topic string) bool` — true when any message's ledger holds an `AdviceSpan{Origin: OriginEngine, Topic: topic}` — delegating to the existing unexported `Message.hasEngineNote` (`advice.go`); (b) in `autoPrune`, after `res.Pruned == 0` returns early, add `if a.stepNoticeLive && !a.conv.HasEngineNote(stepNoticeTopic) { a.rearmStepNotice() }` before the `PruneEvent` emit, with a comment stating the rule (the latch is the note's presence; a stub that removed it re-arms it, like a fold). Document the third re-arm site in the `stepBudgetNotice` doc comment beside the fold and the rollback. No change to `PruneResult`, `dropStaleAdvice` or the prune policy.
**Regression guard.** A ledger row is not proof the note stands: `dropStaleAdvice` clears the ledger only when `Offset > len(Content)`, so a stub longer than a short noted body (the ~66-byte `pruneStub`) keeps the row and the model is still never told again. `HasEngineNote` counts a row only when the content at `span.Offset` still begins with `RenderEngineNote`'s header (`"\n\n" + EngineNoteFencePrefix + topic + "]"`) — a stub never does. The agent test states that its noted body must exceed the stub's length.
**Files:** `internal/domain/hooks.go`, `internal/domain/hooks_test.go`, `internal/agent/prune.go`, `internal/agent/stepnotice.go`, `internal/agent/stepnotice_test.go`
**Read first:** internal/agent/prune.go — autoPrune; internal/agent/stepnotice.go — stepBudgetNotice, rearmStepNotice; internal/domain/advice.go — Message.hasEngineNote, dropStaleAdvice, RenderEngineNote; internal/domain/hooks.go — Conversation.SetMessageContent; internal/context/prune.go — pruneStub; internal/agent/stepnotice_test.go — stepNoticeChild
**Tests:** `internal/domain`: `TestConversationHasEngineNote` — a conversation with a noted tool message answers true for the topic, false for another topic, false after `SetMessageContent` shortened that message below the note's offset, and false after `SetMessageContent` replaced it with a body LONGER than the offset that does not carry the header (the stub case). `internal/agent/stepnotice_test.go`: `TestStepNoticeIsToldAgainAfterAPruneStubbedIt` — build a capped child with `stepNoticeChild(t, pruneConfig(sink), cap)` (merge the two configs: `PruneToolResults = true`, a `MaxContextTokens` that makes `pruneCandidates` non-empty), seed enough noted-then-old tool Turns with `seedToolTurns` so the noted result falls outside `PruneKeepTurns` — the noted result's body is SHORTER than the stub that replaces it (a few bytes, as `adviseOneCall(t, a, "one")` seeds), so the ledger row survives `dropStaleAdvice` and only the header check re-arms; the test says so in a comment — call `a.autoPrune(turn)`, assert the stub replaced the noted result (`toolResultContents` shows the `[pruned:` prefix), `a.stepNoticeLive == false`, and the NEXT appended tool result carries the note again (`assertStepNoted`). Control case in the same test: a prune that leaves the noted result inside the kept window keeps `stepNoticeLive` true and does not double-note. Bite: the first case fails on the pre-item tree.
**Acceptance:** `go build ./... && go test ./internal/domain/ -run 'HasEngineNote|AdviceSurvivesAPruned' && go test ./internal/agent/ -run 'StepNotice|AutoPrune' -p 2 -parallel 2`
**Sidecar (CHANGELOG *Fixed*):** "A capped delegate whose step-budget note was pruned away with an old tool result is told the note again on its next result; previously the engine believed the note still stood."
**Commit:** `fix(agent): re-arm the step-budget notice when a prune stubs the noted result`

## 3. `→ resolves to` is silent on a workspace reached through a symlink (`apogee-qm8`) — ✅ DONE (2026-09-19)

NOTES (2026-09-19): `expected` is computed by a small unexported helper `expectedTarget(named, root)` in `workspace_scoped.go` called from `resolveTargetUnbounded` (the item's sole producer) rather than inline — same value, one named seam; `resolveTargetUnbounded` remains the only caller.
NOTES (2026-09-19): "a root that cannot be resolved" has no separate branch: `security.EvalRealPath` never fails (it climbs to the nearest existing ancestor), so the only `expected == Named` case is an argument not lexically under the cleaned root — which also covers an empty root, where `filepath.Rel` errors.
NOTES (2026-09-19): race evidence deferred: no-TSan host (acceptance run without `-race` per the plan's execution-host note).

**What:** Fix `apogee-qm8`: `resolveTargetUnbounded(input, root)` (`internal/tools/workspace_scoped.go`) computes `Named` = root-joined argument and `Real` = `security.EvalRealPath(named)`, and both `writeTarget.note()` (`write_target.go`) and `ResolvedWriteTarget` fire on `Real != Named` — so a root that is itself behind a symlink (macOS `/tmp` → `/private/tmp`, any `cd` through a link) puts the note on EVERY write and read, against the contract "drawn only when the two differ … nothing on the common path". Binding shape: `writeTarget` gains an unexported field `expected string` — the argument's path spelled under the root's REAL path: when `Named` is lexically under `filepath.Clean(root)`, `filepath.Join(security.EvalRealPath(filepath.Clean(root)), rel)`; otherwise (absolute argument outside the root, or a root that cannot be resolved) `Named` itself. Set it in `resolveTargetUnbounded`. One method `func (t writeTarget) redirected() bool { return t.Real != t.expected }` replaces the two `Real != Named` comparisons — in `note()` and in `ResolvedWriteTarget`; `resolvedTargetNote` (read_file's path) follows through `note()`. Producers: `resolveTargetUnbounded` only. Consumers of the comparison: `note()` (all write tools + `read_file`), `ResolvedWriteTarget` (`agent.resolvedPath` → `ToolCallEvent.ResolvedPath` and `ApprovalRequest.ResolvedPath`). `Named`, `Real`, `pin()`, `journalTarget()`, `WorkspaceWriteTarget` are unchanged — the undo journal and the lexical fences relativise against the configured root and must keep receiving `Named`. Rewrite the `writeTarget` doc comment: they differ when the argument travels through a symlink OF ITS OWN; the root's own link is not a disclosure. The note's text still names the fully resolved `Real`.
**Files:** `internal/tools/workspace_scoped.go`, `internal/tools/write_target.go`, `internal/tools/write_target_test.go`
**Read first:** internal/tools/workspace_scoped.go — resolveTargetUnbounded, ResolvedWriteTarget, resolvedTargetNote; internal/tools/write_target.go — writeTarget.note, pin, journalTarget; internal/security/pathsafety.go — EvalRealPath; internal/tools/write_target_test.go — workspaceTarget
**Tests:** extend `TestWriteTargetNote` (keep its existing three cases) with a symlinked-root block: `base := security.EvalRealPath(t.TempDir())`, `real-root/real/` and a symlink `link-root → real-root`; targets built via `workspaceTarget(t, filepath.Join(base, "link-root"), …)`: (1) `real/f.txt` → `note() == ""` and `ResolvedWriteTarget`'s comparison (call `redirected()`) false, `Named` still under `link-root`, `Real` under `real-root`; (2) `real-root/link → real` then `link/f.txt` → note is `" → resolves to " + filepath.Join(base, "real-root", "real", "f.txt")`; (3) an absolute argument outside the root keeps today's behaviour (`expected == Named`). Add `TestResolvedTargetNoteIsQuietOnASymlinkedRoot` for `resolvedTargetNote(input, linkRoot)` (read_file's producer) returning `""` on the ordinary path and the note on the per-file link. Bite: case (1) fails on the pre-item tree. Existing consumers' suites (`write_file_test.go`, `read_file_test.go`, `internal/agent/resolvedpath_test.go`) must stay green unchanged.
**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'WriteTargetNote|ResolvedTargetNote|WriteFile|ReadFile|FileEdit|FindReplace|DeleteFile|FileOps' -p 2 -parallel 2 && go test ./internal/agent/ -run 'ResolvedPath' -p 2 -parallel 2`
**Sidecar (CHANGELOG *Fixed*):** "The `→ resolves to` disclosure no longer appears on every write and read when the workspace itself is reached through a symlink (macOS `/tmp`, `/var`): it names only a target whose own path went somewhere else."
**Commit:** `fix(tools): the resolves-to note compares against the workspace's real path, not its spelling`

## 4. Anthropic SSE parser: fixture test for the 8 MiB text cap (`apogee-2kj`) — ✅ DONE (2026-09-19)

NOTES (2026-09-19): race evidence deferred: no-TSan host

**What:** Close `apogee-2kj`: `anthropicStream.text` (`internal/provider/wire_anthropic_stream.go`) emits the terminal `DeltaError` "apogee: streamed reply exceeded the N MiB text limit" past `maxReplyTextBytes` (`client.go`) and never yields the crossing fragment, but only the openai wire pins that (`TestStream_ReplyTextIsCapped`, `TestStream_ThinkingCountsTowardTheCap` in `stream_test.go`). Binding shape: extract the `[]Delta` assertion body of `assertReplyTextCapFires` (`stream_test.go`) into `assertReplyTextCapDeltas(t, deltas []Delta)` — exactly one `DeltaError`, naming `"%d MiB"`, not `Retryable`, zero `DeltaDone`, delivered `len(Content)+len(Thinking)` ≤ `maxReplyTextBytes` — and have `assertReplyTextCapFires` call it. New `TestAnthropicParseSSE_ReplyTextIsCapped` in `wire_anthropic_stream_test.go`, subtests `text_delta` and `thinking_delta`: build the body in memory (no server) as a `content_block_start` for index 0 of the matching block type (idiom of `anthropicToolStreamSSE`) followed by `maxReplyTextBytes/chunk + 32` repetitions of one 64 KiB `content_block_delta` line (`{"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"aaa…"}}` / `thinking_delta`+`thinking`), drained through `parseAnthropicSSE(t, body)`, then `assertReplyTextCapDeltas`. Also assert the parser stopped reading: no delta follows the error (the error is the last element). No production change.
**Files:** `internal/provider/wire_anthropic_stream_test.go`, `internal/provider/stream_test.go`
**Read first:** internal/provider/stream_test.go — assertReplyTextCapFires, TestStream_ReplyTextIsCapped; internal/provider/wire_anthropic_stream_test.go — parseAnthropicSSE, anthropicToolStreamSSE; internal/provider/wire_anthropic_stream.go — anthropicStream.text, anthropicStream.deltaBlock, anthropicCodec.parseSSE; internal/provider/client.go — maxReplyTextBytes
**Tests:** the two new subtests; the two openai cap tests unchanged in behaviour after the helper split.
**Acceptance:** `go build ./... && go test ./internal/provider/ -run 'ReplyTextIsCapped|ThinkingCountsTowardTheCap' -p 2 -parallel 2`
**Commit:** `test(provider): pin the 8 MiB reply-text cap on the anthropic wire`

## 5. `probe context --live` asserts the first Agent's armed sync lane (`apogee-n5k`) — ✅ DONE (2026-09-19)

NOTES (2026-09-19): race evidence deferred: no-TSan host (acceptance run without `-race` per the plan's execution-host note).

**What:** Close `apogee-n5k`: `measureTurn1` (`cmd/apogee/probecontext.go`) arms the "as configured" Agent's lane via `gen := a.Generation(); gen.Sync = sync; a.SetReactions(gen)` and the Bypass Agent via `gen.Bypass = true`, but `TestProbeContextLiveSendsTwiceWhenReactionsArmed` (`probecontext_test.go`) proves it only by request count — a user advise Reaction fires at `post-tool-result`/`file-changed`, so the wire cannot show the lane. Binding shape: add the package-level seam `var newProbeAgent = apogee.New` beside `errProbeContextNeedsEndpoint` and call it from `measureTurn1` and `readContextCost` (house idiom: `acquireDaemonLock`, `probeKeyStore`). The `*apogee.Agent` is closed before `measureTurn1` returns; `Agent.Close` leaves `a.gen` untouched and `Generation()` only takes `genMu`, so the test reads it after the command returns. The existing test keeps its count assertion and its comment gains one sentence pointing at the new test.
**Files:** `cmd/apogee/probecontext.go`, `cmd/apogee/probecontext_test.go`
**Read first:** cmd/apogee/probecontext.go — measureTurn1, readContextCost, errProbeContextNeedsEndpoint; cmd/apogee/probecontext_test.go — TestProbeContextLiveSendsTwiceWhenReactionsArmed, probeContextLiveStub, runProbeContext; cmd/apogee/seams_guard_test.go — TestNoParallelTestSwapsAPackageSeam; internal/agent/agent.go — Agent.Generation
**Tests:** `TestProbeContextLiveArmsTheSyncLaneOnTheFirstAgentOnly` — SERIAL (no `t.Parallel`; `TestNoParallelTestSwapsAPackageSeam` derives the seam set from the non-test `var`s, so the new seam is guarded automatically): swap `newProbeAgent` for a wrapper that calls `apogee.New` and appends each `*apogee.Agent` to a slice, restore it in `t.Cleanup`; run the `lint` advise config exactly as the existing "advise armed" subtest does (`probeContextLiveStub`, `eventLinesHome`, `appendHomeConfig`, `runProbeContext`); assert two Agents were built; `agents[0].Generation()`: `Bypass == false`, `len(Sync) == 1`, `Sync[0].ID == "lint"`, `Sync[0].Class == domain.ClassAdvise`; `agents[1].Generation()`: `Bypass == true`, `len(Sync) == 0`. Bite: with the arming lines removed the first assertion fails.
**Acceptance:** `go build ./... && go test ./cmd/apogee/ -run 'TestProbeContextLive|TestNoParallelTestSwapsAPackageSeam' -p 2 -parallel 2`
**Commit:** `test(probe): context --live proves the sync lane is armed on the as-configured Agent`
