# Audit 2026-09-20 — the local, test-pinnable fixes (plan D) — plan

**Goal:** close the 2026-09-20 code audit's findings that are local to one seam and pinnable by a
test: the memory-bomb class (PDF, skills catalog, provider stream, session records, MCP transport),
the FIFO read wedge, the glob-interpreted git pathspecs, the reactions close-panic, the un-held
session rename, the pre-opened thinking channel, and the MCP environment claim. Not gated on plans
A/B/C (`2026-09-20 - 01/02/03`) and must not touch the seams they own.

**Date:** 2026-09-21
**Status:** unexecuted
**sized for:** ~200k-context host
**Standing requirements:** skills: coding-standards; any authorized deviation from item text lands as a dated NOTES line under the item; no version identifier changes; `CHANGELOG.md` entries travel in item sidecars and land at closeout.

**Regression check (2026-09-21, e5b07e04):**
1: recast — the inflate budget is charged only to the streams the text path decodes (a `/Contents`-referenced stream regardless of label); test (a)'s `TotalAlloc` measurement runs serial; test (f) added.
2: guard folded — the byte counter is pinned on `Catalog`; a split-fill case is added.
3: guard folded — `open` sets `tripped` and `fold` reads it before the nil-drop; the byte cap sets the same field.
4: guard folded — the `TotalAlloc` delta is dropped or measured serially; a stat error falls through to `os.ReadFile` unchanged.
5: guard folded — the held map lives under its own `heldMu`; the map check is `Rename`'s only; the existing `renameErr`/`failRenames` are reused; `TestSessionHostRenameActiveSticks` is pinned.
6: recast — `buildStdioTransport` stays start-free behind apogee's `stdioTransport`/`stdinLadder`; supersedes `internal/mcp/client.go:224-236` and `docs/design/mcp-client.md:63-69`.
7: guard folded — the bounded body delegates `Close`; the HTTP-lane outcome is stated; the tool-list cap returns the capped list silently.
9: guard folded — `doc.go`'s file map and `DocMap` join; the FIFO read test moves to a `!windows` file; the socket ENXIO case and the `O_NONBLOCK` rationale are corrected.
10: guard folded — the `:(literal)` unmatched-pathspec wording is admitted or stripped; the comment sweep is by grep, git_show alone stays bare.
11: guard folded — the racing tests use an ungated executor or close the gate before `closeRunner`; a closed-set drop carries no "(queue full)" line.
12: recast — ratified: the shipped `minimax-m3` entry is pre-opened, the hold is released on split and is silent until the closer; `Resolve` copies `PreOpened`; supersedes `internal/domain/config.go:683-684` and `internal/processing/thinking.go:8-11` for the non-splitting pre-opened case.
1: round 2 — ratified: bound (c) is scoped to a `/Type /XRef` dictionary; the charge rule (Type/reference wins over any label) replaces the label list; the `/Contents` clause names all three forms and the ObjStm-hidden page dictionary is a stated gap; test (a) builds through `deflatedZeros`.
6: round 2 — guard folded: every comment or doc line naming the SDK's ladder as the stdio shutdown is re-pointed by grep; `internal/platform/teardown.go:86-93` joins the superseded sources and Files.
12: round 2 — guard folded: `internal/processing/parserfor_test.go` joins Files and passes `false` at its two `IsMidChannel` sites; a splitting server whose reply carries no DeltaThinking is admitted as the same held-reply bound.

## Authoritative sources

- `docs/reviews/code-audit-2026-09-20.md` — the findings (Critical & High, Medium) and the
  verification note on the CI-gate finding; **where an item disagrees with the audit, the item wins**.
- ADR 0011 (single-goroutine event path), ADR 0012 (read tools run unapproved in every mode),
  ADR 0049 §4 (control-plane writes), ADR 0031 (wire-silent engine); `SECURITY.md` threat model;
  `docs/design/mcp-client.md`; `docs/design/test-drivers.md`; `internal/reactions/doc.go`.
- Base commit `e5b07e04` (2026-09-21) — the tree the anchors below were read against.

## Ratified design calls

- **MCP stdio env default stays full-inherit** (owner, 2026-09-21): `SECURITY.md` states the MCP exception; the flip is declined.
- **PDF bound is a pre-flight scan over the raw bytes** (owner, 2026-09-21): no fork of `ledongthuc/pdf`; the scan inflates every FlateDecode stream against a budget before `pdf.NewReader` runs.
- **MCP responses are bounded at the read** (owner, 2026-09-21): a line-bounded reader on stdio stdout through an apogee `IOTransport` that re-implements the SDK close ladder, and on HTTP bodies through a RoundTripper; post-decode caps ride on top.
- **The thinking flag is `pre-opened: true`** (owner, 2026-09-21) on `ThinkingProfile`, the on-disk `thinking:` block and the shipped `minimax-m3` entry.
- **Author calls (2026-09-21):** cap values are the constants named in each item; `SafeOpen` refuses pipes/sockets/devices (never directories — `statInRoot`, `list_dir` open directories through it); `Rename` takes the same hold as `Delete`, a hold this Store already holds is honoured; the reactions fix is a per-`hookSet` `sync.RWMutex` plus a `closed` bit; a count-capped stream ends through the byte cap's `DeltaError` path.

## Out of scope

- The CI-gate finding — refuted and closed at `d3bc3ccb`.
- The `git config` command-valued key (plan B item 7), the undo-journal lock scope (after plan B item 4).
- `.beads/hooks` activation, `present.command` rung 3, both winlabel findings, the landlock/bwrap UDP and UNIX-socket residuals, session-snapshot ingestion — design work, beads at closeout.
- `security.Fence` / the `Safe*` write wrappers (plan B), `execHost` (plan A), the pane table (plan C).

---

## 1. A PDF is pre-flighted: inflated bytes, `/Size`, `/W` and `/Columns` are bounded before the parser runs

**What.** Recast at the regression check (2026-09-21). `fix(doctext)`: closes the audit's Critical "PDF extraction memory bombs" (both vectors). `internal/doctext/pdf.go`: a new `preflightPDF(ctx, data []byte) (cause string)` runs in `ExtractPDF` after `refuseAbsurdObjectCount` and before `pdf.NewReader`. It walks every `stream … endstream` body (reuse `pdfStreamBody`'s span logic) and its preceding dictionary: (a) a dictionary naming `/FlateDecode` has its body inflated through `zlib.NewReader` wrapped in a counting reader charged against `pdfMaxInflatedBytes = 64 << 20` — for the streams the regression guard names, not every stream (one budget for the whole document; a stream whose inflate errors is skipped, not refused — the library will report it); (b) `/DecodeParms` `/Columns` > `pdfMaxPredictorColumns = 1 << 16` refuses; (c) an `/W [a b c]` array in a dictionary that also names `/Type /XRef` whose any entry > 8 or whose sum > 24 refuses (a CIDFont's glyph-width `/W` array is never read). `refuseAbsurdObjectCount`'s `/Size` scan becomes lexer-faithful: the value is read by a small scanner that skips PDF whitespace (NUL, TAB, LF, FF, CR, SP) and `%` comments between the name and the digits — never the `\s`-only regex. Every refusal renders through `pdfUnreadableFormat` with a new cause constant per bound (`pdfInflatedCause = "inflated content exceeds the %d MiB budget"`, `pdfPredictorCause`, `pdfXrefWidthCause`); the existing `pdfAbsurdSizeFormat` keeps its wording. Budget and page-walk behaviour after the pre-flight are unchanged. Binding standards: the scanner and the inflate walk are package-private funcs with one reason to change each; no regex for the lexer-faithful read.
**Regression guard.** RATIFIED (owner, 2026-09-21, round 2) — the charge RULE, not a label list: a stream whose dictionary carries a `/Type` or `/Subtype` is SKIPPED unless that type is one the parser decodes (`/Type /XRef`, `/Type /ObjStm`, `/Type /CMap`) or a `/Contents` or `/ToUnicode` reference names it (`/Contents N 0 R`, `/ToUnicode N 0 R`, the inline array `/Contents [7 0 R 8 0 R]`, or the members of an array object a `/Contents` reference resolves to); an untyped stream (page content is untyped) and every named one is charged; Type and reference win over any label (a `/Subtype /Image` on an XRef/ObjStm stream does not exempt it), so image and form XObjects, embedded fonts, `/Type /EmbeddedFile` attachments and every other undecoded typed stream go uncharged while a relabelled bomb wired as page content is still caught. Bounded gap, stated in the item: a page dictionary compressed inside an ObjStm hides its `/Contents` reference from the raw scan. Bound (c) applies only to a dictionary that also names `/Type /XRef`. Test (a) builds its stream through a `deflatedZeros(t, inflated int) []byte` helper that streams zeros in chunks into a `zlib.Writer` (~80 MiB inflated, ~80 KiB deflated) — never a materialised `text` string; `deflatedStream(t, text)` stays for (e)/(f). Test (a)'s `TotalAlloc` measurement runs WITHOUT `t.Parallel()` (a sibling-free serial measurement); its cause assertion may stay in a parallel case. Add a test (f): a 200 KiB PDF whose one `/Subtype /Image` XObject inflates past the budget still extracts its page text.
**Files:** internal/doctext/pdf.go, internal/doctext/pdf_test.go
**Read first:** internal/doctext/pdf.go — ExtractPDF, refuseAbsurdObjectCount, pdfStreamBody, withoutStreamBodies, pdfDeclaredSize, pdfUnreadableFormat; internal/doctext/pdf_test.go — hostilePDF, onePagePDF, textPagesPDF, TestExtractPDF_RefusesAnAbsurdSizeInAnXrefStreamDictionary, TestExtractPDF_CapsAPhantomPageCount;
github.com/ledongthuc/pdf read.go — readXrefStream, readXrefStreamData, Value.Reader, applyFilter, Reader.resolve; github.com/ledongthuc/pdf page.go — Font.charmapEncoding, readCmap, Page.GetPlainText; github.com/ledongthuc/pdf ps.go — Interpret; github.com/ledongthuc/pdf lex.go — isSpace
**Tests.** New in `pdf_test.go` via `hostilePDF`/`onePagePDF`: a `deflatedStream(t, text)` helper (zlib-compressed `<< /Length N /Filter /FlateDecode >>`) for (e)/(f), and a `deflatedZeros(t, inflated int) []byte` helper that streams zeros in chunks into a `zlib.Writer` for (a) — (a) a PDF whose one untyped stream is `deflatedZeros` at ~80 MiB inflated (~80 KiB deflated), past `pdfMaxInflatedBytes` → `failMessage` contains `pdfInflatedCause`'s rendering and the test's `runtime.MemStats.TotalAlloc` delta stays under 128 MiB (the delta is measured in a serial case without `t.Parallel()`; the cause assertion may stay parallel); (b) `/Size\x001000000` in a 300-byte file → the absurd-size refusal (`pdfAbsurdSizeFormat`), FAILS pre-item; (c) `/W [1 2000000000 1]` placed in an xref-stream dictionary (`/Type /XRef`, the way `TestExtractPDF_RefusesAnAbsurdSizeInAnXrefStreamDictionary` builds its fixture) → `pdfXrefWidthCause`, and a CIDFont dictionary's `/W [32 126 600]` in a PDF that extracts today still extracts; (d) `/DecodeParms << /Columns 2000000000 >>` → `pdfPredictorCause`; (e) a legitimate deflated one-page PDF still extracts its text; (f) a 200 KiB PDF whose one `/Subtype /Image` XObject inflates past `pdfMaxInflatedBytes` still extracts its page text (the stream is not charged), and the same for a `/Type /EmbeddedFile` attachment; (g) a `/Type /XRef` stream that also spells `/Subtype /Image` and inflates past the budget → `pdfInflatedCause` (Type wins over the label). Must keep passing: every existing `TestExtractPDF_*`, `TestPDFAnnotation`. Cases (a)–(d) must FAIL against the pre-item tree (a: by OOM or a missing cause — assert the cause). CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go build ./... && go test ./internal/doctext/... -run 'PDF|Extract' && go test ./internal/tools/... -run 'ReadFile'`
**Commit:** `fix(doctext): a PDF is pre-flighted — inflated bytes, /Size, /W and /Columns are bounded before the parser allocates`

## 2. The skills catalog caps its aggregate bytes

**What.** `fix(skills)`: closes the audit's High "a cached skill catalog can exhaust ~2 GiB". `internal/skills/load.go`: `maxSkillCatalogBytes = 32 << 20`; `walkSkills` tracks the bytes `loadSkillFile` read (a `bytes` field on `Catalog`, read at `walkSkills` the way `cat.Len() >= maxSkills` is) and, when the next file would push the total over the cap, records `cat.addSkip(SkipError{Path: p, Err: fmt.Errorf("catalog byte cap (%d MiB) reached; this and any later skills under %s were not loaded", maxSkillCatalogBytes>>20, src.root)})` and returns `fs.SkipAll` — the soft-skip precedent of the skill cap at `walkSkills` (:385-393). The home library keeps its precedence: the one counter on `Catalog` spans both walks in the existing source order, so the home tree is read first and the workspace tree is what gets cut (mirror `TestLoadCapNeverEvictsTheHomeLibrary`). Doc comment on the constants explains the pair (per-file 1 MiB, catalog 32 MiB).
**Regression guard.** Pin the counter on `Catalog` (a bytes field beside `byID`, read the way `cat.Len() >= maxSkills` is at `walkSkills`) — never a `walkSkills`-local counter, which would let a 20 MiB home tree plus a 20 MiB workspace tree load 40 MiB while the skip announces "catalog byte cap (32 MiB) reached". Add a split-fill case — 20 MiB under home, 20 MiB under workspace → the workspace walk is the one cut, every home skill present.
**Files:** internal/skills/load.go, internal/skills/catalog.go, internal/skills/load_test.go
**Read first:** internal/skills/load.go — walkSkills, loadSkillFile, readBounded, Load, maxSkills; internal/skills/catalog.go — Catalog.set, Catalog.addSkip, Catalog.Len; internal/skills/load_test.go — writeSkill, writeCapFillingSkills, TestLoadCapNeverEvictsTheHomeLibrary; internal/skills/skill.go — SkipError.Reason
**Tests.** `TestLoadCatalogByteCapSkipsTheRest` — `writeSkill` 40 skills of 1 MiB−1 bytes each under a workspace tree plus one `ok` skill in the home tree: `cat.Get("ok")` present, exactly one skip whose `Reason()` contains `"catalog byte cap"`, `cat.Len() < 40`; FAILS pre-item. `TestLoadCatalogByteCapNeverEvictsTheHomeLibrary` — the same fill under the workspace tree with a 2-skill home tree keeps both home skills. `TestLoadCatalogByteCapSpansBothTrees` — 20 MiB of skills under home plus 20 MiB under the workspace → exactly one skip, naming the workspace root, and every home skill present. Must keep passing: `TestLoadOversizeSkillFileRefused`, `TestLoadCapNeverEvictsTheHomeLibrary`, `TestLoadCapAndCollisionStillFavourHome`, the `/skills` wording tests in `internal/tui/skill_test.go`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/skills/... && go test ./internal/tui/... -run 'Skill'`
**Commit:** `fix(skills): the catalog caps its aggregate bytes at 32 MiB and skips the rest with a reason`

## 3. A streamed reply may open at most 64 tool calls

**What.** `fix(provider)`: closes the audit's High "a hostile upstream streams unbounded open tool calls". `internal/provider/client.go`: `maxOpenToolCalls = 64`. `internal/provider/stream.go`: `openToolCalls.open` (the one choke point both wires reach through `fold → address → open`) refuses to append past the cap; `fold` returns true (the existing "ended" signal) when either the byte cap or the count cap trips, and `openToolCalls` gains a `tripped string` field naming which — `"apogee: tool call arguments exceeded size limit"` (unchanged) or `"apogee: streamed reply opened more than 64 tool calls"`. Both wires' inline `DeltaError` sites (`wire_openai.go:324-327`, `anthropicStream.foldCall`) read the message from `open.tripped` instead of the literal, so one message source exists; `Retryable` stays false. Binding standards: no second cap mechanism — the count lives beside the byte count in the same struct.
**Regression guard.** `open` is reached only through `address` (`fold → address → open`) and `fold` returns false on a nil target, so an `open` that merely returns nil past the cap is dropped silently and no `DeltaError` fires: `open` sets `o.tripped` to the count message and returns nil; `fold` returns true whenever `o.tripped != ""` (checked before the nil-drop); the byte-cap branch sets `o.tripped` too, so both wires read one field.
**Files:** internal/provider/client.go, internal/provider/stream.go, internal/provider/wire_openai.go, internal/provider/wire_anthropic_stream.go, internal/provider/stream_test.go, internal/provider/wire_anthropic_stream_test.go
**Read first:** internal/provider/stream.go — openToolCalls, fold, address, open, flush; internal/provider/wire_openai.go — openaiCodec.parseSSE; internal/provider/wire_anthropic_stream.go — anthropicStream.foldCall, startBlock; internal/provider/client.go — maxToolCallBytes, maxReplyTextBytes; internal/provider/stream_test.go — sseServer, collectStream, TestStream_ToolCallSizeCapSumsAcrossCalls; internal/provider/wire_anthropic_stream_test.go — parseAnthropicSSE
**Tests.** `TestStream_OpenToolCallCountIsCapped` (mirror `TestStream_ToolCallSizeCapSumsAcrossCalls`): an `sseServer` body with `maxOpenToolCalls+1` `delta.tool_calls` fragments at distinct indexes and empty arguments → kinds exactly `[]DeltaKind{DeltaError}` with the count message, zero calls; FAILS pre-item (today: 65 calls). `TestAnthropicParseSSE_OpenToolCallCountIsCapped` via `parseAnthropicSSE`: 65 `content_block_start` tool_use events → the same error. A 64-call body still yields 64 calls in index order. Must keep passing: `TestStream_ToolCallSizeCapSumsAcrossCalls`, `TestStream_InterleavedIndexedToolCallsStayApart`, `TestStream_ToolCallsEmitInIndexOrder`, `TestAnthropicParseSSE_TextAndInterleavedToolCalls`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/provider/... -run 'Stream|Anthropic'`
**Commit:** `fix(provider): a streamed reply may open at most 64 tool calls; the 65th ends the stream as the byte cap does`

## 4. A session record is refused at the read when it exceeds 256 MiB

**What.** `fix(session)`: closes the audit's High "session records decode with unbounded unmarshal". `internal/session/store.go`: `maxRecordBytes = 256 << 20`, sentinel `ErrRecordTooLarge = errors.New("apogee: session record exceeds the 256 MiB limit")`. `loadPath` stats before it reads: a file whose SUCCESSFUL stat reports `Size() > maxRecordBytes` returns `fmt.Errorf("apogee: read session %q: %w", path, ErrRecordTooLarge)` without reading it (a stat error falls through to today's `os.ReadFile`, whose error and wording stand); the read itself goes through `io.LimitReader(f, maxRecordBytes+1)` so a file that grows between stat and read is refused the same way. `decodeAnyRecord` keeps its shape — the byte cap is what bounds the three unmarshals. `internal/session/transcript.go`: `DecodeTranscript` refuses `len(data) > maxRecordBytes` with `ErrTranscriptTooLarge` before `json.Unmarshal`. `scan` already soft-skips a refused file; `resolveResumeArg` already folds any load error into its "not a known session id … nor a readable session file" line; `resumeLoaded` notes `"could not load session: " + err` — all unchanged and pinned below.
**Regression guard.** (1) `TestLoadRefusesAnOversizeRecord`'s `TotalAlloc` delta is process-wide while `TestDecodeTranscriptRefusesAnOversizeBlob` allocates 256 MiB+1 in the same package under `t.Parallel()`: drop the `TotalAlloc` assertion (the `errors.Is(err, ErrRecordTooLarge)` check already bites — pre-item the sparse file reads and fails as a JSON decode error) or, if kept, run both tests without `t.Parallel()` and compare the delta against `maxRecordBytes/2`. (2) `loadPath` acts only on a SUCCESSFUL stat with `Size() > maxRecordBytes`; on any stat error it falls through to today's `os.ReadFile` so its error and wording (`open <path>: no such file…` inside `could not load session: `, and Rename's ENOENT retry path) stand unchanged.
**Files:** internal/session/store.go, internal/session/transcript.go, internal/session/store_test.go, internal/session/transcript_test.go
**Read first:** internal/session/store.go — loadPath, decodeRecord, decodeAnyRecord, scan, Load; internal/session/transcript.go — DecodeTranscript, ErrTranscriptVersion; internal/session/store_test.go — TestLoadRejectsFutureRecordVersion, TestListSkipsCorruptFile; internal/session/transcript_test.go — TestDecodeTranscriptRefusesANewerVersion; cmd/apogee/wire_session.go — resolveResumeArg; internal/tui/sessions.go — resumeLoaded
**Tests.** `TestLoadRefusesAnOversizeRecord` (mirror `TestLoadRejectsFutureRecordVersion`): plant `<dir>/big.json` as a sparse file (`os.Truncate` to `maxRecordBytes+1` — no real bytes written) → `errors.Is(err, ErrRecordTooLarge)`, `List` hides it; the `TotalAlloc` delta assertion is dropped or, if kept, both oversize tests run without `t.Parallel()` and the delta is compared against `maxRecordBytes/2` (regression guard); FAILS pre-item. `TestDecodeTranscriptRefusesAnOversizeBlob`: a `make([]byte, maxRecordBytes+1)` blob → `ErrTranscriptTooLarge` without parsing (assert by timing < 1s or by a blob that is not valid JSON past its first byte). Must keep passing: `TestListSkipsCorruptFile`, `TestLoadRejectsFutureRecordVersion`, `TestDecodeTranscriptRejectsAMalformedBlob`, `TestDecodeTranscriptRefusesANewerVersion`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/session/... && go test ./cmd/apogee/... -run 'Resume'`
**Commit:** `fix(session): a record over 256 MiB is refused at the read, before any unmarshal`

## 5. `Store.Rename` takes the cross-process hold; the browser says when a rename was refused

**What.** `fix(session)`: closes the audit's High "Store.Rename is a stale read-modify-write". `internal/session/store.go`: `Rename` follows `Delete`'s pattern — `validateID`, `s.mu`, then `release, err := s.hold(id)` around the load→retitle→save, returning the `*HeldError` unchanged. A hold THIS Store already holds is honoured: `hold` records the id in a `held map[string]struct{}` (under its own `heldMu sync.Mutex`, never `s.mu`) and its release deletes it; `Rename` — and `Rename` only; `hold` and `Hold` keep refusing a second flock — of an id in that map skips the second flock (the live session the host holds for its whole life — `Hold` doc :355-367) and runs under the existing hold. `internal/tui/sessionsave.go` `foldRecordWrite`: a `writeRename` error whose write was NOT `retryTitle` (the browser's explicit rename, `renameSession`) adds the note `"could not rename session: " + err.Error()` — the exact shape of Delete's `"could not delete session: …"`; the autotitle's quiet `retryTitle` path stays silent as documented (:432-435). `fakeSessionHost` reuses its existing `renameErr`/`failRenames`.
**Regression guard.** (1) `Delete` holds `s.mu` and calls `hold` and `release()` under it, so the held map is guarded by its own `heldMu sync.Mutex` (never `s.mu`); `hold` inserts and the release closure deletes under `heldMu` only. (2) The "this Store already holds it → skip the flock" check lives in `Rename` only (`hold` and `Hold` keep refusing a second flock); `TestStoreDeleteRefusesAHeldSession` and `TestStoreHoldRefusesASecondHolder` join "must keep passing". (3) `fakeSessionHost` already has `renameErr` and `failRenames` (`internal/tui/seam_test.go`): reuse them — no new field, and `seam_test.go` leaves `**Files:**` unless a doc comment changes. (4) `TestSessionHostRenameActiveSticks` (`cmd/apogee/wire_session_test.go`) proves the honoured-own-hold call: it joins "must keep passing" and `go test ./cmd/apogee/... -run 'SessionHost'` joins Acceptance.
**Files:** internal/session/store.go, internal/session/store_test.go, internal/tui/sessionsave.go, internal/tui/sessions_test.go
**Read first:** internal/session/store.go — Rename, Delete, hold, Hold, save; cmd/apogee/wire_session.go — sessionHost.Rename, sessionHost.Save, holdLocked; internal/tui/sessionsave.go — foldRecordWrite, recordWrite.retryTitle; internal/tui/sessions.go — renameSession;
internal/session/store_test.go — holdStore, TestStoreDeleteRefusesAHeldSession, TestStoreHoldRefusesASecondHolder; internal/tui/seam_test.go — fakeSessionHost.renameErr, failRenames; internal/tui/sessions_test.go — TestSessionBrowserDeleteOfAHeldSessionNotesAndStays; cmd/apogee/wire_session_test.go — TestSessionHostRenameActiveSticks
**Tests.** `TestStoreRenameRefusesAHeldSession` (mirror `TestStoreDeleteRefusesAHeldSession` with `holdStore`): a second Store's `Rename` → `errors.As(err, &*HeldError)`, the title on disk unchanged; FAILS pre-item. `TestStoreRenameRunsUnderItsOwnHold`: `st.Hold(id)` then `st.Rename(id, "x")` succeeds and the title persists. `TestSessionBrowserRenameOfAHeldSessionNotesAndStays` (mirror the Delete sibling at `sessions_test.go:491`): `host.renameErr = &session.HeldError{ID: "sess-1", PID: 4242}` → note exactly `could not rename session: session sess-1 is open in another apogee (pid 4242) — fork it to work alongside`, browser stays open. Must keep passing: `TestStoreDeleteRefusesAHeldSession`, `TestStoreHoldRefusesASecondHolder`, `TestSessionHostRenameActiveSticks` (`cmd/apogee`), `TestRenamePersists`, `TestSaveAndRenameNeverLoseTheNewerPayload`, `TestSessionBrowserRenameCommits`, `TestAutoTitleAppliesThroughRename`, `TestTitleSurvivesARenameOfARecordNotOnDiskYet`, `TestRenameWithoutPersistenceSaysSo`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/session/... -run 'Rename|Hold|Delete' && go test ./internal/tui/... -run 'Rename|AutoTitle|SessionBrowser' && go test ./cmd/apogee/... -run 'SessionHost'`
**Commit:** `fix(session): Rename takes the cross-process hold and the browser notes a refused rename`

## 6. A stdio MCP server's output is read through a line-bounded reader

**What.** Recast at the regression check (2026-09-21). `fix(mcp)`: the read half of the audit's High "an MCP server's response is read with no size cap". New `internal/mcp/bounded.go`: `lineBoundedReader{r io.Reader, max int}` — an `io.Reader` that counts bytes since the last `\n` and, past `maxMCPMessageBytes = 4 << 20`, returns `errMCPMessageTooLarge` (`"apogee: mcp message exceeds the 4 MiB limit"`) so the SDK's `json.Decoder` fails and the connection ends. `internal/mcp/transport.go` `buildStdioTransport`: replace `mcpsdk.CommandTransport` with apogee's start-free `stdioTransport` whose `Connect` takes `cmd.StdoutPipe()`/`StdinPipe()`, starts the process and connects an `&mcpsdk.IOTransport{Reader: bounded stdout, Writer: stdinLadder}`; `stdinLadder.Close` re-implements the SDK ladder the wedged-server tests pin — close stdin → wait → SIGTERM → wait → SIGKILL → wait, exactly as the regression guard specifies. `stdioHost.ScopeEnv` and the env composition are untouched. Binding standards: the ladder lives in one func with the SDK's `cmd.go:62-99` order cited in its comment; `lineBoundedReader` has no MCP knowledge (item 7 reuses it on HTTP bodies).
**Regression guard.** `buildStdioTransport` stays START-FREE — it returns an apogee `stdioTransport{cmd, teardown}` implementing `mcpsdk.Transport` whose `Connect(ctx)` takes `cmd.StdoutPipe()`/`cmd.StdinPipe()`, calls `cmd.Start()` (a failed Start still reaches `connectOne`'s `reapProcess` path) and returns `(&mcpsdk.IOTransport{Reader: io.NopCloser(&lineBoundedReader{…}), Writer: stdinLadder}).Connect(ctx)` — the reader is NopCloser-wrapped so closing the connection is the stdin ladder alone (item 7 wraps HTTP bodies, which DO close, with a delegating Close). The ladder in `stdinLadder.Close`: close stdin → wait up to the terminate duration → `cmd.Process.Signal(syscall.SIGTERM)` (on a Signal error skip that wait, as the SDK does on Windows) → wait → `cmd.Process.Kill` → wait, with `cmd.Wait` run once in a goroutine as the SDK's `pipeRWC.Close` does; a non-positive `stdioTerminateDuration` maps to `defaultStdioTerminateDuration = 5 * time.Second` (the SDK's value) and the var's comment says so; `Client.Close`'s cancel + `reapProcess` stay after it unchanged. The test asserts an `IsError` result whose Content does not carry the 4 MiB text and names the failure (`errMCPMessageTooLarge`'s wording or the SDK's closed-connection text), then that a following `echo` call also fails (the session is dead); keep the TotalAlloc bound and the pre-item bite (today: a non-error 4 MiB result). Supersedes `internal/mcp/client.go:224-236` and `docs/design/mcp-client.md:63-69` explicitly — both record the SDK's close-stdin → wait → SIGTERM → SIGKILL ladder as the shutdown and are re-pointed at apogee's ladder in this item. Round 2 — the supersession is a RULE, not that closed list: every comment or doc line naming the SDK's (spec-shaped) shutdown/ladder as the stdio shutdown, found by `grep -rn "spec-shaped\|SDK's shutdown\|SDK's own shutdown\|SDK's stdio shutdown\|SDK's ladder\|shutdown ladder" internal/ docs/design/`, is re-pointed at apogee's ladder — today that also reaches `internal/platform/teardown.go:86-93` (`ProcessWaitDelay`'s comment, superseded here alongside the two named sources), `internal/mcp/client.go:47`, `internal/mcp/transport.go:140-144` and `192-193`, and `internal/mcp/mcp_test.go:316,362,404`.
**Files:** internal/mcp/bounded.go, internal/mcp/bounded_test.go, internal/mcp/transport.go, internal/mcp/client.go, internal/mcp/mcp_test.go, internal/platform/teardown.go, docs/design/mcp-client.md
**Read first:** internal/mcp/transport.go — buildStdioTransport, stdioTerminateDuration, buildTransport; SDK mcp/cmd.go — CommandTransport.Connect, pipeRWC.Close; SDK mcp/transport.go — IOTransport, rwc, newIOConn; SDK internal/jsonrpc2/conn.go — readIncoming, updateInFlight, shuttingDown;
internal/mcp/client.go — connectOne, Client.Close, reapProcess, liveSession; internal/mcp/tool.go — serverTool.Execute, errorResult; internal/mcp/mcp_test.go — runFixtureServer, connectFixture, TestBuildStdioTransport_CancelArmsTheCmdsTeardown, TestClose_BoundsTheDrainOfAWedgedStdioServer, buildStdioCmd;
internal/platform/teardown.go — ProcessWaitDelay
**Tests.** `bounded_test.go`: table over `lineBoundedReader` — a 4 MiB+1 line without newline errors, two 3 MiB lines pass, the error is sticky. `mcp_test.go`: the fixture server gains a `bloat` tool returning a `TextContent` of `size` bytes (arg); `TestExecute_OversizeResponseEndsTheConnection` — `bloat` at `maxMCPMessageBytes+1` → `Execute` returns an `IsError` result whose Content does not carry the 4 MiB text and names the failure (`errMCPMessageTooLarge`'s wording or the SDK's closed-connection text), then a following `echo` call also fails (the session is dead); the process TotalAlloc delta stays under 32 MiB; FAILS pre-item (today: a non-error 4 MiB result). Must keep passing: `TestBuildStdioTransport_CancelArmsTheCmdsTeardown`, `TestClose_TearsDownSessions`, `TestConnect_SurfacesServerToolsAndCalls`, `TestExecute_ServerToolErrorIsErrorResult`, `TestExecute_CancelledContextIsGoError`, the wedged-server close-ladder tests, `TestBuildStdioTransport_EnvAllowlistScopesTheChildEnvironment`, `TestConnect_StdioServerLaunchesUnderAnEmptyEnvAllowlist`. The comment sweep is checked by the grep the regression guard names returning no line that still calls the SDK's ladder the stdio shutdown. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/mcp/... && go build ./internal/platform/...`
**Commit:** `fix(mcp): a stdio server's output is read through a 4 MiB line-bounded reader behind apogee's own transport`

## 7. HTTP MCP bodies are bounded; a result and a tool list are capped after decode

**What.** `fix(mcp)`: the remaining half of the same finding. Depends on item 6. `internal/mcp/transport.go` `newGuardedHTTPClient`: the client's `Transport` is wrapped in a `boundedBodyTransport` RoundTripper that replaces `resp.Body` with a `bounded{lineBoundedReader; io.Closer}` whose `Close` closes the original body (SSE events and streamable JSON responses are newline-framed; a long-lived SSE stream is never cut by a cumulative cap). The HTTP-lane outcome, stated here and in the CHANGELOG line: the body read errors; a streamable SSE reply stalls the call to its ctx or the SDK's retry budget. `internal/mcp/tool.go` `renderContent`: the flattened text is clipped at `maxMCPResultBytes = 2 << 20` with the marker `"\n[mcp result truncated at %d bytes]"` (the `internal/tools/network.go` precedent). `internal/mcp/client.go` `listServerTools`: at most `maxMCPToolListPages = 64` pages and `maxMCPToolsPerServer = 512` tools — past either, stop and return the capped list silently — no Client report or notice path exists (`client.go` returns tools and errors only); a tool whose normalised schema exceeds `maxMCPToolSchemaBytes = 64 << 10` is skipped like an empty-name tool.
**Regression guard.** (1) `resp.Body` is an `io.ReadCloser` the SDK closes itself (the SSE stream in Close, `handleJSON` after ReadAll, `processStream` on drain): a `NopCloser`-wrapped reader would never close the real body and every connection would leak, so the wrapped body is a `bounded{lineBoundedReader; io.Closer}` whose Close closes the original `resp.Body`; `TestGuardedClient_PinsTheEndpointAndRefusesEverythingElsePrivate` already calls `resp.Body.Close()` on the production client — add one assertion that Close reaches the underlying body. (2) "The connection ends" is not the HTTP-lane outcome for a POST answered as SSE (`processStream` breaks on the read error, `handleSSE` returns silently without an event id, and with one it retries up to `maxRetries=5`): the item and the CHANGELOG line state "the body read errors; a streamable SSE reply stalls the call to its ctx or the SDK's retry budget", and the item's test on `client.Do` stays the pin; no code change keeps the memory bound either way.
**Files:** internal/mcp/transport.go, internal/mcp/transport_test.go, internal/mcp/tool.go, internal/mcp/client.go, internal/mcp/mcp_test.go
**Read first:** internal/mcp/transport.go — newGuardedHTTPClient, vetEndpoint; internal/mcp/transport_test.go — endpointClient, TestGuardedClient_PinsTheEndpointAndRefusesEverythingElsePrivate; internal/mcp/tool.go — renderContent, normaliseSchema, newServerTool; internal/mcp/client.go — listServerTools, connectOne; internal/mcp/mcp_test.go — TestRenderContent (lives here, not tool_test.go), runFixtureServer;
SDK mcp/streamable.go — handleJSON, processStream, handleSSE; SDK mcp/server.go — DefaultPageSize (1000: the 64-page cap needs ServerOptions.PageSize small); internal/tools/http_request.go — the `[response truncated at %d bytes]` marker precedent (network.go carries only readCappedBody)
**Tests.** `transport_test.go`: an `httptest.NewServer` answering a streamable POST with a 4 MiB+1 single-line body → the client's `Do` body read errors with `errMCPMessageTooLarge`; FAILS pre-item. `TestRenderContent` gains "oversize text is clipped with the marker" (2 MiB+1 text → length ≤ 2 MiB + marker, marker present). `TestListServerTools_CapsPagesAndTools`: an in-process `mcpsdk.NewServer` (or the re-exec fixture with a `many` env switch) advertising 600 tools → 512 returned; a tool with a 65 KiB schema is absent. `TestGuardedClient_PinsTheEndpointAndRefusesEverythingElsePrivate` gains one assertion that `resp.Body.Close()` reaches the underlying body. Must keep passing: `TestGuardedClient_PinsTheEndpointAndRefusesEverythingElsePrivate`, `TestGuardedClient_DoesNotFollowRedirects`, `TestRenderContent`'s existing subtests. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/mcp/...`
**Commit:** `fix(mcp): HTTP bodies are line-bounded; a result is clipped at 2 MiB and a tool list at 512 tools`

## 8. `SECURITY.md` states the stdio MCP environment exception

**What.** `docs(security)`: closes the audit's Medium "MCP stdio servers launch with the full inherited environment" by the ratified route — the claim is narrowed, the default stays. `SECURITY.md` "Secrets" bullet (:32-34): after "every tool subprocess and reaction command", one sentence: a stdio MCP server is the exception — it inherits apogee's full environment unless its entry sets `env-allowlist:` (the wording `docs/manual/configuration.md:1568-1569` already uses; link to it). No code, no config, no test changes.
**Files:** SECURITY.md
**Read first:** SECURITY.md — the "Secrets" bullet; docs/manual/configuration.md — the `api-key-env:` scrub paragraph's "A stdio MCP server is the exception" sentence; internal/mcp/transport.go — the TRUST NOTE on buildStdioTransport; docs/design/mcp-client.md — §2 environment paragraph
**Tests.** None — docs-only; the acceptance grep is the check.
**Acceptance.** `grep -n 'env-allowlist' SECURITY.md && go build ./...`
**Commit:** `docs(security): SECURITY.md states that a stdio MCP server inherits the full environment unless env-allowlist is set`

## 9. `SafeOpen` never blocks on a FIFO and refuses pipes, sockets and devices

**What.** `fix(security)`: closes the audit's High "a FIFO planted in the workspace wedges read_file and grep". The audit's "fstat the opened descriptor" is insufficient: `os.Root.Open` is `O_RDONLY` and blocks on a FIFO before any Stat. `internal/security/safeio.go` `SafeOpen`: open through `r.OpenFile(rel, os.O_RDONLY|openNonblock, 0)` where `openNonblock` is `syscall.O_NONBLOCK` in a new `safeio_open_unix.go` (`//go:build !windows`) and `0` in `safeio_open_windows.go`; then `f.Stat()` and, when `info.Mode()&(os.ModeNamedPipe|os.ModeSocket|os.ModeDevice|os.ModeCharDevice) != 0`, close and return `fmt.Errorf("%w: %s", ErrNotRegular, input)` with the new sentinel `ErrNotRegular = errors.New("not a regular file")`. Directories still open (`statInRoot`, `list_dir`, `path_suggest`, `directoryFiles` rely on it). The flag stays on the descriptor; `read(2)` and `getdents` ignore `O_NONBLOCK` on a regular file and a directory, which the test below proves (the comment the implementer writes says the same, never that Go clears the flag). `internal/tools/path_read.go` `escapeOrMessage`: `ErrNotRegular` passes through verbatim like `ErrRootInaccessible`, so read_file renders `not a regular file: <path>` and never `file not found`. A UNIX socket never reaches the Stat gate — `os.Root.OpenFile` refuses it with ENXIO before any fstat — so the socket outcome is the regression guard's choice, pinned by `TestSafeOpen_RefusesASocket`. This is the ONE `Safe*` read-side edit this plan makes; `SafeReadFile` and the write wrappers are plan B's.
**Regression guard.** (1) `TestDocMapNamesEveryFile` (`internal/security/docmap_test.go`) goes red the moment `safeio_open_unix.go` and `safeio_open_windows.go` land unnamed: `internal/security/doc.go` joins `**Files:**` with one file-map line per new file, and the acceptance runs `-run 'SafeOpen|SafeCopy|DocMap'`. (2) `syscall.Mkfifo` is undefined on GOOS=windows, so `TestReadFile_RefusesAFIFO` lives in a new `//go:build !windows` file, `internal/tools/read_file_unix_test.go` (the `exec_pgroup_unix_test.go` pattern), listed in `**Files:**` instead of `read_file_test.go`. (3) A UNIX socket in the workspace never reaches the Stat gate — `os.Root.OpenFile` returns openat ENXIO before any fstat, so `ErrNotRegular` never fires and read_file renders today's absent wording `file not found: <path>`: state in **What** that a socket is refused by open(2) itself (ENXIO) and keeps today's absent wording, or map `syscall.ENXIO` to `ErrNotRegular` in `SafeOpen`; add `TestSafeOpen_RefusesASocket` (`net.Listen("unix", …)`) to pin whichever is chosen. (4) The rationale "Go clears `O_NONBLOCK` on a non-pollable regular file at `newFile`" is false (Go clears it only when it set the flag itself): the mechanism in **What** and the comment the implementer writes read "the flag stays on the descriptor; read(2) and getdents ignore O_NONBLOCK on a regular file and a directory, which the test proves".
**Files:** internal/security/safeio.go, internal/security/safeio_open_unix.go, internal/security/safeio_open_windows.go, internal/security/doc.go, internal/security/safeio_fifo_unix_test.go, internal/tools/path_read.go, internal/tools/read_file_unix_test.go
**Read first:** internal/security/safeio.go — SafeOpen, openCopySource, mapRootEscape; internal/tools/path_read.go — escapeOrMessage, readFileErrorMessage, readScope.open; internal/tools/path_safety.go — safeOpen, statInRoot; internal/security/doc.go — the file map; internal/security/docmap_test.go — TestDocMapNamesEveryFile;
internal/tools/exec_pgroup_unix_test.go — the !windows test-file pattern; internal/tools/read_file_test.go — TestReadFile_Execute_RefusesEscapingSymlink (fixture shape); internal/tools/list_dir.go — Execute, directoryNotFoundMessage
**Tests.** `safeio_fifo_unix_test.go` (`//go:build !windows`, fixtures via `os.WriteFile`, never `SafeWriteFile`): `TestSafeOpen_RefusesAFIFOWithoutBlocking` — `syscall.Mkfifo` in the root, `SafeOpen` under a 2 s deadline goroutine returns `errors.Is(err, ErrNotRegular)`; FAILS pre-item (hangs — the test fails on the deadline). `TestSafeOpen_RegularFileStillReadsBlocking` — a 1 MiB file opened through `SafeOpen` reads fully with `io.ReadAll` and no `EAGAIN`. `TestSafeOpen_DirectoryStillOpens`. `TestSafeOpen_RefusesASocket` — `net.Listen("unix", …)` in the root pins the socket outcome the regression guard chooses (ENXIO's absent wording, or `ErrNotRegular`). `read_file_unix_test.go` (`//go:build !windows`, the `exec_pgroup_unix_test.go` pattern): `TestReadFile_RefusesAFIFO` → result `IsError` with content `not a regular file: <path as read_file spells it>`. Must keep passing: `TestDocMapNamesEveryFile`, `TestSafeOpen_*`, `TestSafeCopyFile_RefusesNonRegularSource`, `TestReadScopeOpen`, `TestReadScopeReadBounded`, the `"not a file: "` directory pins in `path_read_test.go`, `file_ops_test.go`, `diff_test.go`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go build ./... && GOOS=windows go build ./internal/security/... && go test ./internal/security/... -run 'SafeOpen|SafeCopy|DocMap' && go test ./internal/tools/... -run 'ReadFile|ReadScope|Grep|ListDir|FileOps'`
**Commit:** `fix(security): SafeOpen opens non-blocking and refuses pipes, sockets and devices instead of wedging on a FIFO`

## 10. Model-supplied git pathspecs are literal on commit, diff and log

**What.** `fix(tools)`: closes the audit's High "model-supplied git pathspecs are glob-interpreted". `internal/tools/git.go`: `CommitPathspecs` wraps each `workspacePathspec` result in `literalPathspec` (moved from `git_stage.go` to `git.go` beside `workspacePathspec`, same body — `git_stage.go` keeps calling it); `GitDiffRange.Execute` and `GitLog.Execute` wrap their `workspacePathspec` results the same way. `GitShow.Execute` is NOT touched — its `<ref>:./<rel>` is an object name, not a pathspec. `workspacePathspec` itself stays bare (plan B item 5 re-bodies it). Every comment naming `workspacePathspec` or the pathspec rule in `git.go` (`grep -n 'workspacePathspec\|pathspec' internal/tools/git.go` — the `gitWrite` doc, `gitReadCall.pathspecs`, the commit staging comment, git_log's pathspec comment) is reworded: commit, diff and log carry `:(literal)` pathspecs exactly as the staging helper does, and the git_show comment says git_show alone stays bare. git's unmatched-pathspec error now quotes the `:(literal)` spelling (`fatal: pathspec ':(literal)nope.txt' did not match any files`), as the staging helper's skipped-note already does — unless the rendered `add` failure has the prefix stripped instead (regression guard). `internal/agent/secretsguard.go` follows automatically through `CommitPathspecs`.
**Regression guard.** (1) A `files` entry git cannot match renders git's own stderr verbatim through `gitResultText`, so the model-facing wording changes from `fatal: pathspec 'nope.txt' did not match any files` to `fatal: pathspec ':(literal)nope.txt' did not match any files`: admit it in **What** ("git's unmatched-pathspec error now quotes the :(literal) spelling, as the staging helper's note already does") — or strip the `literalPathspec` prefix from the rendered `add` failure; no code guard is needed for behaviour. (2) The `gitWrite` doc (:255-257) is a closed site — `gitReadCall.pathspecs` (:169-171), the commit staging comment (:558-559), "the same rule git_log and git_show follow" (:733-734, false after the item) and git_log's pathspec comment (:1117-1119) all describe the rule: the sweep is "every comment naming `workspacePathspec` or the pathspec rule in git.go" plus `grep -n 'workspacePathspec\|pathspec' internal/tools/git.go`, and :733-734 says git_show alone stays bare.
**Files:** internal/tools/git.go, internal/tools/git_stage.go, internal/tools/git_test.go, internal/tools/git_stage_test.go
**Read first:** internal/tools/git.go — CommitPathspecs, workspacePathspec, GitDiffRange.Execute, GitLog.Execute, gitWrite; internal/tools/git_stage.go — literalPathspec, stageGitPaths; internal/tools/git_test.go — gitRepo, recordGitArgv, TestCommitPathspecsMatchTheCommitTool, TestGitLog_PathNarrowsAfterTheDoubleDash; internal/tools/git_stage_test.go — TestStageGitPaths_GlobMetacharacterFilename; internal/agent/secretsguard.go — commitSecretsCheck
**Tests.** `TestGitCommit_BracketFilenameStagesOnlyItself` (real git via `gitRepo`): workspace holding `a[1]` and `a1`, `git_commit` with `files: ["a[1]"]` → `git show --stat HEAD` names `a[1]` only; FAILS pre-item (stages `a1`). `TestGitDiffRange_BracketPathIsLiteral` and `TestGitLog_BracketPathIsLiteral` the same way. Updated argv pins: `TestGitCommit_StagesWorkspaceRelativePathspecs` wants `add -- :(literal)a.txt :(literal)docs/`, `TestCommitPathspecsMatchTheCommitTool` wants the `:(literal)` spellings, the argv table's "git_diff_range with paths" and `TestGitLog_PathNarrowsAfterTheDoubleDash` likewise. Must keep passing: `TestStageGitPaths_GlobMetacharacterFilename`, `TestGitLog_PathFiltersTheHistory`, `TestGitDiffRange_PathEscapeRejected`, `TestGitLog_PathShapedRefIsNotAPathspecLog`, the secrets-guard tests in `internal/agent`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test ./internal/tools/... -run 'Git|Stage|CommitPathspecs' && go test ./internal/agent/... -run 'Secrets'`
**Commit:** `fix(tools): git_commit, git_diff_range and git_log pass model-supplied paths as :(literal) pathspecs`

## 11. A Reaction reload or close can no longer panic the engine's Emit goroutine

**What.** `fix(reactions)`: closes the audit's High "send on closed channel". `internal/reactions/runner.go`: `hookSet` gains `mu sync.RWMutex` and `closed bool`. `fanOut` takes `set.mu.RLock()` for the duration of its sends and returns at once when `set.closed` (its firings are counted on `w.dropped` without `noteDrop`'s "(queue full)" first-drop line — the set is going away; the queue was never full). `drainSet` takes `set.mu.Lock()`, sets `closed = true`, closes every `w.queue`, unlocks, then waits as today. An `RLock` waits only while `drainSet` holds the write lock across the `close` calls — microseconds — so `Emit`'s "never blocks" contract (runner.go:86-90) holds in substance; the doc comment on `active` (:102-104) and on `fanOut` (:212-214) are rewritten to state the pairing: the atomic pointer decides WHICH set, the set's own lock decides whether its queues are still open. No change to `Replace`/`Close` ordering, `swapMu`, `queueDepth` or the drain grace.
**Regression guard.** (1) A gated `fakeExecutor` whose gate is never closed leaves the last set's worker blocked in `Run`, so `closeRunner` waits `awaitDeadline` (5 s), `Close` returns "gave up waiting" and the racing tests fail: use an ungated `newFakeExecutor()` (no gate is needed to provoke the send-on-closed-channel) or `close(exec.gate)` before `closeRunner`, as `TestRunnerDropsTheNewestFiringWhenAHooksQueueIsFull` does. (2) A firing that lands after `drainSet` set `closed` must not route through `noteDrop`, whose first-drop line reads "reaction X: dropped 1 event (queue full)" — a reload would announce a queue-full drop for a queue that was never full: count the closed-set firing on `w.dropped` without the "(queue full)" first-drop line (or word it "(reloading)"); leave `TestRunnerDropsTheNewestFiringWhenAHooksQueueIsFull`'s pinned wording untouched.
**Files:** internal/reactions/runner.go, internal/reactions/runner_test.go
**Read first:** internal/reactions/runner.go — hookSet, Runner.fanOut, Runner.drainSet, Runner.Emit, Runner.Replace, Runner.Close, Runner.noteDrop; internal/reactions/runner_test.go — fakeExecutor, newFakeExecutor, commandHook, turnEvent, closeRunner, awaitDeadline, TestRunnerDropsTheNewestFiringWhenAHooksQueueIsFull
**Tests.** `TestEmitRacingReplaceNeverPanics`: a Runner with one `commandHook` and an ungated `newFakeExecutor()` (or `close(exec.gate)` before `closeRunner`, as `TestRunnerDropsTheNewestFiringWhenAHooksQueueIsFull` does); goroutine A emits `turnEvent` in a tight loop for 200 ms, goroutine B calls `Replace` with a fresh one-hook list every 1 ms for the same window, then `closeRunner`. Runs under `-race`; pre-item it panics (`send on closed channel`) or reports a race within a few hundred iterations — the test must FAIL against the pre-item tree (`go test -race -count=20 -run TestEmitRacingReplaceNeverPanics`). `TestEmitRacingCloseNeverPanics` the same with `Close`. Must keep passing: `TestReplaceStopsTheOldHookAndStartsTheNew`, `TestCloseWithAnExpiredContextCancelsTheRunningHook`, `TestCloseIsIdempotent`, `TestEmitReturnsWhileAHookIsBlocked`, `TestRunnerDropsTheNewestFiringWhenAHooksQueueIsFull`, `TestSeamClosedProjectionIsTakenBeforeEmitReturns`. CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go test -race -count=5 ./internal/reactions/...`
**Commit:** `fix(reactions): a queue is closed under its set's lock, so a firing racing a reload never sends on a closed channel`

## 12. `ThinkingProfile.PreOpened` — a pre-opened channel holds the live stream until the first closer

**What.** Recast at the regression check (2026-09-21). `fix(processing)`: closes the audit's High "the streaming thinking-channel hold misses the pre-opened-template case" (minimax-m3, the shape `thinking.go:40-48` documents). `internal/domain/config.go`: `ThinkingProfile` gains `PreOpened bool` (doc: the chat template opens the channel before the model's first byte, so content starts mid-think and carries only the closer). `internal/processing/thinking.go`: `ThinkingConfig` gains `PreOpened bool`; `IsThinking` returns true when `cfg.PreOpened` and `raw` holds no `EndToken` yet AND no `StartToken` (the implicit leading span is open); once a closer has landed, today's last-opener rule governs. `StripThinking` is unchanged (it already treats the orphan closer as an implicit span). `internal/processing/parserfor.go` `stripperFor` carries the flag into the config; the stripper seam and the hold's release on a splitting server are the regression guard's. `internal/profiles/shipped.go`: the `minimax-m3` entry sets `PreOpened: true`; the `entry.go` axis predicate for thinking treats `PreOpened` as part of the thinking axis (a user entry that spells `thinking:` replaces the whole axis, as today). The on-disk key is item 13's.
**Regression guard.** RATIFIED (owner, 2026-09-21): the shipped `minimax-m3` entry DOES set `PreOpened: true`, and the hold is released on split — `internal/agent/loop.go` joins Files: `streamResponse`'s observe closure tracks `splitSeen` (set on the first `provider.DeltaThinking` of this stream); `ContentStripper.IsMidChannel` becomes `IsMidChannel(raw string, splitSeen bool) bool` (noneStripper and harmonyStripper ignore the bool; delimitedStripper evaluates `IsThinking` against a config copy whose `PreOpened` is cleared when `splitSeen`), so a server that splits reasoning into `reasoning_content` streams live exactly as today and only a server that leaks the closer is held; a no-closer reply on a non-splitting server stays held to its MessageEvent — bounded and stated in the item. Round 2 — the same bound admits a splitting server whose reply carries no `DeltaThinking` (the model skipped reasoning, or thinking is off for the turn): `splitSeen` stays false and every content delta is held to the MessageEvent where today it streams live; nothing on the wire tells the two apart before the first delta, and no code change keeps liveness without re-leaking the pre-opened span — the item states this beside the non-splitting case. `internal/processing/parserfor_test.go` joins Files: its two `IsMidChannel` call sites (`TestParserFor_StripperByStyle`, `TestParserFor_NoneStripperIsByteIdentical`) pass `false` as the new second argument so the processing test package keeps compiling. `internal/profiles/match.go` joins Files: `Resolve` copies `PreOpened` beside Start/End; `internal/profiles/match_test.go` joins Files with the rule "every TestResolve row whose thinking style resolves from the shipped minimax-m3 entry gains `PreOpened: true` in its wantProfile" (grep `wantProfile: delimited("<mm:think>"` plus the effort-only row). The hold is SILENT: no ReasoningEvent is emitted for the pre-opened span until the closer lands (StripThinking unchanged), the item states this, and the live-stream test asserts exactly one ReasoningEvent, emitted at the closer, carrying the whole pre-opened text; a second live-stream test feeds a `stubllm.Turn` with `ReasoningChunks` (split) under the pre-opened profile and asserts the tokens stream live (tokenTexts non-empty before the MessageEvent). Supersedes the pass-through default note at `internal/domain/config.go:683-684` and `internal/processing/thinking.go:8-11` for the pre-opened case in which no split arrives (a non-splitting server, or a splitting one whose reply carries no `DeltaThinking`) — the item names both lines and rewords them.
**Files:** internal/domain/config.go, internal/processing/thinking.go, internal/processing/thinking_test.go, internal/processing/parserfor.go, internal/processing/parserfor_test.go, internal/profiles/shipped.go, internal/profiles/shipped_test.go, internal/profiles/entry.go, internal/profiles/match.go, internal/profiles/match_test.go, internal/agent/loop.go, internal/agent/streamsuppress_test.go
**Read first:** internal/processing/thinking.go — ThinkingConfig, IsThinking, StripThinking; internal/processing/parserfor.go — ContentStripper, stripperFor, delimitedStripper, noneStripper, harmonyStripper; internal/processing/parserfor_test.go — TestParserFor_StripperByStyle, TestParserFor_NoneStripperIsByteIdentical;
internal/agent/loop.go — streamResponse, emitVisibleDelta, emitReasoningDelta; internal/agent/streamsuppress_test.go — chunkedResponder, TestStream_DelimitedThinkingHeldOffLiveStream, tokenTexts, reasoningTexts, assertNoLeak;
internal/profiles/match.go — Resolve; internal/profiles/match_test.go — TestResolve, delimited; internal/profiles/shipped.go — shippedTable
**Tests.** `thinking_test.go`: `TestIsThinking_PreOpenedChannelIsMidThinkUntilTheCloser` — table: `("The user said hi.", PreOpened) → true`, `("The user said hi.</mm:think>Hello", PreOpened) → false`, `("Hello", !PreOpened) → false`, `("</mm:think>Hello<mm:think>more", PreOpened) → true`; FAILS pre-item (row 1). `streamsuppress_test.go`: `TestStream_PreOpenedThinkingHeldOffLiveStream` (the `TestStream_DelimitedThinkingHeldOffLiveStream` idiom with `PreOpened: true`, Start `<mm:think>`, End `</mm:think>`): chunks `{"The user ", "said hi.", "</mm:think>", "Hello there!"}` → `tokenTexts` join to exactly `Hello there!`, `reasoningTexts` carry `The user said hi.` as exactly one ReasoningEvent, emitted at the closer and carrying the whole pre-opened text (the hold is silent until then), `assertNoLeak`; FAILS pre-item. `TestStream_PreOpenedSplitReasoningStreamsLive` — a `stubllm.Turn` with `ReasoningChunks` (split) under the same pre-opened profile → the tokens stream live (`tokenTexts` non-empty before the `MessageEvent`). `shipped_test.go`: the minimax entry's `Thinking.PreOpened` is true. `match_test.go`: every `TestResolve` row whose thinking style resolves from the shipped minimax-m3 entry gains `PreOpened: true` in its wantProfile (grep `wantProfile: delimited("<mm:think>"` plus the effort-only row). `parserfor_test.go`: `TestParserFor_StripperByStyle` and `TestParserFor_NoneStripperIsByteIdentical` pass `false` at their `IsMidChannel` sites and keep their assertions. Must keep passing: `TestStripThinking_OrphanCloser_ImplicitLeadingSpan`, `TestIsThinking_PortedOracleVectors_MatchTypeScript`, `TestParserFor_StripperByStyle`, `TestParserFor_NoneStripperIsByteIdentical`, `TestStream_DelimitedThinkingHeldOffLiveStream`, `TestSetProfileMovesTheStripperToo`, `internal/profiles/match_test.go` (with those rows updated). CHANGELOG sidecar, `[Unreleased]/Fixed`.
**Acceptance.** `go build ./... && go test ./internal/processing/... ./internal/profiles/... && go test ./internal/agent/... -run 'Stream_|SetProfile'`
**Commit:** `fix(processing): a pre-opened thinking channel holds the live stream until its first closer; minimax-m3 ships pre-opened`

## 13. `thinking.pre-opened:` — the on-disk key, its validation and the manual

**What.** `feat(config)`: depends on item 12. `internal/config/config.go`: `thinkingConfig` gains `PreOpened bool `yaml:"pre-opened"``; `toModelProfile` maps it; `validateModelProfiles` refuses `pre-opened: true` with any `style` other than `delimited` (message `model-profiles.<name>.thinking.pre-opened needs style: delimited`, shaped like the block's existing refusals). `internal/config/defaults/config.yaml` (:1131-1142 thinking block): a commented `pre-opened: false` line with a one-line explanation. `docs/manual/configuration.md` model-profiles section: one paragraph on the key beside `start:`/`end:`. The registry needs no new row (the `model-profiles` structured row carries the block — check and say so in a NOTES line if a row is required).
**Files:** internal/config/config.go, internal/config/config_test.go, internal/config/defaults/config.yaml, docs/manual/configuration.md
**Read first:** internal/config/config.go — thinkingConfig, modelProfileConfig.toModelProfile, validateThinkingAxes, validateDelimitedTokens; internal/config/config_test.go — TestApplyConfigModelProfiles, TestApplyConfigModelProfileEffort; internal/config/registry_test.go — TestRegistryIsBijectionWithFileConfig, walkSchema (the `model-profiles` structured row terminates the descent, so no new row);
internal/config/defaults/config.yaml — the `thinking:` style/effort axis prose and the commented `model-profiles:` example (near :1161-1177 and :1203-1208 at BASE, not :1131-1142); docs/manual/configuration.md — the model-profiles `start:`/`end:` paragraph
**Tests.** `config_test.go`: `TestApplyConfigModelProfileThinkingPreOpened` — a `model-profiles:` entry with `pre-opened: true` parses to `Thinking.PreOpened == true`; a `style: harmony` + `pre-opened: true` entry refuses with the exact message above; FAILS pre-item (unknown key or missing field). Must keep passing: every `TestApplyConfigModelProfile*`, `validateModelProfiles` tests, the defaults-template parse test. CHANGELOG sidecar, `[Unreleased]/Added`.
**Acceptance.** `go test ./internal/config/... -run 'ModelProfile|Defaults|Template'`
**Commit:** `feat(config): model-profiles.<name>.thinking.pre-opened marks a template that opens the channel before the model's first byte`
