# Fence, sanitize and the in-passing defects (2026-09-20 architecture review, plan B) — plan

**Goal:** land candidates #4 (the approved-escape permit is a typed `security.Fence`) and #7 (one
sequence-aware strip, one rune clamp), fix the confirmed in-passing defects they neighbour (the
`apogee undo` verb's missing generation stamp and session hold; the `git config` command-key
write the shell write view never sees; `userexec`'s leaked process tree; the Console's raw group
kill), and record the review's standing denials as an ADR. Gated on plan A
(`2026-09-20 - 01`) archiving; plan C (`2026-09-20 - 03`) is gated on this one.

**Date:** 2026-09-20
**Status:** unexecuted
**sized for:** ~200k-context host
**Standing requirements:** skills: coding-standards; any authorized deviation from item text lands as a dated NOTES line under the item; no version identifier changes; `CHANGELOG.md` entries travel in item sidecars and land at closeout.

**Regression check (2026-09-20, f30c37e0):** items 1, 2 and 12 SAFE (read-first anchors only).
3: guard folded — the `scope.root` readers and the `journaled` body's `string` escape reach two more tests and `file_ops.go:501`; `internal/tools/doc.go` stays item 5's.
4: guard folded — `internal/undo/journal_test.go`'s three `Safe*` fixtures and the comment/doc rule; `CONTEXT.md` joins Files, `internal/tools/doc.go` stays item 5's.
5: guard folded — owns the one dated rewrite of `internal/tools/doc.go`'s path_safety.go and write_target.go paragraphs.
6: guard folded — the headless end-to-end test's two-step, the empty-journal-before-stamp order, the stale-generation seed through `snapshot.OpenJournal`, the hold taken after `OpenStored` returns a journal.
7: recast — yields to `internal/gitexec/gitexec.go:336-340` and `CHANGELOG.md:5616-5617` (`core.hooksPath` is a deliberate absence from the probe's regexp); the shell write view gets its own superset `security.GitCommandConfigName`.
8: guard folded — `delegationName` joins the callers; the run.go/subagent.go comments move with it.
9: recast — the two-rune rule keeps a control the form keeps; `internal/sanitize/sanitize.go:12-14` and the NOTES of `docs/plans/archived/2026-08-26 - 02 - untrusted-text-approval-integrity-plan.md` (the ESC-only drop with inert residue) are superseded; `internal/console/ansi.go`'s third strip is named in Out of scope.
10: guard folded — the clean-exit reap is announced beside the timeout sentence; the import count counts the import spelling; the grandchild and terminal-prompt comments are reworded.
11: guard folded — the `syscall.Kill` grep is scoped to process.go.
5 (round 2, f30c37e0): guard widened — item 5 owns EVERY `internal/tools/doc.go` sentence naming a deleted `Safe*` function or the string permit, closing the doc.go:128,154,163 residue.
6 (round 2, f30c37e0): guard settled — the hold is taken BEFORE `snapshot.OpenStored` (the ratified order); the open alternative is gone.
7 (round 2, f30c37e0): SAFE; guard added — the shared source string lives in internal/security, gitexec imports it, never the reverse.
9 (round 2, f30c37e0): guard folded — the `\u001b[31m` spelling is filtered; the `plain()`/`ansiPattern` re-pins take an OSC form; the RIS re-pin is a rule with a grep; "kept or folded by the form" with the ToLine row `"a\x1b\nb"` → `"a b"`.
10 (round 2, f30c37e0): guard settled — the clean-exit Reap and Setpgid ARE adopted and announced (ratified "full RunWithTeardown"); the open alternative is gone.

## Authoritative sources

- `docs/reviews/architecture-review-2026-09-20.html` §4, §7, §11, *Adjudicated earlier*,
  *In-passing defects* (Undo, Process tree, Git filters, Cosmetic) — fact-checked 2026-09-20 by
  two read-only explorers at `f30c37e0`; **where an item disagrees with the review, the item wins**.
- ADR 0020, 0022 §2, 0049 (+ 2026-08-14 amendment), 0051 D7, 0052 §5, 0059, 0073 §6, 0074 D8/D9;
  `docs/design/confinement-execution-contract.md` §2.4; `docs/plans/archived/2026-08-26 - 02`
  (why the inert ESC residue was the proof; superseded by item 9).

## Ratified design calls

- **`apogee undo <id> confirm <generation>` — the stamp is a required third positional** (owner, 2026-09-20); bare `confirm` refuses with "preview first: …". The manual edit lands in the same commit.
- **The free `Safe{WriteFile,CopyFile,CopyFileFrom,Remove,Rename}` are deleted; `Fence` methods are the only spelling** (owner, 2026-09-20). `SafeReadFile`/`SafeOpen` stay — reads take no permit (ADR 0049 D2).
- **`Fence.Rename` keeps the no-permit rule** and `safeio.go`'s rationale verbatim: one rename is one syscall through one `os.Root` and cannot span the fence; `move`'s copy+remove fallback is the designed route, not a workaround (author — corrects the review).
- **Open/Stat do not join the fence** in this plan; `writeTarget.pin` keeps the permit-aware read-back and asks `Governs`.
- **`journalTarget` returns `(path, security.Fence)`**; an ordinary write records `WorkspaceFence(root)` so a record never claims a permit it did not run under.
- **The undo verb HOLDS the session for the verb's whole run** (not a probe-and-release); headless/daemon runs never hold, and that gap is recorded, not fixed.
- **The gitexec command-config memo stays** (documented staleness); the fix is the shell-write-view yield alone.
- **Strip rule** (author): an unterminated CSI/OSC is swallowed to the end of its LINE; C1 U+0080–U+009F are dropped; only `agent`'s and `skills`' clamps fold into `ClampRunes` — `capRunes`, `truncate`, `Clip`, `clipSubAgentTask`, tui `clipRunes` keep their ellipsis rules (a fold would change output).
- **`FirstLine` moves to `sanitize`** though it sheds no `title` importer.
- **`userexec` adopts the full `RunWithTeardown` contract** (clean-exit Reap included); posture stays unconfined (ADR 0073).
- **Candidate #11 is declined**; ADR 0022 §2 / 0052 §5 stand; the denial and the five *Adjudicated earlier* rulings become one ADR (owner, 2026-09-20).

## Out of scope

- Candidates #12 (symmetric `Revert(generation)`), #13 (one spawner lifecycle) — only their defects land here.
- `snapshot.storedWorkspace`'s second decoder (harmless: `OpenStored` re-checks through `undo.Load`).
- A stat-keyed invalidation of `commandConfigProbes` (bead at closeout).
- Folding tui `clipRunes` / `delegationCause`'s ellipsis into a `ClampRunesEllipsis`.
- Everything in plans A and C.
- `internal/console/ansi.go`'s unexported sequence-aware strip stays — two of three definitions fold; the console's keeps its incomplete-CSI pin (`console/ansi_test.go`).

## 1. Verify plan A is archived

**What.** Refuse to proceed unless `docs/plans/2026-09-20 - 01 - test-seams-stubllm-discovery-and-exec-host-plan.md` is absent from `docs/plans/` and present under `docs/plans/archived/` — plan A reshapes `internal/tools`' exec files this plan's item 5 edits.
**Files:** none
**Read first:** docs/plans/ — the plan A file (untracked, present at BASE, so the gate refuses today as intended); docs/plans/archived/ — where it must land
**Tests.** none
**Acceptance.** `test -f "docs/plans/archived/2026-09-20 - 01 - test-seams-stubllm-discovery-and-exec-host-plan.md" && ! test -f "docs/plans/2026-09-20 - 01 - test-seams-stubllm-discovery-and-exec-host-plan.md"`
**Commit:** none — a gate, nothing to commit

## 2. `security.Fence` carries root + typed permit and the ADR 0049 rule once

**What.** Depends on item 1. New `internal/security/fence.go` (named in `doc.go`'s map): `type Fence struct { Root string; Permit domain.WriteEscapePermit }`; `WorkspaceFence(root) Fence`; `(Fence) Governs(input string) bool` = `f.Permit.Names(EvalRealPath(permittedName(f.Root, input)))` — replaces `namesPermittedTarget`; `openMutationRoot`/`openPermittedRoot` take a `Fence`. Verbs: `WriteFile(input, data, perm)`, `Remove(input)`, `CopyFile(src, dst)`, `CopyFileFrom(srcRoot, src, dst)` (the source root is the read side's, ungoverned), `Rename(old, new)` (never permitted — the ratified rule and rationale on the method). `domain.WriteEscapePermit` gains `Names(resolved string) bool` (`p.Real != "" && filepath.Clean(resolved) == filepath.Clean(p.Real)`). The free `Safe*` mutating functions become one-line wrappers over `Fence` THIS item so callers stay untouched; item 4 deletes them.
**Files:** internal/security/fence.go, internal/security/fence_test.go, internal/security/writepermit.go, internal/security/safeio.go, internal/security/doc.go, internal/security/writepermit_test.go, internal/domain/confinement.go
**Read first:** internal/security/writepermit.go — openMutationRoot, openPermittedRoot, namesPermittedTarget, permittedName; internal/security/safeio.go — SafeWriteFile, SafeCopyFileFrom, SafeCopyFile, SafeRename, SafeRemove; internal/domain/confinement.go — WriteEscapePermit, WriteEscapePermitFrom; internal/security/writepermit_test.go — permitFor, TestSafeWriteFile_PermittedTargetWritesOutsideTheWorkspace, TestSafeCopyFile_PermittedDestination; internal/security/doc.go — the file map (docmap_test.go TestDocMapNamesEveryFile fails until fence.go is named); internal/security/pathsafety.go — EvalRealPath
**Tests.** New table `TestFenceGoverns`: no permit; in-workspace path under a permit; the permitted target absolute; the permitted target through a workspace-internal link (the ADR 0049 amendment case); a diverged argument; a relative spelling that resolves to the permit. `writepermit_test.go`'s eight tests rewritten onto `Fence` methods; `safeio_test.go` untouched (wrappers).
**Acceptance.** `go build ./... && go test ./internal/security/... ./internal/domain/...`; `! grep -n namesPermittedTarget internal/security/*.go`
**Commit:** `refactor(security): security.Fence carries root and typed permit; Governs is the ADR 0049 rule once`

## 3. `writeScope` holds a Fence; `pin` and `journalTarget` ask Governs

**What.** Depends on item 2. `writeScope{fence security.Fence; journal *undo.Journal}`; `writeScopeOf` builds the fence from `domain.WriteEscapePermitFrom(ctx)` — `writeEscapeTarget` (path_safety.go) is deleted. `pin()` and `journalTarget()` replace `t.Real != filepath.Clean(t.scope.permit)` and the redundant `resolveInRoot` pre-check with `t.scope.fence.Governs(t.input)`; `journalTarget` returns `(path string, fence security.Fence)` per the ratified call. `write()` → `fence.WriteFile`; `journaled`/`journaledTargets` take a `security.Fence` instead of an escape string; `delete_file.go` → `fence.Remove`; `file_ops.go`'s copies → `fence.CopyFileFrom(sourceRoot, …)`; `move` → `fence.Rename`, terminal-escape test `!fence.Governs(args.Destination)`, fallback `fence.CopyFile` + `security.WorkspaceFence(t.root).Remove(src)` (source stays unpermitted, ADR 0049 D5). `workspace_scoped.go`'s three `writeScope{root: root}` literals → `writeScope{fence: security.WorkspaceFence(root)}`. `preImage.commit` stays on `Mutation{Root: fence.Root, Permitted: fence.Permit.Real}` until item 4.
**Regression guard.** `writeScope` losing `root`/`permit` reaches past the two named tests: `write_target_test.go:55` reads `target.scope.root`, `write_target_test.go:341`'s `journaled` body takes an escape `string`, `file_ops.go:501` passes `scope.root` and `undo_journal_test.go`'s bodies are `func(string)` — so `TestWriteScopeTargetResolvesOnceAndRefusesAnEmptyPath` (`scope.fence.Root`) and `TestWriteTargetJournaled` (the body receives a `security.Fence`; assert `== security.WorkspaceFence(root)`) join Tests and the acceptance grep gains `scope\.root\b`, which also catches the `func(string)` bodies. Rule: every comment naming `writeEscapeTarget`, the permit string or "hands the security core the one permitted target" reads the Fence — `grep -rn 'writeEscapeTarget\|approved-escape permit\|permitted out-of-workspace target' internal/tools/*.go | grep -v '^internal/tools/doc.go'`; `internal/tools/doc.go` is left alone here — its path_safety.go and write_target.go paragraphs are item 5's one dated rewrite.
**Files:** internal/tools/write_target.go, internal/tools/path_safety.go, internal/tools/file_ops.go, internal/tools/delete_file.go, internal/tools/workspace_scoped.go, internal/tools/write_permit_test.go, internal/tools/write_target_test.go, internal/tools/undo_journal_test.go
**Read first:** internal/tools/write_target.go — writeScope, writeScopeOf, writeTarget.pin, writeTarget.journalTarget, writeTarget.write, journaled, journaledTargets, preImage.commit; internal/tools/file_ops.go — MoveFile.move, copyDirectory, checkFileOpsPaths, checkFileOpsDestination; internal/tools/path_safety.go — writeEscapeTarget, resolveInRoot; internal/tools/workspace_scoped.go — resolveTargetUnbounded, the three writeScope{root: root} literals; internal/tools/delete_file.go — DeleteFile.Execute; internal/tools/write_permit_test.go — escapePermit, escapeCases, mustRefuseEscape; internal/tools/write_target_test.go — TestWriteTargetPin, TestWriteTargetJournaled; internal/tools/undo_journal_test.go — mutationOf, TestJournaledTargetsCommitsOnlyLandedPaths
**Tests.** `TestWriteScopeOfReadsThePermitOffTheContext` and `TestWriteTargetPin` assert the fence; `TestWriteScopeTargetResolvesOnceAndRefusesAnEmptyPath` reads `scope.fence.Root`; `TestWriteTargetJournaled`'s body receives a `security.Fence` and asserts it `== security.WorkspaceFence(root)`; the nine `write_permit_test.go` tests stay green as behaviour pins; `undo_journal_test.go`'s five `journaledTargets(scope.permit, …)` calls pass `scope.fence` and its `func(string)` bodies take a `security.Fence`; `TestUndoCaptureHasExactlyTwoCallers` unchanged.
**Acceptance.** `go build ./... && go test ./internal/tools/... ./internal/agent/...`; `! grep -nE 'filepath.Clean\(t.scope.permit\)|scope\.permit\b|scope\.root\b|writeEscapeTarget' internal/tools/*.go`
**Commit:** `refactor(tools): writeScope holds a security.Fence; the permit question is asked through Governs`

## 4. `undo.Mutation` carries the Fence; the `Safe*` wrappers go

**What.** Depends on item 3. `undo.Mutation{Fence security.Fence; Path; Perm; Pre; PreExisted; Post; PostExists}` (`Root`, `Permitted` removed — the fence is in-process state, never serialised; `GroupRecord` proves it); `entry` holds the fence, `readBackRoot`/`treePath` read `m.Fence.Root`, `entry.apply` → `e.fence.WriteFile`/`Remove`; `undo/snapshot.go`'s tracked paths → `security.WorkspaceFence(t.root).X`; `write_target.go`'s `commit` → `Fence: p.fence`. Delete the free `SafeWriteFile/SafeCopyFile/SafeCopyFileFrom/SafeRemove/SafeRename`; `safeio_test.go`'s 47 sites rewritten mechanically (`SafeWriteFile(root, ` → `WorkspaceFence(root).WriteFile(`, drop the trailing permit). Dated one-line amendments: `internal/security/doc.go`'s permit paragraph, the contract's `openMutationRoot` mention, ADR 0049's amendment naming `namesPermittedTarget` — "now `Fence.Governs`".
**Regression guard.** `internal/undo/journal_test.go:27,49,73` call the free `security.SafeWriteFile`/`SafeRemove`/`SafeRename` as fixtures beside `safeio_test.go`'s 47 sites — they become `security.WorkspaceFence(root).WriteFile/Remove/Rename`, or `go test ./internal/undo/...` does not compile. Rule: every comment or doc naming a deleted `Safe*` mutator reads the Fence method — `grep -rn 'Safe\(WriteFile\|CopyFile\|CopyFileFrom\|Remove\|Rename\)\b' --include=*.go --include=*.md --exclude-dir=archived internal CONTEXT.md docs/design | grep -v '^internal/tools/doc.go'` (today: `CONTEXT.md:1271`, `internal/tools/delete_file.go:99,113,119`, `workspace_scoped.go:111`, `write_target.go:293`, `file_ops.go:434-436`, `internal/undo/doc.go:74`, `internal/security/doc.go:110-119` — safeio.go's own map line); ADRs stay historical except the 0049 amendment; `internal/tools/doc.go` is item 5's and is left alone here.
**Files:** internal/undo/journal.go, internal/undo/snapshot.go, internal/undo/journal_test.go, internal/undo/persist_test.go, internal/undo/snapshot_test.go, internal/agent/undo_surface_test.go, internal/tools/write_target.go, internal/security/safeio.go, internal/security/safeio_test.go, internal/security/doc.go, docs/design/confinement-execution-contract.md, docs/adr/0049-an-approved-write-escape-executes-through-a-permit-pinned-to-the-disclosed-target.md, CONTEXT.md
**Read first:** internal/undo/journal.go — Mutation, entry, Journal.Record, entry.apply, readBackRoot, treePath, readCurrent; internal/undo/snapshot.go — tracked, tracked.apply; internal/undo/persist.go — GroupRecord, records, materialise (proves the fence is never serialised); internal/tools/write_target.go — preImage, preImage.commit; internal/security/safeio_test.go — TestSafeRename_MovesWithinRootAndRefusesEscapes, TestSafeRename_RefusesSymlinkedParentOnEitherChain; internal/undo/journal_test.go — the Safe* fixture helpers, the six Mutation{} literals; internal/undo/persist_test.go and internal/undo/snapshot_test.go — the `Permitted: outside` literals; internal/agent/undo_surface_test.go — the two Mutation{} literals
**Tests.** Every `Mutation{}` literal → `Fence:`; `journal_test.go`'s three `Safe*` fixture helpers → `security.WorkspaceFence(root).WriteFile/Remove/Rename`; `TestSafeRename_*` → `Fence.Rename`; new `TestRevertThroughTheRecordedFence` (a permitted record reverts through `Fence{Root, Permit}`, an ordinary one through `WorkspaceFence`).
**Acceptance.** `go build ./... && go test ./internal/security/... ./internal/tools/... ./internal/undo/... ./internal/agent/... ./cmd/apogee/...`; `! grep -rn 'Permitted\b' internal/undo internal/tools --include=*.go`; `! grep -rnE 'func Safe(WriteFile|CopyFile|CopyFileFrom|Remove|Rename)\b' internal/security`
**Commit:** `refactor(undo,security): a Mutation carries its Fence; the permit-string wrappers go`

## 5. The path_safety aliases and the mcp result duplicates fold

**What.** Depends on item 3. Delete `resolveInRoot`/`safeReadFile`/`safeOpen` (internal/tools/path_safety.go); the 25 call sites call `security.ResolveInRoot(root, input)` / `SafeReadFile` / `SafeOpen` with the argument order swapped per site (`statInRoot` stays, calling `security.SafeOpen`). `domain.OKResult(callID, content)` / `domain.ErrorResult(callID, msg)` are added beside `ToolResult`; `internal/mcp/tool.go`'s `okResult`/`errorResult` are deleted and its four sites call domain; `internal/tools/tools.go`'s `okResult`/`errorResult` become one-line wrappers (182 sites untouched).
**Regression guard.** Item 5 owns EVERY `internal/tools/doc.go` sentence naming a deleted `Safe*` function or the string permit — the rule, with grep `Safe(WriteFile|CopyFile|CopyFileFrom|Remove|Rename)|permitted` over internal/tools/doc.go — not only the path_safety.go and write_target.go paragraphs (items 3 and 4 leave doc.go alone; this closes the residue at doc.go:128,154,163).
**Files:** internal/tools/path_safety.go, internal/tools/diagnostics.go, internal/tools/exec_common.go, internal/tools/file_ops.go, internal/tools/git.go, internal/tools/list_dir.go, internal/tools/path_read.go, internal/tools/path_suggest.go, internal/tools/run_tests.go, internal/tools/write_target.go, internal/tools/git_test.go, internal/tools/path_read_test.go, internal/tools/tools.go, internal/mcp/tool.go, internal/domain/tools.go, internal/tools/doc.go
**Read first:** internal/tools/path_safety.go — resolveInRoot, safeReadFile, safeOpen, statInRoot, confinementBox, ErrPathEscape; internal/security/pathsafety.go — ResolveInRoot; internal/security/safeio.go — SafeReadFile, SafeOpen; internal/tools/tools.go — okResult, errorResult, okSummary; internal/mcp/tool.go — serverTool.Execute, okResult, errorResult; internal/domain/tools.go — ToolResult; internal/tools/path_read.go — readScope (six alias sites); internal/tools/git_test.go, internal/tools/path_read_test.go — the four alias sites
**Tests.** Existing suites; the change is mechanical.
**Acceptance.** `go build ./... && go test ./internal/tools/... ./internal/mcp/... ./internal/domain/...`; `! grep -nE 'func (resolveInRoot|safeReadFile|safeOpen)\b' internal/tools/path_safety.go`; `! grep -nE 'func (okResult|errorResult)\b' internal/mcp/tool.go`
**Commit:** `refactor(tools,mcp): fold the path_safety pass-through aliases and the mcp result duplicates`

## 6. `apogee undo confirm` carries the previewed generation and holds the session

**What.** `fix(undo)`: `applyUndoVerb` (cmd/apogee/undo.go) calls `journal.Revert()` with no generation although `Index.Generation` is persisted and ADR 0051 D7 / 0074 D8 promise the stamp; nothing probes `session.Store.Hold`, so the verb can rewrite `journal.json` under a live apogee whose next `persist()` overwrites it. Grammar `apogee undo <id> confirm <generation>` (ratified): `previewUndoVerb` prints `step.Generation` in `undoVerbHint`; `undoArgs` accepts 1–3 args, the third `strconv.ParseUint`; bare `confirm` refuses with the exact sentence `preview first: apogee undo <id>, then run the line it prints`; `applyUndoVerb(cmd, journal, id, generation)` refuses `journal.Generation() != generation` wrapping `undo.ErrStaleGeneration` and reprints the fresh preview (the outside-check shape `Agent.UndoRevert` uses). Before `snapshot.OpenStored`, `runUndoVerb` takes `session.NewStore(roots.sessions).Hold(id)` and keeps it for the verb's life; a `*session.HeldError` returns its own sentence. `docs/manual/headless.md`'s grammar and the command's `Long` help updated in the same commit.
**Regression guard.** `TestHeadlessReportsAnUndoTheVerbCanActuallyPerform` (cmd/apogee/undo_test.go:404) runs a bare confirm and goes red: it lifts the id from the report, PREVIEWS, lifts the generation from the printed `undoVerbHint` line, then confirms with it — that two-step IS its end-to-end claim. Order: an empty journal (`Preview()` false / `ErrNothingToUndo`) answers `undoNothingToDo(id)` BEFORE the stamp is compared (the shape `internal/tui/undo.go` `confirmUndo` reaches via `previewUndo`), so undo_test.go:110-113's `nothing to undo for session s-undo-1` keeps its wording. The stale-generation seed cannot go through `undo.Load`+`Record`+`Close` (`undo.Load` refuses a nil Snapshotter once the index holds groups, persist.go:176,199, and `snapshot.journalSource` is unexported): reopen through `snapshot.OpenStored`/`OpenJournal` + `MarkPre` + a file write + `Close`, exactly as undo_test.go:131-144 does (`Record`+`Close` on that journal also moves `Generation`, journal.go:275). The hold is taken BEFORE `snapshot.OpenStored` (the ratified order, as the What says): `Store.hold` (store.go:378-380) `MkdirAll`s the sessions dir and creates `<id>.lock`, which only `Delete` unlinks, so an unknown id now leaves `sessions/<id>.lock` behind — the accepted consequence of the ratified order; `TestUndoVerbOnAnUnknownSessionSaysNothingToUndo` keeps asserting the sentence only.
**Files:** cmd/apogee/undo.go, cmd/apogee/undo_test.go, docs/manual/headless.md
**Read first:** cmd/apogee/undo.go — runUndoVerb, applyUndoVerb, previewUndoVerb, undoArgs, undoVerbHint, newUndoCommand; cmd/apogee/undo_test.go — undoStore, runUndoCmd, TestUndoVerbPreviewsThenReverts, TestUndoVerbRestoresAFileTheExchangeChanged, TestHeadlessReportsAnUndoTheVerbCanActuallyPerform; internal/session/store.go — Store.Hold, HeldError; internal/undo/journal.go — Journal.Generation, Journal.Revert, Journal.Preview, ErrStaleGeneration; internal/undo/persist.go — Load, Index; internal/tui/undo.go — confirmUndo; internal/snapshot/journal.go — OpenStored, OpenJournal; docs/manual/headless.md — the `apogee undo <session-id>` paragraph
**Tests.** `TestUndoVerbPreviewsThenReverts` and `TestUndoVerbRestoresAFileTheExchangeChanged` pass the printed generation; `TestHeadlessReportsAnUndoTheVerbCanActuallyPerform` previews, lifts the generation from the printed `undoVerbHint` line and confirms with it; new `TestUndoVerbRefusesAStaleGeneration` (preview, record another exchange by reopening through `snapshot.OpenJournal` + `MarkPre` + a file write + `Close` as undo_test.go:131-144 does, confirm with the old stamp ⇒ stale + fresh preview, files untouched), `TestUndoVerbRefusesAHeldSession` (hold the id in-test ⇒ `HeldError` sentence, `journal.json` byte-identical), `TestUndoVerbRefusesABareConfirm` (the exact refusal sentence). Bite: the three new tests fail against the pre-item tree.
**Acceptance.** `go test ./cmd/apogee/ -run 'UndoVerb|Headless.*Undo|DaemonFire.*Undo'`; `grep -n 'journal.Revert()' cmd/apogee/undo.go` shows one call preceded by a `Generation()` check
**Commit:** `fix(undo): apogee undo confirm carries the previewed generation and holds the session`

## 7. A repo-local `git config` write of a command-valued key names `.git/config`

**What.** Recast at the regression check (2026-09-20). `fix(security)`: `git config [--local|--worktree] filter.x.clean cmd` (also `core.hooksPath`, `diff.*.command`, …) run through a confined `terminal` in Auto escapes the `write-git-control-plane` hard refuse because `gitTargets` (internal/security/shellwrites.go) yields the key, not `.git/config`; `gitexec`'s memoised command-config probe then never refuses `git_commit`'s add. `gitexec.CommandConfigName` stays at today's pattern; the shell write view gets its own superset `security.GitCommandConfigName` (the probe pattern + `core\.hookspath`), both built from one shared POSIX-ERE source string; in `gitTargets`, when the subcommand is `config`, no `--global`/`--system`/`-f`/`--file` operand is present, the invocation is not a read form, and any lowered operand matches `GitCommandConfigName`, append `.git/config` to the yielded targets. `git config user.name x` and `--get` reads are untouched.
**Regression guard.** `gitexec.CommandConfigName` stays at today's pattern — the probe's, with `core.hooksPath` deliberately absent (gitexec.go:336-340, CHANGELOG) — and the item yields to that decision; the shell write view gets its own superset `security.GitCommandConfigName` = the probe pattern + `core\.hookspath`, both built from ONE shared source string that stays POSIX-ERE (no `(?i)`); the yield lowers the operand before matching and excludes the read forms `--get`, `--get-all`, `--get-regexp`, `--get-urlmatch`, `--get-color`, `--get-colorbool`, `-l`, `--list`; `TestWriteTargetsOf` rows carry EXACT wants: `git config --local filter.x.clean cmd` ⇒ `filter.x.clean cmd .git/config`; `git config core.hooksPath /tmp/h` ⇒ `core.hooksPath /tmp/h .git/config`; `git config --global filter.x.clean cmd` ⇒ `filter.x.clean cmd`; `git config --get filter.x.clean` ⇒ `filter.x.clean`; a tools test pins that a repo whose `.git/config` sets `core.hooksPath` is NOT refused by `Capture`. The documented decision (`internal/gitexec/gitexec.go:336-340`, `CHANGELOG.md:5616-5617`: refusing `core.hooksPath` in the probe "would cost a repository its git tools") is not superseded — the item yields to it. The shared source string lives in internal/security: gitexec imports security (`go list` at f30c37e0), never the reverse, so `gitexec.CommandConfigName` is built from security's string.
**Files:** internal/security/shellwrites.go, internal/security/shellwrites_test.go, internal/security/dangerous_test.go, internal/gitexec/gitexec.go, internal/gitexec/doc.go, internal/tools/git_test.go
**Read first:** internal/security/shellwrites.go — gitTargets, operandTargets, valueOperands, gitReadSubcommands, writeTargetsOf; internal/gitexec/gitexec.go — CommandConfigName, repoLocalCommandConfig, probeCommandConfig, Capture, hardeningOptions (gitexec imports security, so the shared source string lives in security); internal/security/shellwrites_test.go — TestWriteTargetsOf; internal/security/dangerous_test.go — TestShellWriteViewJudgesWhatTheCommandWrites, terminalCall, shellTool; internal/security/rules.go — write-git-control-plane; internal/gitexec/doc.go — package comment; internal/tools/git_test.go — TestGit_RefusesRepoLocalCommandConfig, TestGitBranch_DoesNotRunRepoSuppliedHook, gitRepo, runInRepo, installRepoHook
**Tests.** `TestWriteTargetsOf` rows with exact wants: `git config --local filter.x.clean cmd` ⇒ `filter.x.clean cmd .git/config`; `git config core.hooksPath /tmp/h` ⇒ `core.hooksPath /tmp/h .git/config`; `git config --global filter.x.clean cmd` ⇒ `filter.x.clean cmd`; `git config user.name x` ⇒ `user.name x`; `git config --get filter.x.clean` ⇒ `filter.x.clean`. A `dangerous_test.go` row drives `ShellWriteView` for the terminal with the exact line and asserts the `write-git-control-plane` verdict. A `git_test.go` test builds a repo whose `.git/config` sets `core.hooksPath` and asserts `Capture` does not refuse it. Bite: the new `TestWriteTargetsOf` rows and the dangerous row fail against the pre-item tree.
**Acceptance.** `go test ./internal/security/... ./internal/gitexec/... ./internal/tools/... -run 'WriteTargets|Dangerous|CommandConfig|Git'`
**Commit:** `fix(security): a repo-local git config write of a command-valued key names .git/config in the shell write view`

## 8. One rune clamp and one first-line rule in `sanitize`

**What.** `sanitize.ClampRunes(s string, n int) string` — at most n runes on a rune boundary, no trim, no ellipsis, returns s when it fits (`agent/gate.go`'s body verbatim); `sanitize.FirstLine(s string) string` — `strings.Cut(s, "\n")` then `TrimSpace` (`title.FirstLine`'s body verbatim). Delete `agent/gate.go clampRunes`, `skills/parse.go clampRunes`, `title.FirstLine`. Callers: gate.go, children.go → `sanitize.ClampRunes`; subagent.go → `sanitize.ClampRunes(sanitize.FirstLine(args.Task), title.MaxDelegateRunes)`; parse.go trims `summary` once before `validate` at both sites, then `sanitize.ClampRunes` (output identical — the private clamp trimmed first); `title.DelegateLabel` and `run/run.go` → `sanitize.FirstLine`. `capRunes`, `truncate`, `Clip`, `clipSubAgentTask`, tui `clipRunes` untouched (ratified).
**Regression guard.** The caller list also holds `subagent.go delegationName` (internal/agent/subagent.go:744), a second `title.FirstLine` call → `sanitize.FirstLine`; and the doc comments spelling `title.FirstLine` at internal/run/run.go:826,838 (and subagent.go's) are rewritten with the move, or the `! grep -rn 'title\.FirstLine'` acceptance fails on prose.
**Files:** internal/sanitize/sanitize.go, internal/sanitize/doc.go, internal/sanitize/sanitize_test.go, internal/title/title.go, internal/title/title_test.go, internal/agent/gate.go, internal/agent/children.go, internal/agent/subagent.go, internal/skills/parse.go, internal/run/run.go
**Read first:** internal/agent/gate.go — clampRunes, gateReasonRunes; internal/agent/subagent.go — delegationLabel, delegationName; internal/agent/children.go — delegationCause; internal/skills/parse.go — clampRunes, parseWithFrontmatter, validate; internal/title/title.go — FirstLine, DelegateLabel; internal/run/run.go — firstTaskLine, delegationName; internal/title/title_test.go — TestFirstLine, TestDelegateLabel; internal/skills/parse_test.go — TestParseSkillSummaryClampedTo200
**Tests.** New `TestClampRunes` table (fits / exactly n / over n / multibyte boundary / not trimmed / no ellipsis); `TestFirstLine` moves from title_test.go unchanged; `skills/parse_test.go`'s summary rows, `agent/children_test.go`'s cause rows and `TestDelegateLabel` stay green unchanged.
**Acceptance.** `go build ./... && go test ./internal/sanitize/... ./internal/title/... ./internal/agent/... ./internal/skills/... ./internal/run/... ./cmd/apogee/...`; `! grep -rn 'func clampRunes' internal/`; `! grep -rn 'title\.FirstLine' --include=*.go .`
**Commit:** `refactor(sanitize): one rune clamp and one first-line rule`

## 9. The strip removes a whole CSI/OSC sequence, not just its ESC

**What.** Recast at the regression check (2026-09-20). Depends on item 8. `fix(sanitize)`: `sanitize.StripEscapes` drops only the ESC byte, leaving `[31m` and OSC payloads visible (the review's *Cosmetic* defect), while `title.StripEscapes` parses whole sequences — two exported definitions of one invariant that disagree; `tui/sessionrule.go` imports title's, `tui/sanitize.go` wraps sanitize's. Binding rule for `sanitize.StripEscapes`/`StripEscapesToLine`: ESC `[` through the first final byte `@`–`~`; ESC `]` through BEL or ESC `\`; any other ESC plus its follower unless the follower is a control kept or folded by the form (`\n`/`\t`) — then ESC alone; an unterminated sequence is swallowed to the end of its line (the newline survives); C0 (except `\n`/`\t` per form), DEL, C1 U+0080–U+009F and `BidiControl` are dropped; the `strings.ContainsFunc` fast path stays. `title.escapeRunes` moves into sanitize (unexported); delete `title.StripEscapes`/`strippableControl`; `title.SanitizeTo` and `tui/sessionrule.go` call `sanitize.StripEscapesToLine`. Every comment saying the ESC byte alone goes or a sequence's tail stays inert is rewritten (the rule and its grep are in the guard); the rewritten `sanitize.go` comment names `docs/plans/archived/2026-08-26 - 02 - untrusted-text-approval-integrity-plan.md`'s NOTES (the ESC-only drop with inert residue for chunk-split streams) as superseded explicitly. A lone mid-line CR, VT, FF or NEL now vanishes in a title (`"a\rb"` → `"ab"`, was `"a b"`; the same for `\v`, `\f` and U+0085) — pinned by one row each.
**Regression guard.** The two-rune rule reads "ESC plus its follower unless the follower is a control the form keeps (`\n`/`\t`) — then ESC alone", pinned by a `"a\x1b\nb"` → `"a\nb"` row (else `mcpServerGrantNote`, internal/tui/approval.go:592, welds `"ok\x1b\nReason: forged"` and approval_test.go:209,223 goes red); internal/tui/approval_test.go, internal/tui/sessions_test.go, internal/tui/picker_test.go, internal/tui/transcriptbridge_test.go and internal/tui/sessionrule_test.go join Files, and the RIS fixtures re-pin to the sequence-aware output (`"reset  me"`, `"(:9999)"`, `"hihere"`); the comment guard is a RULE — "every comment saying the ESC byte alone goes or a sequence's tail stays inert" — with the grep `ESC alone|the ESC byte|ESC only|ESC introducer|ESC dropped|left inert|inert text|inert literal` over `--include=*.go` repo-wide; the residue acceptance grep also filters `\033[31m` spellings; the What announces "a lone mid-line CR, VT, FF or NEL now vanishes in a title" with one row each; the rewritten `sanitize.go` comment and the header name `docs/plans/archived/2026-08-26 - 02` NOTES as superseded explicitly; `internal/console/ansi.go`'s unexported sequence-aware strip stays and is named in Out of scope ("two of three definitions fold; the console's keeps its incomplete-CSI pin"). Superseded sources: `internal/sanitize/sanitize.go:12-14` and `docs/plans/archived/2026-08-26 - 02 - untrusted-text-approval-integrity-plan.md` NOTES (254-259). Round 2: the two-rune rule reads "a control kept or folded by the form", with the ToLine row `"a\x1b\nb"` → `"a b"` beside the body row; the residue acceptance filters the third spelling too (`grep -vE 'x1b\[31m|033\[31m|u001b\[31m'` — transcriptbridge_test.go:1620 and headless_test.go:3590 are input fixtures spelt `\u001b`); rule for the re-pins: a CSI fixture read through `plain()`/`ansiPattern` (CSI-only, model_test.go:83,86 — TestModelApprovalEscapeStrips, TestModelAskEscapeStrips, TestFooterContentStripsEscapes's host, TestFooterWorkdirIsEscapeStripped's footer half) becomes an OSC form (`\x1b]8;;x\x07`, as opts.Model/effort at 6638,6642 already are) and asserts clean label + no ESC in the plain output, while a test reading a field or a printed string directly (contextfiles_test.go:137, advicepane_test.go:59, headless_test.go) keeps the CSI fixture; the RIS re-pin is the rule "every want carrying `reset c me`, `:c9999` or `hithere`" — `grep -rnE 'reset c me|:c9999|hithere' internal/tui` — with the map `\x1bc` → `` (sessions_test.go:1226's `· cother` becomes `· other`).
**Files:** internal/sanitize/sanitize.go, internal/sanitize/doc.go, internal/sanitize/sanitize_test.go, internal/title/title.go, internal/title/title_test.go, internal/tui/sessionrule.go, internal/tui/sanitize.go, internal/tui/doc.go, internal/tui/transcript_test.go, internal/tui/contextfiles_test.go, internal/tui/advicepane_test.go, internal/tui/thinking_test.go, internal/tui/model_test.go, internal/tui/approval_test.go, internal/tui/sessions_test.go, internal/tui/picker_test.go, internal/tui/transcriptbridge_test.go, internal/tui/sessionrule_test.go, cmd/apogee/headless_test.go, cmd/apogee/e2e_hostile_test.go, cmd/apogee/testdata/frames/t12-skills.txt
**Read first:** internal/sanitize/sanitize.go — StripEscapes, StripEscapesToLine, dropControl; internal/title/title.go — StripEscapes, strippableControl, escapeRunes, SanitizeTo; internal/tui/sessionrule.go — sessionRuleLayout; internal/tui/approval.go — mcpServerGrantNote, flattenField; internal/tui/model_test.go — ansiPattern, plain, TestModelApprovalEscapeStrips, TestModelAskEscapeStrips, TestFooterContentStripsEscapes, TestFooterWorkdirIsEscapeStripped; internal/tui/sessions.go — sessionRows; cmd/apogee/e2e_hostile_test.go — TestE2EHostileProbeKeepsItsOwnRows, hostileModelShown, assertNoLeakedColour, ansiRed; internal/tuitest/golden.go — Golden, updateGolden
**Tests.** One `TestStripEscapes` table in sanitize_test.go replaces sanitize's rows, `title_test.go`'s strip tests (the title copy becomes a pipeline test) and the tui delegate copy: CSI colour, CSI screen game, OSC-BEL, OSC-ST, bare ESC+letter, ESC before a kept control (`"a\x1b\nb"` → `"a\nb"`; the ToLine form folds it, `"a\x1b\nb"` → `"a b"`), unterminated CSI at end of line with the next line surviving, C1 CSI (U+009B), lone CR, lone VT, lone FF, lone NEL (one row each), ZWJ/soft-hyphen survive, idempotence, `ToLine` folding. Residue expectations (`host[31m`, `child]52;…`) become "label present clean + no ESC + not painted red" as `e2e_hostile_test.go` already does — a CSI fixture read through `plain()`/`ansiPattern` becomes an OSC form first, a directly-read field or printed string keeps its CSI fixture; the split-chunk thinking case pins `"before31mafter"` and is renamed; every want carrying `reset c me`, `:c9999` or `hithere` (`grep -rnE 'reset c me|:c9999|hithere' internal/tui`) re-pins to the sequence-aware output — `"reset  me"`, `"(:9999)"`, `"hihere"`, `· other` for the workspace cell; approval_test.go:209,223 (`TestModelApprovalFlattensAServerAliasThatCouldForgeARow`) and sessionrule_test.go:140 stay green; the `t12-skills.txt` golden is regenerated through the project's update flag. Bite: the CSI/OSC rows fail against the pre-item tree.
**Acceptance.** `go build ./... && go test ./internal/sanitize/... ./internal/title/... ./internal/tui/... ./internal/session/... ./cmd/apogee/...`; `! grep -rnE 'func (StripEscapes|escapeRunes|strippableControl)\b' internal/title/`; `! grep -rn 'title\.StripEscapes' --include=*.go .`; `! grep -rn '\[31m' --include=*_test.go internal/tui cmd/apogee internal/sanitize | grep -vE 'x1b\[31m|033\[31m|u001b\[31m'`
**Commit:** `fix(sanitize): the strip removes a whole CSI/OSC sequence, not just its ESC`

## 10. A timed-out user command takes its process tree with it

**What.** `fix(userexec)`: `userexec.Run` uses `exec.CommandContext` with no tree teardown — on timeout only the leader dies and a wrapper's grandchild (an `api-key-cmd` helper) survives; confirmed empirically. After `exec.CommandContext`: `td := platform.NewProcessTeardown(cmd); defer td.Release()`; set `cmd.WaitDelay = WaitGrace` after it; replace `cmd.Run()` with `platform.RunWithTeardown(cmd, td)`. The result switch is unchanged (`ErrWaitDelay` still lands in the timed-out branch). Reword the package doc's "leaf: imports internal/security and nothing else" and its *Outside confinement* paragraph (teardown is a safety net, never the fence — ADR 0020); `docs/manual/reactions.md`'s "gets a further two seconds" sentence now says the grandchild dies with the group (a `setsid` escapee still survives).
**Regression guard.** `RunWithTeardown` reaps on the CLEAN exit too (internal/platform/teardown.go:129; teardown_unix.go `pgroupTeardown.Reap`), so an observe Reaction like `run: ["sh","-c","afplay done.wav &"]` or an `api-key-cmd` wrapper that leaves a helper behind, which works today, is SIGKILLed the moment the leader exits: the clean-exit Reap and Setpgid ARE adopted (ratified "full RunWithTeardown") and announced where the timeout sentence lives — docs/manual/reactions.md's `timeout:` bullet, the userexec package doc and the item's CHANGELOG sidecar: "a process the command leaves behind dies when the command does; `setsid` detaches it". `NewProcessTeardown` sets `Setpgid` (teardown_unix.go:45), so the child leaves the terminal's foreground group and a `/dev/tty` read now stops with SIGTTIN until the run times out: rule — every comment naming the pipe-holding grandchild or the terminal-prompt outcome (`grep -rn 'grandchild\|draw over the frame' internal/userexec internal/reactions internal/keystore`) is reworded to the `setsid` escapee / SIGTTIN outcome; keystore's own runner keeps its wording (it does not adopt the teardown). The acceptance import count counts the import spelling, not lines naming `internal/platform` in prose.
**Files:** internal/userexec/userexec.go, internal/userexec/userexec_test.go, internal/reactions/command_test.go, docs/manual/reactions.md
**Read first:** internal/userexec/userexec.go — Run, WaitGrace, Options, Result; internal/platform/teardown.go — RunWithTeardown, ProcessTeardown, ProcessWaitDelay; internal/platform/teardown_unix.go — NewProcessTeardown, killProcessGroup, pgroupTeardown.Reap; internal/subprocess/subprocess.go — run (the reference caller: NewProcessTeardown before Confine, deferred release); internal/platform/teardown_unix_test.go — waitForPIDFile, pidAlive, killPID, setsidEscapeScript; internal/userexec/userexec_test.go — requireShell, shell, TestRunReportsTheDeadlineRatherThanWaitingOnASleep; internal/reactions/command_test.go — the 200ms-deadline table (within = userexec.WaitGrace + time.Second); docs/manual/reactions.md — the `timeout:` bullet under "The rest of the posture"
**Tests.** New `TestRunKillsAWrappersGrandchildOnTimeout` (`//go:build !windows`, `requireShell`): `sh -c 'sleep 30 & echo $! > $PIDFILE; wait'` with a 200 ms timeout; after `Run` returns, poll `syscall.Kill(pid, 0)` until ESRCH (borrow the `pidAlive`/`waitForPIDFile` shapes from `internal/platform/teardown_unix_test.go`). Bite: fails against the pre-item tree. `TestRunReportsTheDeadlineRatherThanWaitingOnASleep` stays, its comment (and internal/reactions/command_test.go:114-118's) reworded off the pipe-holding grandchild.
**Acceptance.** `go build ./... && go test -race ./internal/userexec/... ./internal/reactions/... ./internal/config/...`; `! grep -n 'cmd.Run()' internal/userexec/userexec.go`; `test $(grep -c '"github.com/airiclenz/apogee/internal/platform"' internal/userexec/userexec.go) -eq 1`
**Commit:** `fix(userexec): a timed-out user command takes its process tree with it`

## 11. The Console's group kill goes through platform's teardown

**What.** `fix(console)`: `internal/console/process.go`'s `killProcessGroup` is a raw `syscall.Kill` twin of `platform.killProcessGroup` outside the tested `ProcessTeardown` contract. In `Start`, call `td := platform.NewProcessTeardown(cmd)` right after `exec.CommandContext` and drop the hand-written `cmd.Cancel`; keep `cmd.WaitDelay = waitDelay` after it; keep `Setpgid=false; Setsid=true` (a session leader's PGID == PID, so the negative-PID kill still covers the group — say so in the comment); store `td` on `Process`; `reap()` → `p.td.Reap(p.cmd); p.td.Release()`. Delete `killProcessGroup`. Behaviour identical.
**Regression guard.** The `syscall.Kill` acceptance grep is scoped to the production file — `! grep -n 'syscall.Kill' internal/console/process.go` — because the pre-existing liveness probe `processExists` (internal/console/process_test.go:252, `syscall.Kill(pid, 0)`) is untouched by this item.
**Files:** internal/console/process.go, internal/console/doc.go
**Read first:** internal/console/process.go — Start, Process, reap, killProcessGroup, Kill, Close; internal/platform/teardown_unix.go — NewProcessTeardown, pgroupTeardown.Reap, killProcessGroup (planTreeKill with treeHeld=true); internal/platform/teardown.go — ProcessTeardown, NoTeardown; internal/console/process_test.go — TestProcessCloseKillsBackgroundedChildren, TestProcessKillStopsALongRunningCommandPromptly, processExists, startProcess; internal/console/process_other.go — Process (the Windows twin: separate struct, unaffected by a td field); internal/console/doc.go — the "imports internal/platform and the pseudo-terminal dependency" paragraph; internal/subprocess/subprocess.go — run (teardown built before Confine, Prepare never clears cmd.Cancel)
**Tests.** `TestProcessCloseKillsBackgroundedChildren` and `TestProcessKillStopsALongRunningCommandPromptly` pin it; no new test.
**Acceptance.** `go build ./... && go test -race ./internal/console/... ./internal/platform/... ./internal/tools/...`; `! grep -n 'syscall.Kill' internal/console/process.go`; `! grep -rn 'func killProcessGroup' internal/console/`
**Commit:** `fix(console): the Console's group kill goes through platform's process teardown`

## 12. The standing denials become an ADR

**What.** New `docs/adr/<next free number>-the-standing-denials-of-the-architecture-reviews.md` (number read from the tree at run time; status `accepted`; linked from `CONTEXT.md` only where an existing entry already cites the denied topic). It records, one section each with the deciding date and the review that raised it: (1) candidate #11 — the session record keeps presenter verdicts on the wire; ADR 0022 §2 (2026-08-31 addendum) and ADR 0052 §5 stand; `fromWireToolView` re-deriving `Solo` is the documented exception, not a precedent; (2) the ten live settings stay ten fields, not one Posture value (09-14 #5); (3) a delegate's runtime state is not one value / delegation is not the whole spawn (09-16 #26); (4) the bound entry stays six host-side assembly sites — `Options.StartupEntry` only (09-14 #7 "IN, reduced"); (5) host boot stays three Drivers, not one module (09-14 #18); (6) wire traits stay inside the codec, the seven `== WireAnthropic` branches outside the adapters tolerated (09-14 #8). Each section names what evidence would reopen it.
**Files:** docs/adr/, CONTEXT.md
**Read first:** docs/adr/ — next free number is 0082 at f30c37e0 (0081-linux-falls-back-to-a-namespace-fence-through-bwrap.md is the last); docs/adr/0022-*.md — Decision 2 (the 2026-08-31 owner amendment is inline in decision 2, not an "Addendum" heading); docs/adr/0052-*.md — Decision 5 and the 2026-08-24 amendment; docs/reviews/architecture-review-2026-09-20.html — #adjudicated section (the five rulings, verbatim), candidate #11 card; internal/tui/transcriptbridge.go — fromWireToolView; CONTEXT.md — the ADR 0022 / ADR 0052 §5 citations (the only existing entries that already cite a denied topic)
**Tests.** none
**Acceptance.** `test -n "$(ls docs/adr/ | grep -- '-the-standing-denials-of-the-architecture-reviews.md')"`; `grep -c '^## ' docs/adr/*-the-standing-denials-of-the-architecture-reviews.md` prints at least 6
**Commit:** `docs(adr): record the standing denials of the architecture reviews`
