# Delegation bounds, loop resilience and child streams — session-mining fixes, wave B

**Goal:** close the delegation and control-flow findings of the 2026-09-14 session-mining review: a child is warned before its step cap and can still write its output at it, transient stream faults are retried, every delegation is bounded in depth, tokens and wall-clock, degenerate results are faulted instead of returned, the loop breaker catches A-B-A-B, and a child's streamed text is committed once.

**Date:** 2026-09-14
**Status:** unexecuted
**Sized for:** ~200k-context host

**Sources:**
- `docs/reviews/session-mining-2026-09-14.md` (headlines 3, 4, 6, 11, 12, 13, 15; items 6, 7, 8, 10a, 11, 23a, 25a; open questions 1, 5, 8)
- ADR 0005 (per-task roster narrowing), ADR 0013 (sub-agent depth bound), ADR 0023 (+ 2026-09-02 report block), ADR 0039 (fan-out), ADR 0046 (capped reply), ADR 0071/0076 (Floor guards, Bypass), ADR 0077 (advise Reaction pattern, `context-fill-notice`), CONTEXT.md **Step cap**, **Sub-agent**, **Floor guard**; archived plans `2026-08-26 - 00`, `2026-08-30 - 00`, `2026-09-01 - 02` (capped wrap-up, tool-less — reversed by item 8), `docs/design/test-drivers.md` (stubllm turn kinds)

**Ratified design calls (owner, 2026-09-14):**
- **Step warning:** an engine advise Reaction `step-budget-notice`, off by default behind a top-level key, ADR 0077 class; fires once at 75 % of the cap (writer: fixed threshold).
- **Wrap-up writer:** the step-cap wrap-up Turn offers `write_file` alone, only when the delegation named `output_path` — reverses the 2026-09-01 tool-less call (CONTEXT.md **Step cap** amended).
- **OUT detection:** an explicit `output_path` argument on `sub_agent`; no task-text regex.
- **Depth:** `delegate-max-depth` key, default 1; `ask_user` and `present_document` never offered at depth > 0; a `tools:` argument narrows further.
- **Child asks (owner, 2026-09-14):** both withheld; ADR 0039 §6 superseded.
- **Budgets:** `delegate-max-tokens` 20M prompt tokens and `delegate-timeout` 2h, on by default, 0 disables; both end the child through the wrap-up path.
- **EOF retry (writer):** reuses the one per-Turn re-stream latch; no separate budget.
- **Loop breaker (writer):** exact A-B-A-B over the last four tool Turns, byte-identical keys, no fuzzy match; result cap 64 KiB constant.
- **Child stream (writer; owner, 2026-09-14):** `displace` always parks on a cross-depth switch, so no fragment precedes the canonical `MessageEvent`; a delegation's parked residue is committed at EVERY `SubAgentFinished` phase (`runRef{depth: e.Depth, spawn: e.CallID}`), cancelled or not, with `closeRun` kept as belt-and-braces; a parked run paints nothing until its own next token (the existing sibling behaviour).
- **Compaction cap (owner, 2026-09-14):** unchanged — ADR 0060 D9 stands; wording fix only.
- **Step-cap result head (writer, re-check 2026-09-14):** items 4 and 9 agree — the step-cap marker (`stepCapResultFormat`) is always first; every note (clamp line, missing-output note) is appended in the `SeatFallbackNote` slot.

**Regression check (2026-09-14, 3fca8c2cb9bb48fe5cd4ee1daf600e105eed172b):**
- 2: guard folded — `cut`/`error` are stream terminators, not kinds; `cut` kills the TCP connection (Hijack / `http.ErrAbortHandler`); Files are `server.go` / `server_test.go`.
- 3: guard folded — the two e2e cases pay the unexported 1 s `restreamHoldoff`.
- 4: recast — clamp line appended in the `SeatFallbackNote` slot; `MaxDepth == 0` reads as the default 1; schema floor is schema text only; real config/settings sites listed; ADR 0013 decision 4 superseded by a dated note.
- 5: recast — both child asks withheld (owner); child-ask tests and contracts retired or moved to depth 0; read-only roster = `planAdmits(parent tool)` minus the two withheld; ADR 0039 §6 and ADR 0013's child-roster sentence superseded by dated notes.
- 6: guard folded — TUI recogniser broadened to the three markers; row modelled on `delegate-max-steps` (write alone, next start); `child.now = a.now`; `delegate-timeout` modelled on `ui.stall-after`; real config/settings sites listed.
- 7: guard folded — every `ContextFillNotice` / `context-fill-notice` site gains the sibling (rule + grep); `reactions_test.go`'s `buildBuiltins` calls; template path corrected.
- 8: guard folded — `wrapUpDirectiveFormat` untouched, separate `wrapUpOutputClauseFormat`; `write_file` offered only when held and Mode-admitted; e2e is `e2e_delegation_test.go`; tool-less prose rule + grep.
- 9: recast — rule (c) is a one-word reply only; (a) runs on the `StepCapped` branch too; `HasToolCallMarkup` never keys on the fence; the 8/9/11 tail order is stated.
- 10: guard folded — A-B-A-B fires only when the repeated pair's tool results are byte-identical (yields to ADR 0059's polling design); prose rule + grep; ADR 0071 amended by dated note.
- 11: guard folded — cap applied in `delegationResult`, `dispatch.go` untouched; clamp test re-shaped; yields to `dispatch.go`'s one-elision-idiom rule.
- 12: recast — a cancelled delegation's residue is committed at the cancelled `SubAgentFinished` phase; the replace-fragments step is dropped.
- 13: recast — `cappedReplyErrFmt` keeps its verbs and key; `ReasoningMandatory` crosses into the engine via `RebindSpec` / `domain.Config`; the applied cap is formatted; ADR 0046 §4's retry sentence superseded by a dated note.

**Re-check (2026-09-14, 3fca8c2cb9bb48fe5cd4ee1daf600e105eed172b):**
- 4: guard folded — `TestSeat_ChildRosterDropsTheRunOnArgument` / `TestSeat_PlainParentRosterIsInheritedUnchanged` set `MaxDepth = 2`; `TestSubAgent_RecursionPointRefusesAtBound` rewritten and `TestSubAgent_DepthLimitConstant` re-pointed; the row joins `settingKeysWithNoMemberToReach`; the What's repair-guard claim struck (`validateArguments` reads only `required`); acceptance runs `./cmd/apogee/` whole; the What says appended, the schema description is rewritten, no line at cap 0.
- 5: guard folded — `newChildAgent`/`newChildAgentOn`/`defaultSubAgentTools()` signatures stay (roster as a per-spawn field); both narrowings are name-lists into `Subset`; `TestDelegationName_RidesApprovalAndAsk` is the fifth; read-only = the Plan floor without the scratch-dir writers (`planAdmits`, not `planOffers`); prose grep widened to `internal/agent/*.go`; e2e housed in `e2e_delegation_test.go`, yaml NEW; `go test ./internal/agent/` whole; `layout.md` §ask-prompt / child Run view superseded by dated notes.
- 9: recast (owner) — (b) fires only on a JSON-object body or a reply that begins with the container; (c) keys on a fixed acknowledgement set; (a) on `StepCapped` keeps the marker first, non-error, note appended, skipped when the wrap-up writer was withheld; `delegationResult` keeps its signature; CONTEXT.md **Step cap** stands.
- 12: recast (owner) — residue committed on EVERY `SubAgentFinished` phase, cancelled or not; re-pins stream with `EventBase{Depth: 1, CallID: "s1"}`; the visible change is stated; `parkedText`'s doc comment rewritten.
- 13: yields to ADR 0060 D9 and archived plan `2026-08-31 - 02` (owner) — the compaction-cap allowance is REMOVED (parked on `apogee-tfp`); no `ReasoningMandatory` crosses; wording fix + applied-cap formatting only; retitled.

**Standing requirements:**
- `skills: coding-standards`
- Deviations from item text land as a dated NOTES line under the item.
- The hard invariant (ADR 0076): the one new steering text (item 7) is an advise Reaction off by default; every other model-facing string here is an error result or a structural marker on the existing wrap-up/step-cap path, on under Bypass like the wrap-up directive — with three ratified structural exceptions (owner, 2026-09-14): item 4's schema floors (`minimum` / `minLength` on `sub_agent`), item 5's `tools` property, and item 9(c)'s success-branch marker `[delegate returned no report]`.
- Bubble Tea v2; the `Model` is value-copied — no `strings.Builder` by value.

**Out of scope:**
- The delegate ledger and re-dispatch note (`apogee-clb`); shared context files on `sub_agent` (`apogee-vi5`); a raised-cap retry and the forced-reasoning compaction-cap allowance — item 13's former first half, removed at the 2026-09-14 re-check (`apogee-tfp`); the deepseek stream capture (`apogee-l8s`); cloud `parallel-agents` default (`apogee-9se`, plan 04 announces the width).
- Fuzzy loop detection, a "Nothing to do" text heuristic, a wall-clock bound on the parent, VERSION.

## 1. Gate: plan 2026-09-14 - 01 is archived — ✅ DONE (2026-09-15)

NOTES (2026-09-15): gate verified — plan 01 is absent from `docs/plans/`, present at `docs/plans/archived/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` with all 7 items marked ✅ DONE (2026-09-14), archived by commit 0d64c233; its header `**Status:** unexecuted` line is stale but matches the other archived 2026-09-14 plans (house convention, not a defect).

**What:** Verify `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` is gone from `docs/plans/` and archived under `docs/plans/archived/` as executed. Items 5, 8 and 9 edit `internal/agent/loop.go` (`toolMenu`), `dispatch.go` and `internal/domain/config.go`, which that plan rewrites. No code change.

**Files:** none

**Read first:** `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` — items 4, 5 (Files: `internal/agent/loop.go`, `dispatch.go`, `internal/domain/config.go`, `floorguards_test.go` — the overlap with items 3, 5, 8, 9, 10 here); `docs/plans/archived/` — the two `2026-09-14 - 00` plans already there

**Tests:** none.

**Acceptance:** `test ! -e "docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md" && ls "docs/plans/archived/" | grep -q "2026-09-14 - 01"`

**Commit:** none (gate only).

## 2. stubllm gains `cut` and `error` turn kinds — ✅ DONE (2026-09-15)

NOTES (2026-09-15): `cut` and `error` are implemented as terminators per the regression guard — `kindCount` and the one-kind wording are untouched; `validateTerminators` refuses each with `http`, `hang`, the other terminator, and `usage`/`finish_reason` (never reached), plus a negative `after_runes`.
NOTES (2026-09-15): `internal/stubllm/wire.go` is edited though the item's Files list omits it — the in-band `error` member belongs on `sseEnvelope`/`wholeReply` there per the package's own file split (wire shapes live in wire.go, and the item's Read-first names `wire.go — sseEnvelope`).
NOTES (2026-09-15): consequential edit — internal/stubllm/doc.go: made necessary by the two new Turn types (the package map's script.go line and the fault list in the package comment name them).
NOTES (2026-09-15): the kill is Hijack + `conn.Close()`, falling back to `panic(http.ErrAbortHandler)` when the writer cannot be hijacked; `TestServerCutTurnKillsTheConnection` was checked to FAIL on a plain handler return (clean EOF) and pass on the kill. A cut at or past the end of the text streams every delta (tool-call fragments included) and kills in the terminator's place; the non-streamed path kills after the 200 header. The `error` event is followed by `[DONE]` so a client that ignores the object is caught committing an empty reply; the whole-reply path carries the object as a body member.

**What:** `internal/stubllm/script.go` `Turn` gains two kinds beside `text`, `tool_calls`, `http`, `hang` (one kind per turn, `kindCount` extended): `cut: {after_runes: N}` — the server streams the turn's `text` and closes the response body after N runes without a terminal chunk (an `unexpected EOF` at the client); `error: {code, message}` — an in-band `{"error": {...}}` object on a 200 response mid-stream (code 502 default). `serve` implements both; `record` is untouched. `docs/design/test-drivers.md` turn-kind table gains the two rows.

**Regression guard.** `cut` and `error` are stream TERMINATORS on a text / tool_calls / empty turn, not kinds — the What's "`kindCount` extended" does not apply: `validate` refuses each with `http`, `hang` and the other terminator; `kindCount` and its "more than one of text, tool_calls, http and hang" wording (`script.go` validate, `script_test.go`, `test-drivers.md`) stay untouched. The `cut` kind must kill the TCP connection (Hijack + close the socket, or `http.ErrAbortHandler`) so the client sees `io.ErrUnexpectedEOF` — a handler return is a clean EOF the provider commits as Done(stop); say so and pin it in `serve_test.go` (which is `internal/stubllm/server_test.go` — the handler and its tests are `server.go` / `server_test.go`, not `serve.go`).

**Files:** `internal/stubllm/script.go`, `internal/stubllm/script_test.go`, `internal/stubllm/server.go`, `internal/stubllm/server_test.go`, `docs/design/test-drivers.md`

**Read first:** `internal/stubllm/script.go` — Turn, validate, kindCount, streamDeltas; `internal/stubllm/server.go` — writeStream, reply, writeWhole; `internal/stubllm/wire.go` — sseEnvelope; `internal/stubllm/server_test.go` — TestServerStreamsTextInConfiguredChunks, TestServerHTTPTurnWritesNoStream; `internal/stubllm/match.go` — Turn.expand

**Tests:** `script_test.go`: a turn setting `cut` and `text` is valid; `cut` + `http`, `cut` + `hang` and `cut` + `error` are refused by `validate` (the one-kind rule and its wording untouched). `server_test.go`: an httptest client reading a `cut` turn gets an error with `errors.Is(err, io.ErrUnexpectedEOF)` true — pinned to the connection kill (Hijack + close, or `http.ErrAbortHandler`), never a handler return; an `error` turn yields the in-band object after the leading deltas.

**Acceptance:** `go build ./... && go test ./internal/stubllm/`

**Commit:** `feat(stubllm): cut and error turn kinds script a mid-stream EOF and an in-band upstream error`

## 3. A mid-stream EOF is retried like an in-band 502; dropped malformed chunks are counted — ✅ DONE (2026-09-15)

NOTES (2026-09-15): the text-cap `Retryable: false` case named in Tests was already pinned by `assertReplyTextCapFires` (TestStream_ReplyTextIsCapped / TestStream_ThinkingCountsTowardTheCap); no duplicate test added.
NOTES (2026-09-15): "zero text" for the malformed-only fault is content plus reasoning (stream.go's `textBytes`, the same sum the text cap counts), so a reasoning-only reply with skipped chunks still commits its reasoning; the fault is non-retryable (the same request would decode no better).
NOTES (2026-09-15): `TestE2EUpstreamEOFTwiceFaults` builds its two-cut script in Go from `upstream-eof.yaml` (turn 1 twice) rather than a second fixture; the e2e helper reuses `eventLinesHome` from e2e_eventlines_test.go.

**What:** Depends on item 2. Closes open question 5: `internal/provider/stream.go` yields `read stream: <err>` with `Retryable` unset, so `unexpected EOF` never reaches the one re-stream `internal/agent/loop.go` already grants at `reply.retryable && !t.restreamSpent`. Fix: the scanner fault is `Retryable: true` when `errors.Is(err, io.ErrUnexpectedEOF)` or the error is a net timeout — never for the text-cap overflow. A chunk that fails strict `json.Unmarshal` is no longer dropped silently: the assembler counts them and the `DeltaDone`/fault carries `(N malformed chunks skipped)`; a reply that ends with zero text, zero tool calls and N > 0 is a fault naming the count (`apogee-l8s` bisects from that signal). The existing `StreamResetEvent` is emitted on the re-stream as today; a child gets the same one re-stream as depth 0 (writer call).

**Regression guard.** The two e2e cases pay the unexported 1 s `restreamHoldoff` — note it in Tests. They observe a retry only because item 2's `cut` kills the TCP connection.

**Files:** `internal/provider/stream.go`, `internal/provider/stream_test.go`, `internal/agent/loop.go`, `internal/agent/loop_test.go`, `cmd/apogee/testdata/stubllm/upstream-eof.yaml`, `cmd/apogee/e2e_upstream_test.go`

**Read first:** `internal/provider/stream.go` — parseSSE, Delta, inBandErrorDelta; `internal/agent/loop.go` — respondAndReview, streamResponse, restreamHoldoff, replyFault; `internal/agent/compact.go` — the second Stream consumer; `internal/provider/stream_test.go` — sseServer, collectStream, TestStream_TerminatesWithoutDone; `internal/agent/overflow_test.go` — TestRespondAndReviewReStreamsATransientFaultOnce, TestReStreamLatchIsPerTurn; `cmd/apogee/e2e_naming_test.go` — headlessAgainst; `internal/stubllm/log.go` — Server.Requests

**Tests:** `stream_test.go`: a body cut mid-chunk ⇒ `DeltaError{Retryable: true}`; the text-cap overflow ⇒ `Retryable: false`; two malformed chunks ⇒ count on `DeltaDone`. `loop_test.go`: an EOF fault re-streams once and the second reply commits. e2e `TestE2EUpstreamEOFIsRetriedOnce` (headless, `upstream-eof.yaml`: turn 1 `cut`, turn 2 text): the answer is turn 2's text and the stub saw exactly two requests; `TestE2EUpstreamEOFTwiceFaults`: two `cut` turns ⇒ the fault line and three requests never happen. Both e2e cases pay the unexported 1 s `restreamHoldoff` on every headless re-stream, so each costs ≥ 1 s.

**Acceptance:** `go build ./... && go test ./internal/provider/ && go test ./internal/agent/ -run 'Restream|Stream' && go test ./cmd/apogee/ -run 'TestE2EUpstream'`

**Commit:** `fix(provider): a mid-stream EOF is a retryable fault; malformed chunks are counted, never dropped silently`

## 4. `sub_agent` schema floor, announced clamp, `delegate-max-depth` default 1 — ✅ DONE (2026-09-15)

NOTES (2026-09-15): consequential edit — internal/agent/children_test.go: made necessary by the default depth bound dropping to 1 — `TestInterjectChild_ReachesAGrandchild` drives a grandchild and now sets `cfg.Delegation.MaxDepth = 2` exactly as the plan's four named depth-2 tests do; its assertions are untouched.
NOTES (2026-09-15): `TestSubAgent_StepCapZeroIsUnbounded` now asks for `max_steps: 2` in its spawning call so its exact-`Content` pin actually proves "no clamp line at cap 0"; the plan named the pin as exact but its call carried no `max_steps`.
NOTES (2026-09-15): the clamp is carried on the child as an `Agent.capRequested` field (the ask that was above the cap) so `delegationResult` — a method on the child — writes the note on every outcome that produces a result, beside `seatFallback`; the depth refusal text is spelled once (`depthLimitReason`) and `resolutionInput` gained `maxDepth` so the resolver's refusal names the configured bound.
NOTES (2026-09-15): `TestSubAgent_DepthLimitConstant` re-pointed as `TestSubAgent_DepthZeroReadsAsOne` (unit + end-to-end: a zero `MaxDepth` delegates once); `TestSubAgent_DepthBoundFollowsTheKey` covers default/1/2 at depths 0 and 1 via the child roster; the file-only key is a plain int (a 0 on disk resolves as absent, to 1) while the settings surface refuses 0 with `must be at least 1`.

**What:** Recast at the regression check (2026-09-14). `internal/tools/sub_agent.go` schema: `max_steps` gains `"minimum": 1`, `task` gains `"minLength": 20`; no validator reads `minimum`/`minLength` — schema text only. `internal/agent/subagent.go`: a `max_steps` above the cap is applied as the cap AND the delegation's result carries the line `[max_steps N requested; the configured cap is M — M applied]` appended in the `SeatFallbackNote` slot (closes open question 8; today the clamp is silent); no line when the cap is 0 (a request against an unbounded cap stays ignored); the `max_steps` schema description is rewritten in the same schema edit so it no longer says "ignored when 0 or above the configured cap". New top-level key `delegate-max-depth` (int, default 1, registered beside `delegate-max-steps` in `internal/config/config.go`, seeded in `defaults/config.yaml`, `/settings` row, manual): `maxSubAgentDepth` becomes `Config.Delegation.MaxDepth`; at depth ≥ MaxDepth `sub_agent` is withheld exactly as today at 2. CONTEXT.md **Sub-agent** entry states the default. `TestTemplateMentionsEveryRegistryKey` covers the template.

**Regression guard.** The clamp line is APPENDED as a body note in the `SeatFallbackNote` slot ("appended, never prefixed"), never at the head, so `stepCapResultFormat`/`subAgentFaultPrefix` stay the first line and `TestSubAgent_MaxStepsArgumentOnlyLowersTheCap` is re-pinned to the appended line; the engine reads `Delegation.MaxDepth == 0` as the built-in default (1), never as "no delegation"; `TestSubAgent_MaxDepthRefusesAndWithholdsTool` and `TestFanOut_DepthOneStaysSerial` set `cfg.Delegation.MaxDepth = 2`; Files gain `internal/agent/fanout_test.go`, `internal/agent/dispatch.go`, `internal/agent/resolution.go` (every `maxSubAgentDepth` reader); the schema floor is SCHEMA TEXT the model reads — the engine refuses nothing new beyond the existing empty-task check (no fixture sweep); `wantPlainSubAgentSchema` in `sub_agent_test.go` is updated in the same commit with its comment re-worded, and `%s` (run_on) stays last so ADR 0069's "appended at the end" pin holds; replace the phantom paths `cmd/apogee/defaults/config.yaml` → `internal/config/defaults/config.yaml` and `internal/tui/settings_rows.go` → the real sites `internal/config/registry.go` (row + Validate), `internal/config/options.go`, `internal/config/config.go`, `cmd/apogee/settingsrows_test.go` (want map), `cmd/apogee/wire_boot.go` + `cmd/apogee/wire_firing.go` (DelegationConfig literals), and `cmd/apogee/wire_settings.go` + `wire_settings_test.go` when the row is editable — model the row on `delegate-max-steps` (write-alone apply, "takes effect at the next start"); ADR 0013 decision 4 (`maxSubAgentDepth = 2`, "three levels is ample") is superseded by a dated note the item writes.

**Regression guard (re-check 2026-09-14).** `TestSeat_ChildRosterDropsTheRunOnArgument` and `TestSeat_PlainParentRosterIsInheritedUnchanged` (`seat_test.go`, a depth-0 parent's `defaultSubAgentTools()` must keep `sub_agent`) also set `cfg.Delegation.MaxDepth = 2` and stay green; `TestSubAgent_RecursionPointRefusesAtBound` (`&Agent{depth: maxSubAgentDepth}`) is rewritten as an Agent literal with `cfg.Delegation.MaxDepth` set, and `TestSubAgent_DepthLimitConstant` is retired or re-pointed to the "0 reads as 1" rule — the constant's deletion must leave the package compiling; the `delegate-max-depth` row joins `settingKeysWithNoMemberToReach` and `TestApplySettingAcceptsTheStartupOnlyKeys`'s table (`wire_settings_test.go`), or `TestApplySettingRefusesEveryKeyItCannotReach` reds; the repair guard (`internal/floor/repair.go` `validateArguments`/`requiredParams`) reads only `required` — nothing new is refused, no fixture sweep; `subagent_test.go`'s cap-0 pin (`TestSubAgent_StepCapZeroIsUnbounded`, exact `Content`) stays exact because no clamp line is written at cap 0.

**Files:** `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/seat_test.go`, `internal/agent/fanout_test.go`, `internal/agent/dispatch.go`, `internal/agent/resolution.go`, `internal/domain/config.go`, `internal/config/registry.go`, `internal/config/options.go`, `internal/config/config.go`, `internal/config/config_test.go`, `internal/config/defaults/config.yaml`, `cmd/apogee/settingsrows_test.go`, `cmd/apogee/wire_boot.go`, `cmd/apogee/wire_firing.go`, `cmd/apogee/wire_settings.go`, `cmd/apogee/wire_settings_test.go`, `docs/manual/configuration.md`, `docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md`, `CONTEXT.md`

**Read first:** `internal/agent/subagent.go` — maxSubAgentDepth, runSubAgent, defaultSubAgentTools, withoutSeatChoice, delegationResult, SeatFallbackNote; `internal/agent/seat_test.go` — TestSeat_ChildRosterDropsTheRunOnArgument, TestSeat_PlainParentRosterIsInheritedUnchanged; `internal/agent/subagent_test.go` — TestSubAgent_MaxDepthRefusesAndWithholdsTool, TestSubAgent_RecursionPointRefusesAtBound, TestSubAgent_DepthLimitConstant, TestSubAgent_MaxStepsArgumentOnlyLowersTheCap, TestSubAgent_StepCapZeroIsUnbounded;
`cmd/apogee/wire_settings_test.go` — settingKeysWithNoMemberToReach, TestApplySettingAcceptsTheStartupOnlyKeys, TestApplySettingRefusesEveryKeyItCannotReach, TestEveryEditableSettingKeyHasAnApply, TestSettingsTableIsInRegistryOrder; `internal/floor/repair.go` — validateArguments, requiredParams; `internal/agent/resolution.go` — resolutionInput.atDepthBound; `internal/agent/dispatch.go` — resolutionInput literal;
`internal/config/defaults_test.go` — TestTemplateMentionsEveryRegistryKey; `internal/tools/sub_agent.go` — subAgentSchemaTemplate

**Tests:** `subagent_test.go` (`TestSubAgent_MaxStepsArgumentOnlyLowersTheCap` pattern, re-pinned): `max_steps: 12` under cap 3 ⇒ the result's first line is unchanged and the exact clamp line is appended in the `SeatFallbackNote` slot; depth 1 default ⇒ a child's tool menu lacks `sub_agent`; `delegate-max-depth: 2` ⇒ present at depth 1, absent at 2; a `Config` with `Delegation.MaxDepth` 0 delegates once (the default); `TestSubAgent_MaxDepthRefusesAndWithholdsTool`, `TestFanOut_DepthOneStaysSerial`, `TestSeat_ChildRosterDropsTheRunOnArgument` and `TestSeat_PlainParentRosterIsInheritedUnchanged` set `cfg.Delegation.MaxDepth = 2` and stay green; `TestSubAgent_RecursionPointRefusesAtBound` is rewritten as an Agent literal with `cfg.Delegation.MaxDepth` set; `TestSubAgent_DepthLimitConstant` is retired or re-pointed to the "0 reads as 1" rule; cap 0 ⇒ no clamp line (`TestSubAgent_StepCapZeroIsUnbounded` exact). `sub_agent_test.go`: `wantPlainSubAgentSchema` carries the floor and the rewritten `max_steps` description, `%s` (run_on) last. `config_test.go`: key round-trip, 0 refused (`delegate-max-depth: must be at least 1`). `settingsrows_test.go` want map and `wire_settings_test.go` (`TestEveryEditableSettingKeyHasAnApply`, `TestSettingsTableIsInRegistryOrder`, the row in `settingKeysWithNoMemberToReach` and `TestApplySettingAcceptsTheStartupOnlyKeys`'s table) cover the row; `TestTemplateMentionsEveryRegistryKey` lives in `internal/config/defaults_test.go`.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'SubAgent' && go test ./internal/agent/ -run 'SubAgent|Seat' && go test ./internal/config/ && go test ./cmd/apogee/`

**Commit:** `feat(agent): sub_agent schema floor, announced max_steps clamp, delegate-max-depth default 1`

## 5. Children never get `ask_user` or `present_document`; `sub_agent` takes a `tools:` roster

**What:** Recast at the regression check (2026-09-14). Depends on items 1, 4 (runs after item 4 — both rewrite the schema template and `defaultSubAgentTools`). `internal/agent/subagent.go` `defaultSubAgentTools`: at depth > 0 `ask_user` and `present_document` are withheld unconditionally (ADR 0019 gates them for the human's seat; a child has none). `sub_agent` gains `tools` — either the string `"read-only"` (every tool whose class is read-only under the ladder — the Plan floor without the scratch-dir writers: `planAdmits`, not `planOffers`) or an array of tool names; unknown names ⇒ `IsError` naming them; the roster is intersected with the parent's, never widened (ADR 0005). The child's menu in `loop.go` `toolMenu` reads the narrowed set. `docs/manual/configuration.md` delegation paragraph; CONTEXT.md **Sub-agent**.

**Regression guard.** OWNER CALL 2026-09-14 — withhold BOTH `ask_user` and `present_document` at depth > 0; ADR 0039 §6 (queued child questions naming the child) and ADR 0013's child-roster sentence are superseded by dated notes the item writes; the prose-guard RULE: every test, comment and contract that has a CHILD ask or present — `grep -rln 'ask_user\|present_document' internal/agent/*_test.go internal/tools/ask_user.go internal/tools/present_document.go internal/domain/ask.go internal/domain/approval.go` — is retired or moved to depth 0 in this item (`TestFanOut_ConcurrentChildQuestionsQueueAndNameTheirChild`, `TestSubAgent_SingleDelegatedQuestionIsUnchanged`, `TestFanOut_ApprovalAndQuestionShareOnePrompt`, `TestPresentIdentity_RidesTheDispatchCtxOntoTheRequest` are the known four), and `AskRequest.SubAgentTask` / the `PresentRequest` depth plumbing get a dated "no longer reachable" note; the read-only roster is DEFINED as `planAdmits(parent tool)` minus the two withheld tools, with `sub_agent` following item 4's depth rule — the test compares to that set; `wantPlainSubAgentSchema` updated in the commit; the docs edit targets `docs/manual/configuration.md`'s delegation paragraph (`grep -n 'max_steps' docs/manual/*.md`), not commands.md; item 5 runs after item 4 (both rewrite the schema template and `defaultSubAgentTools`); recorded in the header: "**Child asks (owner, 2026-09-14):** both withheld; ADR 0039 §6 superseded."

**Regression guard (re-check 2026-09-14).** The existing `newChildAgent`/`newChildAgentOn`/`defaultSubAgentTools()` signatures STAY (19 test call sites in 14 files; `defaultSubAgentTools()` is called zero-arg in `seat_test.go` and `subagent_test.go`): the roster arrives as a per-spawn field and the zero-arg form is today's default; both the depth > 0 withholding and the `tools:` narrowing are name-lists fed to `a.tools.Subset(names...)` as `defaultSubAgentTools` does today, so `TestSeat_PlainParentRosterIsInheritedUnchanged`'s identity pin (`childTool == parentTool`) holds; `TestDelegationName_RidesApprovalAndAsk` (`delegationname_test.go`, depth-1 child through `askUserCallScript`, asserts `AskRequest.Depth == 1`) is the FIFTH known test — its ask half retired, its approval half kept (it proves ADR 0068's rename-reaches-the-prompt row); `askUserCallScript` survives (`promptslot_test.go` shares it); the prose grep widens to `internal/agent/*.go` — `construct.go`, `dispatch.go`, `agent.go`, `loop.go` comments that assert child asks (the `WithSubAgentTask`/`WithSubAgentDepth` carriers "for ask_user/present_document") get dated "no longer reachable" notes only; `planmenu_test.go` and `construct_test.go`'s depth-0 hits are no-ops; the e2e is housed in `cmd/apogee/e2e_delegation_test.go` over its existing `childRequests`/`childToolMenus` helpers, `delegate-roster.yaml` is NEW; `layout.md`'s "`ask_user` runs in a delegate exactly as it runs at the top level … concurrent children can put questions to you" paragraph and the child Run view's ask-box sentence are superseded by dated notes the item writes (the owner's both-withheld call supersedes the TUI spec); the `tools` property is a ratified schema-text exception named in the header's invariant line (ADR 0069 §4's growth concern and `wantPlainSubAgentSchema`'s KV-prefix warning acknowledged: the plain schema changes once, in this item and item 4 together).

**Files:** `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/seat_test.go`, `internal/agent/loop.go`, `internal/agent/resolution.go`, `internal/agent/construct.go`, `internal/agent/dispatch.go`, `internal/agent/agent.go`, `internal/agent/askqueue_test.go`, `internal/agent/promptslot_test.go`, `internal/agent/delegationname_test.go`, `internal/agent/fanout_test.go`, `internal/tools/ask_user.go`, `internal/tools/present_document.go`, `internal/domain/ask.go`, `internal/domain/approval.go`, `cmd/apogee/testdata/stubllm/delegate-roster.yaml` (NEW), `cmd/apogee/e2e_delegation_test.go`, `layout.md`, `docs/manual/configuration.md`, `docs/adr/0039-delegations-fan-out-concurrently-bounded-by-the-servers-parallel-agents-cap.md`, `docs/adr/0013-the-sub-agent-orchestrator-is-the-recursion-point-with-isolated-live-guard-state.md`, `CONTEXT.md`

**Read first:** `internal/agent/subagent.go` — defaultSubAgentTools, withoutSeatChoice, newChildAgentOn, runSubAgent; `internal/agent/resolution.go` — classifyTool, planAdmits, planOffers; `internal/agent/loop.go` — toolMenu; `internal/domain/tools.go` — ToolRegistry.Subset; `internal/agent/delegationname_test.go` — TestDelegationName_RidesApprovalAndAsk, TestPresentIdentity_RidesTheDispatchCtxOntoTheRequest;
`internal/agent/askqueue_test.go` — askUserCallScript, TestFanOut_ConcurrentChildQuestionsQueueAndNameTheirChild, TestSubAgent_SingleDelegatedQuestionIsUnchanged; `internal/agent/seat_test.go` — TestSeat_PlainParentRosterIsInheritedUnchanged, TestSeat_ChildRosterDropsTheRunOnArgument; `cmd/apogee/e2e_delegation_test.go` — childToolMenus, childRequests; `internal/tools/sub_agent.go` — subAgentSchemaTemplate, SubAgentArgs

**Tests:** `subagent_test.go`: child menu never contains `ask_user`/`present_document`; `tools: "read-only"` ⇒ menu equals `planAdmits(parent tool)` minus the two withheld tools, `sub_agent` per item 4's depth rule; `tools: ["read_file","grep"]` ⇒ exactly those; `["frobnicate"]` ⇒ exact error (raised before `ToolRegistry.Subset` drops it). The known five child-ask tests (`TestDelegationName_RidesApprovalAndAsk` keeps its approval half) and every other hit of the guard's grep are retired or moved to depth 0; `TestSeat_PlainParentRosterIsInheritedUnchanged` stays green by identity; `wantPlainSubAgentSchema` carries the `tools` property. e2e `TestE2EDelegateReadOnlyRosterReachesTheChild` in `e2e_delegation_test.go` (`childToolMenus` over the stub's captured child request, `delegate-roster.yaml` NEW): the tool list the stub received for the child holds no write tool and no `terminal`.

**Acceptance:** `go build ./... && go test ./internal/agent/ && go test ./internal/tools/ -run 'SubAgent' && go test ./cmd/apogee/ -run 'TestE2EDelegat'`

**Commit:** `feat(agent): children never get ask_user or present_document; sub_agent takes a tools roster`

## 6. `delegate-max-tokens` and `delegate-timeout` bound every child

**What:** Depends on item 4. Two new top-level keys beside `delegate-max-steps`: `delegate-max-tokens` (int, default 20000000, the child's cumulative prompt tokens from its `usageTally`) and `delegate-timeout` (duration, default `2h`, wall clock from the child's first request); 0 disables either. `DelegationConfig` gains `MaxTokens`, `Timeout`. When a bound trips, the child ends through the SAME path as the step cap (`finishAtStepCap` → one wrap-up Turn), with its own markers: child directive and parent result lines read `[delegate stopped at its token budget (N tokens)]` / `[delegate stopped at its time limit (2h0m)]` in the `stepCapResultFormat` shape, and an `ErrorEvent` names the bound. Live reload applies to children spawned after the swap. Registry rows, template lines, `/settings` rows, manual, CONTEXT.md **Step cap** (renamed sentence: "step, token or time bound").

**Regression guard.** The parent's delegation row reads the result head through `delegationStepCapHead` (`^\[delegate stopped at its step cap …`): broaden the recogniser to the three markers and word the verdict per bound, with cases in `internal/tui/toolbranch_test.go` ("a run stopped at its step cap did not finish") and `subagentblock_test.go` (`envelopeCapMarker`). "Live reload" has no seam — a child's bounds are read from `a.cfg.Delegation` at spawn: mirror `delegate-max-steps` exactly (write-alone apply, `liveSettings` mirror for Firings, "takes effect at the next start" in Desc). `newChildAgentOn` never copies `a.now` (`newAgent` seeds `time.Now`): thread `child.now = a.now` and read the wall clock through `a.now()`; the test pins the parent's `now`. Same path corrections as item 4 (template at `internal/config/defaults/config.yaml`; rows from `KeyRegistry` — `internal/config/registry.go`, `options.go`, `config.go`, `cmd/apogee/settingsrows_test.go`, `wire_settings.go`, `wire_boot.go`, `wire_firing.go`); there is no duration Kind — model `delegate-timeout` on `ui.stall-after` (KindString + Validate hook, parsed to `time.Duration` in the accessor).

**Files:** `internal/domain/config.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/agent.go`, `internal/agent/loop.go`, `internal/tui/toolregistry.go`, `internal/tui/toolbranch_test.go`, `internal/tui/subagentblock_test.go`, `internal/config/registry.go`, `internal/config/options.go`, `internal/config/config.go`, `internal/config/config_test.go`, `internal/config/defaults/config.yaml`, `cmd/apogee/settingsrows_test.go`, `cmd/apogee/wire_boot.go`, `cmd/apogee/wire_firing.go`, `cmd/apogee/wire_settings.go`, `cmd/apogee/wire_settings_test.go`, `docs/manual/configuration.md`, `CONTEXT.md`

**Read first:** `internal/agent/agent.go` — Run (exchangeTurns, the cap check), finishAtStepCap, stepCapErrFormat, usageTally.record, Agent.now; `internal/agent/subagent.go` — stepCapResultFormat, wrapUpDirectiveFormat, delegationResult, newChildAgentOn (child.stepCap); `internal/agent/loop.go` — wrapUp directive site (AppendToSystem wrapUpMarker); `internal/domain/config.go` — DelegationConfig; `internal/agent/usagetally_test.go` — DeltaDone with Usage; `internal/tui/toolregistry.go` — delegationStepCapHead, delegationCappedVerdict; `cmd/apogee/wire_settings.go` — applyDelegateMaxSteps, setDelegateMaxSteps, optionsLocked

**Tests:** `subagent_test.go`: a stub child whose usage reports 25M prompt tokens after Turn 2 ends with the exact token marker and one wrap-up Turn; a parent whose pinned `now` (threaded to the child) is past 2h ends with the time marker; both keys at 0 ⇒ the 80-step cap alone. `config_test.go`: duration parsing (`ui.stall-after` posture), negative refused. `toolbranch_test.go` / `subagentblock_test.go`: the token and time markers render their own verdict, never `done`. `settingsrows_test.go` want map and `wire_settings_test.go` cover the two rows.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'SubAgent|StepCap' && go test ./internal/config/ && go test ./cmd/apogee/ -run 'Template'`

**Commit:** `feat(agent): delegate-max-tokens and delegate-timeout end a child through the wrap-up path`

## 7. `step-budget-notice`: a child hears it is at 75 % of its cap

**What:** Depends on item 1. The engine's second advise Reaction after `context-fill-notice` (ADR 0077 pattern, byte-for-byte the same plumbing): id `step-budget-notice`, fires at `post-tool-result` for depth > 0 only, once per Exchange, when the child's Turn count first reaches `ceil(0.75 × cap)`; text `steps: N of M used — K left before the wrap-up Turn; write your output now`, rendered as the fenced advise trailer (`RenderAdvice`). Off by default behind the top-level file-only key `step-budget-notice: false` (registry, template, `/settings` row beside `context-fill-notice`, `Config`/`Generation` switch, Bypass skips it, `reactions:` may not take the id). Books `ReactionFiredEvent{Action: "notice", Detail: "step 60 of 80"}`. ADR 0077 gains a dated addendum naming the second notice; manual `reactions.md`, `configuration.md`.

**Regression guard.** `buildBuiltins(gates, notice bool)` gains a second switch, so its signature changes: `internal/agent/reactions_test.go`'s four `buildBuiltins` calls are updated in the commit. The notice's plumbing is stated as a rule, not a list — every site of `grep -rn 'ContextFillNotice\|context-fill-notice' --include=*.go internal cmd` gains the `StepBudgetNotice`/`step-budget-notice` sibling (Generation, construct, SetReactions rebuild, reserved id, child inherits, `reactions:` refusal, registry/options/config, wire_boot/wire_firing/wire_live/wire_settings, settingsrows_test). Same path corrections as item 4: the template is `internal/config/defaults/config.yaml`; the `/settings` rows derive from `KeyRegistry` (`internal/config/registry.go`, `options.go`, `config.go`, `cmd/apogee/settingsrows_test.go`, `wire_settings.go`, `wire_boot.go`, `wire_firing.go`).

**Files:** `internal/agent/builtins.go`, `internal/agent/stepnotice.go`, `internal/agent/stepnotice_test.go`, `internal/agent/reactions_test.go`, `internal/agent/construct.go`, `internal/agent/agent.go`, `internal/agent/reactions.go`, `internal/agent/subagent.go`, `internal/domain/config.go`, `internal/domain/reaction.go`, `internal/config/reactions.go`, `internal/config/registry.go`, `internal/config/options.go`, `internal/config/config.go`, `internal/config/config_test.go`, `internal/config/defaults/config.yaml`, `cmd/apogee/settingsrows_test.go`, `cmd/apogee/wire_boot.go`, `cmd/apogee/wire_firing.go`, `cmd/apogee/wire_live.go`, `cmd/apogee/wire_settings.go`, `cmd/apogee/testdata/stubllm/step-notice.yaml`, `cmd/apogee/e2e_stepnotice_test.go`, `docs/adr/0077-the-context-fill-notice-is-the-first-engine-advise-reaction.md`, `docs/manual/reactions.md`, `docs/manual/configuration.md` — plus every other hit of the guard's grep

**Read first:** `internal/agent/fillnotice.go` — contextFillNotice, contextFillNoticeID, actionNotice, rearmFillNotice; `internal/agent/builtins.go` — buildBuiltins, classedBuiltin; `internal/agent/agent.go` — Run (turns.exchangeTurns is incremented AFTER step() returns, so at post-tool-result of Turn N it reads N-1), SetReactions; `internal/agent/reactions.go` — armReactions (taken ids), bypassSkips; `internal/domain/reaction.go` — Generation; `internal/config/reactions.go` — contextFillNoticeKey, entryReactions; `cmd/apogee/wire_settings.go` — setContextFillNotice, applyContextFillNotice, settingsTable; `cmd/apogee/e2e_fillnotice_test.go` — TestE2EFillNoticeLandsAsTheAnnouncedLine, headlessFillNotice, toolResultHandedOn

**Tests:** `stepnotice_test.go`: cap 4 ⇒ fires after Turn 3's tool result with the exact text, never twice, never at depth 0, never when off, never under Bypass. e2e `TestE2EStepNoticeReachesTheChild` (pattern `e2e_fillnotice_test.go`): with the key on, the child's third tool message the stub received ends with the `RenderAdvice` trailer for `{step-budget-notice, engine origin, post-tool-result}`; with the key absent, no fence anywhere.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'StepNotice|Builtins' && go test ./internal/config/ && go test ./cmd/apogee/ -run 'StepNotice|Template'`

**Commit:** `feat(agent): step-budget-notice advise Reaction warns a child at 75 % of its step cap (off by default)`

## 8. `output_path` on `sub_agent`; the wrap-up Turn keeps `write_file` for it

**What:** Depends on items 1, 4. Reverses the 2026-09-01 "tool-less wrap-up" call (owner, 2026-09-14). `sub_agent` gains `output_path` (string, optional): the file the child is expected to write, resolved through the write fence like `write_file`'s target (workspace or scratch dir). When set: the delegation's task text is unchanged; the wrap-up Turn (`agent.go` `finishAtStepCap` / `loop.go` `toolMenu` under `wrapUp`) offers exactly `write_file`, and `wrapUpDirectiveFormat` gains the clause `You may still call write_file once, for <path> only.`; a `write_file` in the wrap-up to any other path is refused with `wrap-up: only <path> may be written`; the loop dispatches that one call before ending (`step()`'s `wrapUp` exit). Without `output_path` the wrap-up stays tool-less. CONTEXT.md **Step cap** amended; `docs/manual/configuration.md` (`output_path` documented beside `max_steps`).

**Regression guard.** `wrapUpDirectiveFormat` stays untouched (`TestWrapUpRequestWithdrawsToolsAndSaysWhy` and `delegatereport_test.go` pin `fmt.Sprintf(wrapUpDirectiveFormat, cap)`; a path verb would change its arity): a separate `wrapUpOutputClauseFormat` is appended to the system directive only when the child carries an `output_path`. `write_file` and the clause are offered only when the child's registry holds `write_file` AND its live Mode admits a workspace write (a Plan-mode child inherits Plan; `planAdmits` refuses the call) — otherwise the wrap-up stays tool-less and clause-less, never announced-then-refused. `cmd/apogee/e2e_delegate_test.go` does not exist: the step-cap e2e is `cmd/apogee/e2e_delegation_test.go` `TestE2EDelegationStepCap`, whose `childToolMenus == [true,true,true,false]` pin is the no-output_path run; the OUT variant is its own subtest answering the write gate (`approveEdit`, `e2e_outcome_test.go`) or running under `mode:`; `max_steps` is documented at `docs/manual/configuration.md`, not commands.md. Prose rule: every comment naming the tool-less / wholesale-withdrawn wrap-up — `grep -rn 'tool-less\|withdraws the menu WHOLESALE' internal/agent` (agent.go wrapUp field and finishAtStepCap, subagent.go, loop.go, builtins.go, construct.go, swaptools.go, turn.go) — gains the `output_path` exception, not CONTEXT.md alone.

**Files:** `internal/tools/sub_agent.go`, `internal/tools/sub_agent_test.go`, `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/agent/agent.go`, `internal/agent/loop.go`, `internal/agent/resolution.go`, `internal/agent/builtins.go`, `internal/agent/construct.go`, `internal/agent/swaptools.go`, `internal/agent/turn.go`, `cmd/apogee/testdata/stubllm/delegate-cap.yaml`, `cmd/apogee/e2e_delegation_test.go`, `CONTEXT.md`, `docs/manual/configuration.md`

**Read first:** `internal/agent/agent.go` — finishAtStepCap, wrapUp, Run; `internal/agent/loop.go` — step, toolMenu, buildRequest; `internal/agent/subagent.go` — runSubAgent, newChildAgentOn, wrapUpDirectiveFormat, delegationResult; `internal/tools/sub_agent.go` — SubAgentArgs, subAgentSchemaTemplate, PromptArgKeys; `internal/tools/workspace_scoped.go` — WorkspaceWriteTarget, resolveTargetUnbounded; `internal/agent/builtins.go` — salvageToolCall; `internal/agent/subagent_test.go` — wrapUpAgent, runWrapUpAgent, TestWrapUpDropsToolCallsAndKeepsTheText; `cmd/apogee/e2e_delegation_test.go` — TestE2EDelegationStepCap, childToolMenus

**Tests:** `subagent_test.go`: cap 2 + `output_path` ⇒ wrap-up menu is `[write_file]`, the directive is `wrapUpDirectiveFormat` + `wrapUpOutputClauseFormat`, the scripted write lands and the parent result embeds the file's existence; another path in the wrap-up ⇒ exact refusal; no `output_path` ⇒ wrap-up menu empty and the directive byte-identical (existing T-04 tests and `TestWrapUpRequestWithdrawsToolsAndSaysWhy` green); a Plan-mode child with `output_path` ⇒ menu empty, no clause. e2e (`e2e_delegation_test.go`, `delegate-cap.yaml` gains a variant as its own subtest answering the write gate or under `mode:`): the OUT file exists after the run; the `childToolMenus` pin of the no-output_path run is unchanged.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'SubAgent|StepCap|WrapUp' && go test ./internal/tools/ -run 'SubAgent' && go test ./cmd/apogee/ -run 'TestE2EDelegate'`

**Commit:** `feat(agent): sub_agent output_path keeps write_file in the step-cap wrap-up Turn`

## 9. A child's result is validated: missing output, vendor markup and sub-sentence replies are faults

**What:** Recast at the regression check (2026-09-14). Depends on item 8. `internal/agent/subagent.go` `delegationResult` `default:` branch returns `finalMessageText()` whatever it says. Add, in order: (a) `output_path` set and the file absent after the child's last Turn ⇒ `IsError` result `sub-agent ended without writing <path>; its last text follows:` + the text; (b) final text matching a vendor tool-call container (reuse `internal/floor/salvage.go`'s container detection — export `HasToolCallMarkup(text string) bool`) ⇒ `IsError` `sub-agent reply is unparsed tool-call markup, not a report` + the text; (c) final text that is a bare acknowledgement (`Done.`, `Understood`) ⇒ treated as no text (`stepCapNoTextMarker` shape, non-error, marker `[delegate returned no report]`). `dispatch.go`'s bare `unknown tool %q` sites are untouched here (plan 02 item 16). The TUI verdict for (a)/(b) is the existing error slot.

**Regression guard.** OWNER CALL 2026-09-14 — apply the guards: (b) fires only when a container's captured BODY is a JSON object (`json.Valid` on the submatch) or the trimmed reply BEGINS with the container; (c) keys on a fixed case-insensitive acknowledgement set (done, understood, ok, okay, acknowledged, noted, sure) plus trailing punctuation — delete the "under 40 runes with no path in it" phrase from What, the acknowledgement rule governs; (a) on the `StepCapped` branch keeps `stepCapResultFormat` first and the NON-error shape, with the missing-output note appended in the `SeatFallbackNote` slot, and is skipped when the wrap-up writer was withheld (Plan-mode child) — name the re-pin of item 8's Plan-child test; `delegationResult` keeps its signature and reads the output path off the child field item 8 adds ("" skips (a)); CONTEXT.md **Step cap** ("non-error result whose first line marks it partial") stands and is NOT reversed — say so; add the header note that items 4 and 9 agree: the step-cap marker is always first. Applied: item 8's Plan-mode-child case (`subagent_test.go`: a Plan-mode child with `output_path` ⇒ menu empty, no clause) is re-pinned to carry no missing-output note; CONTEXT.md **Step cap** stands unedited; the header carries the items-4-and-9 note. Still binding from the first round: `HasToolCallMarkup` keys on `toolCallTagPattern` plus a new DSML container pattern only — never `fencedBlockPattern`; the order in which items 8, 9 and 11 rewrite `delegationResult`'s tail: 11's degenerate check → 9's (a)/(b)/(c) → 11's cap → seat note → steered trailer.

**Files:** `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `internal/floor/salvage.go`, `internal/floor/salvage_test.go`, `internal/tui/toolregistry.go`, `internal/tui/toolregistry_test.go`

**Read first:** `internal/agent/subagent.go` — delegationResult, finalMessageText, lastVisibleText, stepCapResultFormat, stepCapNoTextMarker, runSubAgent; `internal/agent/seat_test.go` — TestSeat_FallbackNoteIsTheBodysLastLine, TestSeat_ResultWithoutAFallbackIsByteIdentical; `internal/agent/subagent_test.go` — runSteeredDelegation, TestSubAgent_UnsteeredChildResultIsUnchanged, TestSubAgent_StepCapMarksAWordlessDelegate, TestSubAgent_ParentNoticeOnEveryOutcomeButCancelled;
`internal/floor/salvage.go` — toolCallTagPattern, fencedBlockPattern, salvageFromContainers; `internal/tui/toolregistry.go` — delegationVerdict, delegationFailure, delegationStepCapHead; `internal/tui/subagentblock_test.go` — TestCollapsedRunSlotCarriesTheResultEnvelope; `CONTEXT.md` — Step cap

**Tests:** `subagent_test.go`: each of the three shapes yields the exact marker — (c) on the acknowledgement set only (`Done.`, `Understood`, `ok`, `Noted!`), while `Yes.`, `child done`, `child one done` and every other reply are byte-identical to today (`TestSubAgent_UnsteeredChildResultIsUnchanged` and the `fanout_test.go` `Contains("child one done")` pins green, `fanout.yaml`'s `One file.` untouched); (b) faults a reply whose container body is a JSON object or that begins with the container, and passes a report that merely QUOTES `<tool_call>…</tool_call>` in prose; (a) on a step-capped child whose `output_path` is absent ⇒ `stepCapResultFormat` first, `IsError` false, the missing-output note appended in the `SeatFallbackNote` slot (`TestSeat_FallbackNoteIsTheBodysLastLine` shape); item 8's Plan-mode child with `output_path` (wrap-up writer withheld) ⇒ no note (re-pinned); a bare `&Agent{}` literal (`seat_test.go`) skips (a). `salvage_test.go`: `<｜DSML｜tool_calls>` and `<tool_call>` with a JSON body detected, prose with a code fence or a quoted tag pair not.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'SubAgent|Delegation' && go test ./internal/floor/ && go test ./internal/tui/ -run 'ToolRegistry'`

**Commit:** `fix(agent): a child's missing output, vendor markup or sub-sentence reply is a fault, not a report`

## 10. The loop breaker catches exact A-B-A-B repeats

**What:** `internal/floor/loopbreak.go` `ToolLoopBreak` compares the current tool call key with `previousToolCallKey` only, so `sub_agent(noop)` / `task_list` alternation (review `28b8d620`, 14 calls) never fires. Extend: over the last FOUR tool Turns of the Exchange (`ExchangeView`), fire when `key(now) == key(-2) && key(-1) == key(-3)` with byte-identical keys (name + canonical arguments), in addition to the existing immediate repeat. Directive text unchanged; the guard stays a Floor guard under its existing key (ADR 0071). No fuzzy or windowed-count matching (writer call).

**Regression guard.** The A-B-A-B rule fires only when the repeated pair's TOOL RESULTS are also byte-identical (read from the Exchange body via `RangeAfter`) — a poll whose output moves (`console_read`/`read_file` while a build runs, ADR 0059) is progress, a stalled one is the loop, and `sub_agent(noop)`/`task_list` still fires on the identical task_list result; the item thereby yields to ADR 0059's design of `console_read` for polling a dev server. Add the prose-guard rule + grep: every doc/comment describing the breaker as an identical / previous-Turn repeat — `grep -rn -i 'identical repeat\|previous Turn.s exact\|identical repeated tool call' CONTEXT.md docs internal/config internal/domain internal/floor` — gains the A-B-A-B clause; ADR 0071 ("fires only on an identical repeat Turn") by dated amendment note.

**Files:** `internal/floor/loopbreak.go`, `internal/floor/loopbreak_test.go`, `internal/agent/floorguards_test.go`, `CONTEXT.md`, `docs/adr/0071-floor-guards-are-engine-behaviour-and-the-nudge-catalogue-retires.md`, `docs/manual/configuration.md`, `internal/config/defaults/config.yaml`, `internal/domain/config.go` — plus every other hit of the guard's grep

**Read first:** `internal/floor/loopbreak.go` — ToolLoopBreak, previousToolCallKey, computeToolCallKey, buildToolLoopDirective; `internal/domain/exchange.go` — CurrentExchange, ExchangeView.RangeAfter, ExchangeView.Found; `internal/floor/loopbreak_test.go` — loopResponse, TestToolLoopBreakDoesNotFireAcrossAnExchangeBoundary; `internal/agent/floorguards_test.go` — TestFloorGuard_ToolLoopBreakerOnAnIdenticalRepeat, TestFloorGuard_LoopBreakerDoesNotFireOnARepeatedRequest, guardFireCountFor; `internal/agent/subagent_test.go` — TestSubAgent_BreakerIsolatedFromParent; `CONTEXT.md` — "tool-loop breaker" Floor guard bullet

**Tests:** `loopbreak_test.go`: A-B-A-B with byte-identical keys AND byte-identical tool results fires on the fourth call; the same keys with a moving result (the `console_read` / `read_file` poll) does not; A-B-A-C and A-B-A with changed args do not; A-A still fires; a three-Turn Exchange never fires the new rule; `sub_agent(noop)`/`task_list` fires on the identical task_list result. `floorguards_test.go`: an Exchange-scoped case (the `e87ca5ee` scope) stays green. The prose grep returns no clause-less hit.

**Acceptance:** `go build ./... && go test ./internal/floor/ && go test ./internal/agent/ -run 'LoopBreak|Guard'`

**Commit:** `fix(floor): the tool-loop breaker fires on an exact A-B-A-B repeat`

## 11. Degenerate child narration is a fault; `sub_agent` results have an absolute cap

**What:** Depends on item 9. In `delegationResult`, before the validations of item 9: a final text whose most frequent non-blank line occurs ≥ 50 times ⇒ `IsError` `sub-agent reply is degenerate (one line repeated N times)` with the first 20 lines only; every `sub_agent` result (report or fault) is capped at `delegateResultMaxBytes = 64 KiB` (constant), head 48 KiB + `[… N bytes elided …]` + tail 16 KiB, applied before the structural clamp in `dispatch.go` `appendToolResult`. CONTEXT.md **Sub-agent** states the cap.

**Regression guard.** `appendToolResult` sees a `ToolResult` with a CallID and no tool name, so it cannot single out `sub_agent` results: the cap is applied in `delegationResult` — `dispatch.go` untouched — in the order item 9's guard states (11's degenerate check → 9's (a)/(b)/(c) → 11's cap → seat note → steered trailer), so the steered trailer follows the capped tail and the parent notice survives by construction. `TestSubAgent_ParentNoticeSurvivesTheStructuralClamp` feeds one line × 4000, which the ≥ 50-repeat rule would fault and pass vacuously: re-shape the fixture to distinct (numbered) lines past 64 KiB and assert the trailer follows the cap's tail. The item yields to `dispatch.go`'s one-elision-idiom rule (`apogeectx.TruncateToolResult`'s marker "whichever seam produced it"): the cap's elision reuses that marker idiom, not a second `[… N bytes elided …]` one.

**Files:** `internal/agent/subagent.go`, `internal/agent/subagent_test.go`, `CONTEXT.md`

**Read first:** `internal/agent/subagent.go` — delegationResult, userSteeredTrailer, SeatFallbackNote; `internal/agent/dispatch.go` — appendToolResult, clampToBound, clampToolResult; `internal/context/toolresult.go` — TruncateToolResult, toolResultElisionMarker; `internal/agent/subagent_test.go` — TestSubAgent_ParentNoticeSurvivesTheStructuralClamp, runSteeredDelegation; `internal/tui/toolregistry.go` — delegationSteeredTail, readDelegationSteering

**Tests:** `subagent_test.go`: "Emit. / write_file. / GO." × 2,000 ⇒ exact fault with 20 lines; a 200 KB report of distinct numbered lines ⇒ 64 KiB with the elision marker and both ends intact, the steered trailer after the capped tail; a 30 KB report byte-identical; `TestSubAgent_ParentNoticeSurvivesTheStructuralClamp` re-shaped to distinct numbered lines past 64 KiB, asserting the trailer follows the cap's tail.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'SubAgent|Delegation|Dispatch'`

**Commit:** `fix(agent): degenerate child narration is a fault; sub_agent results cap at 64 KiB`

## 12. A child's streamed text is committed once: `displace` parks on every cross-depth switch

**What:** Recast at the regression check (2026-09-14). Fixes the fragment-then-duplicate bug (review headline 13): `internal/tui/transcript.go` `displace` parks the buffered run on a same-depth switch but COMMITS it as an entry on a cross-depth switch ("a parent cannot stream while its delegate does") — false under fan-out with nested children, where sibling B's grandchild streaming mid-A commits A's partial text as a fragment and A's later `MessageEvent` commits the whole text again. Fix (writer call): `displace` always parks; `closeRun` keeps committing an abandoned run's residue; `commitAssistant` on a canonical `MessageEvent` replaces any fragment entries already committed for that run (matched by `SpawnCallID` + depth) instead of appending. The session record (`session.Entry` per run) follows. Visible change (stated, not guarded): a parked run paints nothing — `render.go`'s preview reads only the live buffer, never `t.parked` — so from the other run's first token until its own next token the parked run's view shows no partial text, where today the cross-depth switch left it on screen as a committed fragment; this is the existing same-depth sibling behaviour, now uniform.

**Regression guard.** OWNER CALL 2026-09-14 — the parked residue is committed on EVERY `SubAgentFinished` phase (`runRef{depth: e.Depth, spawn: e.CallID}`), cancelled or not, with `closeRun` kept as belt-and-braces; the two re-pinned tests stream with `EventBase{Depth: 1, CallID: "s1"}` and fold `SubAgentPhaseEvent{Depth: 1, CallID: "s1", Phase: SubAgentFinished, Cancelled: true}` (`subAgentReport` is in transcript_test.go); the item STATES the visible change (a parked run paints nothing from the other run's first token until its own next token — the existing sibling behaviour); the `parkedText` doc comment (transcript.go) is rewritten with the new exit; update the header's "Child stream (writer)" call accordingly. Why every phase, not the cancelled exit alone: a fan-out group is dropped whole when ANY member is cancelled (no `ToolResultEvent` follows for any slot), but a sibling that FAULTED mid-stream got its finished phase earlier with `Cancelled=false` — keyed on the cancelled exit only, its residue would never be committed. DROP the "replace already-committed fragments" step — entries carry no fragment mark and a run's per-Turn narrations share `SpawnCallID` + depth, so once displace parks on every switch no fragment precedes the `MessageEvent` (the header's writer call reworded accordingly).

**Files:** `internal/tui/transcript.go`, `internal/tui/transcript_test.go`, `internal/tui/fanout_test.go`, `internal/tui/sessionsave_test.go`

**Read first:** `internal/tui/transcript.go` — displace, park, takePending, closeRun, addSubAgentPhase, addToolResult, commitCancelled, place; `internal/agent/dispatch.go` — dispatchFanOut, runDelegationPool, skipDelegation, executeDelegate, emitSubAgentPhase; `internal/tui/render.go` — previewAt; `internal/tui/transcript_test.go` — streamAt, subAgentCall, subAgentReport, TestCommitCancelledRecoversAParkedTopLevelPartial;
`internal/tui/fanout_test.go` — TestAbandonedChildStreamCommitsWhenItsRunEnds, TestConcurrentSiblingStreamsCommitWhole, headIndex; `internal/tui/model.go` — foldCancelled; `internal/tui/worker.go` — stepToBoundary; `internal/domain/events.go` — SubAgentPhaseEvent

**Tests:** `fanout_test.go`: two depth-1 siblings, B's depth-2 child streams and calls a tool mid-A ⇒ exactly one assistant entry for A holding the whole text, none for the fragment; a delegation's parked residue lands as its run's entry at its `SubAgentFinished` phase — `TestSubAgentStreamResidueIsNotAttributedToTheParent` and `TestParentMessageKeepsTheDelegatesStreamInsideItsRun` re-pinned to stream with `EventBase{Depth: 1, CallID: "s1"}` (the `token` closure of `TestConcurrentSiblingStreamsCommitWhole`) and fold `SubAgentPhaseEvent{Depth: 1, CallID: "s1", Phase: SubAgentFinished, Cancelled: true}`; a sibling that faulted mid-stream (`Cancelled: false` phase) inside a group later cancelled whole has its residue committed at its own phase; existing park/closeRun tests green; `sessionsave_test.go`: the saved record has one entry for A.

**Acceptance:** `go build ./... && go test ./internal/tui/ -run 'Transcript|FanOut|SessionSave'`

**Commit:** `fix(tui): a child's streamed reply is committed once — displace parks on every cross-depth switch`

## 13. The capped-reply fault stops promising a retry fails

**What:** Recast at the regression check (2026-09-14). Wording only — no cap moves and no effort fact crosses into the engine. (i) `internal/agent/loop.go` `cappedReplyErrFmt` no longer claims "a retry meets the same ceiling" (false for reasoning models — `30a3b2df` [322]): only its LAST CLAUSE changes. (ii) `internal/agent/compact.go`: `cappedSummaryErrFmt` is formatted with the cap the summariser request was actually sent with (the `maxTok` set on the request), not the bare constant, so the fault names the applied cap. The forced-reasoning compaction-cap allowance this item first carried (`compactMaxTokens × 2` when reasoning is mandatory) is REMOVED — parked on `apogee-tfp` (Out of scope). ADR 0046's no-retry decision stands (`apogee-tfp`).

**Regression guard.** OWNER CALL 2026-09-14 — the item YIELDS to ADR 0060 D9 (Mandatory stays host-side) and the archived `2026-08-31 - 02` plan's rejection: the compaction-cap allowance half is REMOVED (parked on bead `apogee-tfp` — say so in Out of scope), no `ReasoningMandatory` crosses anywhere; the item keeps only (i) `cappedReplyErrFmt`'s LAST CLAUSE change — keep `%d`, `%s` and `max-output-tokens:`, replace "a retry meets the same ceiling" with "a retry may succeed on a reasoning model", `TestCutOffReplyNamesTheOutputCap` stays green, `CutOffReply` in the acceptance — and (ii) `cappedSummaryErrFmt` formatting the APPLIED cap; ADR 0046 §4's sentence superseded by a dated note; retitle "The capped-reply fault stops promising a retry fails"; Files shrink to `internal/agent/loop.go`, `loop_test.go`, `compact.go`, `compact_test.go`, `docs/adr/0046-*.md`; update the header's "Compaction allowance (writer)" call to "**Compaction cap (owner, 2026-09-14):** unchanged — ADR 0060 D9 stands; wording fix only." Yielded-to sources: `docs/adr/0060-…md` D9 as amended 2026-08-31 ("the dialect is still the ONLY effort fact that crosses … `Mandatory` stay host-side"), restated at `rebind.go`, `tui.go` and `heartbeat.go`, and `docs/plans/archived/2026-08-31 - 02 - routed-effort-dialect-plan.md` (raising `compactMaxTokens` for a mandatory reasoning pass rejected) — none is amended; `ServerHost.Rebind`, `DelegationTarget` and `dispatch.go`'s fold-budget comment are untouched.

**Files:** `internal/agent/loop.go`, `internal/agent/loop_test.go`, `internal/agent/compact.go`, `internal/agent/compact_test.go`, `docs/adr/0046-the-engine-bounds-every-reply-with-an-output-cap.md`

**Read first:** `internal/agent/loop.go` — cappedReplyErrFmt, emptyReplyFault; `internal/agent/compact.go` — compactMaxTokens, compactCompleter.Complete, cappedSummaryErrFmt; `internal/agent/emptyreply_test.go` — TestCutOffReplyNamesTheOutputCap; `internal/agent/compact_test.go` — TestCompactCappedSummaryFaultNamesOnlyWhatTheRequestAsked; `docs/adr/0060-…md` — D9 (yielded to, unedited)

**Tests:** `compact_test.go`: a capped summary fault names the cap the request was sent with (`TestCompactCappedSummaryFaultNamesOnlyWhatTheRequestAsked` shape; 4096 today, and the value the request carried if it ever differs). `loop_test.go` / `emptyreply_test.go`: the capped-no-text fault carries the exact new last clause with the `max-output-tokens:` key and the reasoning spend intact — `TestCutOffReplyNamesTheOutputCap` stays green unchanged (it pins the key and the spend, not the last clause).

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'Compact|Capped|CutOffReply'`

**Commit:** `fix(agent): the capped-reply fault stops promising a retry fails; the capped-summary fault names the applied cap`
