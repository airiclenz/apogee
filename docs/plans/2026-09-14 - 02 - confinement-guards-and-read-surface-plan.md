# Confinement, guard truthfulness and the read surface — session-mining fixes, wave A

**Goal:** close the confinement, guard and tool-surface findings of the 2026-09-14 session-mining review: the Go toolchain works under confinement, every path apogee announces is readable by its own read tools, the guards say why they refuse and stop refusing reads, and `read_file` / `@file` / `grep` / `web_fetch` are bounded by default.

**Date:** 2026-09-14
**Status:** unexecuted
**Sized for:** ~200k-context host

**Sources:**
- `docs/reviews/session-mining-2026-09-14.md` (headlines 1, 2, 7, 8, 9, 10; items 1, 2, 3, 5, 15–22)
- ADR 0012 (blast radius), ADR 0049 (dangerous-action guard + 2026-08-28 scratch amendment, Q6 approval grain), ADR 0056 (D1 preamble, D3 scratch dir), ADR 0061 (skill discovery deferred), ADR 0065 §6, `docs/design/confinement-execution-contract.md` §4/§7, `docs/design/tool-surface-findings.md` (name-not-param lesson)
- Beads: `apogee-2ay` (item 10), `apogee-m3p` (Windows twin, out of scope)

**Ratified design calls (owner, 2026-09-14):**
- **Go caches:** cold per-session caches under the scratch dir, seeded on confined runs only; the writable box is not widened.
- **Toolchain read roots:** the host probes `go env GOROOT GOMODCACHE` once at boot; no config key.
- **read_file cap:** 400 lines / 40 KiB as a constant; the `@file` cap (32k tokens) likewise a constant. No new keys.
- **WritesOnly on shell text:** a verb-aware write view (redirect targets and mutating leaders feed the WritesOnly rules; read leaders' arguments do not).
- **Forced gate popup:** the "always allow this session" row is hidden, `s` dropped, one disclosure line; ADR 0049 Q6 stands.
- **Plan mode:** prompt.txt wording qualified + a Plan-only orientation bullet naming the withheld families.
- **Read at a revision:** a new named `git_show` tool, not a `revision:` parameter.
- **Skills clause:** none — orientation unchanged, discovery stays with ADR 0061.
- **Scratch read mount (writer):** the `Read-only library roots:` line lists LIBRARY roots — skills roots plus the toolchain roots item 4 probes; the scratch dir is kept off it (its own `Scratch dir:` bullet already announces it), which is why it rides a second fold beside `ExtraReadRoots`; items 2 and 4 share one constant for the toolchain pins.
- **Write view scope (owner, 2026-09-14):** only `write-git-control-plane` opts into the shell write view.
- **Exit 1 with stdout (writer):** stays `IsError` (documented design); only the fail-fast note changes.

**Regression check (2026-09-14, 3fca8c2cb9bb48fe5cd4ee1daf600e105eed172b):**
- 2: guard folded — `Confinement.Box` is a value and the funnel is `run` (no `subprocess.Run`); the seed is one shared helper called from `run` and from `console_open`'s confined branch; the orientation clause is qualified to confined runs; `TestConfigConfinementBox` re-pinned; the e2e gets its own fixture.
- 3: guard folded, writer call replaced (decision) — the roots line lists library roots (skills + toolchain), the scratch dir rides a second fold; the scratch root is mounted as its real path; `domain.Config.ScratchReadRoot` is the seam (no Agent at `hostTools(cfg)`); `delegationname_test.go` follows the `NewPresentDocument` shape; the remote-session rung-0 degradation is stated.
- 4: guard folded — the probe runs in the apogee home with the toolchain pins, off the boot path; roots are mounted as real paths and only when they exist; the two `slices.Equal` Firing pins are re-pinned; the roots bullet's tail is softened (a third bound orientation clause).
- 5: guard folded — the stopped-command line never lands inside the `[exit code …]` brackets (`exitCodeMarker`); the bash/dash rows run `bash -c` / `dash -c` directly.
- 6: guard folded — a locate miss renders the plain body, windows merge and a whole-file union is byte-identical; the presenter pin lands in `toolregistry.go` / `toolpresent_test.go` (`toolview_test.go` does not exist).
- 7: guard folded — the clip is a note event, never an `ErrorEvent`; the 32k sentence fires only when the constant bound clipped; blank lines never join in the PDF accumulator.
- 8: guard folded — `doc.go` names `htmltext.go`; the hoist is a page cleaner beside the fragment cleaner; the cap nudge reads `— narrow with include or path` here (decision; item 14 extends it).
- 9: guard folded — both `rm` mirror rules change; the masked token ends at whitespace or a shell metacharacter; the three deeper-path `TestMaskExempt` rows are rewritten; the bite rows spell `.git/config`; `configuration.md` carries the wording.
- 10: recast (owner decision) — the write view is opt-in per rule and only `write-git-control-plane` opts in, keyed on a `ShellCommandTool` marker; `rules.go`/`rules_test.go`'s "WritesOnly narrows by declared class, not by parsing shell text" prose is superseded by the ADR 0049 dated note.
- 11: guard folded — the menu is derived per request; ordinary-gate fixtures get a CacheKey; the forced panes wait on a `forcedMarker`; the golden is `t10-forced-pane.txt`, owned by `TestE2EApprovalForcesALookAtTheControlPlane`.
- 12: recast (decision); yields to plan `2026-09-14 - 01` items 4–5 (ADR 0012 second loosen) — the Plan bullet is worded from the live menu; the mode is re-introduced as a block input with `orientationPlanLine`; the manual note names the embedded prompt.
- 13: guard folded — registry/roster pins extended; `configuration.md` lines 145-148 name `git_show`; `list` keeps one process; the "read trio" prose rule; `git_show` inherits item 6's cap through `renderFile` (decision).
- 14: guard folded — `exclude` is a per-call skip set (the package map is never written); the target rides on `grepMatch`; the nudge is extended here (decision); manual edits point at real sections (decision).
- 15: recast (decision) — the directory copy journals every destination; `cwd:` is stripped in the TUI detail; `view_diff` detects absence by `fs.ErrNotExist`; `file_ops_test.go`'s "a directory source is refused" comment and `safeio.go`'s non-regular-source refusal are superseded and reworded.
- 16: guard folded — `doc.go` names `nearname.go`; the sweep rule is by renderer, with `diff.go` struck (decision); the prefix lands at the `find_replace.go` call sites; `write_file` uses `statWriteTarget`; `commands.md`'s near-miss paragraph is re-worded.
- Re-check (2026-09-14, 3fca8c2c…):
- 12: guard folded (decision) — the agent-package "the mode is NOT an input" comment is the current one at HEAD after plan 01 items 4–5; item 12 re-introduces the mode as an input, re-words that comment, and brings the ADR 0023 §6 / ADR 0056 D3 notes up to date (`docs/adr/0023-*.md`, `docs/adr/0056-*.md` join Files).
- 15: recast (decision) — one exported `StripCwdLine` serves the three `cwd:` consumers (TUI success detail, TUI failure body, headless narration); the `IsDir` arm stays for `move_file`; the shell sentence is appended to the description; a dir copy to an out-of-workspace destination is refused before the Gate; the shipped-mount branch keeps refusing directories; `python_exec_test.go`, `headless.go`, `headless_test.go` join Files.

**Standing requirements:**
- `skills: coding-standards`
- Deviations from item text land as a dated NOTES line under the item.
- Bubble Tea v2 (`charm.land/bubbletea/v2`): `tea.KeyPressMsg`, `msg.String()`; the `Model` is value-copied — no `strings.Builder` by value.
- Nothing new is put in front of the model except tool-result text and the orientation clauses this plan binds verbatim (items 2, 4, 12).

**Out of scope:**
- Denial-kill on stdout text (`apogee-1mj`, ADR 0056 D2); Windows caches (`apogee-m3p`); a `read-roots:` config key.
- DENIED: classifying Plan mode by `IsReadOnly` (contract §4 fn 2 stands); a `load_skill` list mode (ADR 0065 §6); refusing GOROOT for `terminal` (no read fence exists); a post-refusal obfuscation audit event (bench-evidence class).
- Line-number prefixes in `read_file` output (open question 9 — unmined); `grep -c`-style counts beyond `count_only`; any change to VERSION.

## 1. Gate: plan 2026-09-14 - 01 is archived — ✅ DONE (2026-09-15)

NOTES (2026-09-15): gate verified — plan 01 is absent from docs/plans/ and present under docs/plans/archived/; the archived copy keeps `**Status:** unexecuted` (archived-plan convention) and all seven items carry `✅ DONE (2026-09-14)`; acceptance command passed; the three files items 3 and 12 will edit (cmd/apogee/e2e_announced_test.go, cmd/apogee/wire_boot.go, internal/agent/orientation.go — orientationBlock at line 112) exist at HEAD.

**What:** Verify `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` no longer exists under `docs/plans/` and its archived copy under `docs/plans/archived/` reads `**Status:** executed` (or every item carries a DONE marker). Items 3 and 12 of this plan edit `cmd/apogee/e2e_announced_test.go`, `cmd/apogee/wire_boot.go` and `internal/agent/orientation.go`, which that plan rewrites. No code change.

**Files:** none

**Read first:** `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` — items 4–7 (no DONE marker, `**Status:** unexecuted`; items 1–3 DONE); `docs/plans/archived/` — archived plans keep `**Status:** unexecuted`, the per-item DONE markers are the signal; `internal/agent/orientation.go` — orientationBlock

**Tests:** none.

**Acceptance:** `test ! -e "docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md" && ls "docs/plans/archived/" | grep -q "2026-09-14 - 01"`

**Commit:** none (gate only).

## 2. Toolchain temp and cache dirs point into the scratch dir on confined runs — ✅ DONE (2026-09-15)

NOTES (2026-09-15): `ScratchEnv` returns `([]string, error)` rather than the plan's bare `[]string` — the mkdir is part of the helper and a failed mkdir must not be swallowed; `run` and the `console_open` hook return it wrapped (`seed scratch env for <argv0>: …`) the way a non-demote Confine failure already is.
NOTES (2026-09-15): the seeded key list is exported as `subprocess.ScratchEnvKeys()` (a function over the one entry table, so keys and values cannot drift) and `goToolchainEnvKeys` is built from it — which also lets a host `GOCACHE`/`GOTMPDIR` through to an UNCONFINED `diagnostics` vet where the allowlist used to drop them; that is the "pass the seeded keys through" reading and the one constant item 4's probe can share.
NOTES (2026-09-15): the `orientation_test.go` pin is a new `TestOrientation_ScratchLineStatesTheConfinedCaches` holding the clause verbatim — the file has no goldens, `scratchLine` composes the bullet from the template itself, so no existing golden needed updating.
NOTES (2026-09-15): consequential edit — internal/domain/config.go: made necessary by the box's new `ScratchDir` field (the `Config.ScratchDir` comment named only the WritablePaths fold).
NOTES (2026-09-15): consequential edit — docs/manual/configuration.md (Windows paragraph): made necessary by the seed — "not something Apogee does yet" was false once the variables are set on every platform; reworded to say the Windows toolchains honouring them under the low-integrity token is unverified (`apogee-m3p` stays the tracker, Windows caches out of scope).
NOTES (2026-09-15): `internal/subprocess/doc.go` gained the `scratchenv.go` file-map line (new file in a mapped package).

**What:** Under a confinement box every subprocess apogee spawns (`terminal`, `python_exec`, `run_tests`, `diagnostics`, `console_open`) gets `TMPDIR`, `TMP`, `TEMP`, `GOTMPDIR` = `<scratch>/tmp`, `GOCACHE` = `<scratch>/go-build`, `XDG_CACHE_HOME` = `<scratch>/cache` (gh, pip and friends follow it), the directories created before the spawn. Unconfined runs keep the host env byte-identical. Binding calls:
- `domain.ConfinementBox` gains `ScratchDir string`; `ConfinementBox()` (`internal/domain/confinement.go`) folds it from the session scratch dir the box already lists as writable.
- The seeding lives in ONE shared helper (`subprocess.ScratchEnv(box) []string`, mkdir included) called from the funnel `run` (`internal/subprocess/subprocess.go`) and from `console_open`'s confined branch, applied when `ok && conf.Box.ScratchDir != ""` — never per tool. `diagnostics.go`'s `goToolchainEnvKeys` allowlist must pass the seeded keys through.
- The orientation `Scratch dir:` bullet (`internal/agent/prompts/orientation.txt` line 3) ends `… /tmp may be denied by workspace confinement; under workspace confinement TMPDIR and the Go build cache point here.` — exact text; `orientation_test.go` goldens updated.
- Contract §7's "never implemented" recommendation is marked implemented with this shape (dated note); ADR 0056 D3 gains a dated note; `docs/manual/configuration.md` confinement section says which variables are set.

**Regression guard.** `Confinement.Box` is a value `domain.ConfinementBox` (`internal/domain/confinement.go`) and the funnel is `RunSubprocess`/`RunSubprocessTo` → `run` (there is no `subprocess.Run`): the predicate is `ok && conf.Box.ScratchDir != ""` inside `run`, after the Confine call, so the JSON-encoded box the landlock helper re-execs with (`internal/platform/landlock_linux.go` `encodeBox`) carries the field. `console_open` never passes through the funnel — it builds its Env itself (`internal/tools/console_open.go`, `subprocessEnvScopedPath`) and starts via `console.Start` — so the seed is one shared helper called from `run` AND from `console_open`'s confined branch (the `consolePrepare` site); its toolchain pins are the one constant item 4's probe shares. `TestConfigConfinementBox` (`internal/domain/confinement_test.go`) DeepEquals the box: its two `want` boxes gain `ScratchDir: <the cfg's>`, the empty-ScratchDir case stays byte-identical. The seed fires only on a confined run, so the orientation clause is qualified to those runs (the exact text above) and pinned in `orientation_test.go`. The new e2e gets its own fixture `announced-scratch-caches.yaml` (same capture pattern `Scratch dir: (\S+) — writable`); `announced-scratch.yaml` stays untouched (`TestE2EAnnouncedScratchDirRunsUnpromptedInAuto` pins `len(results) != 2` and `AssertConsumed`).

**Files:** `internal/domain/confinement.go`, `internal/domain/confinement_test.go`, `internal/subprocess/subprocess.go`, `internal/subprocess/subprocess_test.go`, `internal/tools/console_open.go`, `internal/tools/console_open_test.go`, `internal/tools/diagnostics.go`, `internal/agent/prompts/orientation.txt`, `internal/agent/orientation_test.go`, `cmd/apogee/e2e_announced_test.go`, `cmd/apogee/testdata/stubllm/announced-scratch-caches.yaml`, `docs/design/confinement-execution-contract.md`, `docs/adr/0056-*.md`, `docs/manual/configuration.md`

**Read first:** `internal/subprocess/subprocess.go` — run, RunSubprocess, SubprocessSpec.Env; `internal/domain/confinement.go` — ConfinementBox, Config.ConfinementBox, ConfinementFromContext; `internal/domain/confinement_test.go` — TestConfigConfinementBox; `internal/tools/console_open.go` — Execute, consolePrepare;
`internal/tools/diagnostics.go` — goVetPins; `internal/agent/orientation.go` — orientationBlock; `cmd/apogee/e2e_announced_test.go` — TestE2EAnnouncedScratchDirRunsUnpromptedInAuto, announcedScratchDir

**Tests:**
- `subprocess_test.go`: a confined `RunSubprocess` with `Box.ScratchDir` set spawns `sh -c 'echo $TMPDIR $GOCACHE $XDG_CACHE_HOME'` and prints the three scratch paths, the dirs exist; a run with no box prints the host values unchanged.
- `internal/domain/confinement_test.go`: `TestConfigConfinementBox`'s two set-ScratchDir `want` boxes carry `ScratchDir`; the empty case is byte-identical. `console_open_test.go`: a confined Console's child env carries the seeded `TMPDIR`. `orientation_test.go`: the qualified clause is pinned verbatim.
- e2e (`TestE2EAnnouncedScratchDirHoldsTheToolchainCaches`, fixture `announced-scratch-caches.yaml`, Auto confined, stubllm script runs `go env GOCACHE; echo $TMPDIR` through `terminal`): both printed paths start with the `Scratch dir: (\S+)` value captured from the orientation block the stub received — the exact announced string, never a fixture path.

**Acceptance:** `go build ./... && go test ./internal/subprocess/ ./internal/domain/ ./internal/tools/ ./internal/agent/ -run 'Orientation|Subprocess|Diagnostics' && go test ./cmd/apogee/ -run 'TestE2EAnnounced'`

**Commit:** `feat(subprocess): confined runs get TMPDIR and the Go/XDG caches under the session scratch dir`

## 3. The announced scratch dir and skills root are readable by every read tool — ✅ DONE (2026-09-15)

NOTES (2026-09-15): the TUI's live scratch read root is wired in `cmd/apogee/wire_live.go` (beside the `Config.ScratchDir` seed, which lives there rather than in `wire_boot.go`) as a new `lateEngine.ScratchDir()` accessor in `cmd/apogee/wire_engine.go` — it reads the bound Agent's live value (the one the orientation announces) and needs no per-call mkdir, where `sessionHost.SessionScratchDir` would create the dir on every read; the Firing mints its dir once and answers it from a fixed func.
NOTES (2026-09-15): the two e2e tests ride NEW fixtures (`announced-scratch-read.yaml`, `announced-skill-present.yaml`) rather than edits to `announced-scratch.yaml` / `announced-skill.yaml`, whose existing tests pin their exact call and result counts; the skills-root fixture captures the FIRST root on the `Read-only library roots:` line (the home library — the workspace anchor follows after a comma).
NOTES (2026-09-15): consequential edit — internal/agent/construct_test.go: made necessary by the new `HostTools.ScratchReadRoot` field (the every-field pin `TestHostToolsFillsEveryHostField` sets `Config.ScratchReadRoot`).
NOTES (2026-09-15): consequential edit — cmd/apogee/wire_tools_test.go: made necessary by the same field (`TestHostToolsForFillsEveryHostField`).
NOTES (2026-09-15): consequential edit — internal/domain/present.go: made necessary by present_document resolving over read mounts (the `PresentRequest.Path` / `DisplayPath` docs said "inside the workspace root" / "workspace-relative"; a mounted document's display name is its absolute resolved path).
NOTES (2026-09-15): consequential edit — docs/adr/0019-documents-are-presented-not-opened.md: made necessary by the same change (§5's "resolved inside the workspace root" bound; a dated note appended, the extension allow-lists and the doc server's workspace fence untouched).
NOTES (2026-09-15): consequential edit — docs/manual/configuration.md: the scratch-directory paragraph gained the read-back sentence (user-facing behaviour changed).
NOTES (2026-09-15): the mount degradation is stated on every rung of a mounted document's result as a trailing sentence ("Outside the workspace it is served locally; a remote session shows the path only.") — the tool cannot see which kind of session it runs in; workspace documents' wording is byte-identical.
NOTES (2026-09-15): improvement idea, not acted on — a Driver that sets `Config.ScratchDir` but not `ScratchReadRoot` (bench, an embedder) still gets a writable-but-unreadable scratch dir; an engine-side default reading the Agent's live value would close that for every Driver at once, but the plan binds the seam to the Config func.

**What:** Regression of the announced-path class (review headline 2c, live on 09-14): `read_file`, `grep`, `list_dir`, `find_files` refuse the `Scratch dir:` path with `security: path resolves outside the workspace root` (`security.ErrPathEscape`), and `present_document` refuses both it and the announced skills root because it takes no `ReadMounts` at all. Fix:
- `HostTools` gains `ScratchReadRoot func() string` beside `ExtraReadRoots`; `internal/tools/path_read.go` `readScope` folds it as one more live read root. It is NOT merged into `ExtraReadRoots`, so the `Read-only library roots:` line stays skills-only (writer call).
- `NewPresentDocument` takes the same `ReadMounts` the other readers take and resolves through `readScope`, not bare `resolveInRoot`.
- All three wiring sites (`cmd/apogee/wire_boot.go`, `wire_firing.go`, `wire_tools.go`) pass the live scratch dir (`Agent.ScratchDir()`); a session with no scratch dir passes "" (workspace-only, byte-identical).
- CONTEXT.md **Scratch dir** entry: "writable, and readable by every read tool".

**Regression guard.** The `Read-only library roots:` line lists LIBRARY roots — skills roots plus the toolchain roots item 4 probes; the scratch dir is kept off it (its own `Scratch dir:` bullet already announces it), which is why it rides a second fold; items 2 and 4 share one constant for the toolchain pins (writer decision, replacing the "roots line stays skills-only" call). `matchRoot` (`internal/tools/path_read.go`) silently skips any root that is not its own real path and `Agent.ScratchDir()` is spelled under `filepath.Abs(home)` (`internal/config/config.go` `ApogeeHome`), so under a symlinked `~/.apogee` — `announcedSkillFixture`'s very home — the scratch root would be dropped: the fold hands readScope `security.EvalRealPath(scratch)` (the way `readRoots` does for a trusted skill anchor, `internal/skills/load.go`) and `TestReadScopeScratchRootIsLive` pins a symlinked-scratch case; `resolveInRoot` then accepts the announced symlink spelling as `TestReadScopeReadsAnExtraRootFileBySymlinkSpelling` shows. "Three wiring sites pass `Agent.ScratchDir()`" has no seam — the engine builds HostTools from `domain.Config` alone (`internal/agent/construct.go` `hostTools(cfg)`) before any Agent exists: binding, `domain.Config.ScratchReadRoot func() string`, wired live by the host (`sessionHost.SessionScratchDir` / lateEngine) in `wire_boot.go` and `wire_firing.go`, copied in `construct.go`'s `hostTools` and `wire_tools.go`'s hand-assembly. `NewPresentDocument`'s new shape is followed at `internal/agent/delegationname_test.go` (its two calls pass a zero `ReadMounts{}`). `present_document` on a mount path renders only rung 0 on a remote session — `DocServer.Serve` (`internal/present/server.go`) fences every grant to the workspace root and the presenter reports `could not serve: …` — the item states that degradation in its text and in the tool-result wording (`served locally; a remote session shows the path only`); the scripted-presenter unit test cannot see it and the e2e runs a Local session.

**Files:** `internal/tools/registry.go`, `internal/tools/path_read.go`, `internal/tools/path_read_test.go`, `internal/tools/present_document.go`, `internal/tools/present_document_test.go`, `internal/domain/config.go`, `internal/agent/construct.go`, `internal/agent/delegationname_test.go`, `cmd/apogee/wire_boot.go`, `cmd/apogee/wire_firing.go`, `cmd/apogee/wire_tools.go`, `cmd/apogee/e2e_announced_test.go`, `cmd/apogee/testdata/stubllm/announced-scratch.yaml`, `cmd/apogee/testdata/stubllm/announced-skill.yaml`, `CONTEXT.md`

**Read first:** `internal/tools/path_read.go` — ReadMounts, readScope, matchRoot, rootUsable; `internal/tools/registry.go` — HostTools.ExtraReadRoots, readMounts; `internal/tools/present_document.go` — NewPresentDocument, Execute; `internal/agent/construct.go` — hostTools;
`internal/domain/config.go` — Config.ExtraReadRoots, Config.ScratchDir; `cmd/apogee/wire_tools.go` — hostToolsFor; `internal/present/server.go` — Serve, grant; `cmd/apogee/e2e_announced_test.go` — announcedSkillFixture, toolResults

**Tests:**
- `path_read_test.go`: `TestReadScopeScratchRootIsLive` (pattern of `TestReadScopeExtraRootsAreLive`) with a symlinked-scratch case (the announced spelling reads); another session's scratch dir is still refused.
- `present_document_test.go`: a document under a read mount renders and the result carries the remote-session rung-0 wording; outside every root refused with `ErrPathEscape`. `delegationname_test.go` compiles against the new `NewPresentDocument` shape.
- e2e `TestE2EAnnouncedScratchDirIsReadableByTheReadTools`: capture `Scratch dir: (\S+)` from the orientation the stub received, script `write_file` then `read_file`, `grep`, `list_dir`, `find_files`, `present_document` on that path; no tool result contains `security: path resolves outside the workspace root`. `TestE2EAnnouncedSkillsRootIsPresentable`: `present_document` on a path under the announced `Read-only library roots:` value succeeds. Both must fail against the pre-item tree.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'ReadScope|PresentDocument' && go test ./cmd/apogee/ -run 'TestE2EAnnounced'`

**Commit:** `fix(tools): the announced scratch dir and skills root are read roots for every read tool`

## 4. GOROOT and GOMODCACHE are probed read-only roots

**What:** Depends on item 3. At boot the host runs `go env GOROOT GOMODCACHE` once (through `internal/subprocess.RunSubprocess`, 5 s timeout, silently skipped when `go` is absent or the probe fails) and adds the two absolute paths to `ExtraReadRoots`, so they appear on the orientation `Read-only library roots:` line and `read_file`/`grep`/`list_dir`/`find_files`/`copy_file` accept them — the same fence `terminal` already lets through. Binding: the probe is a host function in `cmd/apogee` (`probeToolchainRoots`), never in `internal/tools` (ADR 0031: the seam stays driver-agnostic); the composed roots func is skills roots + toolchain roots, order stable; a Firing composes the same. Manual `commands.md` read-tools paragraph names the two roots.

**Regression guard.** `go env` run in a repo whose go.mod carries a newer `go`/`toolchain` line attempts a toolchain DOWNLOAD and fails (probed 2026-09-14), so the probe runs with `Dir` = the apogee home (never the workspace) and an env pinning `GOTOOLCHAIN=local`, `GOWORK=off`, `GOFLAGS=-mod=readonly` — `goVetPins`' shape (`internal/tools/diagnostics.go`), the one constant item 2's seed shares — and off the boot path (a goroutine feeding the live roots func) so no session pays the wait. `matchRoot` skips a root that is not its own real path and `rootUsable` skips one that does not exist (a symlinked `$HOME/go/pkg/mod`, a GOROOT symlink, a not-yet-created GOMODCACHE would be announced and refused): a probed path is mounted and announced as `security.EvalRealPath(p)` and only when `os.Stat(p)` is a directory; `TestE2EAnnouncedToolchainRootsAreReadable` runs under `announcedSkillFixture` (symlinked home). `TestFiringConfigSetsEveryUnattendedField` (`cmd/apogee/wire_firing_test.go`) and `TestScheduleFiringSharesTheSessionsSkillsProvider` (`cmd/apogee/schedule_test.go`) pin `slices.Equal(cfg.ExtraReadRoots(), provider.ReadRoots())` — red on every host with `go` on PATH: both are re-pinned to assert the skills roots are the leading prefix of `ExtraReadRoots()` and the toolchain roots follow. The roots bullet's tail (`orientation.txt` line 4, "never through terminal commands") would tell the model never to read through `terminal` the two trees every `go build` must read: the bullet ends `— read them with read_file, list_dir, grep, find_files or copy_file; a toolchain command may still read its own roots.` — a third orientation clause bound verbatim beside items 2 and 12, pinned in `orientation_test.go`.

**Files:** `cmd/apogee/wire_boot.go`, `cmd/apogee/wire_firing.go`, `cmd/apogee/wire_tools.go`, `cmd/apogee/toolchain_roots.go`, `cmd/apogee/toolchain_roots_test.go`, `cmd/apogee/wire_firing_test.go`, `cmd/apogee/schedule_test.go`, `cmd/apogee/e2e_announced_test.go`, `internal/agent/prompts/orientation.txt`, `internal/agent/orientation_test.go`, `docs/manual/commands.md`

**Read first:** `cmd/apogee/wire_boot.go` — hostConfig assembly (ExtraReadRoots); `cmd/apogee/wire_firing.go` — firingConfig; `cmd/apogee/wire_tools.go` — hostToolsFor; `internal/skills/load.go` — readRoots; `internal/tools/path_read.go` — matchRoot, rootUsable;
`internal/tools/diagnostics.go` — goVetPins, goVetEnv; `cmd/apogee/wire_firing_test.go` — TestFiringConfigSetsEveryUnattendedField; `cmd/apogee/e2e_support_test.go` — ptyEnv (HOME = suiteTempHome)

**Tests:**
- `toolchain_roots_test.go`: a fake `go` on PATH printing two lines yields two roots (real paths, existing directories only; a symlinked or absent path is dropped); the probe's argv carries the pins and `Dir` is the home; a missing `go` yields none; a failing probe yields none and no error surfaces.
- `wire_firing_test.go` / `schedule_test.go`: the skills roots are the leading prefix of `ExtraReadRoots()`, the toolchain roots follow. `orientation_test.go`: the softened roots bullet verbatim.
- e2e `TestE2EAnnouncedToolchainRootsAreReadable` (skips when `go` is absent): the `Read-only library roots:` line captured from the stub's system prompt contains the probed GOROOT; `read_file` of `<GOROOT>/VERSION` through the announced spelling succeeds.

**Acceptance:** `go build ./... && go test ./cmd/apogee/ -run 'ToolchainRoots|TestE2EAnnounced'`

**Commit:** `feat(host): GOROOT and GOMODCACHE are probed read-only roots for the read tools`

## 5. The fail-fast note names the command that stopped the line and never blames `set -e` for a confinement kill

**What:** `internal/tools/terminal.go` `subprocessToolResult` appends `failFastExitNote` whenever `res.failFast && !res.timedOut` — including when `res.denialStopped` or the exit is −1/signalled. Fix: the note is appended only when `!res.denialStopped && res.exitCode > 0`; a denial-stopped line carries the confinement label alone. The preamble (`internal/platform/host.go` `failFastPreamble`, a constant per ADR 0056 D1) gains a bash-only, self-detecting `trap` that prints `failed at: $BASH_COMMAND` to stderr (`[ -n "$BASH_VERSION" ] && trap … ERR`; silent under dash), and the note quotes that line when present: `fail-fast: the line stopped at \`<cmd>\` (exit N)`. Exit 1 with stdout stays `IsError` (writer call). The escape-probe harness stays green.

**Regression guard.** `internal/tui/toolregistry.go` `exitCodeMarker` (`\[exit code (-?\d+)[^\]]*\]\s*$`) matches nothing once a `]` from the quoted command (`[ -f missing ]`, `ls x[1]`) lands inside the brackets, so the outcome slot would read "error" and the marker stay in the body: the bracketed note stays free of the command — `fail-fast: the line stopped at \`<cmd>\`` goes on its own line BEFORE the `[exit code N — fail-fast …]` marker (the marker stays the last line the regex anchors on), and that shape joins the `toolpresent_test.go` marker table. The terminal always runs `sh` (`resolveShell` → `shellHost.Shell()`; dash on Debian-family hosts), so the "under bash the note names the command / under dash it is generic" rows are driven by running `bash -c` / `dash -c` on `FailFastPreamble()+cmd` directly (`exec.LookPath`, skip when absent) and feeding the captured output into `subprocessToolResult`; the end-to-end Terminal run asserts only the generic clause.

**Files:** `internal/tools/terminal.go`, `internal/tools/terminal_test.go`, `internal/platform/host.go`, `internal/platform/host_test.go`, `internal/tui/toolpresent_test.go`

**Read first:** `internal/tools/terminal.go` — subprocessToolResult, failFastExitNote, Terminal.Execute; `internal/platform/host.go` — failFastPreamble, FailFastPreamble; `internal/tools/exec_common.go` — subprocessResult, resolveShell; `internal/tui/toolregistry.go` — exitCodeMarker, exitMarkerPhrase;
`internal/tools/terminal_test.go` — TestSubprocessToolResultFailFastNote, TestTerminal_FailFastNoteReachesTheModelEndToEnd; `internal/platform/host_test.go` — TestFailFastPreambleRunsUnderEveryHostShell; `internal/tui/toolpresent_test.go` — the "fail-fast note inside the marker" case

**Tests:**
- `terminal_test.go` note table: denial-stopped ⇒ no `set -e` note; exit 1 ⇒ note; exit −1 ⇒ no note; under `bash -c` (direct, skipped when absent) the note names the failing command on its own line before the marker; under `dash -c` the note is the generic form; a command carrying `]` keeps the marker parseable.
- `toolpresent_test.go` marker table: the stopped-command line above the marker still reads as the code.
- `host_test.go`: preamble still a constant; runs under `sh` and `bash` without error.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'Terminal|FailFast' && go test ./internal/platform/`

**Commit:** `fix(tools): fail-fast note names the stopped command and is silent on a confinement kill`

## 6. `read_file` is bounded by default, refuses inverted ranges, windows `locate` and sniffs binaries

**What:** `internal/tools/read_file.go` `renderFile`:
- No `start_line`/`end_line`/`max_lines` ⇒ at most 400 lines or 40 KiB (`defaultReadLines`, `defaultReadBytes`, constants), the body followed by the exact tail `[showing lines 1-400 of M — pass start_line/end_line for the rest]`. An explicit range or `max_lines` is honoured as today; the `read-cache` guard's rewritten `max_lines` composes unchanged.
- `end_line < start_line` ⇒ `IsError` `read_file: end_line (N) is before start_line (M)`; `start_line > total` ⇒ `IsError` `read_file: start_line (N) is past the end of the file (M lines)`.
- `locate:` with no range ⇒ ±10-line windows around each hit, joined with `…`, instead of the whole file; with a range unchanged.
- A NUL in the first 512 bytes (hoist grep's sniff into `path_read.go` `looksBinary`, both tools share it) ⇒ `IsError` `read_file: <path> is a binary file (<size> bytes)`; PDF handling unchanged.
- `domain.ReadSpan` producers/consumers: the TUI presenter (`internal/tui/toolview.go`) paints the header from the span — the capped read's span is `1-400`, not `1-M`. Manual `commands.md` read_file paragraph and `docs/design/tool-surface-findings.md` updated.

**Regression guard.** `TestReadFile_Execute_LocatesASubstring` (`internal/tools/read_file_test.go`) pins the FULL body and `ReadSpan{Start:1,End:5,Total:5}` for a no-range locate on a 5-line file and the full body on a miss, and `TestReadFile_Execute_ReadsAPDFAsExtractedText` pins `1-3`: a miss renders the plain (capped) body; windows are clipped to the file and merged, and a union that covers the whole file is byte-identical to the plain read (both pins stay green); the span is the union's first Start / last End (`readSpanStat` counts End-Start+1). The ReadSpan reader is `readSpanStat` in `internal/tui/toolregistry.go` and `internal/tui/toolview_test.go` does not exist — the presenter pin lands in `internal/tui/toolpresent_test.go`.

**Files:** `internal/tools/read_file.go`, `internal/tools/read_file_test.go`, `internal/tools/path_read.go`, `internal/tools/grep.go`, `internal/tui/toolregistry.go`, `internal/tui/toolpresent_test.go`, `docs/manual/commands.md`, `docs/design/tool-surface-findings.md`

**Read first:** `internal/tools/read_file.go` — renderFile, readableText, readFileArgs, locateReport; `internal/tools/path_read.go` — readOpenedBounded, maxFileReadBytes; `internal/tools/grep.go` — searchFile (the NUL sniff to hoist);
`internal/tools/read_file_test.go` — TestReadFile_Execute_LocatesASubstring; `internal/tui/toolregistry.go` — readSpanStat; `internal/floor/readcache.go` — capReadArguments; `internal/domain/toolsummary.go` — ReadSpan

**Tests:** `read_file_test.go`: 1,000-line file ⇒ 400 lines + exact tail; 41 KiB of 30 lines ⇒ byte cap tail; inverted range ⇒ exact error; past-EOF ⇒ exact error; `locate` ⇒ windows (a miss ⇒ the plain body; a whole-file union ⇒ byte-identical to the plain read; span = first Start / last End); NUL file ⇒ binary error; explicit `max_lines: 800` ⇒ 800 lines. `floorguards_test.go` read-cache case still green. `toolpresent_test.go`: the capped read's header paints `1-400`.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'ReadFile|Grep' && go test ./internal/agent/ -run 'ReadCache' && go test ./internal/tui/ -run 'ToolView|ReadSpan|Present'`

**Commit:** `feat(tools): read_file caps at 400 lines by default, errors on bad ranges, windows locate and refuses binaries`

## 7. `@file` references get an absolute cap; PDF text is joined into lines

**What:** `internal/agent/loop.go` `refBound` becomes `min(share of the History allocation, fileRefMaxTokens)` with `fileRefMaxTokens = 32_000` (constant); the existing clamp marker stays; the Driver's notice path that reports a missing reference also reports `@<name> clipped to 32k tokens — read_file ranges for the rest` when a clip happened. Skills attached by reference share the same cap. `internal/doctext/pdf.go` `pageAccumulator.record` joins single newlines inside a sentence (a line not ending in `.`, `:`, `;`, `?`, `!` and not followed by a blank line is joined to the next with a space). CONTEXT.md **File reference** entry states the cap.

**Regression guard.** The Driver's missing-reference path is `refIgnored` → `domain.ErrorEvent{Source:"loop"}` (`internal/agent/loop.go`), which every Driver renders as an ERROR (`EntryKindError` in `internal/run/transcript.go`, `addError` in `internal/tui/transcript.go`, `kindError` in `internal/eventjson/encode.go`) — a successful clip must never be announced as a fault: the clip is carried as a note, a `PruneEvent`-style `domain.RefClippedEvent` rendered `EntryKindNote` by the three Drivers. The 32k sentence fires only when `fileRefMaxTokens` was the bound that clipped (on a small window the share `refBound` = History/refs binds — `floorWindow` = 8192 in the floor tests — and the sentence then names the bound that did), and the block body stays byte-identical to `clampToolResult`'s output (`TestClampToBound_SharedByToolResultsAndFileRefs`). PDF join: `pageAccumulator.record` receives page text that BEGINS with a newline for `minimal.pdf` — a blank (whitespace-only) line is never a join source and is kept verbatim; only a line carrying text joins — so `TestReadFile_Execute_ReadsAPDFAsExtractedText`'s `[Page 1]\n\nHello Apogee` pins (3 lines / `ReadSpan{Total:3}` / `LocatedOn:[3]`) stay byte-identical.

**Files:** `internal/agent/loop.go`, `internal/agent/filerefs_test.go`, `internal/domain/events.go`, `internal/run/transcript.go`, `internal/tui/transcript.go`, `internal/eventjson/encode.go`, `internal/doctext/pdf.go`, `internal/doctext/pdf_test.go`, `internal/tools/read_file_test.go`, `CONTEXT.md`, `docs/manual/commands.md`

**Read first:** `internal/agent/loop.go` — refBound, resolveFileRefs, refIgnored, resolveSkillRefs; `internal/agent/dispatch.go` — clampToBound, structuralFloor, clampToolResult; `internal/agent/filerefs_test.go` — TestClampToBound_SharedByToolResultsAndFileRefs;
`internal/doctext/pdf.go` — pageAccumulator.record, walkPages; `internal/domain/events.go` — ErrorEvent, PruneEvent; `internal/run/transcript.go` — transcriptFold.apply

**Tests:** `filerefs_test.go`: a 1.2 MB reference on a 1.3M-token window is clipped to ≤ 32k tokens (× `CharsPerToken`) with the marker, and the note event's text is exactly the bound string (no `ErrorEvent` is emitted); a share-bound clip on a small window carries no 32k sentence; the existing share-split and `ClampToBound` tests stay green. `pdf_test.go`: a page whose runs are one word per line joins into one sentence; paragraph breaks survive; a leading blank line is kept verbatim (`minimal.pdf` pins in `read_file_test.go` byte-identical).

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'FileRef|ClampToBound' && go test ./internal/doctext/`

**Commit:** `feat(agent): @file references cap at 32k tokens; PDF extraction joins word-per-line runs`

## 8. `web_fetch` returns text for HTML; `grep` clips wide rows and nudges on the cap

**What:** `internal/tools/web_fetch.go` `renderFetchResult`: a `text/html` body runs through the cleaner `web_search_render.go` already owns (hoist it to `internal/tools/htmltext.go`, one function both call), `raw: true` (new bool arg) returns the body as today; description says so. `internal/tools/grep.go` `plainMatchLines`: a matched line longer than 512 chars is clipped to 512 around the first match with `…` at the cut ends and the header counts them `(N rows clipped at 512 chars)`; when `total >= maxGrepMatches` the header ends `— narrow with include or path` (item 14 extends it to `exclude` when it adds the argument). `docs/manual/commands.md` paragraphs for both.

**Regression guard.** `TestDocMapNamesEveryFile` (`internal/tools/docmap_test.go`) fails for any non-test `.go` file `internal/tools/doc.go` does not name: `doc.go` names `htmltext.go`. `cleanHTMLText` (`web_search_render.go`) strips TAGS only and collapses all whitespace to ONE line — `<script>`/`<style>` bodies survive and a page loses every line break — so the hoist is a page cleaner (drops `script`/`style`/`noscript` bodies, keeps block breaks) beside the fragment cleaner `web_search` keeps: one function with a fragment/page mode, `web_search` calls the fragment form and its output stays byte-identical. The cap nudge reads `— narrow with include or path` in this item and item 14 extends it to `— narrow with include, exclude or path` when it adds `exclude` (one owner per string, stated in both items) — `decodeArgs` ignores unknown fields silently, so a nudge naming `exclude` before item 14 would announce an argument the tool does not honour.

**Files:** `internal/tools/web_fetch.go`, `internal/tools/htmltext.go`, `internal/tools/web_search_render.go`, `internal/tools/web_fetch_test.go`, `internal/tools/web_search_test.go`, `internal/tools/grep.go`, `internal/tools/grep_test.go`, `internal/tools/doc.go`, `docs/manual/commands.md`

**Read first:** `internal/tools/web_fetch.go` — renderFetchResult, webFetchArgs; `internal/tools/web_search_render.go` — cleanHTMLText, contentLooksHTML, renderSearch; `internal/tools/grep.go` — plainMatchLines, renderMatches, maxGrepMatches;
`internal/tools/network_test.go` — TestWebFetch_GetsBody; `internal/tools/grep_test.go` — TestGrep_Execute (header pins); `internal/tools/doc.go` — the file map; `internal/tui/toolregistry.go` — firstLineDetail

**Tests:** `web_fetch_test.go`: an HTML fixture returns text without tags/scripts (block breaks kept), `raw: true` returns the markup, `text/plain` unchanged. `grep_test.go`: a 3,000-char `.jsonl` row is clipped with the exact header count; 1,000+ matches header ends with the exact nudge `— narrow with include or path`; `web_search` output byte-identical after the hoist.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'WebFetch|WebSearch|Grep'`

**Commit:** `feat(tools): web_fetch renders HTML as text; grep clips wide rows and nudges at the match cap`

## 9. The `rm -rf` refusal says "absolute target" with a hint; the scratch exemption masks the whole token

**What:** `internal/security/rules.go` `DefaultDangerousRules` `rm-rf-root-home-system`: Reason becomes `recursive force-delete of an absolute path` and the rule gains Hint `re-issue the path relative to the workspace, or delete through the native tools` (the Hint infrastructure from `6323ea92` renders it). `maskExempt` (ADR 0049 2026-08-28 amendment) replaces the whole path token starting at an exempt dir (`<scratch>/repo/.git/config` ⇒ `<exempt>`), not only the prefix, so `write-git-control-plane` no longer trips on a repo cloned under the scratch dir. Every test pinning the old Reason text is updated; `TestMaskExempt` extended.

**Regression guard.** `rules.go` carries TWO mirror rules with the old Reason — `rm-rf-root-home-system` and `rm-fr-root-home-system` (`rm -fr` / `rm -f -r` / `--force --recursive`, kept in step by the shared fragments): the Reason changes and the Hint is added on both. "Whole path token" is bound as the masked span `dir(?:/[^\s;|&<>()'"\x60]*)?` — it ends at whitespace or a shell metacharacter, so `cat <scratch>/x/.git/config;rm -rf /` masks to `cat <exempt>;rm -rf /` and the Tier-1 refusal still fires (a row in `TestMaskExempt`); the three deeper-path rows of `TestMaskExempt` (`cp … <exempt>/b`, `cp <exempt>/a <exempt>/b`, `cd <exempt> && cat <exempt>/x`) are rewritten to `<exempt>`, as is `maskExempt`'s doc comment (`<dir>/gocache` → `<exempt>/gocache`). `write-git-control-plane` needs `\.git/(?:hooks|config|modules)\b` in the text, so `git -C <scratch>/repo config user.name x` is allowed TODAY and cannot be the bite row: the rows spell the path — `git config -f <scratch>/repo/.git/config user.name x` (allowed after) and `git config -f .git/config user.name x` / `echo x >> .git/config` (refused). `docs/manual/configuration.md`'s Tier-1 hard-refuse paragraph ("an `rm -rf` aimed at a root, home or system path") carries the old wording (rule: `grep -n 'root, home' docs/manual/*.md`); `commands.md` has no rm wording to change.

**Files:** `internal/security/rules.go`, `internal/security/rules_test.go`, `internal/security/dangerous.go`, `internal/security/dangerous_test.go`, `internal/agent/floorguards_test.go`, `docs/manual/configuration.md`

**Read first:** `internal/security/rules.go` — DefaultDangerousRules, deleteTargetAnchor, rmFlag; `internal/security/dangerous.go` — maskExempt, maskSpelling, exemptPlaceholder, Inspect; `internal/security/dangerous_test.go` — TestMaskExempt, TestInspectMasksTheSessionScratchDir;
`internal/security/rules_test.go` — TestDefaultDangerousRules_HomeAnchoredRulesMatchTheMacOSHome; `docs/manual/configuration.md` — the Tier-1 hard-refuse paragraph

**Tests:** `rules_test.go`: `rm -rf /workspace/repos/x` and `rm -fr /etc` refused with the exact new Reason and Hint (both mirror rules); `rm -rf ./build` allowed. `dangerous_test.go`: `git config -f <scratch>/repo/.git/config user.name x` allowed; `git config -f .git/config user.name x` and `echo x >> .git/config` still refused; `TestMaskExempt` gains the `;rm -rf /` row and its three deeper-path rows read `<exempt>`.

**Acceptance:** `go build ./... && go test ./internal/security/ && go test ./internal/agent/ -run 'Guard'`

**Commit:** `fix(security): rm -rf refusal names the absolute target with a hint; scratch exemption masks the whole path`

## 10. WritesOnly rules see a verb-aware write view of shell text (`apogee-2ay`)

**What:** Recast at the regression check (2026-09-14). Closes bead `apogee-2ay`: `internal/security/dangerous.go` `Inspect` narrows the write view only for tools declaring `ReadSourceKeys`; `terminal` and `python_exec` declare none, so `ls -la .git/hooks`, `cat .git/config`, `cmp a b` trip `write-git-control-plane`. Add `internal/security/shellwrites.go` `writeTargetsOf(command string) string` — the text the WritesOnly rules inspect for shell tools: redirect targets (`>`, `>>`, `|& tee`, `tee`), the arguments of mutating leaders (`rm`, `mv`, `cp`, `install`, `ln`, `chmod`, `chown`, `touch`, `mkdir`, `rmdir`, `truncate`, `sed -i`, `perl -i`, `git` with a mutating subcommand, `dd of=`), and every argument of an unknown leader (fail closed); read leaders (`ls`, `cat`, `head`, `tail`, `less`, `cmp`, `diff`, `stat`, `wc`, `grep`, `find` without `-delete`/`-exec`, `file`, `git` read subcommands) contribute nothing. Pipelines and `&&`/`;`/`||` chains are split first. Rules that are not WritesOnly keep seeing the full text. Bead closed in the CHANGELOG sidecar; ADR 0049 gains a dated note.

**Regression guard.** OWNER CALL 2026-09-14 — the write view is opt-in per rule and ONLY `write-git-control-plane` opts in (a `ShellWriteView bool` on the rule); every other WritesOnly rule — the secret-file rules (`write-ssh-keys`, `write-credential-persistence`) and the `~/.apogee` control-plane look — keeps inspecting the full shell text, so the T-10 suite, `guard.yaml`, the forced-pane golden and the Hint "needs approval, even for a read" are untouched; `Inspect` keys the write view on a domain marker `ShellCommandTool{ShellCommandKeys() []string}` implemented by Terminal and ConsoleOpen only (never every SubprocessTool), and the item's tests pass the `dangerous_test.go` stubTool implementing it, never nil; drop the `git config user.name x ⇒ .git/config-class match` test row — `git config` is a mutating leader whose ARGUMENTS feed the view, nothing is synthesised; `rules.go`/`rules_test.go`'s "WritesOnly narrows by declared class, not by parsing shell text" prose is superseded by the ADR 0049 dated note the item writes; `cat ~/.ssh/id_rsa` stays refused and is pinned in `dangerous_test.go`; record in the header's Ratified design calls: "**Write view scope (owner, 2026-09-14):** only `write-git-control-plane` opts into the shell write view."

**Files:** `internal/security/dangerous.go`, `internal/security/shellwrites.go`, `internal/security/shellwrites_test.go`, `internal/security/dangerous_test.go`, `internal/security/rules.go`, `internal/security/rules_test.go`, `internal/domain/tools.go`, `internal/tools/terminal.go`, `internal/tools/console_open.go`, `docs/adr/0049-*.md`

**Read first:** `internal/security/dangerous.go` — Inspect, inspectableText, Rule.WritesOnly; `internal/security/rules.go` — DefaultDangerousRules (write-git-control-plane, write-apogee-control-plane Hint); `internal/domain/tools.go` — ReadSourceTool, SubprocessTool;
`internal/security/dangerous_test.go` — stubTool, TestWritesOnlyRulesSkipADeclaredReadOnlyTool; `internal/security/rules_test.go` — TestDefaultDangerousRules_ApogeeControlPlaneReadHintsTheSanctionedRoute

**Tests:** `shellwrites_test.go` table: `ls -la .git/hooks` ⇒ ""; `cat .git/config > out` ⇒ `out`; `rm -rf .git/hooks` ⇒ `.git/hooks`; `git config -f .git/config user.name x` ⇒ `.git/config` (a mutating leader's arguments); `frobnicate .git/config` ⇒ `.git/config` (unknown leader). `dangerous_test.go` (calls pass the stubTool implementing `ShellCommandTool`, never nil): the three review commands (`ls -la .git/hooks`, `cmp .beads/hooks/commit-msg .git/hooks/commit-msg`, `cat .git/config`) pass through `terminal`; `echo x > .git/config` still refused; `cat ~/.ssh/id_rsa` and `cat ~/.aws/credentials` still refused (full-text rules); `ls ~/.apogee` still forces the look; a python line naming `.git/hooks` keeps the full view (no marker).

**Acceptance:** `go build ./... && go test ./internal/security/ && go test ./internal/agent/ -run 'Guard'`

**Commit:** `fix(security): WritesOnly rules inspect a verb-aware write view of shell text (apogee-2ay)`

## 11. The approval popup hides "always allow this session" on a forced gate

**What:** `domain.ApprovalRequest.CacheKey` is already empty on a forced gate and the engine never remembers one; the TUI popup (`internal/tui/approval.go` `approvalMenu`) still offers the row and the `s` key. Fix: when `req.CacheKey == ""` the row is not painted, `s` is ignored, and one faint line `a forced look is asked every time` sits under the menu. `docs/manual/commands.md` "Approving a call — how far an approval reaches" gains the sentence. The forced-approval golden (`e2e_approval_test.go` `TestJudgeForcedApprovalPaneReadsAsHelp`) is regenerated with `-update` and the new frame reviewed line by line in the commit.

**Regression guard.** `approvalMenu` is a fixed 4-row package var read by `handleApprovalKey` (up/down bounds), `promptWheel`, `resolveApproval` (indexes the highlight), `mouse.go` `handleApprovalClick` (`approvalSel.seat(row, len(approvalMenu))`) and the popup rows — hiding only the painted row would leave a 4-slot index over 3 painted rows: the menu is derived per request (`approvalMenuFor(req)`, `approvalKeysFor(req)`) and every `len(approvalMenu)` / `approvalKeys` site routes through it — rule: `grep -n 'approvalMenu\|approvalKeys' internal/tui/*.go`. Keying "forced" on `req.CacheKey == ""` makes every hand-built test request forced (some 80 `domain.ApprovalRequest{…}` literals across `internal/tui/*_test.go` set no CacheKey; only `approval_test.go`'s MCP grant does): the ordinary-gate fixtures get a CacheKey once, in `newUnarmedApprovalModel` / `approvalPaneModel` / `approvalClickModel` and the literal fixtures those helpers bypass — rule: `grep -n 'ApprovalRequest{' internal/tui/*_test.go`. `cmd/apogee/e2e_approval_test.go` `awaitApprovalPane`, `decide` and the auto-mode test wait on `approvalMarker = "Always allow this session"`, gone from a forced pane after the item: the forced panes wait on a `forcedMarker` (`a forced look is asked every time`); ordinary panes keep `approvalMarker`. The golden is the literal `t10-forced-pane.txt`, written by `TestE2EApprovalForcesALookAtTheControlPlane` (`tuitest.Golden`) — that test regenerates it with `-update`; `TestJudgeForcedApprovalPaneReadsAsHelp` is the env-gated judge and writes no golden.

**Files:** `internal/tui/approval.go`, `internal/tui/mouse.go`, `internal/tui/approval_test.go`, `internal/tui/model_test.go`, `internal/tui/mouse_test.go`, `internal/tui/cursor_test.go`, `cmd/apogee/e2e_approval_test.go`, `cmd/apogee/testdata/frames/t10-forced-pane.txt`, `docs/manual/commands.md`

**Read first:** `internal/tui/approval.go` — approvalMenu, approvalKeys, handleApprovalKey, resolveApproval, approvalPrompt; `internal/tui/mouse.go` — handleApprovalClick, promptWheel; `internal/agent/dispatch.go` — the sessionKey emptying on force; `internal/domain/approval.go` — ApprovalRequest.CacheKey;
`internal/tui/model_test.go` — newUnarmedApprovalModel, TestModelApprovalMenuSpacing; `internal/tui/mouse_test.go` — approvalPaneModel, approvalClickModel; `cmd/apogee/e2e_approval_test.go` — approvalMarker, awaitApprovalPane, TestE2EApprovalForcesALookAtTheControlPlane

**Tests:** `approval_test.go` (`TestModelApprovalDisclosesTheMCPServerGrant` pattern): forced request ⇒ no `always allow` row, disclosure present, `s` leaves the popup open, ↓ from the last painted row stays put and a click on the painted "Deny" seats Deny; ordinary request (CacheKey set) ⇒ row present, `s` resolves. `model_test.go` / `mouse_test.go` / `cursor_test.go` fixtures carry a CacheKey and stay green; e2e forced panes are awaited on `forcedMarker`.

**Acceptance:** `go build ./... && go test ./internal/tui/ -run 'Approval' && go test ./cmd/apogee/ -run 'Approval'`

**Commit:** `fix(tui): the approval popup hides the session-allow row on a forced gate`

## 12. Plan mode says what it withholds

**What:** Recast at the regression check (2026-09-14). Depends on item 1. Announced-surface regression (review headline 10): `internal/config/defaults/prompt.txt` line 8 tells every mode to "run the project's own tests, build or linter" while Plan withholds `terminal`/`run_tests`. Fix: (a) the line becomes `Verify by running: after a change, run the project's own tests, build or linter where the mode allows it` — `prompt.txt` is `//go:embed`ded and reaches every default-prompt install at the next binary; the manual notes "the embedded default prompt now says …". (b) `internal/agent/orientation.go` renders, in Plan only, the exact bullet `- Mode: plan — reads, plus Apogee's own writers into the session scratch dir only; terminal, run_tests, python_exec, web_fetch, web_search, http_request and MCP tools are withheld. Report what you would run.` (the writers clause dropped when no scratch dir is set; `orientation.txt` gains the template line as `orientationPlanLine` before `orientationContextFilesLine`, `orientationLineCount` bumped; the item RE-INTRODUCES the mode as a block input and re-words the KV-cache paragraph plan 01 item 5 rewrote). Contract §4 unchanged.

**Regression guard.** Word the Plan bullet from the live menu — exactly `- Mode: plan — reads, plus Apogee's own writers into the session scratch dir only; terminal, run_tests, python_exec, web_fetch, web_search, http_request and MCP tools are withheld. Report what you would run.` (the writers clause dropped when no scratch dir is set); the e2e asserts the subprocess/network families only; the item RE-INTRODUCES the mode as a block input (re-word the KV-cache paragraph plan 01 item 5 rewrote), adds `orientationPlanLine` before `orientationContextFilesLine` and bumps `orientationLineCount`; the manual note reads "the embedded default prompt now says …" (prompt.txt is `//go:embed`ded and reaches every default-prompt install — no seeding claim); Acceptance `-run 'Orientation|PlanToolMenu'`. The item yields to plan `2026-09-14 - 01` items 4–5 (ADR 0012 second loosen: Plan offers Apogee's writers whenever a session scratch dir is set, `planOffers` in `resolution.go`) — the bullet is exactly a sentence that loosen qualifies. Re-check (2026-09-14): the agent-package "the mode is NOT an input of this block" comment (`orientation.go`, and `orientation_test.go`'s `TestOrientation_EveryModeStatesTheScratchDir` comment) is the CURRENT one at HEAD after plan 01 items 4–5 — item 12 re-introduces the mode as an input, re-words that comment to the Plan bullet, and brings the ADR 0023 §6 (2026-09-14 note) and ADR 0056 D3 notes up to date — "the mode is a block input again: Plan renders the Mode bullet; the scratch bullet rides in every mode" — since "Plan omits the scratch bullet" is false since d12dec5f.

**Files:** `internal/config/defaults/prompt.txt`, `internal/agent/orientation.go`, `internal/agent/prompts/orientation.txt`, `internal/agent/orientation_test.go`, `cmd/apogee/e2e_planmode_test.go`, `cmd/apogee/testdata/stubllm/planmode.yaml`, `docs/manual/configuration.md`, `docs/adr/0023-*.md`, `docs/adr/0056-*.md`

**Read first:** `internal/agent/orientation.go` — orientationBlock, orientationLineCount, orientationContextFilesLine; `internal/agent/prompts/orientation.txt`; `internal/agent/resolution.go` — planOffers;
`internal/agent/orientation_test.go` — TestOrientation_OmitsFactsTheSessionDoesNotHave (pins header+1 line, mode "" so unaffected), TestOrientation_EveryModeStatesTheScratchDir; `internal/agent/promptseam_test.go` — the two SetMode(ModePlan) seam tests (assert on "write_file"/"ask-before" substrings, the bullet must name neither)

**Tests:** `orientation_test.go`: Plan with a scratch dir ⇒ the exact bullet; Plan without one ⇒ the bullet minus the writers clause; every other mode ⇒ absent; `orientationLineCount` still matches the template. e2e `TestE2EPlanModeAnnouncesWhatItWithholds` (stubllm capture of the system prompt in Plan): the bullet is present and the tool menu the stub received contains none of the subprocess/network families named (`terminal`, `run_tests`, `python_exec`, `web_fetch`, `web_search`, `http_request`) — the announced list and the menu agree.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'Orientation|PlanToolMenu' && go test ./cmd/apogee/ -run 'PlanMode'`

**Commit:** `fix(agent): Plan mode announces the tool families it withholds; the default prompt qualifies "run the tests"`

## 13. `git_show`, `git_log` with a path, `git_branch` marks remote refs

**What:** `internal/tools/git.go`: new `git_show` tool (`ref` validated by `validRef`, `path` resolved through the read fence like `read_file`, output = `git show <ref>:<relpath>` rendered through `renderFile`, so it inherits item 6's 400-line default cap and range arguments with the same `[File: … showing lines a-b]` header; a missing path at that ref ⇒ `IsError` naming both); `git_log` gains `path` (a pathspec placed after the existing `--`; `validRef` untouched, `TestGitLog_PathShapedRefIsNotAPathspecLog` stays green); `git_branch` suffixes remote refs ` (remote)`. Registered in `registry.go` under the read-only class (`readOnlySubprocess` marker, so Plan admits `git_show`); manual edits at the real sections — the `docs/manual/configuration.md` tool list around lines 145-148 and the delegation/tool paragraphs `grep -n 'read_file\|grep' docs/manual/*.md` finds.

**Regression guard.** A 27th built-in reddens four unlisted pins — `internal/tools/registry_test.go` (`len(registry.All()) != 26`, the menu-order list, the read-only map with no `git_show`) and `internal/tools/roster_test.go` `TestReadOnlySubprocessNamesTheGitReadTrio` (want = exactly the trio): each is extended (27, `git_show` after `git_log`, `git_show: true`, marker map += `git_show`). `TestManualListsEveryKnownToolName` (`internal/tools/manual_drift_test.go`) reads the menu-order list at `docs/manual/configuration.md` lines 145-148, which must name `git_show`; `commands.md` has no git tools table — the manual edits point at the real sections (`docs/manual/configuration.md` tool list around lines 145-148 and the delegation/tool paragraphs `grep -n 'read_file\|grep' docs/manual/*.md` finds). `git_test.go` pins `list` at `confineCount() == 3` (two config probes + ONE branch process): remotes are marked from that one process — the list format becomes `%(refname) %(HEAD)` and Go strips `refs/heads/` / tags `refs/remotes/` as ` (remote)`; `git.go`'s `branch -a --format` line is the only argv line. The "read trio" is a closed set spelled in prose: every site naming git_status/git_log/git_diff_range as the whole marked set is updated — rule: `grep -rn 'read trio\|git_status, git_log' --include=*.go --include=*.md` (`planmenu_test.go`, `resolution.go`, `loop.go`, `registry.go`, `doc.go`, `readonly_subprocess.go`, `dispatch_test.go`, `CONTEXT.md`). `git_show` reuses `renderFile` and therefore inherits item 6's 400-line default cap and range arguments (decision; never "capped at maxFileReadBytes").

**Files:** `internal/tools/git.go`, `internal/tools/git_test.go`, `internal/tools/registry.go`, `internal/tools/registry_test.go`, `internal/tools/roster_test.go`, `internal/tools/readonly_subprocess.go`, `internal/tools/doc.go`, `internal/agent/planmenu_test.go`, `internal/agent/resolution.go`, `internal/agent/loop.go`, `internal/agent/dispatch_test.go`, `CONTEXT.md`, `docs/manual/configuration.md`, `docs/manual/commands.md`

**Read first:** `internal/tools/git.go` — GitLog.Execute, buildBranchArgs, validRef, gitDiffHardeningArgs, runGit; `internal/tools/readonly_subprocess.go` — readOnlySubprocess; `internal/tools/registry.go` — builtinTools, KnownToolNames; `internal/tools/read_file.go` — renderFile;
`internal/tools/git_test.go` — gitRepo, recordGitArgv, TestGitBranch_RunsUnderConfine; `internal/tools/registry_test.go` — TestNewDefaultRegistry_HoldsTheBuiltInTools; `internal/tools/roster_test.go` — TestReadOnlySubprocessNamesTheGitReadTrio

**Tests:** `git_test.go` (`gitRepo`, `recordGitArgv` helpers): `git_show` at `HEAD~1` returns the earlier content; a path outside the workspace ⇒ `ErrPathEscape`; `git_log path: internal/` argv ends `-- internal/`; `git_branch` output tags `origin/main (remote)` with `confineCount()` still 3; `git_show` on a 1,000-line file renders 400 lines with item 6's tail. `planmenu_test.go`: `git_show` is in Plan's menu; `registry_test.go` / `roster_test.go` pins extended to 27 and the four-name set; `TestManualListsEveryKnownToolName` green.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'Git' && go test ./internal/agent/ -run 'PlanMenu'`

**Commit:** `feat(tools): git_show reads a file at a revision; git_log takes a path; git_branch marks remote refs`

## 14. `grep` gains `exclude`, `paths`, `count_only`, `files_only`; `include` refuses a slash with a hint

**What:** `internal/tools/grep.go`: `include` containing `/` ⇒ `IsError` `grep: include matches file names only — use paths or exclude for directories`; new `exclude` (basename or dir glob, joined to a per-call skip set copied from `grepExcludeDirs` — the package map is never written), `paths []string` (each resolved through the read fence; `path` kept as the single form), `count_only` (per-file counts, header total) and `files_only` (one path per line). The cap nudge item 8 wrote becomes `— narrow with include, exclude or path`. Description updated; manual edits at the real sections (the `docs/manual/configuration.md` tool list around lines 145-148 and the delegation/tool paragraphs `grep -n 'read_file\|grep' docs/manual/*.md` finds); `docs/design/tool-surface-findings.md` records the additions as toggleable surface (standing directive: prune on bench evidence).

**Regression guard.** `grepExcludeDirs` (`grep.go`) is a package-level map read by `find_files` too and grep is ReadOnly — fanned out concurrently under ADR 0039 — so a per-call write to it is a data race and leaks the exclusion into `find_files` and every later call: `exclude` builds a per-call skip set (a copy of `grepExcludeDirs` plus the exclude globs) threaded into `search`; the package map is never written. `searchTarget` (`path_read.go`) pins ONE root and opener and `renderContextMatches` / `readContextLines` reopen every match through that one target while `grepMatch` carries only `openPath`, so a second path accepted under an extra read root would have its `context_lines` reopened through the first path's fence: the target rides on `grepMatch` (or rendering is per (target, group)) so context reopens through the fence each path was accepted under, and `searchScope` names all `paths` in the header. The grep cap nudge reads `— narrow with include or path` in item 8 and this item extends it to `— narrow with include, exclude or path` when it adds `exclude` (one owner per string, stated in both items; the pinned test is re-pinned here). `commands.md` has no grep paragraph: the manual edits point at the real sections (`docs/manual/configuration.md` tool list around lines 145-148 and the delegation/tool paragraphs `grep -n 'read_file\|grep' docs/manual/*.md` finds); `tool-surface-findings.md` stays the record.

**Files:** `internal/tools/grep.go`, `internal/tools/grep_test.go`, `internal/tools/path_read.go`, `docs/manual/configuration.md`, `docs/manual/commands.md`, `docs/design/tool-surface-findings.md`

**Read first:** `internal/tools/grep.go` — Grep.Execute, search, searchFile, matchesInclude, searchScope, renderContextMatches, grepExcludeDirs; `internal/tools/path_read.go` — readScope.searchTarget, searchTarget.open; `internal/tools/find_files.go` — FindFiles.Execute;
`internal/tools/grep_test.go` — TestGrep_Execute_IncludeGlobNarrows, callWith; `internal/tui/toolregistry.go` — grepTarget, matchedLinesStat

**Tests:** `grep_test.go`: `include: internal/**/*.go` ⇒ exact error; `exclude: "*_test.go"` drops tests and leaves `grepExcludeDirs` and a following `find_files` unchanged (run under `-race` with two concurrent calls); two `paths` union, the second under an extra read root with `context_lines` rendered through its own fence and both named in the scope header; `count_only` and `files_only` shapes; the cap nudge re-pinned to `— narrow with include, exclude or path`; existing include/cap tests green.

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'Grep'`

**Commit:** `feat(tools): grep takes exclude, paths, count_only and files_only; include refuses a slash with a hint`

## 15. `copy_file` copies a directory, `view_diff` diffs a new file, exec results state the cwd, the shell is named

**What:** Recast at the regression check (2026-09-14). `internal/tools/file_ops.go` `CopyFile`: a directory source copies recursively — the source enumerated through its own fence, every destination path handed to one `journaledMutation`, each file copied with `SafeCopyFileFrom` (symlink-spelling rules of `1de41dea` unchanged; extra-root sources allowed as today; destination through `checkFileOpsDestination`; result lists the file count); `file_ops_test.go`'s "a directory source is refused" comment and `safeio.go`'s non-regular-source refusal are reworded. `internal/tools/diff.go` `ViewDiff`: a path that is absent (`errors.Is(err, fs.ErrNotExist)` on the safeOpen error) diffs against empty rather than `file not found`; escape/root/dir/too-large refusals stay verbatim. `internal/tools/terminal.go` and `python_exec.go` results start with `cwd: <dir>` on the first line (`subprocessToolResult`, the spec dir threaded onto `subprocessResult`; no line when the result carries no dir); one shared helper `StripCwdLine`, exported from `internal/tools`, strips that line for its three consumers — the TUI success detail, the TUI failure body and headless narration. The terminal description reads `POSIX sh (dash on Debian-family hosts — no bash arrays, [[ ]] or process substitution); use python_exec for anything bash-only`, and a result whose stderr contains `Bad substitution`, `Syntax error: "(" unexpected` or `shopt: not found` gains the line `hint: the shell is sh, not bash`.

**Regression guard.** The directory copy enumerates the source through its own fence, hands EVERY destination path to one `journaledMutation` and copies per file with `SafeCopyFileFrom`, with an undo probe for the dir case — this supersedes the `file_ops_test.go` "a directory source is refused" comment and `safeio.go`'s non-regular-source refusal, both reworded in the item; `internal/tui/toolregistry.go` joins Files and strips the `cwd:` line from terminal/python_exec detail the way `consoleDetail` strips its marker, pinned in `toolview_test`; `internal/tools/exec_common.go` joins Files (thread the spec dir onto `subprocessResult`); `TestTerminal_PreambleLeavesSuccessOutputUntouched` is re-pinned to `cwd: …\nhello\n`; view_diff detects absence by `errors.Is(err, fs.ErrNotExist)` on the safeOpen error (small seam beside `readWorkspaceFileBounded`), keeps escape/root/dir/too-large refusals verbatim, drops the "index/HEAD" clause, re-pins `TestViewDiff_ToolErrors`. (The presenter pin's file is `internal/tui/toolpresent_test.go` — `toolview_test.go` does not exist; item 16 strikes `diff.go` from its sweep, so the two items no longer contradict.) Superseded sources: `internal/tools/file_ops_test.go` (`TestCopyFile_RefusesDirectories` comment) and `internal/security/safeio.go` (`SafeCopyFileFrom` doc: "a source that is not a regular file … is refused"). Re-check (2026-09-14): (1) `internal/tools/python_exec_test.go` joins Files — `TestPythonExec_WorkspaceDoesNotShadowTheStdlib` (`strings.Contains(res.Content, root)`) is re-pinned on the output with the leading `cwd:` line removed, and no `cwd:` line is rendered when `subprocessResult` carries no dir (the `TestSubprocessToolResultFailFastNote` table cases build results with none). (2) The `IsDir` arm of `checkFileOpsPathsFrom` stays for `move_file` — it is shared with `MoveFile.Execute` via `checkFileOpsPaths`, and a dir move would run `SafeRename` unjournalled; `copy_file` branches on a directory source BEFORE the shared check (or the check takes an allowDir flag), and `file_ops_test.go` gains a `move_file` dir-source refusal row (none exists today) so the bite check fires. (3) DECISION: the `cwd:` line has three result consumers (TUI success detail, TUI failure body, headless narration) — ONE shared strip helper keeps them in step: `internal/tools` exports `StripCwdLine`, called from the TUI's terminal/python_exec `detail` AND `failure` hooks (`absorbFailure` → `exitCodeFailure` → `outputBody` is a route separate from the success `detail`) and from `cmd/apogee/headless.go`'s `narrationSink.resultLine` (else `← terminal error: cwd: /ws`); `headless.go` and `headless_test.go` join Files with a narration pin. (4) The shell sentence is APPENDED to `terminalSpec.description`; the fail-fast sentence stays verbatim (`TestTerminal_DescriptionDisclosesFailFast` pins "fail-fast", "set -e", "|| true"). (5) A directory copy whose destination resolves outside the workspace is refused before the Gate with one refusal naming the reason — the write permit is exact-path (`openPermittedRoot` compares the resolved name to the ONE approved target), so an approved out-of-workspace destination would refuse every child with `ErrPathEscape`. (6) The virtual-mount branch (`copyFromMount`) keeps refusing directories; its refusal reads "directories under a shipped mount are not supported" and `commands.md` says so; `checkFileOpsDestination`'s "(give the full path of the new file)" is re-worded for the dir-onto-existing-dir case.

**Files:** `internal/tools/file_ops.go`, `internal/tools/file_ops_test.go`, `internal/security/safeio.go`, `internal/tools/undo_journal_test.go`, `internal/tools/diff.go`, `internal/tools/diff_test.go`, `internal/tools/path_read.go`, `internal/tools/terminal.go`, `internal/tools/terminal_test.go`, `internal/tools/exec_common.go`, `internal/tools/python_exec.go`, `internal/tools/python_exec_test.go`, `internal/tui/toolregistry.go`, `internal/tui/toolview.go`, `internal/tui/toolpresent_test.go`, `cmd/apogee/headless.go`, `cmd/apogee/headless_test.go`, `docs/manual/commands.md`

**Read first:** `internal/tools/file_ops.go` — CopyFile.Execute, copyFromMount, checkFileOpsPathsFrom, checkFileOpsDestination; `internal/security/writepermit.go` — openPermittedRoot; `internal/tools/terminal.go` — subprocessToolResult, terminalSpec;
`internal/tui/toolregistry.go` — outputDetail, exitCodeFailure; `internal/tui/toolview.go` — absorbFailure; `cmd/apogee/headless.go` — narrationSink.resultLine

**Tests:** `file_ops_test.go`: directory copy of 3 files lands all three and `/undo` takes all three back (`undo_journal_test.go` gains the dir-copy probe); `diff_test.go`: new untracked file ⇒ all-added diff, `TestViewDiff_ToolErrors` re-pinned (absent ⇒ diff; escape/dir/too-large refusals verbatim); `terminal_test.go`: first line `cwd: …`, `TestTerminal_PreambleLeavesSuccessOutputUntouched` re-pinned to `cwd: …\nhello\n`, the bash-ism hint on the exact stderr strings; `toolpresent_test.go`: the `cwd:` line is stripped from the terminal/python_exec detail (`true` still reads `(no output)`, `echo hello` still promotes one line) AND from a failed run's body (`exitCodeFailure` route); `headless_test.go`: a failed `terminal` narrates `← terminal error: <the command's first output line>`, never `cwd: …`; `python_exec_test.go`: `TestPythonExec_WorkspaceDoesNotShadowTheStdlib` re-pinned with the `cwd:` line stripped; `TestSubprocessToolResultFailFastNote` rows (no dir) render no `cwd:` line; `terminal_test.go`: `TestTerminal_DescriptionDisclosesFailFast` still passes with the shell sentence appended; `file_ops_test.go`: `move_file` of a directory ⇒ `not a file` (new row), `copy_file` of a directory to an out-of-workspace destination ⇒ one refusal before the Gate, `copy_file` of a `shipped:`-mounted directory ⇒ "directories under a shipped mount are not supported".

**Acceptance:** `go build ./... && go test ./internal/tools/ -run 'FileOps|CopyFile|MoveFile|ViewDiff|Terminal|PythonExec' && go test ./internal/tui/ -run 'ToolView' && go test ./cmd/apogee/ -run 'Headless'`

**Commit:** `feat(tools): copy_file copies directories, view_diff diffs new files, exec results name the cwd and the shell`

## 16. Every not-found refusal routes; atomic edits say nothing was written; unknown tools get a near-match

**What:** `internal/tools/path_suggest.go` `notFoundMessage` (`— did you mean: a; b`) already serves `read_file`/`list_dir`/`grep`/`find_files`; route the remaining bare disk-rooted sites through it: `find_replace.go` (both tools), `file_edit.go` (`edit_existing_file`), `file_ops.go` (`copy_file`/`move_file` sources), `delete_file.go`, `diagnostics.go`, `present_document.go` (`diff.go` is item 15's — its absence path diffs against empty). `multi_find_and_replace` / `single_find_and_replace` failure text is prefixed `no changes were written — `. `write_file` pre-checks with `statWriteTarget(ctx, args.Path, t.root)` + `.IsDir()` (as `checkFileOpsDestination` does) and returns `write_file: target is a directory: <path>` instead of the raw `*os.LinkError`. `internal/agent/dispatch.go`'s two `unknown tool %q` fallbacks (fan-out and serial, reached only with the repair guard off) append ` — did you mean: <closest registered name>` via a small `internal/tools/nearname.go` (`ClosestToolName(names, want)`, prefix-then-Levenshtein ≤ 3). Rule for the sweep: every disk-rooted refusal rendered by `readFileErrorMessage`/`escapeOrMessage` with a not-found literal goes through `withSiblingSuggestions` (`path_read.go`) or `notFoundMessage` (`grep -n 'readFileErrorMessage\|not found: "' internal/tools/*.go`); the virtual-mount sites (`path_virtual.go`, `path_read.go`'s mount branches) keep their wording — no disk parent to list.

**Regression guard.** `TestDocMapNamesEveryFile` (`internal/tools/docmap_test.go`) fails the moment `nearname.go` exists without a line in `internal/tools/doc.go` (one line beside `nearmatch.go`'s). The sweep grep `'not found'` misses the sites that render via the helper (`find_replace.go`, `file_edit.go`, all through `readFileErrorMessage`) and hits sites the Files list omitted (`delete_file.go`, `diagnostics.go`, `present_document.go`, `file_ops.go`) plus the virtual-mount sites (`path_virtual.go`, `path_read.go`'s mount `searchTarget` branches — no disk parent for `suggestSiblings` to list, so they keep their wording): the rule is the renderer-based one in the What; `diff.go` is struck from the sweep (item 15 owns view_diff's absence path). `find_replace_test.go` pins the EXACT "old text found 2 times (must appear exactly once) — at lines 1, 3" / "replacement #1: …" strings and the `replacement #2: old text not found in file — closest match` prefix, and `nearmatch_test.go` pins `closestRegion`'s bare sentence: the `no changes were written — ` prefix is added at the two `find_replace.go` call sites, never inside `closestRegion` (`nearmatch.go` contract), and those three `find_replace_test.go` assertions are re-pinned. A raw `os.Stat` of `args.Path` resolves a relative path against the process cwd, follows symlinks unfenced and ignores an ADR 0049 permit (`statWriteTarget`'s `escapeTargetPin`, `path_safety.go`): `write_file` uses `statWriteTarget(ctx, args.Path, t.root)` + `.IsDir()` exactly as `checkFileOpsDestination` does. `docs/manual/commands.md`'s near-miss paragraph enumerates the four tools that suggest ("When `read_file`, `list_dir`, `grep` or `find_files`…") — re-worded for the sweep.

**Files:** `internal/tools/find_replace.go`, `internal/tools/file_edit.go`, `internal/tools/file_ops.go`, `internal/tools/delete_file.go`, `internal/tools/diagnostics.go`, `internal/tools/present_document.go`, `internal/tools/write_file.go`, `internal/tools/nearname.go`, `internal/tools/nearname_test.go`, `internal/tools/doc.go`, `internal/tools/find_replace_test.go`, `internal/tools/write_file_test.go`, `internal/tools/file_ops_test.go`, `internal/agent/dispatch.go`, `internal/agent/dispatch_test.go`, `docs/manual/commands.md`

**Read first:** `internal/tools/path_suggest.go` — notFoundMessage, suggestSiblings; `internal/tools/path_read.go` — withSiblingSuggestions, readFileErrorMessage, escapeOrMessage; `internal/tools/find_replace.go` — SingleFindReplace.Execute, MultiFindReplace.Execute; `internal/tools/nearmatch.go` — closestRegion;
`internal/tools/path_safety.go` — statWriteTarget; `internal/tools/file_ops.go` — checkFileOpsPathsFrom; `internal/agent/dispatch.go` — prepareDelegation, lookupTool; `internal/floor/repair.go` — toolKnown

**Tests:** each routed site (`edit_existing_file`, `delete_file`, `present_document`, `copy_file`/`move_file` sources, the diagnostics name): a near-miss path yields the `did you mean` suffix; a virtual-mount miss keeps its wording; `find_replace_test.go`: failed atomic edit text starts with the exact prefix and the file is byte-identical, the three exact-string assertions re-pinned with the prefix, `nearmatch_test.go` untouched; `TestDocMapNamesEveryFile` green; `write_file_test.go`: directory target ⇒ exact message; `nearname_test.go`: `read_fil` ⇒ `read_file`, `bash` ⇒ no match (distance > 3 to every name); `dispatch_test.go`: guard off + `read_fil` ⇒ result contains the suffix.

**Acceptance:** `go build ./... && go test ./internal/tools/ && go test ./internal/agent/ -run 'Dispatch|UnknownTool'`

**Commit:** `fix(tools): not-found refusals suggest the nearest path, atomic edits say nothing was written, unknown tools get a near-match`
