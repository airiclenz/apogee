# Queued commands, announced delegation width and honest accounting — session-mining fixes, wave C

**Goal:** close the host-side findings of the 2026-09-14 session-mining review: a slash command typed mid-run is queued and runs at idle instead of being refused, the coordinator is told how wide its fan-out really runs, and the session record stops distorting every number — usage resets on `/clear`, the served model is recorded, entries carry timestamps and result sizes, and a fold leaves a trace.

**Date:** 2026-09-14
**Status:** unexecuted
**Sized for:** ~200k-context host

**Sources:**
- `docs/reviews/session-mining-2026-09-14.md` (headlines 6, 14; §1 "Skill commands typed mid-run"; items 9a, 14, 24; open questions 6, 7)
- ADR 0025 (Interjection; D10 "commands never queue") + ADR 0027 D6, ADR 0022 (session records, tolerant codec), ADR 0023 2026-08-25 amendment (orientation facts), ADR 0031 (wire-silent engine), ADR 0039 D2 (width resolution), ADR 0045 (routed child model), CONTEXT.md **Interjection**, **Orientation block**, `docs/manual/commands.md` (keys paragraph), `internal/tui/wireargs.go` doc (why args are bounded)

**Ratified design calls (owner, 2026-09-14):**
- **Queued commands:** every idle-only verb queues (clear, color-scheme, compact, continue, model, new, redo, rename, server, sessions, settings, stop-server, undo, unload-model), runs FIFO at idle before the held-queue note; staged as a row, Backspace pops, Esc×2 keeps it. Reverses ADR 0025 D10 / ADR 0027 D6 (dated amendment).
- **Usage reset:** `/clear` and `/new` zero the ENGINE tally (`ClearContext`, like `RestoreSession`) and the TUI view.
- **Served model:** META `model` stays the bound profile; new `served_models` (set, order first seen) from the response's own `model` field.
- **Width fact (owner, 2026-09-14):** per-group trailer line only; no orientation change.
- **tool.args (writer):** the `wireargs.go` bound stays; entries gain `chars` (result size), not full args.
- **Cloud width default:** unchanged — ADR 0039 D2 stands (`apogee-9se`).

**Regression check (2026-09-14, 3fca8c2cb9bb48fe5cd4ee1daf600e105eed172b):**
- 2: guard folded — `commandsAtIdleNote` readers outside the item's Files listed (picker/skills/undo refusal tests, console e2e step 10), the drain stops at the first worker-launching verb, one `stagedRowCount()` feeds band/paint/status/hold note, the autocomplete accept route cuts only the token and the two idle-only LINES queue, the queue drains at the errored→idle dismissal; ADR 0025 D7 amended by item 3 (commands run at the stop's idle, messages stay held). `internal/tui/commandrun_test.go` and `cmd/apogee/e2e_interject_test.go` are created, not edited.
- 3: guard folded — acceptance anchored to the command sentences (CONTEXT.md:121 is the scheduler's "never queued"), `interject_test.go:467` is item 2's to drop, `internal/tui/doc.go` and `internal/tui/autocomplete.go` added to Files.
- 4: recast — the orientation half is DROPPED (owner call); the item yields to ADR 0069 D4/D6 and the ADR 0023 2026-08-25 amendment (session-constant orientation facts), which stand untouched; only the per-group trailer line remains. `cmd/apogee/e2e_fanout_test.go` is created.
- 5: guard folded — acceptance spelled `TestE2EClearResetsUsage|TestE2EUsage` (no `TestE2ECachedUsage` exists).
- 6: guard folded — `SessionHost.Save` / `fakeSessionHost.Save` / `savePayload` extended, `compact.go`'s second `usage.record` caller threaded, `ServedModel` folded in `fold.go` before the Depth guard, `ResumedSession.ServedModels` seeded on resume and zeroed in `resetSessionView`, eventlines golden regenerated, `Delta.Model` (not `DeltaDone`), Meta key `servedModels`; the record's readers are the `/sessions` pane and `internal/run` (no `apogee sessions` CLI) — written by the TUI fold only.
- 7: guard folded — `json:"at,omitzero"`, the note keyed on `UsageEvent{Maintenance}` at any depth (no `ChildEvent` exists), `compacted` as a new entry kind with rule row + paint case, one transcript clock, list order is run order in a fan-out; `apogee sessions show` dropped — Firing records via `internal/run` carry neither field.
- Edit order on `resetSessionView` / `foldStats`: item 5 zeroes the tallies, item 6 folds and resets the served set, item 7 keys the `compacted` note — items run in that order.
- Re-check (2026-09-14, 3fca8c2cb9bb48fe5cd4ee1daf600e105eed172b):
  - 4: recast — the trailer rides ONLY a group whose every slot RAN (a skipped / refused / hook-failed slot ⇒ no trailer, so `assertSkippedDelegation` / `TestFanOut_PendingInterjectionSkipsTheQueuedChildren` stay green); appended in `commitDelegation` after `recordExecuted`; inserted as the last line of the BODY (the `SeatFallbackNote` precedent), so the item yields to ADR 0063 D3 (docs/adr/0063 line 86 — the steered trailer stays the result's final line) and tests say "the body ends with"; `fanout.yaml`'s second consumer (`e2e_livestate_test.go`) is preserved and the e2e pins `parallel-agents: 2`; the manual edit targets `commands.md`'s run-view paragraph (around line 100).

**Standing requirements:**
- `skills: coding-standards`
- Deviations from item text land as a dated NOTES line under the item.
- Bubble Tea v2 (`charm.land/bubbletea/v2`): `tea.KeyPressMsg`, `msg.String()`; the `Model` is value-copied — no `strings.Builder` by value; the mailbox stays held by pointer.
- ADR 0031: items 2–3 are host-only; the engine learns nothing of a queued command.
- Nothing new is put in front of the model except the one structural width fact item 4 binds verbatim (the per-group trailer line).

**Out of scope:**
- Cancel keeping finished Turns (`apogee-2un`); a cloud `parallel-agents` default (`apogee-9se`); the delegate ledger (`apogee-clb`).
- Running a queued command at a between-Steps boundary (engine contract); persisting full `tool.args`; a per-entry `elapsed_ms`; cache-hit rate in the status line; VERSION.
- A headless / Firing record written through `internal/run` carries no `served_models`, `at` or `chars` — items 6 and 7 are written by the TUI fold only; `internal/run/transcript.go` is untouched.
- Any orientation-block change for the delegation width (the `Delegations:` bullet, beat-driven text) — dropped from item 4 by owner call 2026-09-14.

## 1. Gate: plan 2026-09-14 - 01 is archived — ✅ DONE (2026-09-15)

NOTES (2026-09-15): gate verified — `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` is absent from `docs/plans/`, present under `docs/plans/archived/`, all seven items marked ✅ DONE (2026-09-14), archived at commit 0d64c233; the acceptance command passes.
NOTES (2026-09-15): the archived plan's header still reads `**Status:** unexecuted` while every item is ✅ DONE and it sits in archived/ — stale header line only, no gate impact, not touched (no Files on this item).

**What:** Verify `docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md` is gone from `docs/plans/` and archived under `docs/plans/archived/` as executed. Items 2–4 edit `internal/tui/interject.go`, `commandrun.go`, ADR 0025, `docs/manual/commands.md`, `internal/agent/orientation.go` and `dispatch.go`, which that plan rewrites. No code change.

**Files:** none
**Read first:** docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md — items 4–7 still unexecuted at BASE (items 1–3 DONE), item 5 rewrites orientationBlock's comment, item 3 edits the commands.md keys paragraph; docs/plans/archived/ — the two `2026-09-14 - 00` plans already there

**Tests:** none.

**Acceptance:** `test ! -e "docs/plans/2026-09-14 - 01 - queued-message-preemption-and-scratch-in-lower-modes-plan.md" && ls "docs/plans/archived/" | grep -q "2026-09-14 - 01"`

**Commit:** none (gate only).

## 2. An idle-only command typed mid-run is queued and runs at idle — ✅ DONE (2026-09-15)

NOTES (2026-09-15): consequential edit — internal/tui/command.go: made necessary by the deletion of `commandsAtIdleNote` (the `whileRunning` flag's doc named it; comment only).
NOTES (2026-09-15): the two interject_test.go refusal tests were renamed to what they now assert (`TestCommandWhileRunningIsQueued`, `TestAcceptIdleOnlyCommandWhileRunningQueuesTheVerbAndKeepsTheDraft`); the picker/skills/undo test names were kept, only their assertions rewritten. Both new names still match the item's acceptance regex.
NOTES (2026-09-15): Backspace pops the band's row nearest the box — a queued command first (commands paint below the staged messages), then the newest staged message — which is the "existing pop order extended" reading that keeps the band and the pop consistent; a popped command is restored as `/verb args` (parsedInput carries no verbatim text; a dropdown accept never had one).
NOTES (2026-09-15): internal/tui/doc.go still names `commandsAtIdleNote` / `[Model.refuseIdleOnlyCommand]` (lines ~132, ~801) and docs/manual/commands.md, layout.md and ADR 0025/0027 still say "idle only" / "never queued" — left for item 3, which the plan's regression check assigns doc.go and the docs to; not a defect of this item's code (no docmap test reads those names).
NOTES (2026-09-15): the e2e console step 10 deletes the popped `/sessions` line with nine Backspaces and waits for the box's running placeholder before the Esc×2, because the stop keys sent on the heels of the Backspaces were swallowed (three of three runs green after the wait; the PTY Esc timing, not the queue).

**What:** Depends on item 1. `internal/tui/commandrun.go` `refuseIdleOnlyCommand` (reached from `commandRunnable` when a verb lacks `whileRunning`) posts `commands run at idle — not queued` and keeps the draft. Replace: the parsed command is appended to a new `deferredCommands []parsedInput` on the `Model` (value-copied slice, appended copy-on-write like `pendingInterjections`), the draft clears, and the staged band paints it as a row reading `queued command: /<verb> <args>` below any interjection rows; Backspace with an empty draft pops the newest staged row of either kind (existing pop order extended); Esc×2 cancels the Exchange and KEEPS the queued commands. At `finishWorker(stateIdle)` the deferred commands run FIFO through the ordinary command path BEFORE the held-queue note (`noteHeldQueue`) and before any queued interjection is sent — a queued `/clear` therefore clears before a queued message lands. `commandsAtIdleNote` is deleted; a verb that cannot run even at idle (a parse error) reports as it does today. `autocomplete.go` `idleOnlyTag` becomes `— runs at idle`.

**Regression guard.** (1) `commandsAtIdleNote` has readers outside the item's original Files, all listed below: `internal/tui/skillscmd_test.go:241`, `picker_test.go:579,1347,1722,3070`, `undo_test.go:239` and `cmd/apogee/e2e_console_test.go:143` — the refusal tests (`TestModelCommandIsIdleOnly`, `TestServerCommandIsIdleOnly`, `TestModelCommandIsIdleOnlyWithTheLauncher`, `TestSubAgentsServerRunsWhileTheWorkerWorks`, `TestSkillsExportIsRefusedWhileTheModelWorks`, `TestUndoIsRefusedWhileTheModelWorks`) are rewritten to assert the `queued command: /<verb>` row (and `interject_test.go:467`'s "commands are never queued" assertion is dropped here, not by item 3); console step 10 asserts the `queued command: /sessions` row and Backspace-pops it BEFORE the Esc×2/quit, because a `/sessions` that runs at idle opens the browser, which claims every key (`model.go` `keyClaimOrder`) and would swallow `drv.Quit()`'s ctrl+c×2.
(2) The FIFO drain stops at the first verb that leaves `m.busy()` true (`/compact` → `startCompact`, `/continue` → `startExchange`); the interjection flush (`flushAfterCompletion`) and the remaining queued commands wait for that worker's own terminal fold (`foldCompactDone` / the `exchangeDoneMsg` case reach the same drain) — never two workers on one Agent, and a `/clear` queued behind a `/compact` still runs in FIFO order.
(3) One `stagedRowCount()` (interjections + deferred commands) feeds `bandShape` in `frameRowPlan`, `renderPendingInterjections`, `queuedSegment` ("N queued" counts both kinds) and the hold note; `heldNote` counts held MESSAGES only — queued commands are not held, they run at the stop's or the error's idle.
(4) The autocomplete accept route (`acceptAutocomplete`, reached from "fix the parser /clear" + Tab) cuts only the token (`removeCompletionToken`) and queues the bare verb — the rest of the draft stays verbatim; the two idle-only LINES of whileRunning verbs (`/confine off`, `/skills export x` — `safeWhileRunning` reads the parsed LINE) queue too; `TestConfineStatusRunsWhileRunningButOffIsRefused` and `TestAcceptIdleOnlyCommandWhileRunningNotesAndKeepsTheDraft` are rewritten accordingly. (5) `foldLoopError` ends in `finishWorker(stateErrored)` and the ⏎ dismissal flips errored→idle without a fold: the queue drains at that dismissal as well (after the error is cleared, before any held message is sent), so a `/clear` typed at that idle never runs ahead of the commands queued before the fault. ADR 0025 D7 ("Stop and error HOLD the queue") is amended by item 3 beside D10: queued COMMANDS run at the stop's / error's idle, held MESSAGES stay held. `internal/tui/commandrun_test.go` and `cmd/apogee/e2e_interject_test.go` do not exist at BASE — they are created by this item.

**Files:** `internal/tui/commandrun.go`, `internal/tui/commandrun_test.go` (created), `internal/tui/interject.go`, `internal/tui/interject_test.go`, `internal/tui/model.go`, `internal/tui/model_test.go`, `internal/tui/autocomplete.go`, `internal/tui/autocomplete_test.go`, `internal/tui/skillscmd_test.go`, `internal/tui/picker_test.go`, `internal/tui/undo_test.go`, `cmd/apogee/e2e_console_test.go`, `cmd/apogee/testdata/stubllm/queued-command.yaml`, `cmd/apogee/e2e_interject_test.go` (created)
**Read first:** internal/tui/interject.go — stageInterjection, stageChildMessage, popInterjection, flushAfterCompletion, noteHeldQueue, renderPendingInterjections, queuedSegment, bandShape; internal/tui/commandrun.go — commandRunnable, refuseIdleOnlyCommand, runCommand, startNewSession, resetSessionView, foldCompactDone; internal/tui/model.go — finishWorker, foldCancelled, foldLoopError, exchangeDoneMsg case, frameRowPlan, the "backspace" empty-box case, the stateErrored "enter" case
internal/tui/autocomplete.go — acceptAutocomplete, removeCompletionToken, commandSuggestions, idleOnlyTag; internal/tui/command.go — commandSpecs, parsedInput, safeWhileRunning; internal/tui/interject_test.go — runningModel, stageRow, TestCancelHoldsWithSingleNote, TestCommandWhileRunningRefusedWithNote, TestAcceptIdleOnlyCommandWhileRunningNotesAndKeepsTheDraft; cmd/apogee/e2e_console_test.go — step 10; internal/stubllm/script.go — Turn.Hang (answers as the empty-reply turn, which abandons the Exchange)

**Tests:** `interject_test.go` (`TestCancelHoldsWithSingleNote` shape): `/compact` mid-run ⇒ staged row with the exact text, draft empty, no note; Backspace pops it; Esc×2 keeps it; at idle it runs and the note is gone; the accept route with a half-written draft queues the bare verb and keeps the rest of the draft; `/confine off` mid-run queues; the status readout counts a queued command with no staged message ("1 queued") and the band paints its row; a stop with one held message and one queued command runs the command and notes `1 queued message held`. `commandrun_test.go` (created): `/clear` queued behind a queued message ⇒ clear runs first, the message opens the next Exchange; `/compact` then `/clear` queued ⇒ the drain stops at `/compact`, `/clear` runs after `foldCompactDone`; commands queued before a loop error drain at the errored→idle ⏎. `autocomplete_test.go`: the tag text. The picker/skills/undo refusal tests assert the row. e2e `TestE2EQueuedCommandRunsAtIdle` (stubllm `hang` turn then text, `e2e_interject_test.go` created): `/compact` typed mid-run appears as the row in the frame and the compaction note appears after the reply; `e2e_console_test.go` step 10 asserts the row and pops it before the quit.

**Acceptance:** `go build ./... && go test ./internal/tui/ -run 'Interject|Command|Autocomplete|Cancel|IdleOnly|Refused|WhileRunning|Undo|Skills' && go test ./cmd/apogee/ -run 'TestE2EQueuedCommand|TestE2EInterject|TestE2EConsole'`

**Commit:** `feat(tui): an idle-only command typed mid-run is queued and runs at idle`

## 3. The docs record that commands queue — ✅ DONE (2026-09-15)

NOTES (2026-09-15): `internal/tui/interject.go` (in Files) was not edited — item 2 already reworded its doc comments (stageInterjection, stageCommand, popDeferredCommand, stagedRowBodies) and the sweep found nothing stale there.
NOTES (2026-09-15): consequential edit — docs/adr/0028-a-server-switch-rehomes-the-session-and-the-first-beat-completes-it.md: made necessary by the ADR 0025 D10 amendment — its decision 7 said mid-run acceptance of /model and /server "earns the standing note"; a dated parenthetical now records that the line is queued.
NOTES (2026-09-15): the "— idle only" menu tag was renamed to "— runs at idle" by item 2 (`idleOnlyTag`, autocomplete.go:409); the docs and comments naming the old tag text (manual, layout.md ×3, ADR 0027/0028 amendments, doc.go, command.go, autocomplete.go) were updated in the same sweep since the old text is now false. ADR 0025's original D10 text and its 2026-07-28 block keep their wording as the historical record; the new dated block supersedes them explicitly.

**What:** Depends on item 2. ADR 0025 gains a dated amendment superseding D10 ("commands never queue") and ADR 0027 D6, stating the item-2 shape (queued as a staged row, FIFO at idle, before the held queue, survives Esc×2). Rule for the prose sweep: every sentence stating that a command is refused, "runs at idle — not queued", or "never queues" — `grep -rn "not queued\|never queue\|idle-only\|idle only" docs/ CONTEXT.md internal/tui layout.md` — is reworded or deleted; CONTEXT.md **Interjection** entry, `docs/manual/commands.md` keys paragraph (the line plan 01 item 3 already edits), `layout.md` staged-band section (the new row), and the `internal/tui/interject.go` doc comment.

**Regression guard.** The acceptance grep is anchored to the COMMAND sentences: CONTEXT.md:121 ("a tick landing while one is in flight is **skipped**, never queued") is the scheduler's rule (ADR 0033) and stays; `internal/tui/interject_test.go:467` ("commands are never queued") is item 2's to drop, not this item's. The comments naming the deleted constant or the refusal — `internal/tui/doc.go:132` (the while-running policy paragraph), `internal/tui/autocomplete.go:141,425,878`, `internal/tui/command.go:88` — are in this item's sweep, so `internal/tui/doc.go` and `internal/tui/autocomplete.go` join Files. ADR 0027 D6 (docs/adr/0027-…md:108 and its "What this amends in ADR 0025" line 152) is named by section in the amendment, and the ADR 0025 amendment also covers D7 (item 2's guard: queued commands run at the stop's / error's idle; held messages stay held).

**Files:** `docs/adr/0025-*.md`, `docs/adr/0027-*.md`, `CONTEXT.md`, `docs/manual/commands.md`, `layout.md`, `internal/tui/interject.go`, `internal/tui/command.go`, `internal/tui/doc.go`, `internal/tui/autocomplete.go`
**Read first:** docs/adr/0025-interjections-commit-at-the-between-steps-boundary.md — decision 10 and its 2026-07-28 amendment block, decision 7; docs/adr/0027-one-slash-namespace-with-inline-skill-tokens.md — decision 6, "What this amends in ADR 0025"; CONTEXT.md — Interjection entry (the `/command` never queues sentence); docs/manual/commands.md — the keys paragraph, lines 16/345/362 ("idle only"); layout.md — staged-band section, lines 311/2010/2252; internal/tui/doc.go — the while-running policy paragraph

**Tests:** none beyond the grep in Acceptance.

**Acceptance:** `! grep -rn "commands run at idle — not queued\|command.* never queue" docs/manual CONTEXT.md layout.md internal/tui && go build ./...`

**Commit:** `docs(interjection): commands queue at idle — ADR 0025 amendment, CONTEXT.md, manual, layout`

## 4. A fan-out's last result states the delegation width — ✅ DONE (2026-09-15)

NOTES (2026-09-15): `internal/agent/subagent.go` edited beyond the item's Files list — the body-note insertion (`withBodyNote`, `splitUserSteeredTrailer`) lives beside the user-steered trailer it has to recognise, and the trailer's fixed head/tail/separator became named constants so the recognition shares the trailer's own spelling; `delegationResult`'s append site now uses the separator constant (same bytes).
NOTES (2026-09-15): `cmd/apogee/testdata/stubllm/fanout.yaml` is reused unchanged — the e2e keeps its `Count the` lead, alpha/beta/gamma names and `^Fan out` trigger, so `e2e_livestate_test.go` is untouched; the width comes from a one-server home carrying `parallel-agents: 2` (`fanOutWidthHome`, reusing `e2e_parallel_test.go`'s `parallelPin`).
NOTES (2026-09-15): the e2e's "byte-identical to a run without a fan-out" is asserted within one session — the system text of the parent's request after the fan-out equals that of its request before any delegation ran — because the standing block is KV-cache-stable per session and a second run would differ by its workspace path alone.

**What:** Recast at the regression check (2026-09-14). Depends on item 1. One structural fact, engine-composed and no steering (ADR 0023 2026-08-25): `dispatch.go` `dispatchFanOut` — when a group had more delegations than the width it ran under and every slot RAN, the LAST committed result of the group has a body ending with the exact line `[K of this group's N delegations ran after the others finished — the width is W]` (above any steered trailer, ADR 0063 D3), W read from `fanOutWidthFor` at dispatch (the value the group actually ran under), K = N − W. No orientation change: the `Delegations:` bullet in `internal/agent/prompts/orientation.txt` and `orientation.go` are untouched. Nothing counts announced-vs-emitted calls (writer call). Manual: the `configuration.md` width paragraph (around lines 1005-1010) and the `commands.md` run-view paragraph (around line 100 — there is no sub_agent paragraph) name the trailer.

**Regression guard.** OWNER CALL 2026-09-14 — the orientation half is DROPPED: no `Delegations:` bullet change, no beat-driven text (ADR 0069 D4/D6 and ADR 0023's session-constant rule stand untouched — the item yields to docs/adr/0069 D4 lines 76-78 and D6 lines 94-99, and to the ADR 0023 2026-08-25 amendment lines 288-291); the item is only the per-group trailer — the LAST committed result of a fan-out group wider than the width ends with the exact line `[K of this group's N delegations ran after the others finished — the width is W]`, W read from `fanOutWidthFor` at dispatch (the value the group actually ran under). `orientation.go`, `orientation.txt`, `orientation_test.go` and `e2e_seat_test.go` are not this item's concern (`TestE2ESeatChoiceFixedStatesNoDelegationsLine` stays green untouched). `cmd/apogee/e2e_fanout_test.go` does not exist at BASE — it is CREATED by this item, so the acceptance pattern `TestE2EFanOut` is new.

**Regression guard (re-check 2026-09-14).** OWNER CALL 2026-09-14 — the trailer rides ONLY a group whose every slot RAN (all `slot.run`, no pool skip, no refusal, no `hookFailed`); a group with a skipped/refused/hook-failed slot carries none; appended in `commitDelegation` after `recordExecuted` so the audit record keeps the tool's own result; it is inserted as the last line of the BODY (the `SeatFallbackNote` precedent) so the steered trailer stays the result's final line — ADR 0063 D3 stands, tests say "the body ends with"; `assertSkippedDelegation` / `TestFanOut_PendingInterjectionSkipsTheQueuedChildren` stay green and a test pins that a pre-empted group carries no trailer; `fanout.yaml`'s second consumer (`e2e_livestate_test.go` keys on its task lead, names and `^Fan out` trigger) is preserved — the e2e uses a new fixture or keeps those, and pins `parallel-agents: 2` as `e2e_parallel_test.go` does since stubllm answers no /props; the manual edit targets the `docs/manual/commands.md` run-view paragraph (around line 100), not a non-existent sub_agent paragraph. The item yields to ADR 0063 D3 (docs/adr/0063 line 86): the steered trailer is the result's final line on every outcome except a cancelled dispatch — the width line sits above it.

**Files:** `internal/agent/dispatch.go`, `internal/agent/fanout_test.go`, `cmd/apogee/testdata/stubllm/fanout.yaml` (kept for `e2e_livestate_test.go`, or a new fixture created beside it), `cmd/apogee/e2e_fanout_test.go` (created), `docs/manual/configuration.md`, `docs/manual/commands.md`
**Read first:** internal/agent/dispatch.go — dispatchTools, fanOutWidthFor, dispatchFanOut, runDelegationPool, commitDelegation, appendToolResult; internal/agent/subagent.go — delegationResult, SeatFallbackNote, userSteeredTrailer; internal/agent/fanout_test.go — threeWayFanOutParentSeamed, subAgentResults, assertSkippedDelegation, gruntTarget, TestFanOut_RoutedWidthComesFromTheTargetCap;
cmd/apogee/e2e_parallel_test.go — parallelHome, launchParallelSession, childRequests; cmd/apogee/e2e_subagent_view_test.go — resultEndsWith; cmd/apogee/e2e_livestate_test.go — TestE2ELiveStateLauncherMoveKeepsTheSessionWorking, fanoutTask

**Tests:** `fanout_test.go`: width 2, three delegations ⇒ the third's committed result's body ends with the exact line (`[1 of this group's 3 delegations ran after the others finished — the width is 2]`), the first two carry none; width 3 ⇒ no line; a routed group runs under the Delegation target's width (`delegationCap` replaces) and the line states that W; a steered last child ⇒ the width line sits immediately above the `userSteeredTrailer`, which stays the result's final line; width 2, three delegations with a pending interjection pre-empting the third ⇒ NO trailer on any result (`assertSkippedDelegation` / `TestFanOut_PendingInterjectionSkipsTheQueuedChildren` stay green). e2e `TestE2EFanOutTrailerStatesTheWidth` (`e2e_fanout_test.go` created; a `parallel-agents: 2` pin as `e2e_parallel_test.go` does, stubllm answering no /props; the fixture keeps `fanout.yaml`'s `Count the` task lead, alpha/beta/gamma names and `^Fan out` trigger for `e2e_livestate_test.go`, or is a new file): the last delegation's result the stub receives on the next request has a body ending with the line; the system prompt is byte-identical to a run without a fan-out.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'FanOut' && go test ./cmd/apogee/ -run 'TestE2EFanOut'`

**Commit:** `feat(agent): a fan-out's last result states the delegation width`

## 5. `/clear` and `/new` reset the usage tallies — engine and view — ✅ DONE (2026-09-15)

NOTES (2026-09-15): the engine test lives in `internal/agent/minilang_test.go` (the file that already holds the ClearContext tests, under its "Context controls" section) rather than the plan's `agent_test.go`; `internal/tui/fold_test.go` is not edited — its `mainUsage` helper is reused by the new `usage_test.go` cases, which cover the zero base the fold adds onto.
NOTES (2026-09-15): consequential edit — internal/tui/model.go: made necessary by the resetSessionView zeroing (the `usageBase` field comment named only launch and resume as the boundaries the engine's reading restarts at).
NOTES (2026-09-15): `docs/manual/sessions.md` gained one sentence on the `/clear` bullet (user-facing behaviour changed).

**What:** Closes the 2026-08-20 deferred defect the `resetSessionView` comment records (no bead survived the migration): `usage` and `delegateUsage` are inherited across `/new` and `/clear` (`34297e2a` carries 45.8M delegate tokens with zero `sub_agent` calls). Fix: `internal/agent/agent.go` `ClearContext` zeroes `a.usage` exactly as `RestoreSession` does (embedder-visible: the tally is per context, documented on the method); `internal/tui/commandrun.go` `resetSessionView` zeroes `usage`, `usageBase` and `delegateUsage`; `fold.go`'s `usageSum(usageBase, totals)` starts from zero after either verb. The `/usage` pane and the saved META reflect the reset.

**Regression guard.** No `TestE2ECachedUsage` exists — the cached-usage fixture's tests are `TestE2EUsageReportsCachedTokensAndDelegateSpend`, `TestE2EUsageHidesTheCachedColumnWithoutABreakdown` and `TestE2EUsageHeadlessCachedCellIsSelfHiding` (`cmd/apogee/e2e_usage_test.go`) — so the acceptance runs `TestE2EClearResetsUsage|TestE2EUsage`. `saveAtIdle` captures the outgoing payload BEFORE `ClearContext` / `resetSessionView` (`sessionsave.go`, `commandrun.go` `startNewSession`), so the closing record keeps its own tally; only `loop.go` / `compact.go` read `a.usage`. Edit order on `resetSessionView` / `foldStats`: this item zeroes the tallies first; item 6 then folds and resets the served set, item 7 keys the `compacted` note — items run in that order.

**Files:** `internal/agent/agent.go`, `internal/agent/agent_test.go`, `internal/tui/commandrun.go`, `internal/tui/usage_test.go`, `internal/tui/fold_test.go`, `cmd/apogee/e2e_usage_test.go`
**Read first:** internal/agent/agent.go — usageTally, ClearContext, RestoreSession; internal/tui/commandrun.go — startNewSession, resetSessionView; internal/tui/fold.go — foldStats, usageSum; internal/tui/usage.go — delegateUsageTotal, delegateUsageHeads; internal/tui/usage_test.go — resumedUsageModel, TestUsageAccumulatesOverAResumedReading; internal/tui/fold_test.go — mainUsage; internal/agent/restoresession_test.go — usageEvents; cmd/apogee/e2e_usage_test.go — openUsage, usageCells, TestE2EUsageReportsCachedTokensAndDelegateSpend

**Tests:** `agent_test.go`: usage after `ClearContext` is zero. `usage_test.go`: a session with spend, `/clear`, one reply ⇒ `/usage` shows that reply alone; `delegateUsage` zero after `/new`. e2e `TestE2EClearResetsUsage` (stubllm `cached-usage.yaml` pattern): the saved META after `/clear` + one reply carries only that reply's tokens.

**Acceptance:** `go build ./... && go test ./internal/agent/ -run 'ClearContext|Usage' && go test ./internal/tui/ -run 'Usage|Fold' && go test ./cmd/apogee/ -run 'TestE2EClearResetsUsage|TestE2EUsage'`

**Commit:** `fix(tui): /clear and /new reset the usage tallies in the engine and the view`

## 6. The record carries the models that answered

**What:** `internal/provider/wirejson.go` `chatCompletionResponse` and the stream's chunks gain the `model` field; `internal/provider/stream.go`'s `Delta` gains `Model`, populated on the `DeltaDone` delta (from the first chunk of a stream / the whole reply of a non-streamed response); `domain.UsageEvent` gains `ServedModel string` (the response's id; empty when the server sends none). The TUI fold — and only the TUI fold — folds every distinct value in order first seen into `session.Meta.ServedModels []string` (`json:"servedModels,omitempty"`, tolerant decode — old records read back unchanged); `Meta.Model` stays the bound profile. The record's readers are the `/sessions` pane and `internal/run`: the pane is unchanged, and a headless / Firing record written through `internal/run` carries no `served_models` (Out of scope); `/usage` shows `served: a, b` when the set is non-empty. Producers/consumers of `UsageEvent` enumerated: `internal/agent/loop.go` and `internal/agent/compact.go` (emit, both through `usageTally.record`), `internal/tui/fold.go` `foldStats` (folds the set) and `internal/tui/transcript.go` `applyUsage` (a child's reading), `internal/tui/usage.go` (paints `served:`), `internal/run` (headless prints usage), `internal/eventjson` (the `usage` line kind gains `served_model`, additive — manual kinds table).

**Regression guard.** (1) The served set reaches the record only through `SessionHost.Save(…, usage, delegateUsage session.Usage)` (`internal/tui/tui.go`); the new parameter is threaded through `savePayload` / `snapshotPayload` (`internal/tui/sessionsave.go`), the `Model` field (`internal/tui/model.go`), `fakeSessionHost.Save` and `savedCall` (`internal/tui/seam_test.go`, so `sessionsave_test.go` can assert it) and `sessionHost.Save` (`cmd/apogee/wire_session.go`). (2) `usageTally.record` has a SECOND caller, the compaction summary at `internal/agent/compact.go:554` (`event.Maintenance = true`): the served id is threaded through `record` (or set on the returned event at both sites) so a maintenance reading names the model that summarised. (3) `ServedModel` is folded in `fold.go`'s `case domain.UsageEvent` BEFORE its `Depth != 0` break — every depth, order first seen, dedup — so a routed child's id (the ADR 0045 case) reaches the set; `usage.go` only paints. (4) `store.Save` rebuilds Meta on every save and neither `tui.ResumedSession` nor `resumeLoaded` carries the field: `ResumedSession` gains `ServedModels`, seeded in `resumedSession` (`wire_session.go`) and at `model.go`'s resume seam / `sessions.go`'s `resumeLoaded` beside `usageBase`, and zeroed in `resetSessionView` (`commandrun.go`) beside the item-5 tallies — or a reopened record drops its own ids on its first save and a new record inherits the closed session's. (5) `cmd/apogee/testdata/eventlines/run.jsonl` pins the `usage` line's members and stubllm echoes `model` on every chunk: `TestE2EEventLinesGolden` is regenerated (`go test ./cmd/apogee -run TestE2EEventLinesGolden -update`); additive within `v` 2, no bump (`internal/eventjson/writer.go`). (6) `DeltaDone` is a `DeltaKind` constant, not a struct — the field is `Delta.Model`, read off the terminal delta in `loop.go` and `compact.go`; `sseChunk` and `chatCompletionResponse` gain the `model` member. (7) Every Meta key is camelCase — the record key is `servedModels` (the eventjson line keeps snake_case `served_model`, as that wire is throughout). Edit order: item 5's zeroing of `resetSessionView` / `foldStats` lands first; this item folds and resets the served set; item 7 keys the `compacted` note after it.

**Files:** `internal/provider/wirejson.go`, `internal/provider/stream.go`, `internal/provider/stream_test.go`, `internal/domain/events.go`, `internal/agent/agent.go`, `internal/agent/loop.go`, `internal/agent/compact.go`, `internal/tui/tui.go`, `internal/tui/sessionsave.go`, `internal/tui/sessionsave_test.go`, `internal/tui/seam_test.go`, `internal/tui/model.go`, `internal/tui/sessions.go`, `internal/tui/commandrun.go`, `internal/tui/fold.go`, `internal/tui/fold_test.go`, `internal/tui/usage.go`, `internal/tui/usage_test.go`, `internal/session/store.go`, `internal/session/store_test.go`, `cmd/apogee/wire_session.go`, `cmd/apogee/wire_session_test.go`, `cmd/apogee/testdata/eventlines/run.jsonl`, `internal/eventjson/*.go`, `docs/manual/headless.md`, `docs/manual/sessions.md`
**Read first:** internal/tui/tui.go — SessionHost, ResumedSession; internal/tui/sessionsave.go — savePayload, snapshotPayload, saveAtIdle; internal/tui/fold.go — foldStats; internal/tui/transcript.go — applyUsage; internal/tui/seam_test.go — fakeSessionHost, savedCall; internal/agent/agent.go — usageTally.record; internal/agent/compact.go — compactCompleter (the second record caller); internal/provider/stream.go — Delta, sseChunk, parseSSE
internal/provider/wirejson.go — chatCompletionResponse; internal/eventjson/encode.go — usageData; cmd/apogee/wire_session.go — sessionHost.Save, resumedSession; internal/session/store.go — Meta; cmd/apogee/e2e_eventlines_test.go — TestE2EEventLinesGolden, eventLinesRedactions

**Tests:** `stream_test.go`: a chunk with `"model":"x"` ⇒ the delta with `Kind == DeltaDone` has `Model == "x"`; a non-streamed response likewise. `agent` test: the compaction summary's maintenance `UsageEvent` carries `ServedModel`. `fold_test.go`: readings from two servers ⇒ `servedModels: [a, b]`, a repeat not duplicated, a depth-1 (routed) reading's id reaches the set. `sessionsave_test.go`: `savedCall` carries the set; a resumed record's ids are seeded and survive the first save; `/clear` resets the set. `store_test.go`: round-trip; a record without the field decodes. `eventjson` golden: the `usage` line carries `served_model`; `TestE2EEventLinesGolden` regenerated and green.

**Acceptance:** `go build ./... && go test ./internal/provider/ ./internal/session/ ./internal/eventjson/ && go test ./internal/agent/ -run 'Compact|Usage' && go test ./internal/tui/ -run 'Usage|Fold|SessionSave|Seam|Resume' && go test ./cmd/apogee/ -run 'WireSession|TestE2EEventLines'`

**Commit:** `feat(session): the record carries served_models — the ids the upstream actually answered with`

## 7. Entries carry a timestamp and a result size; a fold leaves a `compacted` note at every depth

**What:** `internal/session/transcript.go` `Entry` gains `At time.Time` (`json:"at,omitzero"`, the wall clock at commit, set by the TUI fold only; list order is run order, not time order, in a fan-out) and `ToolView` gains `Chars int` (`json:"chars,omitempty"`, `len(result.Content)` at the fold); the tolerant codec reads old records with zero values. `internal/tui/commandrun.go` `foldCompactDone` writes the note with kind `compacted` (today a plain note) and a child's fold — signalled by its maintenance `UsageEvent` — writes the same note under the child's block — so open questions 6 and 7 become answerable from the record. The transcript replay ignores both fields (no paint change); the record's readers are the `/sessions` pane and `internal/run`, and a headless / Firing record written through `internal/run` carries neither `at` nor `chars` (Out of scope). `docs/manual/sessions.md` record section lists the fields.

**Regression guard.** (1) `omitempty` never omits a struct: `At` is tagged `json:"at,omitzero"` (Go 1.26 module; `Meta` already uses `omitzero` in `internal/session/store.go`) so a zero `At` writes no bytes and `TestTranscriptCodecGoldenV1` / `TestTranscriptGoldenV1RoundTrips` stay byte-identical. (2) No `ChildEvent` type exists; a child's fold — like a depth-0 automatic fold — is quiet on success (`compact.go` `autoCompact`), its only signal the `UsageEvent{Maintenance:true}` from `compactCompleter` at the folding agent's Depth/CallID: the note is keyed on `domain.UsageEvent` with `Maintenance` at ANY depth in `fold.go`'s `foldStats`, BEFORE its `Depth != 0` break, placed under the run via `runOf` / `transcript.place`; `foldCompactDone`'s "context compacted" text stays (pinned at `internal/tui/minilang_test.go:398,441`) and the depth-0 event note is skipped while the `/compact` worker runs, or `/compact` writes two notes. Limit: a server that omits usage leaves no maintenance reading and no note. (3) `compacted` is a NEW TUI entry kind: `session.EntryKindCompacted` joins the enum, `entryKindRules` gains a row (`isHostNote`, so `tailBeforeHostNotes` / `place` treat it as today's note — else `TestEntryKindRulesAnswerForEveryKind` fails) and `renderEntryLines` a case painted as a note. Degrade: an older build skips the unknown kind on replay (`entryKindByName` miss) and shows no compaction note — additive by design. (4) No single commit seam exists (several `append(t.entries…)` sites plus `transcript.place`): stamps come through one clock on `transcript` (`now func() time.Time`, nil → `time.Now().UTC()`, which strips the monotonic reading) at `place` and the direct-append seams; tests pin the clock; `TestTranscriptCodecRoundTripsAFiringBlock` and every codec test compare `At` with `Equal`, never DeepEqual on a wall clock. (5) `transcript.place` INSERTS a run's entry at that run's end, so "non-decreasing" is pinned only on a serial transcript. Edit order: items 5 and 6 land their `resetSessionView` / `foldStats` edits first; this item keys the `compacted` note on the same `UsageEvent` case after them.

**Files:** `internal/session/transcript.go`, `internal/session/transcript_test.go`, `internal/tui/transcript.go`, `internal/tui/transcriptbridge.go`, `internal/tui/transcriptbridge_test.go`, `internal/tui/entrykind.go`, `internal/tui/entrykind_test.go`, `internal/tui/render.go`, `internal/tui/fold.go`, `internal/tui/fold_test.go`, `internal/tui/commandrun.go`, `internal/tui/subagentblock.go`, `docs/manual/sessions.md`
**Read first:** internal/session/transcript.go — Entry, ToolView, EntryKindNote; internal/tui/transcript.go — place, addNote, addToolResult, applyUsage, entry; internal/tui/transcriptbridge.go — encodeTranscript, decodeTranscript; internal/tui/entrykind.go — entryKindRules, entryKindByName; internal/tui/render.go — renderEntryLines; internal/tui/fold.go — foldStats; internal/tui/commandrun.go — foldCompactDone; internal/agent/compact.go — autoCompact, compactCompleter
internal/tui/transcriptbridge_test.go — mixedEntries, firedBlockEntries, TestTranscriptCodecGoldenV1; internal/tui/schedule.go — addFiring

**Tests:** `transcript_test.go`: golden round-trip with `at`/`chars`; a record without them decodes to zero; a zero `At` writes no `at` key (the v1 goldens stay byte-identical). `transcriptbridge_test.go`: a committed tool result carries `Chars` equal to its content length; with the clock pinned, entry times are non-decreasing on a serial transcript and equal (`Equal`) across a round-trip. `entrykind_test.go`: the `compacted` row answers for every rule; an unknown-kind replay skips it. `fold_test.go`: a maintenance `UsageEvent` at depth 0 (outside a `/compact` worker) and one at depth 1 both produce a `compacted` note in the right block; `/compact` via `foldCompactDone` produces exactly one.

**Acceptance:** `go build ./... && go test ./internal/session/ && go test ./internal/tui/ -run 'Transcript|Fold|SubAgentBlock|EntryKind|Compact|Minilang'`

**Commit:** `feat(session): entries carry a timestamp and result size; every fold leaves a compacted note`
