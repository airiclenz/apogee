# Polish wave — task-list block, skill-band precision, config sanity, scratch fence — implementation plan

- **Goal:** close four IDEAS.md entries that are small, host-side and reverse no ADR: the `task_list` block renders as an always-open checklist without the model-facing instruction sentence; the skill-suggestion band stops offering three rows for any sentence; the config loader announces unknown keys and the embedded template is gated against the registry; and the announced scratch dir becomes writable by the native writers in the modes whose contract allows it.
- **Date:** 2026-09-14
- **Status:** unexecuted
- **Sized for:** ~200k-context host
- **Base commit:** `ff2bc6372509f92ea39f4ae74e1be74f47a3a163`
- **Sources:** `IDEAS.md`; ADR 0072 (task list); `docs/layout/tool-layout.md` (task_list row, 2026-09-03 note); `layout.md` §"Collapsed and expanded blocks"; ADR 0061 + ADR 0065 §6 (skill matcher shared with `load_skill`); ADR 0036/0041 (config fold, live reload); ADR 0043 (registry); ADR 0012 (mode ladder), ADR 0049 (escape permits), ADR 0056 D3 (scratch dir); `docs/design/confinement-execution-contract.md` §box; ADR 0031 (wire-silent engine).
- **Ratified design calls (owner, 2026-09-14, via AskUserQuestion):**
  - **Scope:** this wave = IDEAS.md items "Task list UI", "skill prediction", "Config sanity check", and the DEFECT half of "All modes can use the scratch dir". Side panel, key navigation, scheduled-message delivery and Plan/Ask-Before scratch writes are out (see below).
  - **Task-list block:** ALWAYS EXPANDED — every task row (open and done) is painted, no ▶/▼ toggle, no click surface; the header sentence (`Task list — yours to maintain…`) is stripped display-side only; the model's result text is unchanged (ADR 0031); glyphs stay `[✔]`/`[ ]` (ADR 0072); the header stat keeps `N open`.
  - **Skill band gate:** rows must score ≥ 60 % of the top row (Suggest only); the dev-generic stopword set is exactly `file`, `files`, `add` (re-check round: `change` is NOT added). Constants, no config key. [The prefix tightening ("prefix matching only when the DOC term is the longer side") was withdrawn at the regression check, 2026-09-14 — the either-way rule stands.]
  - **`load_skill`:** keeps today's evidence gate (no relative cutoff); the shared matcher changes (stopwords) reach it, and `lookup_test.go` must stay green unchanged.
  - **Negative fixture rows:** the implementer authors ≥ 5 generic dev-chat drafts pinned to empty or ≤ 1 row.
  - **Unknown config key:** one-line notice through the existing `notify` path at STARTUP ONLY (`LoadFileConfig` and the `mayMigrate=true` load at `config.go:2915` are both startup readers); the ADR 0041 live-reload path stays silent — bead `apogee-ibd`; never an error; keys the migration already notes/refuses are not re-reported.
  - **`apogee probe config`:** NOT in this wave — bead filed 2026-09-14 ("apogee probe config — report unknown keys…").
  - **Scratch fence:** native writers into the session's scratch dir are in-fence for the ladder (Allow-Edits and Auto run them unprompted; Ask-Before still gates; Plan still refuses). The terminal is unchanged.
  - **Plan orientation:** Plan mode OMITS the `Scratch dir:` bullet; `{{scratch}}` keeps expanding; other modes keep the line verbatim.
- **Regression check (2026-09-14, ff2bc6372509f92ea39f4ae74e1be74f47a3a163):** 1: guard folded (width arm binding at every width; fixture/stat/helper locators corrected; layout.md:829 and tool-layout.md:127 superseded for task_list — the always-open exception is named there). 2: recast (prefix tightening withdrawn — either-way rule, `isPrefixEitherWay`, minPrefixRunes comment and ADR 0061 stand; stopword set fixed at `file`/`files`/`add`/`change`; existing tests the cutoff rewrites listed). 3: guard folded (negative rows must return ≥ 1 row at BASE; "closest" sites named by rule; acceptance runs `EmbeddedDefaultConfig`; ADR amendment no longer mentions a prefix change). 4: guard folded (startup only — `apogee-ibd` filed; `servers[N].sub-agents` exempted from the walk, yields to configmigrate.go:722-724 / ADR 0035; empty and comment-only files walk to nil). 5: note folded (`tool-call-pattern` example is additive; gate green on landing). 6: guard folded (permit compared against `realPath`, unresolved spelling to SetScratchDir). 7: guard folded (paragraph-aware prose guard; ADR 0023:292-295 and CONTEXT.md:867/:894 amended — ADR 0023's per-session-constant claim superseded for the mode input). 8: SAFE.
  - **Re-check round (2026-09-14, ff2bc6372509f92ea39f4ae74e1be74f47a3a163):** 2: recast (stopword set narrowed to `file`/`files`/`add` — `change` dropped, admission rule restated on stems, trigger terms asserted absent from `stopwords`; `TestSuggestExcludeDropsASkillAndTheNextFillsIn` struck from the rewrite lists; `lookup.go` added with the Score-threshold prose guard; item retitled and its commit line no longer names the withdrawn prefix change). 3: amended by decision (ADR 0061 amendment names the three-word set). 4: guard folded (`mcp-servers` is `[]mcpServerConfig` — a struct slice the walk descends; the any-typed fields are `reactions[].run/advise/gate`; test row flipped to `mcp-servers[0].foo`; title and commit line aligned with the startup-only call). 1, 5, 6, 7, 8: unchanged this round.
- **Standing requirements:** `skills: coding-standards`; any authorized deviation from item text lands as a dated NOTES line under the item; each item's CHANGELOG entry travels in its sidecar.
- **Out of scope:** side panel (IDEAS "Sidepanel"), ←/→ block navigation, scheduled-Firing delivery into the live session (ADR 0033 D7 stands), scratch writes in Plan/Ask-Before (ADR 0012 stands), terminal writes to scratch below Auto, a `probe config` verb (bead `apogee-lpi`), unknown-key notices on live reload (bead `apogee-ibd`), the `hooks:`/`mechanisms:` migration lifetime, any VERSION change.

## 1. task_list block: always open, header sentence stripped — ✅ DONE (2026-09-14)

NOTES (2026-09-14): consequential edit — internal/tui/transcriptbridge.go: made necessary by the item's own text naming the transcript decoder (`fromWireToolView`) as the second producer that sets `alwaysOpen`; the file was absent from the item's Files list.
NOTES (2026-09-14): the git_status regression test (`TestTargetlessBlockStillCapsAndToggles`) lives in `toolblock_test.go` as the Files list places it, rather than in `blocktarget_test.go` beside the other click-surface cases.

**What.** Add a presenter flag `alwaysOpen bool` to `toolPresenter` (`internal/tui/toolregistry.go`) and a matching unexported `alwaysOpen` field on `toolView` (`internal/tui/toolview.go`), set from the registry entry at view-build time in BOTH producers — the live path and the transcript decoder (`fromWireToolView`, the same place `solo` is re-derived) — never persisted on the wire. Set it on the `task_list` entry only. In `collapsedCall` (`internal/tui/toolbranch.go`) an `alwaysOpen` view returns its whole branch list with `truncated=false` and no remainder, so `blockHidesWhenCollapsed` (`internal/tui/blockstate.go`) answers false for it: no ▶/▼ on the header, `toggle == targetNone` on every row, and a click selects rather than folds. Rows are still clipped per line to width by the existing `clipTail`; no row budget applies. Replace `detail: outputDetail` on the entry with a `taskListDetail` that drops the first line when it starts with `tasklist.Fence` (import the constant, do not respell it) and hands every remaining line on as body; a result with no task rows (cleared list, refusal) keeps `outputDetail`'s behaviour. `openTasksStat` is unchanged. Update `docs/layout/tool-layout.md`: the `task_list` row's body cell reads "the list, one row per task — always open, no toggle" and the 2026-09-03 note gains a dated sentence saying the header sentence is display-stripped (ADR 0031: the result text is the model's, unchanged). Binding standards: one predicate (`collapsedCall`) decides the shape — do not special-case `renderToolBlock` or `renderToolBranch` by tool name.

**Regression guard.** `blockHidesWhenCollapsed` (internal/tui/blockstate.go) answers false for an `alwaysOpen` view BEFORE its width arm (:156-159) — a row clipped to width reveals nothing on expand because the expanded paint clips per line identically — so the no-toggle/no-click call holds at every width. The existing fixture (`toolregistry_test.go:200-203`) is 2 open / 1 done, and a targetless block's stat is not on the header: `branchDetails` (`toolbranch.go:267-274`) paints `N open` as a closing `┕` row after the task rows; the click helper `blockMarks` lives in `blocktarget_test.go:39`, which is where the click test goes. Every doc and comment that says the collapsed budget or the two-state rule holds for every tool block names task_list as the always-open exception — `layout.md:829-841` ("every tool-shaped block … whatever tool filled it"), `docs/layout/tool-layout.md:127` ("Exactly two states per call"), `toolbranch.go:45-48` and the `collapsedCall` comment (:218-227), `blockstate.go:129-134`; guard grep `grep -rn "every tool-shaped block\|Exactly two states\|whatever tool filled it\|collapsedBodyCap" layout.md docs/layout/tool-layout.md internal/tui/`. Superseded source: `layout.md:829` records the three-row collapsed budget as holding for EVERY tool-shaped block — the ratified always-open call supersedes it for task_list, and the amended line names the exception.

**Files:** `internal/tui/toolregistry.go`, `internal/tui/toolview.go`, `internal/tui/toolbranch.go`, `internal/tui/blockstate.go`, `internal/tui/toolregistry_test.go`, `internal/tui/toolblock_test.go`, `internal/tui/blocktarget_test.go`, `docs/layout/tool-layout.md`, `layout.md`

**Tests.** `TestToolRegistryPresentsTheTaskListCall` (extend): a three-task result renders the header row without an indicator, then exactly three body rows `[✔] …`/`[ ] …` in order, then the closing `┕ 2 open` row, with no `Task list —` line and no `+N more lines` marker at 80 cols. New: a decoded transcript record of a `task_list` call gets the same paint as the live one (round-trip through the codec). New (in `blocktarget_test.go`, via the existing `blockMarks` helper): clicking any row of the block yields `targetNone` (no toggle) at 80 cols, and the same three-task block at 40 cols — every row clipped to width — still yields `targetNone` on every row and paints no indicator. Regression: a targetless non-`alwaysOpen` tool (`git_status`) still caps at `collapsedBodyCap` and still toggles.

**Acceptance.** `go build ./... && go test ./internal/tui/ -run 'TaskList|ToolRegistry|ToolBlock|Collapsed' -count=1`

**Commit.** `feat(tui): task_list block is always open and drops the model-facing header sentence`

## 2. Skill band: relative cutoff and dev-generic stopwords — ✅ DONE (2026-09-14)

**What.** Recast at the regression check (2026-09-14). In `internal/skills/suggest.go`: (a) new constant `minRelativeScore = 0.6` applied in `Suggest` ONLY, after `rank` and before the limit — every row whose `Score` is below `minRelativeScore × out[0].Score` is dropped (the top row always survives); `rank` is untouched so `load_skill` (`lookup.go`, ADR 0065 §6) keeps its gate. (b) Extend `stopwords` with exactly `file`, `files`, `add` — the dev-generic set (`change` is NOT added: `tokenize` tests stopwords on the raw word before stemming, so it would drop the singular draft "stage this change now" while the plural trigger `stage these changes` kept its term); the constant's comment names them as such and why. Rule (on stems): a word may join `stopwords` only if no tokenized shipped-trigger term equals `stem(word)` or `word` (`grep -A30 '^triggers:' internal/skills/shipped/*/SKILL.md`) and every positive row of `internal/skills/suggest_library_test.go` stays green (`project`, `code`, `update`, `fix`, `change` fail that rule and are NOT added). The prefix rule is unchanged: `isPrefixEitherWay`, the minPrefixRunes comment and every "either way" comment stand as they are. Update the `Suggestion.Score` comment (:124-128) — it now states that Suggest applies a relative cutoff and rank does not — and amend `internal/skills/lookup.go:16-18` to say Suggest's relative cutoff is the other threshold. Prose-guard rule: every comment naming the Score threshold or what rank does not carry — `grep -n 'threshold\|minDraftWords floor\|"file"' internal/skills/*.go` (`suggest.go:37`, `:126`, `:170`, `lookup.go:16`) — is reworded to match. Binding standards: constants with a one-paragraph reason each, matching the file's existing style; no new config key.

**Regression guard.** (owner, 2026-09-14) the doc-side prefix tightening is DROPPED — today's either-way prefix rule, `isPrefixEitherWay`, the minPrefixRunes comment and ADR 0061's "≥ 4-rune prefix either way" all stand unchanged; clause (c) and the "either way" comment grep are removed from the item. (owner, 2026-09-14, re-check round) the dev-generic stopword set is exactly `file`, `files`, `add` — `change` is NOT added; the admission rule is restated on stems: a word may join `stopwords` only if no tokenized shipped-trigger term equals `stem(word)` or `word` (`grep -A30 '^triggers:' internal/skills/shipped/*/SKILL.md`) and every positive row of `suggest_library_test.go` stays green; `TestShippedTriggersKeepTheirTokenCounts` also asserts no trigger term is in `stopwords`. The forced rewrites are exactly `TestSuggestRanksTheStrongerLexicalMatchFirst` (:129), `TestSuggestLimitCapsTheResult` (:315-322) and `TestSuggestPutsATriggerHitFirst` (:146-158, drive its score comparison through `rank`, keep the Suggest half at one row) — `TestSuggestExcludeDropsASkillAndTheNextFillsIn` (:302) is green after the cutoff and is NOT rewritten; `TestTokenize` (:448) is unaffected now that `code` is not a stopword. `internal/skills/lookup.go` is in **Files** under the prose-guard rule "every comment naming the Score threshold or what rank does not carry — `grep -n 'threshold\|minDraftWords floor\|"file"' internal/skills/*.go`"; `lookup.go:16-18` is amended to say Suggest's relative cutoff is the other threshold. Yields to ADR 0061:50 and :84 ("≥ 4-rune prefix either way") — nothing in this item reverses them.

**Files:** `internal/skills/suggest.go`, `internal/skills/suggest_test.go`, `internal/skills/lookup.go`

**Tests.** New unit cases in `suggest_test.go`: a draft where the second skill scores 0.5× the first returns one row; one at 0.7× returns two; the top row survives at any absolute score; a draft matching only via `file`+`add` returns nil; "stage this change now" still returns the commit-hygiene row; `TestShippedTriggersKeepTheirTokenCounts` pins the per-trigger `tokenize` token count of every shipped trigger under `internal/skills/shipped/*/SKILL.md` AND asserts no tokenized trigger term is in `stopwords` (the stopword rule's bite check). Rewrites forced by the relative cutoff — exactly these three: `TestSuggestRanksTheStrongerLexicalMatchFirst` (:129), `TestSuggestLimitCapsTheResult` (:315-322), `TestSuggestPutsATriggerHitFirst` (:146-158 — compare scores through `rank`, unlimited and uncut; the Suggest half asserts one row); `TestSuggestExcludeDropsASkillAndTheNextFillsIn` (:302) passes unchanged; `TestSuggestPrefixMatchesAStemEitherWay` (:162) and `TestTokenize` (:441) pass unchanged. Existing `internal/skills/lookup_test.go` passes unchanged — a lookup fixture that turns red is a REGRESSION of the shared-matcher call and blocks the item (not a fixture edit).

**Acceptance.** `go build ./... && go test ./internal/skills/ -count=1`

**Commit.** `feat(skills): suggestion band keeps only rows within 60% of the top score and drops three dev-generic words`

## 3. Skill band: negative fixture rows, ADR 0061 amendment, manual wording — ✅ DONE (2026-09-14)

NOTES (2026-09-14): negative rows chosen by probe at BASE (bf3202b1) vs HEAD — "add a field to the config struct", "add the missing import to this file", "move these files into a new package", "add a flag to the command line parser", "split this file into two smaller files", "please add a changelog entry for the fix" each returned ≥ 1 row at BASE and nil now; "update the readme with the new flag" still returns brew-release after item 2 (top row always survives the relative cutoff) so it is not pinned; "can you fix the failing test in the parser", "rename this variable across the file", "why does this function return nil here" were already nil at BASE and do not count.
NOTES (2026-09-14): TestSuggestBandPrecision added — the band test double (fakeSkillCatalog, gatedSuggest) does not go through Catalog.Suggest, so the table loads ../skills/testdata/library through skills.Load and drives the real matcher through Options.Skills.
NOTES (2026-09-14): internal/tui/tui.go:43 and :861 still say "the closest skills" in the SkillCatalog / Options doc comments — code comments outside the item's user-facing rule and guard grep, left untouched.

**What.** Depends on item 2. In `internal/skills/suggest_library_test.go` add ≥ 5 negative rows to `TestSuggestOnTheRealLibrary` over the existing 24-skill fixture: generic dev-chat drafts ("can you fix the failing test in the parser", "add a field to the config struct", "rename this variable across the file", "why does this function return nil here", "update the readme with the new flag") each pinned `wantNil`; every row must return ≥ 1 row at BASE (see the guard) — the five named drafts are candidates, not the list; the existing 7 positive rows must still hold ("get me up to speed on this project" stays green by construction now that `project` is not a stopword). Add a `TestSuggestBandPrecision` table in `internal/tui/suggestband_test.go` only if the band test double does not already go through `Catalog.Suggest` — otherwise no band test changes. Amend ADR 0061 with a dated "Amendment 2026-09-14" paragraph: relative cutoff (Suggest-side), the three dev-generic stopwords (`file`, `files`, `add`), and that `load_skill` keeps the evidence gate — it mentions no prefix change (the either-way rule at ADR 0061:50/:84 stands). Reword every user-facing sentence saying the band names the closest skills — `docs/manual/commands.md:233`, `README.md:170`, `docs/manual/configuration.md:560`, `layout.md:1682`, the template comment at `internal/config/defaults/config.yaml:944` and the `suggestband.go:15-16` comment (guard grep `grep -rn "closest" README.md docs/manual layout.md internal/config/defaults/config.yaml internal/tui/suggestband.go`; `layout.md:1632` is about the slot, not the ranking, and stays) — it now shows "the skills that clearly fit, up to three".

**Regression guard.** Each of the ≥ 5 negative rows must return ≥ 1 row at BASE (the verifier reads `Suggest(...) top-N` logged at `suggest_library_test.go:56`) and is pinned `wantNil`; a draft already nil at BASE does not count toward the five — at BASE "can you fix the failing test in the parser", "rename this variable across the file" and "why does this function return nil here" are already nil, while "add a field to the config struct" (signing-plugin-assembly) and "update the readme with the new flag" (brew-release) return one row. The "closest" wording is a rule over every user-facing sentence, not a closed list — the sites and grep are in **What**; the commands.md locator is :233 (:239-241 holds "At most three…"). The embed tests are `TestEmbeddedDefaultConfig*` (`internal/config/defaults_test.go:73-219`), so acceptance runs `-run 'EmbeddedDefaultConfig|Defaults|Template'`. The ADR 0061 amendment no longer mentions a prefix change (it records the relative cutoff, the three stopwords `file`/`files`/`add` — not `change`, per the re-check round — and that `load_skill` keeps the evidence gate); the "get me up to speed on this project" row stays green by construction now that `project` is not a stopword.

**Files:** `internal/skills/suggest_library_test.go`, `internal/tui/suggestband_test.go`, `docs/adr/0061-skill-suggestions-are-driver-side-over-an-engine-matcher.md`, `docs/manual/commands.md`, `README.md`, `docs/manual/configuration.md`, `layout.md`, `internal/tui/suggestband.go`, `internal/config/defaults/config.yaml`

**Tests.** Each new negative row returns ≥ 1 row against the pre-item-2 tree (bite check on the branch before item 2 — the verifier runs them at `git stash`/pre-tree if needed, reading the logged `Suggest(...) top-N`) and returns nil after; positive rows unchanged.

**Acceptance.** `go test ./internal/skills/ ./internal/tui/ -run 'Suggest' -count=1 && go test ./internal/config/ -run 'EmbeddedDefaultConfig|Defaults|Template' -count=1`

**Commit.** `test(skills): pin band precision on the library fixture; amend ADR 0061`

## 4. Config: unknown-key notice at startup — ✅ DONE (2026-09-14)

NOTES (2026-09-14): consequential edit — internal/config/doc.go: made necessary by the new file `internal/config/unknownkeys.go` (TestDocMapNamesEveryFile enumerates the package's files).
NOTES (2026-09-14): docs/manual/configuration.md gained a "Keys apogee does not recognise" section (user-facing behaviour changed; the file is not in the item's Files list — added under procedure step 6, not a consequential edit).
NOTES (2026-09-14): descent follows the type rule (struct, slice/map of struct), so `system-prompt-models.<name>` and `system-prompt-layers[]` are walked too — not named in the item's example list but covered by its stated rule; `<<` merge keys and `*alias` references are honoured rather than reported.
NOTES (2026-09-14): `derefType` moved from registry_test.go to unknownkeys.go beside `schemaKeys` (both readers need it); the working tree also holds items 5 and 6's uncommitted files (defaults/config.yaml, defaults_test.go, internal/agent/*, cmd/apogee/*), untouched.

**What.** Recast at the regression check (2026-09-14). New file `internal/config/unknownkeys.go`: `unknownKeys(data []byte) []keyAt` decodes the migrated bytes into a `yaml.Node` and walks it against the schema derived REFLECTIVELY from `fileConfig`'s yaml tags — move the tag walker `registry_test.go` already uses into a non-test helper (`schemaKeys`) so the registry bijection test and this walk share one reader. Descend only into fields whose Go type is a struct or a slice/map of structs (`ui`, `sessions`, `tools`, `url-safety`, `present`, `context-files`, `servers[]`, `mcp-servers[]` — `[]mcpServerConfig`, `config.go:1418`, seven tagged fields at :2360-2371 — `model-profiles.*`, `reactions[]`, `unconfined-hosts[]`); never into a field typed `any`, `map[string]any` or `yaml.Node` (`reactions[].run` / `.advise` / `.gate`, `internal/config/reactions.go:40-42`, plus the webhook mappings under them). In `parseConfigFile` (`config.go:2681`), after `migrateLegacyConfig` and the successful `yaml.Unmarshal`, call `notify` once per unknown key with the EXACT line: `apogee: config %s: unknown key %q at line %d is ignored` (path, dotted key e.g. `ui.spiner`, `yaml.Node.Line`). Legacy keys the migration folds or refuses (`endpoint`/`api-key`/`host-alias`/`model`, `llama-launcher`, `model-profile`, `hooks`, `mechanisms`, `validated-sets`) are consumed before the walk or refused with an error, so they are never double-reported — assert it. The retired `servers[N].sub-agents` flag (`subAgentsKey`, `configmigrate.go:732`) is NOT consumed by the migration — only the consented TUI offer strips it — so the walk skips it explicitly. A document without a root mapping (zero-byte or comment-only file: `yaml.Node` Kind 0, no Content) walks to nil. Both STARTUP readers (`LoadFileConfig` and the `mayMigrate=true` load at `config.go:2915`) go through `parseConfigFile`, so startup is covered by construction; the ADR 0041 live-reload path (`cmd/apogee/settingsedit.go:331`, `externalEdit.projection`, which passes `func(string) {}`) stays silent — bead `apogee-ibd`. Binding standards: the walk is a pure function over bytes; `notify` stays the only output; no new exported API.

**Regression guard.** (owner, 2026-09-14) the notice lands at STARTUP ONLY — both `LoadFileConfig` and the `mayMigrate=true` load at `config.go:2915` are startup readers; the ADR 0041 live-reload path (`cmd/apogee/settingsedit.go:331`, `externalEdit.projection`, which passes `func(string) {}`) stays silent — bead `apogee-ibd` filed. `servers[].sub-agents` is exempt from the walk: `migrateLegacyConfig` does not consume it (`configmigrate.go:715-724`, ADR 0045/0035 — the consented TUI offer strips it and a declined offer leaves it), so without the exemption every start of such a config (headless and daemon raise no offer at all) would print `unknown key "servers[0].sub-agents"` beside the offer; the test asserts the exemption. Yields to `configmigrate.go:722-724`: a config still carrying `sub-agents: true` "keeps working exactly as it does today" with nothing run unasked. A zero-byte or comment-only config.yaml (both accepted by `parseConfigFile` today) decodes to a `yaml.Node` with Kind 0 and no Content — a walker taking `node.Content[0]` panics at every start of that file, so the walk returns nil for a document without a root mapping. (re-check round) `mcp-servers` is NOT an any-typed field: it is `MCPServers []mcpServerConfig` (`config.go:1418`) with seven fixed tagged fields (`:2360-2371`), a struct slice the descent rule walks — so a typo'd `env-allowlist`/`transport` under `mcp-servers[]` IS reported, and no hard-coded exemption is added; the any-typed fields the walk stops at are `reactions[].run/advise/gate` (`reactions.go:40-42`) and the webhook mappings under them. Startup-only holds by construction: every non-startup reader of `parseConfigFile` (`wire_settings.go:2093`, `settingsedit.go:331`) and every test caller pass a discarding notify.

**Files:** `internal/config/unknownkeys.go`, `internal/config/unknownkeys_test.go`, `internal/config/config.go`, `internal/config/registry_test.go`

**Tests.** `unknownkeys_test.go`: `auto-compct: true` → one notice naming line 1; `ui: {spiner: dots}` → `ui.spiner`; `servers: [{name: a, plaintext-key-ok: true, foo: 1}]` → exactly `servers[0].foo`; a legacy-quadruple file after migration → no unknown-key notice; `mcp-servers: [{name: a, transport: stdio, foo: 1}]` → exactly `mcp-servers[0].foo`; `reactions: [{id: r, on: [x], run: {url: u, body: {deep: 1}}}]` → none (the any-typed action keys are not descended); `servers: [{name: a, sub-agents: true}]` → none (the retired flag is exempt); an empty file and a comment-only file → no notice, no panic. `TestRegistryIsBijectionWithFileConfig` still passes via the shared walker.

**Acceptance.** `go build ./... && go test ./internal/config/ -count=1`

**Commit.** `feat(config): unknown keys are announced once at startup instead of ignored silently`

## 5. Config: template↔registry gate and template sweep — ✅ DONE (2026-09-14)

NOTES (2026-09-14): the registry holds 57 rows today, not the 55 the plan counts; the gate is green over all 57 without an allowlist entry.
NOTES (2026-09-14): a negative case (`TestTemplateMentionsEveryRegistryKeyRejectsAnAbsentKey`, fabricated paths incl. a leaf borrowed from another block) was added beside the gate, mirroring the manual gate's negative cases.

**What.** In `internal/config/defaults_test.go` add `TestTemplateMentionsEveryRegistryKey`: every registry `Path` (the 55 rows) appears in `internal/config/defaults/config.yaml` as an active or commented key (match the leaf spelling under its parent, e.g. `skill-suggestions` under `ui:`), mirroring `cmd/apogee/docs_settings_test.go`'s manual gate; an empty allowlist constant sits beside it for a future exception. The gate is expected green on landing (all 55 leaves are already present in the template) — it is a guard against future drift, not a fix. Sweep the template: add commented example lines for the three read-but-absent sub-keys — `servers[].plaintext-key-ok`, `servers[].effort-dialect`, `model-profiles.<name>.tool-call-pattern` — with one-sentence comments taken from their manual entries (`docs/manual/configuration.md`, grep each key) — `tool-call-pattern` is already described in template prose (`defaults/config.yaml:1041-1042`), so its example line is additive; rewrite line 156's "a Mechanism that synthesises delegations batches…" so it names no retired Mechanism (ADR 0071/0076) — state the fact in Reaction/Floor vocabulary per `CONTEXT.md`. Prose guard rule: every template comment naming `Mechanism`, `mechanism`, `catalogue` (in the retired sense) or `validated-set` — `grep -n -i "mechanism\|validated-set" internal/config/defaults/config.yaml` — is reworded; the skills "catalogue" at :943 is the skills catalog and stays.

**Regression guard.** `tool-call-pattern` is already described in template prose (`defaults/config.yaml:1041-1042`) so its example line is additive, and the gate is expected green on landing (all 55 leaves already present) — the gate is a guard against future drift, not a fix.

**Files:** `internal/config/defaults/config.yaml`, `internal/config/defaults_test.go`

**Tests.** The new gate test; `TestManualDocumentsEverySettingsKey` and `TestRegistryIsBijectionWithFileConfig` unchanged and green; the existing `defaults_test.go` cases (system prompt, api-key, servers schema, model-profiles) unchanged.

**Acceptance.** `go test ./internal/config/ -count=1 && go test ./cmd/apogee/ -run 'DocsSettings|ManualDocuments' -count=1 && ! grep -n -i "mechanism\|validated-set" internal/config/defaults/config.yaml`

**Commit.** `test(config): the embedded template is gated against the settings registry; retired wording swept`

## 6. Scratch fence: native writers into the session scratch dir are in-fence — ✅ DONE (2026-09-14)

NOTES (2026-09-14): `internal/agent/resolution_test.go` (listed under Files) needed no change — the pure resolver's inputs and its `writeTargetInWorkspace` rows are untouched; the fix lives entirely in dispatch's fact-gathering, so the new proofs are dispatch-level and e2e.
NOTES (2026-09-14): `internal/domain/config.go:89-95` left as is — its `ConfineWritablePaths` comment describes the subprocess box and never claimed the classifier reads the slice directly.
NOTES (2026-09-14): `driveScratchToolCall` now delegates to a tool-agnostic `driveScratchCall` (same body, same two callers' behaviour) so the write_file drives share the construct → SetScratchDir → Step ordering rather than duplicating it.

**What.** Defect (not a regression from one commit — the seam predates ADR 0056's scratch fold): `classifyWriteTarget` (`internal/agent/dispatch.go:1304`) checks `a.cfg.ConfineWritablePaths`, which no host populates, and never the live scratch dir — so `write_file`/`edit_file`/`copy_file` targets under the announced `Scratch dir: …` gate in Allow-Edits/Auto and are refused by a Firing's denier, while the orientation calls the dir "writable". Fix: classify against `a.confinementBox().WritablePaths` (the same live-scratch fold every per-call consumer already builds from, `dispatch.go:693-702`) instead of the raw config slice; the return shape is unchanged (`inFence=true, escapeTarget=abs` for a writable path outside the root, so the ADR 0049 permit is still stamped at `dispatch.go:802` and `path_safety` lands the write). Producers/consumers of the classification: `resolve()` ladder cells in `internal/agent/resolution.go:392-455` (read `inFence`), the permit stamp at `dispatch.go:802`, `resolvedPath` disclosure (`dispatch.go` twin), the Approver pane text — none change shape. Ask-Before keeps gating (its default arm gates every write regardless of fence); Plan keeps refusing (`planAdmits`). Terminal untouched. Update the scout-found comment on `ConfineWritablePaths` in `internal/domain/config.go:89-95` only if it claims the classifier reads it directly.

**Regression guard.** The permit carries the EvalRealPath-resolved target (`internal/agent/dispatch.go:1305` via `tools.WorkspaceWriteTarget` → `target.Real`, `internal/tools/workspace_scoped.go:63-68`), so a scratch under `t.TempDir()` on macOS (/var → /private/var) fails a literal `Real == <scratch>/probe.txt` comparison while the suite is green there today. The dispatch tests compare against `realPath(t, scratch)` (`internal/agent/resolvedpath_test.go:215`) exactly as the sibling "declared writable path" test does (`internal/agent/writeescape_test.go:290-294`), and pass the UNRESOLVED spelling to `SetScratchDir` so the test also proves `pathWithin`'s root resolution (`dispatch.go:1387`).

**Files:** `internal/agent/dispatch.go`, `internal/agent/dispatch_test.go`, `internal/agent/resolution_test.go`, `cmd/apogee/e2e_announced_test.go`, `cmd/apogee/testdata/stubllm/announced-scratch-write.yaml`

**Tests.** `dispatch_test.go`: Allow-Edits `write_file` to `<scratch>/probe.txt` → Run, permit `Real == filepath.Join(realPath(t, scratch), "probe.txt")` with the unresolved `scratch` handed to `SetScratchDir`, file lands; Auto-confined same; Ask-Before → Gate; Plan → Refuse; a target under a sibling dir of scratch (`~/.apogee/scratch/other-session/x`) stays out-of-fence. `e2e_announced_test.go`: a new stubllm capture whose model reads the orientation's EXACT `Scratch dir: <path>` line and calls `write_file` on `<path>/probe.txt` in Allow-Edits — no ApprovalEvent, the file exists after the Turn. Bite check: the new dispatch tests fail on the pre-item tree.

**Acceptance.** `go build ./... && go test ./internal/agent/ -run 'Scratch|WriteTarget|Fence|Resolution' -count=1 && go test ./cmd/apogee/ -run 'E2EAnnounced' -count=1`

**Commit.** `fix(agent): native writes into the session scratch dir are in-fence, as the orientation announces`

## 7. Plan mode omits the scratch orientation bullet — ✅ DONE (2026-09-14)

NOTES (2026-09-14): the Auto row of the mode-table test is built over the dispatch tests' `fakeConfiner{caps: capsBoth()}` — `newAgent` refuses Auto without fs-write confinement on this host; no production change.

**What.** Depends on item 6. In `orientationBlock` (`internal/agent/orientation.go:104-110`) skip the `orientationScratchLine` bullet when `a.Mode() == domain.ModePlan` (use the mode constant's real name from `internal/domain`). Update the function's KV-cache comment: the mode is now an input, so a mode flip re-encodes the prefix — accepted, a mode change is a session-level door. `{{scratch}}` (`internal/prompt/prompt.go`, `loop.go:991`) is untouched. Record the change as a dated note under ADR 0056 D3, as a dated one-line note under ADR 0023's 2026-08-25 amendment (`docs/adr/0023-…:292-295`: mode joins the block's inputs; a flip re-encodes the prefix), and in `CONTEXT.md`'s scratch-dir paragraph (:860-876) — one sentence each: Plan announces no scratch dir because Plan writes nothing; amend `CONTEXT.md:867` ("unconditionally, by the Orientation block") and `:894` ("every fact it states is a per-session constant"). Prose guard rule (paragraph-aware — the claim and the word "scratch" seldom share a line): every doc paragraph claiming the orientation block or its scratch bullet is unconditional, mode-independent or a per-session constant — `grep -rn -i -A3 'orientation' CONTEXT.md docs/adr/0023*.md docs/adr/0056*.md docs/manual/*.md | grep -i 'unconditional\|every mode\|per-session constant\|constant \*within\*'` — is corrected.

**Regression guard.** The draft's per-line grep finds ZERO sites at BASE (run verbatim: empty output) — its own target, `CONTEXT.md:867` "and, unconditionally, by the **Orientation block** that rides on every standing system message", has no "scratch" on its line — and ADR 0023's 2026-08-25 bullet (`docs/adr/0023-…:292-295`, "All three are constant *within* a session, so the block is prefix-KV-cache stable") and `CONTEXT.md:894` record the mode-independence this item reverses; both are amended (see **What**, **Files**). Superseded source: `docs/adr/0023-the-system-prompt-is-a-configured-template-rendered-per-request.md:292-295` — the orientation block's inputs are "constant *within* a session", which a mode-keyed bullet is not; the dated note under that amendment records the supersession.

**Files:** `internal/agent/orientation.go`, `internal/agent/orientation_test.go`, `docs/adr/0056-terminal-fail-fast-and-session-scratch.md`, `docs/adr/0023-the-system-prompt-is-a-configured-template-rendered-per-request.md`, `CONTEXT.md`, `docs/manual/configuration.md`

**Tests.** `orientation_test.go`: Plan → block contains no `Scratch dir:` line and the remaining bullets keep their order; Allow-Edits/Ask-Before/Auto → the exact line `- Scratch dir: <path> — writable and outside the workspace; …` is present; a Plan→Allow-Edits mode flip mid-session brings the line back on the next block. `orientationLineCount` loader check unchanged.

**Acceptance.** `go build ./... && go test ./internal/agent/ -run 'Orientation' -count=1`

**Commit.** `feat(agent): Plan mode orientation omits the scratch dir it cannot write`

## 8. IDEAS.md: drop the entries this plan resolved — ✅ DONE (2026-09-14)

NOTES (2026-09-14): `IDEAS.md` is gitignored (`.gitignore:12`) and has never been tracked — the edit is complete in the working tree and the item's acceptance passes, but `git add IDEAS.md` is refused, so this item has nothing to commit; the verifier should record the item as done without a commit (or `git commit --allow-empty` with the SUGGESTED-COMMIT message if the run needs one commit per item).
NOTES (2026-09-14): the scratch entry was flipped from `[P]` to `[ ]` and reworded as the item text prescribes: the defect half is stated as shipped and the remaining question is named as the ADR 0012 one (Plan/Ask-Before scratch writes); no other entry was touched.

**What.** Depends on items 1–7. Remove from `IDEAS.md` the four entries this plan closed — "skill prediction needs work", "Task list UI is bad" (with its example block), "Config sanity check / clean-up sweep", and the defect half of "All modes can use the scratch dir" — per the file's own rule (a resolved item is removed; the CHANGELOG is the closed trail). The scratch entry's remaining Plan/Ask-Before question stays as an open `[ ]` item reworded to say the defect half shipped and what remains is the ADR 0012 question. The other entries stay untouched.

**Files:** `IDEAS.md`

**Tests.** None (docs-only).

**Acceptance.** `! grep -n "Task list UI is bad\|skill prediction needs work\|Config sanity check" IDEAS.md && grep -n "scratch" IDEAS.md`

**Commit.** `docs(ideas): remove the entries closed by the polish wave`
