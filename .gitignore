.DS_Store
*.afdesign
*.afdesign~lock~

# Go build artifacts (the cmd/apogee binary if built at the repo root, test bins)
/apogee
/apogee.exe
*.test
*.out

# Local per-tool loader shim — AGENTS.md is the canonical, checked-in agent guide
/CLAUDE.md
