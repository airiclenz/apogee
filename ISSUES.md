
- [ ] the /clear comand does not update the used context gauge display
- [x] When writing in the prompt window and extending the text beyond one line, the box increases its size correctly but the scroll position is wong. The cursor stays in the top line and an empty line is displayed below. The cursor should be in the last line of the edit box without the empty line displayed. — fixed: `layout` re-seats the textarea caret after each height change to re-clamp the stale scroll offset (bubbles `SetHeight` never clamps a grown box), and `inputContentRows` now counts the trailing full-line row the widget reserves so the box no longer sizes one row short at a wrap-fill boundary.
- [x] I cannot select text in the apogee session chat (transcript) — fixed: a left-click-drag in the transcript viewport now highlights and copies the rendered text over OSC52 (screen-space "copy what you see"). The selection anchors in content coordinates so it survives a mid-drag wheel-scroll, extracts each spanned line with `ansi.Cut` (styling stripped, trailing pad trimmed), and clears on any transcript change or resize. Mouse-region arbitration keeps the prompt and transcript selections from ever coexisting. Drag auto-scroll at the viewport edge is deferred.
- [x] Auto sizing prompt box is not working — superseded by the prompt-box scroll fix above (same root cause): the box did size correctly; what read as broken was the stale scroll offset after auto-grow, now clamped.
- [ ] Functionality that exists in apogee-code has not been fully ported to apogee (`@file`, `/clear`, `/continue`, `/skill` now done; `/server`, session-management UI, inspector still pending). Verified + list collected → see **TODO.md → "apogee-code feature parity — user-facing affordances not yet ported"**. Porting still in progress.
- [x] Mouse clicks in the prompt: a click positions the cursor at the clicked position, and click-drag selects text and copies it to the clipboard (OSC52). Scope is the prompt box; transcript drag-select is still pending (tracked above).
