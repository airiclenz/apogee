// Package session snapshots and resumes Agent state at the quiescent boundary —
// the same primitive the bench composes into forking and counterfactuals, which
// Apogee itself does not expose (ADR 0001). Snapshots are copyable values with a
// versioned schema.
//
// Phase-0 scaffold: no implementation yet.
package session
