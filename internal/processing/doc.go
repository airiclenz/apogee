// Package processing parses Upstream responses: the tool-call formats (native,
// markdown-fenced, custom-regex) and the thinking / harmony channels. It is the
// riskiest port from apogee-code's TypeScript; the TS implementation is the
// behavioural oracle and its ported test vectors are the parity gate.
//
// Phase-0 scaffold: no implementation yet.
package processing
