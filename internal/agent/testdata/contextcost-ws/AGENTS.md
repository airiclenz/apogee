# contextcost fixture

This workspace is the fixed fixture behind TestContextCostGolden (internal/agent/contextcost_golden_test.go): one context file, so the `context files` row renders. Its bytes are pinned in testdata/contextcost.golden — an edit here moves that row and is meant to be a deliberate `-update`.
