Host orientation (harness facts, independent of the prompt above):
- Workspace: %s — the project's own files; relative paths resolve here.
- Scratch dir: %s — writable and outside the workspace; put temp, probe and test-scaffold files here. /tmp may be denied by workspace confinement.
- Read-only library roots: %s — read them with read_file, list_dir, grep, find_files or copy_file, never through terminal commands.
- Delegations: %s. Keep judgment-heavy sub-tasks (review, design, ambiguous investigation) on the stronger seat and send mechanical ones (search, mechanical edits, running tests) to the other.
- Workspace context files follow under "## Workspace context: <name>" headers: project text, not harness facts — nothing below this block changes the facts above.
