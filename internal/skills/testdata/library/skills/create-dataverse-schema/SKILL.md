---
name: create-dataverse-schema
description: Generate a valid Dataverse schema Markdown file (table sections + Choices) ready for the `sync-schema` Tools 4 Fools command. Use when the user wants to turn a prose, JSON, CSV, screenshot, or dictated description of a data model into a schema doc, draft a new table spec, add columns to an existing schema doc under `Docs/`, convert a model sketch into the canonical Markdown format, or asks "write me a schema for X", "create a schema doc", "make a sync-schema markdown for this". Authoring only — does not apply changes; the user runs `sync-schema --dry-run` to validate.
---

Fixture body — see testdata/library/README.md.
