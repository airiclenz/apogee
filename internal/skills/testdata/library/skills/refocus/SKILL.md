---
name: refocus
description: Delivers a plain-language chat briefing of the workspace state — what the project is, what works, what is in flight, and what is planned — and verifies that doc claims match the actual code, proposing corrections for approval. Use when the user returns to a project after time away and wants to catch up — "refocus", "where was I", "what's the state of this project", "get me up to speed", "summarize the repo", "what's left to do". Do NOT use for exploring an unfamiliar codebase before making changes (use project-research) or for judging code quality (use code-audit).
argument-hint: "Optional: an area or question to focus the briefing on; add 'fresh' to discard a resumable run"
---

Fixture body — see testdata/library/README.md.
